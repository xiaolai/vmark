# VMark Developer Documentation

## Active Docs (tracked in git)

- `dev-docs/architecture.md`: system architecture overview — C4 diagram, entry points, data flows, module map.
- `dev-docs/design-system.md`: design tokens, components, patterns (single source of truth).
- `dev-docs/css-reference.md`: visual QA reference document for CSS changes.
- `dev-docs/cjk-gotchas.md`: CJK formatter pitfalls — things that will bite you if you're not careful.
- `dev-docs/large-file-open-pipeline.md`: end-to-end pipeline for the large-file open UX — tiers, routing, forced Source mode, indeterminate indicator, and the perf tricks in TiptapEditor.
- `dev-docs/testing-hardening.md`: opt-in mutation testing (`cargo-mutants`, scoped via `src-tauri/mutants.toml`) and parser fuzzing (`cargo-fuzz`, isolated in `src-tauri/fuzz/`) for the Rust backend — audit L13 / RW-14 (v1-017/v2-016/v2-017). Neither affects the normal `cargo test`/`cargo check`.
- `dev-docs/e2e-testing.md`: driving a running app for E2E via the two MCP bridges — the debug-only Tauri harness (`9323`, for UI/IPC/screenshots) vs VMark's own dynamic-port bridge (for AI-driven features, tested via `mcp__vmark__*` exclusively). Includes the dev-mode reconfigure procedure (rebuild sidecar, point client at the dev binary, restart) and the stale-binary/collision gotchas. Referenced from `AGENTS.md`.
- `dev-docs/plans/20260504-github-actions-workflow-viewer.md`: GitHub Actions workflow viewer + structured editor — IR pivot, ADRs, 10-phase plan with all phases complete. End-user docs at `website/guide/workflow-viewer.md`; perf benchmark at `src/bench/workflow.bench.ts`.
- `dev-docs/house-cleaning/`: recurring framework for paying down measurable debt — daily (T1) / weekly (T2) / monthly (T3) / quarterly (T4) cadence with per-tier runbooks, ledger schema, and findings template. Complements the existing `audit/` chunk reports.
- `dev-docs/audit/20260530-dead-code-and-optimization.md`: multi-round whole-repo audit (dead code, optimization, type safety, duplication, bundle weight, resource lifecycle, Rust panic-safety, concurrency/races, accessibility, test gaps). Grep-verified findings + a cross-round priority list. **Remediated by `dev-docs/plans/20260530-audit-remediation.md`** (branch `chore/audit-remediation`).
- `dev-docs/plans/20260530-audit-remediation.md`: the 6-phase remediation plan for the audit above — WI breakdown, ADRs, Codex cross-model review, and recorded defer/abort decisions. Implemented end-to-end; Status header tracks what shipped vs. deferred (WI-2.6 abort, WI-2.8 defer, T4/O8/D8/A4/C6 deferred).
- `dev-docs/audit/20260530-plan-audit-findings.md`: conformance audit of the shipped remediation against the plan + source audit — flags plan-vs-audit completeness gaps (T4 Medium follow-up) and partial-AC items (WI-0.2 store test, WI-4.1 2-of-4 validators, WI-1.1 visual QA pending).
- `dev-docs/audit/20260531-terminal-integration.md`: deep audit of the integrated terminal (PTY backend, IPC wrapper, xterm addons, session lifecycle) vs. VS Code/Ghostty/WezTerm. Grep-verified findings + prioritized backlog. Top items: PTY output crosses IPC as a JSON number array (T1, P0), no shell integration / OSC 7 / OSC 8 (M1–M3), file links discard `:line:col` (C1), sessions don't survive restart and SerializeAddon is loaded-but-unused (C3). **Remediation plan: `dev-docs/plans/20260531-terminal-industrial-best.md`.**
- `dev-docs/audit/20260601-terminal-gaps.md`: follow-up reinvestigation of the terminal *after* the industrial-best merge. Four parallel deep-dives + a second deeper pass, every finding re-verified in source. Headline: one real user-facing regression — shell integration breaks custom `$ZDOTDIR` setups (`shell_integration.rs` never resolves/passes `USER_ZDOTDIR`; needs a login-shell query) — plus paste bypassing bracketed-paste mode, no screen-reader support, OSC 0/2 title dropped, and security link code (`setupWebLinks.ts`) with zero tests. Confirms multi-window isolation and Channel flow-control are SAFE (corrects first-pass suspicions), and records false positives. Prioritized P0–P3 backlog.
- `dev-docs/audit/20260601-settings-dialog-gaps.md`: multi-perspective audit of the whole settings surface (schema↔UI diff + persistence/migration, hardcoded-constants sweep, dialog UX/a11y/validation). Real gaps: 7 built-but-unexposed settings (update auto-download/frequency, 3 CJK options, `pasteMode`, `associations`), 2 dead settings (`autoResizeCustom`/`inlineThreshold`), a platform-gating slip (`macOptionIsMeta`/`shellIntegration` shown on all platforms), no settings search, no reset-to-default UI, preset-only validation. Persistence/migration is healthy (deep-merge + sanitize). Calibrated anti-bloat stance: most hardcoded timing/perf constants should STAY hardcoded; only a small accessibility/visual cluster (terminal bell/contrast, focus-mode blur) is worth promoting. Prioritized P1–P3 backlog.
- `dev-docs/audit/20260601-website-docs-gaps.md`: which `website/` docs lag the > 0.8.0 changes (almost all terminal). Prioritized gaps: no 0.8 terminal release/blog note (P1); `shortcuts.md` missing prompt-nav `Cmd+↑/↓` (P1); `settings.md` missing the Shell-Integration + Mac-Option-as-Meta rows (P2); `terminal.md` missing the bell/background-activity indicator (P2); OSC 8 mention (P3). Records what's already covered + the released-vs-unreleased boundary.
- `dev-docs/plans/20260601-terminal-gap-remediation.md`: 4-phase, WI-decomposed remediation plan for the gaps in `20260601-terminal-gaps.md`. Phase 1 (P0) fixes the custom-`$ZDOTDIR` regression via a cached login-shell resolver + `USER_ZDOTDIR` (ADR D1); Phase 2 (P1) paste-via-`term.paste()` + security tests for `setupWebLinks`/`setupFileLinks`; Phase 3 (P2) `screenReaderMode` setting + OSC 0/1/2 tab title; Phase 4 (P3) font-sync/scrollback/reader-logging/`wait()`/settings + coverage backfill; Phase 5 lists deferred product scope. Includes ADRs/Decision Log, Open Questions, gap→WI map, per-phase DoD (`scripts/check-terminal-gaps-phase.sh`), and a Manual Test Checklist. Needs Codex review before Phase 1 (rule 60 §6).
- `dev-docs/plans/20260531-terminal-industrial-best.md`: 7-phase, WI-decomposed plan for the terminal audit above. Phase 0 spike gate (Tauri binary `Channel`, zsh shell-integration injection, L1 orphan repro) → Phase 1 throughput (T1–T3, L2) → Phase 2 cwd/OSC 7 → Phase 3 shell integration/OSC 133 → Phase 4 links+display (C1, M3, M4) → Phase 5 persistence (C3) → Phase 6 process-group kill (conditional). Includes ADR-T1..T4, per-phase machine-checkable DoD (`scripts/check-terminal-phase.sh`), and a pre-Phase-1 Codex review gate.
- `dev-docs/audit/20260612-full-improvement-audit.md`: whole-repo 16-dimension improvement audit (51-agent fleet: architecture, stores, editor core, Rust backend, performance, testing, security, CSS/tokens, i18n, dead code, MCP, docs, deps, CI/DX, a11y, product). Every high finding adversarially verified by an independent agent. 0 critical, 28 confirmed high, 93 medium, 39 low; 2 refuted findings recorded so they are not re-chased. Headliners: markdown serializer silently corrupts content on round-trip (`&#x20;` replace + over-aggressive escape stripping), AI-suggestion positions never remapped through edits, MCP checkpoint/retry write races, CI auto-merge gate is decorative, menu-ID contract generator reads a deleted file. Four subsystems remain unaudited (export pipeline, updater client, AI-provider runtime, error recovery/observability). **Remediated first-pass on 2026-06-12** — 27/28 highs fixed (H4 deferred+frozen, H24 needs a manual branch-protection change); see the audit doc's Remediation log.
- `dev-docs/audit/20260531-dead-code-resweep.md`: independent follow-up to the 2026-05-30 dead-code audit. Re-runs `knip` + `cargo check`, verifies the remediation held (high-value dead code confirmed gone), and precisely categorises the deferred tail (14 redundant `default` exports, 91 dead barrel re-exports, ~10 genuinely-dead named exports vs. over-exported-but-alive). Includes a deep-dive on the workflow snapshot half-feature: confirms the WI-1.5 KEEP decision is sound and refines the cost as conditional (only `action/save-file` workflows snapshot). **Outcome:** added a workspace-aware `knip.json`, executed a surgical export-hygiene sweep (14 redundant defaults + 5 dead decls + stray `.d.ts`; verified `tsc`/19,256 tests/`check:all` green), and wired `pnpm knip` into `check:all` with an `error`/`warn` rule split (files/deps/imports fail CI; export/type hygiene reported non-blocking).
- `dev-docs/error-recovery.md`: failure-mode matrix for crash/data-loss scenarios — which paths recover automatically, which are partial, which are open. (Status column is self-declared; the 2026-06-12 audit's deferred follow-up is verifying the "No"/"Partial" rows.)
- `dev-docs/coverage-promotion-plan.md`: plan for promoting coverage-excluded areas back under the gate.
- `dev-docs/plans/20260718-coherence-layer.md`: implementation plan for the Coherence Layer, Phases 0–1 decomposed into WIs tracing to the paper's IDs (format spec + gates G1/G2 + spikes → Rust kernel + breakdown view + read-only MCP); Phases 2a–4 outlined pending the Phase 2a semantic-model design session. Codex-reviewed (MAJOR GAPS → amended → READY TO BUILD; disposition table in the plan).
- `dev-docs/plans/20260721-blank-line-preservation.md`: DRAFT plan to preserve runs of >1 blank line through the WYSIWYG markdown↔ProseMirror round trip. Documents the verified root cause (blank-line counts are lost at MDAST parse time; the `cjkFormatting.*` toggles gate only the explicit formatter, not save; `markdown.preserveLineBreaks` is mislabeled). ADR-1 position-derived `blankLinesBefore` block attribute; Phase 0 spike gate → capture+serialize (flag default-off) → setting/UX honesty/docs. Needs Codex review before Phase 1.
- `dev-docs/plans/20260721-mcp-open-workspace.md`: DRAFT plan for an `open_workspace` MCP tool (the existing `workspace.open` opens a file, not a folder). The real work is consent, not wiring — opening a folder is boundary-*expanding* so it can't reuse `checkBridgePath`; ADR-2 gates it behind a one-shot approval prompt modeled on the browser tools. Phase 1 shared `openWorkspaceByPath` helper + tool/handler/dispatch → Phase 2 approval UI → Phase 3 sidecar/docs. Needs Codex review before Phase 1.
- `dev-docs/specs/coherence-format-v0.md`: **the coherence layer's public contract** — on-disk format spec v0 (ledger entry/segment schemas, pin manifests, frontmatter identity, CAS layout, input-set taxonomy, canonicalized hashing, provenance confidence, context-relative staleness algorithm incl. first-class Diverged, scan reconciliation state machine, performance targets, M1–M5 baselines).
- `dev-docs/coherence-layer-architecture.md`: Phase 1 implementation architecture — ADR-C4 module map (pure kernel / storage / services / frontend funnels), the capture write path, funnel confidence classification, deliberate Phase-1 characteristics (buffer identity lag, tracked-ledger rewind, deletion-as-index-state), and the test topology.
- `dev-docs/grills/coherence/`: Phase 0 gate + spike evidence — G1 capture coverage (write-path inventory + runnable prototype), G2 git operation classification (12/12), S1 ledger merge=union behavior, S2 rusqlite performance (rebuild 1.34s / breakdown 6.4ms at target scale), S3 LLM edge inference (88.9% direct-edge precision), S4 semantic-check precision (M3 baseline 88.9%), and the ADR-C5 dependency review.
- `dev-docs/coherence-layer-paper.md` (v1.1, Codex-reviewed): **design paper (single source of truth) for the VMark Coherence Layer** — semantic version control for recursively developed creative work. Abstract → problem formalization → verified related-work matrix → traceable requirements (R1–R23), kernel model (3 atoms, invariants I1–I4), two-axis staleness + waivers (the contribution), storage tiers, git interop, architecture, extensibility, open problems (O1–O9), evaluation metrics (M1–M5), staged delivery outline. The implementation plan (`dev-docs/plans/`) is written after this paper and must trace WIs to its IDs. Evidence base: the four `deep-researches/20260718-*` docs below.
- `dev-docs/deep-researches/`: archived deep-research reports (verified, cited) — convention `YYYYMMDD-topic.md`, required by the AGENTS.md working agreement.
  - `20260718-canon-provenance-staleness-landscape.md`: competitive/prior-art landscape for the four-capability creative-coherence stack (canon store, versioned dependency edges, staleness propagation, LLM consistency checking). Verdict: no surveyed product ships versioned edges or staleness propagation — slot open within the AI-writing/AI-video space; ComfyUI's auto-embedded provenance is the adoption pattern to copy. USD/ShotGrid, Ink & Switch lineage, academic, and agent-memory areas remain unexamined, not cleared.
  - `20260718-versioned-edges-staleness-prior-art.md`: follow-up examining the areas the first pass left open. Finding: strong versioned-edges + staleness prior art exists in the VFX stack — USD Ar resolver indirection, AYON's automatic pin-file capture at publish, Flow Production Tracking's breakdown apps (version-comparison staleness, scan-time edge capture, human-initiated updates only) — but none of it is semantic, NL, or has waiver semantics, so the slot stays open; VMark = porting the VFX pattern to natural language. Eight concrete design lessons; academic + temporal-KG agent-memory areas still unexamined.
  - `20260718-graphiti-agent-memory-academic-priorart.md`: pass 3 (final) — agent memory, academic NL prior art, waiver semantics. Final three-pass verdict: Zep/Graphiti is the closest system in the world (fact-level bi-temporal edges, LLM contradiction detection, episode provenance) but supersession is fully automatic — no human ratification anywhere; academic belief-revision work is weights-level only and names consequence propagation an unsolved open problem. "Stale-by-version but semantically valid" vs "stale-and-contradicted" is distinguished nowhere — genuinely unclaimed. All 7 Letta claims refuted (unverified, not cleared); DVC waiver semantics and Jacquard/Patchwork still unanswered.
  - `20260718-coherence-layer-conclusion.md`: **synthesis + proposal** over the three passes — the problem formalized (recursive explosion = invisible staleness over a cyclic nondeterministic graph; canon hub linearizes the pairwise blowup), the copy-vs-invent design table (Graphiti's semantic half + the VFX stack's version half; net-new: two-axis staleness, waiver objects, human-ratified supersession), design laws (edge inference not homework, greenhouse mode, zero-effort provenance), and the recommended sequence (identity untouched → dogfood → breakdown view → semantic layer → verticals as format adapters). Includes the core-value statement addendum (§6): maintain/validate/audit/version-control semantic edges — with the three load-bearing refinements (coherence is the sellable value, human-ratified convergence is the unclaimed half, the editor is the mandatory capture sensor). §7 specifies the kernel: three atoms (Semantic Object with persistent identity, Transformation with recorded input set, Context as scope-of-truth) from which edges, staleness, canon, and waivers are all derived — with invariants, VMark substrate mapping, and precedent check. Direction document, not a plan.
- `dev-docs/decisions/`: architecture decision records (ADRs).
  - `heading-ime-composition-fix.md`: How we fixed the WebKit heading IME split-block bug — 5 attempts, root cause analysis, and why prevention beats repair.

## Agent Configuration

- `AGENTS.md`: working agreement + required practices.
- `.claude/rules/`: engineering guardrails, TDD, UI consistency, design tokens, shortcuts.

## Website (User-Facing Docs)

- `website/guide/`: VitePress site for end-user documentation.
- See `.claude/rules/21-website-docs.md` for sync rules.

## Documentation Conventions

- Prefer a single source of truth for each topic.
- Date + status new documents (Active / Historical / Draft) to reduce ambiguity.
- Update docs in the same change that modifies behavior.

## Important History

Records of significant codebase-wide changes — process, decisions, and lessons learned.

- `important-history/20260214-codebase-documentation.md`: How we added AI-maintenance comments to ~400 files using parallel git worktrees.
- `important-history/20260423-smart-cmd-a-hidden-cost.md`: What Issue #816 taught us about replacing universal keyboard conventions — a retrospective + checklist for future "nice features".

## Archive

Historical docs live in `archive/` (local, not tracked in git).
