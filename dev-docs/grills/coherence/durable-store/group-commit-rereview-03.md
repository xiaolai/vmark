# G-B re-review 03 — group-commit ship-readiness (2026-07-21)

Reviewer: Codex gpt-5.6-sol (owner-delegated), thread `019f8214`. Against the ratified Option A design. **10th review of this component.**

| Severity | Finding | file:line | Why it ships-or-not |
|---|---|---|---|
| Critical | `group-prepare` is not power-loss durable when append creates a new ledger segment. The file is fsynced, but its parent directory is never fsynced. | [ledger.rs:156](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/ledger.rs:156) | A newly created or rotated segment can disappear after acknowledged success. With later members in another segment, recovery may retain transformations but lose their manifest. #5 is not durably closed. |
| Critical | Aborting a partial attempt leaves its committed members live, and the claimed superseding rerun cannot repair that attempt. | [accept_group.rs:323](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/accept_group.rs:323), [accept_group.rs:337](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/accept_group.rs:337) | After A commits, B moves, and recovery aborts, A remains the live head because abort records do not invalidate transformations. Retrying the same group creates a new attempt, treats A as missing because its idem is attempt-folded, then rejects A as stale because its old parent is no longer the head. The test at [accept_group.test.rs:363](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/accept_group.test.rs:363) avoids this by aborting an attempt with zero committed members. The O(ledger) reconcile faithfully restores the bad partial state. #6/#7 are not closed. |
| High | The reviewed `GroupPreview` is not bound to the submitted candidate set or `group_id`. | [accept_group.rs:343](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/accept_group.rs:343) | Acceptance compares only structural maps. For groups with no incident or new edges, those maps are empty, so a preview for different content, operator, or intent authorizes another internally valid candidate set over the same bases. This permits an unreviewed commit; #8 is only conditionally correct. |
| High | Whole-group preflight does not enforce candidate parent arity. | [accept_group.rs:125](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/accept_group.rs:125), [group_prepare.rs:94](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/group_prepare.rs:94) | Fresh acceptance checks only `parents.first()` and can commit a multi-parent candidate. Recovery explicitly rejects the same manifest as unreconstructable. A crash can therefore create a prepared group that its recovery path can never complete. #2 is incomplete. |
| High | `with_write_lock` provides no cross-process exclusion on Windows. | [state.rs:421](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/state.rs:421) | The actual `flock` is compiled only on Unix; Windows merely opens the file. Two Windows processes can validate and append overlapping groups concurrently, directly violating ratified R1. |
| Medium | Ratified reconcile telemetry and threshold warnings are absent. | [state.rs:280](/Users/joker/github/xiaolai/myprojects/vmark/src-tauri/src/coherence/state.rs:280) | Reconciliation is correctly unconditional, but no entry count, durations, rolling p95, or once-per-workspace warning is recorded as required by the owner decision. |

Verified as correct: sorted ungrouped-idem group identity, attempt-folded member idems, prepare-before-member ordering, CAS verification/staging, causal lifecycle tip selection, recovery head/resolution/new-incident-edge revalidation, unconditional outer-lock reconciliation, and Unix serialization of both public group operations.

The focused Rust suite could not start because the read-only environment denied Cargo’s `target/debug/.cargo-build-lock`; this verdict is based on direct code and test inspection.

group-commit: ❌ NOT ship-ready (5 blocking)