# S0.4 — Headless markdown → HTML (Node-safe boundary)

> Result: **PASS**
> Date: 2026-06-25

## Probe

Stand up the Node-safe `markdownPipeline` boundary and render markdown → HTML in
pure Node, reusing VMark's remark plugins, with DOMPurify on a Node DOM.

## What was built

- `src/utils/markdownPipeline/nodeSafe.ts` — explicit Node-safe re-export of the
  alias-free plugins (`remarkWikiLinks`, `remarkCustomInline`,
  `remarkDetailsBlock`, `remarkTocBlock`, `remarkResolveReferences`) + node
  types. Confirmed each plugin imports only npm packages + siblings + `./types`
  (no `@/` aliases, no DOM). The barrel `plugins/index.ts` was the only thing
  pulling `../parser` (→ `@/utils/perfLog`); bypassed.
- `vmark-content-server/` package (pnpm workspace member, Node ≥20.12).
- `src/render/renderMarkdown.ts` — unified pipeline: remark-parse → gfm → math →
  frontmatter → {wikiLinks, customInline, detailsBlock, resolveReferences,
  alerts} → remark-rehype (custom handlers) → rehype-katex → rehype-stringify →
  DOMPurify(jsdom).
- `src/render/remarkAlerts.ts` — alert blockquote → `alert` node. **Directly
  addresses review D3.4**: alerts are NOT a remark node in the editor (they are
  created during MDAST→ProseMirror), so the headless path needs its own
  transform. Implemented + tested for all 5 GFM kinds.

## Evidence

`pnpm --filter @vmark/content-server test` → **24/24 pass**, covering:
- core markdown, GFM tables, task lists, frontmatter stripping;
- custom nodes: `==highlight==`→`<mark>`, `~sub~`/`^sup^`, `<details>`;
- alerts: all 5 kinds + ordinary-blockquote regression guard;
- wiki-links: missing vs resolved, alias text, `#anchor` carry-through;
- math: server-side KaTeX;
- XSS corpus (script/onerror/javascript:/iframe/svg-script/onclick) neutralized
  in Node via DOMPurify-on-jsdom.

## Findings / decisions

- The boundary is a **subpath re-export**, not a full extraction — far lower
  regression risk to the editor. The CI bundle step (ADR-2) will vendor these
  files into the content-server package; in-repo, vitest resolves them straight
  from app source via a `@vmark/markdown-plugins` alias so editor + server stay
  in lockstep.
- DOMPurify-on-jsdom is confirmed viable (review D3.3). `USE_PROFILES` with
  html+mathMl+svg keeps KaTeX output intact while scrubbing scripts/handlers.

## Residual (later phases)

- Mermaid/Markmap render client-side in the served page (ship browser bundles) —
  Phase 3/4, not this spike.
- Full fidelity diff against the editor's `ExportSurface` output across the
  `dev-docs/css-reference.md` element set — Phase 3 fixtures.
- Custom-node inventory beyond the rendered set (e.g. footnotes, math-block
  edge cases) — Phase 3.
