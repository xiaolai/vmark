# Audit Findings

**Run**: audit-fix 20260822-131500 | **Scope**: branch `feat/tab-navigation-and-split-view`, 32 changed source files | **Audit type**: mini (5-dim)
**Model**: gpt-5.6-sol | **Effort**: high | **Audit thread**: 5 parallel batch jobs (per-area, not per-file — see report)
**Status values**: open | fixed | not-fixed | partial | regressed | rejected | deferred

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | src-tauri/src/menu/localized/view_menu.rs | 53 | High | Logic & Correctness | WI-DSPL1.2/WI-TNAV2.3’s five native-menu routes lack frontend routing assertions; removing a `VIEW_BINDINGS` row would leave a dead item while current tests pass | Assert all five event-to-command mappings and exercise their dispatch against real tab/pane state | fixed | 1 |
| 2 | src-tauri/src/menu/localized/view_menu.rs | 85 | High | Logic & Correctness | D7’s critical path — exactly-once `Ctrl+Tab` dispatch while the embedded WKWebView owns focus — has no Tauri E2E coverage | Add a Tauri MCP journey that focuses the browser webview, presses `Ctrl+Tab`, and verifies exactly one last-used-tab activation | deferred (coverage/design) | 1 |
| 3 | src/components/StatusBar/StatusBar.css | 164 | High | Logic & Correctness | Absolute chevrons cover the scrollport edges without scroll padding, so scrollIntoView can leave an active pill obscured beneath a chevron | Add logical scroll padding matching the overlay width and verify both edges in real layout | deferred (coverage/design) | 1 |
| 4 | src/components/StatusBar/StatusBar.css | 976 | High | Logic & Correctness | The later, more-specific other-pane rule overwrites the only focus-visible box-shadow, hiding keyboard focus on that pill | Layer both shadows or add a higher-specificity accent focus rule | fixed | 1 |
| 5 | src/components/StatusBar/StatusBarTabStrip.tsx | 87 | High | Logic & Correctness | The critical live focus-flip path is untested because coverage unmounts and freshly renders each direction rather than exercising selector reactivity | Keep one strip mounted, invoke the real pane focus action, and assert indicator/selection movement | deferred (coverage/design) | 1 |
| 6 | src/components/StatusBar/StatusBarTabStrip.tsx | 202 | High | Duplication | The left and right chevron blocks are near-identical and differ only by side, label, direction, and icon | Extract one configured ScrollChevron component | deferred (coverage/design) | 1 |
| 7 | src/components/StatusBar/paneIndicator.ts | 29 | High | Logic & Correctness | Legal openSplit(window, null) focuses an empty secondary, yet this returns the primary ID despite WI-DSPL1.1 requiring no indicator | Return null when the secondary is empty and test the real openSplit(window, null) transition | rejected | 1 |
| 8 | src/components/StatusBar/scrollActiveTabIntoView.ts | 78 | High | Logic & Correctness | The activation key is consumed before the conditional region or target exists, so a pill later rendered with the same key is never scrolled into view | Retry on attachment/content changes and record the key only after finding the target | deferred (coverage/design) | 1 |
| 9 | src/components/Tabs/Tab.tsx | 183 | High | Logic & Correctness | R1 permits activeTabId=null with document pills present, making every pill tabindex=-1 and leaving the tablist without a keyboard entry point | Decouple selection from roving tab-stop state and choose a fallback focusable pill | deferred (coverage/design) | 1 |
| 10 | src/components/Tabs/Tab.tsx | 233 | High | Logic & Correctness | The described text is nested inside a name-from-content tab, so “other pane” enters its accessible name and is then announced again through aria-describedby | Place a shared hidden description outside the role=tab element and reference it | fixed | 1 |
| 11 | src/components/Tabs/useTabContextMenuActions.ts | 78 | High | Duplication | Four bulk-close callbacks repeat the same await-and-dismiss lifecycle and differ only in tab-ID selection | Extract one bulk-close executor and retain pure ID-selection logic per action | fixed | 2 |
| 12 | src/components/Tabs/useTabContextMenuActions.ts | 127 | High | Logic & Correctness | Replacing the non-focused pane first activates its old tab and then the target, recording a phantom user activation that corrupts MRU order | Add an atomic pane operation that replaces and focuses with one activation | fixed | 1 |
| 13 | src/components/Tabs/useTabContextMenuActions.ts | 190 | High | Logic & Correctness | WI-DSPL1.5 requires the item absent for browser targets, but it is always emitted as disabled; the hook/UI action and dismissal seam is also untested | Conditionally omit it and add hook-level presence, availability, action, and dismissal tests | fixed | 1 |
| 14 | src/components/Tabs/useTabContextMenuActions.ts | 193 | High | Logic & Correctness | Eligibility accepts a browser or hidden-instance active ID, allowing Open to the Side to seed a split with a non-document or non-visible primary | Require a distinct visible active document and test browser-active and hidden-active cases | fixed | 2 |
| 15 | src/hooks/useCommandBootstrap.ts | 88 | High | Logic & Correctness | The five new native-menu routes are untested, so mapping typos can leave Ctrl-Tab or pane commands dead while other contract gates pass | Add table-driven assertions for every new menuEvent-to-commandId pair | fixed | 1 |
| 16 | src/hooks/useCommandBootstrap.ts | 233 | High | Logic & Correctness | An unmount or StrictMode remount during sequential listener setup leaves the cancelled instance installing duplicate listeners until mounting finishes, potentially double-firing toggle commands | Make mounting cancellable, immediately dispose partial listeners, and test cancellation during individual listener registration | deferred (coverage/design) | 1 |
| 17 | src/hooks/useTabStripOverflow.ts | 78 | High | Logic & Correctness | Only the capped region is resize-observed; layout-only descendant width changes, including close buttons appearing on hover or focus, can change scrollWidth without any callback and leave chevrons stale on an untested critical path | Also resize-observe the intrinsic-width tablist and test scrollWidth changes without DOM mutation or region resize | fixed | 1 |
| 18 | src/services/commands/tabCommands.ts | 69 | High | Logic & Correctness | The new tab.lastUsed CommandBus path is untested; existing tests omit it and reimplement its wrapper, so a no-op handler passes | Execute the registered command against real MRU/tab stores and cover target and null-target behavior | deferred (coverage/design) | 1 |
| 19 | src/services/mcpBridge/v2/workspaceOpen.ts | 87 | High | Logic & Correctness | D5 origin wiring lacks a behavioral handler test; current tests source-scan or manually reproduce the lambda, so MCP-to-MRU contamination can regress | Invoke handleWorkspaceOpen with real tab/MRU stores and assert the background tab is absent from MRU | deferred (coverage/design) | 1 |
| 20 | src/services/mcpBridge/v2/workspaceOpen.ts | 92 | High | Logic & Correctness | With a browser active over an enabled split, createTab replaces the focused pane, but restoring the browser is ignored by pane convergence, leaving the background document in the split | Snapshot and restore pane state for paired background activation and add a browser-active split test | deferred (pre-existing) | 1 |
| 21 | src/services/mcpBridge/v2/workspaceOpen.ts | 146 | High | Shortcuts & Patches | An ingest failure after tab creation and workspace claim reports an error but leaves the new orphaned tab and ownership state behind | Roll back newly created tabs and ownership on ingest failure while preserving pre-existing tabs | deferred (pre-existing) | 1 |
| 22 | src/services/navigation/restoreWorkspaceTabs.ts | 90 | High | Logic & Correctness | If ingest fails, closing the newly active tab selects and announces a raw-strip neighbor; when every restore fails, created remains zero and focus/MRU are never repaired | Preserve the prior active tab, label rollback activations as restore, and restore the prior tab after failure | deferred (pre-existing) | 1 |
| 23 | src/services/navigation/restoreWorkspaceTabs.ts | 173 | High | Logic & Correctness | restoreWorkspaceTabs collapses MRU before callers restore the split, after which setActiveTab and openSplit fabricate user MRU entries for primary and secondary | Mark layout activations as restore and collapse MRU after layout restoration | deferred (coverage/design) | 1 |
| 24 | src/services/navigation/toggleSplitDocuments.ts | 49 | High | Logic & Correctness | A browser entry ahead of another document in MRU makes selection abandon MRU and choose the first other document, which is also not necessarily the promised adjacent document | Add a document-filtered MRU resolver and use cycleTabId for the cold adjacent fallback | fixed | 1 |
| 25 | src/services/persistence/hotExit/restoreTabsHelpers.ts | 38 | High | Logic & Correctness | An untitled tab with empty current/saved text but recoverable undo or redo checkpoints is classified as meaningless and discarded | Include history and other recoverable metadata in the meaningful-tab predicate | deferred (pre-existing) | 1 |
| 26 | src/services/persistence/hotExit/restoreTabsHelpers.ts | 247 | High | Logic & Correctness | D4 is not integration-tested through restoreTabs and the real activation bus; current coverage only source-scans this call and tests collapseMruToActive directly | Restore A/B/C through the real pipeline and assert exactly the active tab remains in MRU | deferred (coverage/design) | 1 |
| 27 | src/services/tabs/lastUsedTab.ts | 34 | High | Logic & Correctness | The hidden-workspace-instance path is untested; no test enables workspace rail, so replacing visibleWindowTabs with raw tabs would pass | Add a real rail/instance test with a hidden MRU key ahead of a visible target | deferred (coverage/design) | 1 |
| 28 | src/services/tabs/lastUsedTab.ts | 35 | High | Duplication | Browser-key resolution duplicates getBrowserWorkspaceView’s live-remembered-page-or-first-page policy, allowing Ctrl-Tab and browser-pill behavior to drift | Extract a shared pure browser return-target resolver | deferred (coverage/design) | 1 |
| 29 | src/services/tabs/moveTabToNewWindow.ts | 53 | High | Duplication | The detach transaction duplicates transferTabFromDragOut and has already drifted: this path counts visible tabs while drag-out counts all live window tabs | Extract one live-state detach transaction and parameterize trigger-specific feedback | deferred (pre-existing) | 1 |
| 30 | src/services/tabs/moveTabToNewWindow.ts | 81 | High | Logic & Correctness | Source state is destroyed after window creation without acknowledgment that the target claimed and applied the payload, so target initialization failure can lose the only live copy | Require a target apply acknowledgment with timeout and rollback before committing source removal | deferred (pre-existing) | 1 |
| 31 | src/services/tabs/moveTabToNewWindow.ts | 97 | High | Logic & Correctness | Moving the sole tab from a non-main window creates Undo in that source webview and immediately destroys the webview, making Undo unreachable | Keep the source alive for the undo lifetime or make undo destination/backend-owned | deferred (pre-existing) | 1 |
| 32 | src/services/tabs/openToTheSide.ts | 21 | High | Logic & Correctness | Eligibility does not require a visible active document, so a browser or hidden tab can become primaryTabId and violate the document-only pane invariant | Resolve both tabs from the visible projection and require the active tab to be a document | fixed | 1 |
| 33 | src/services/tabs/openToTheSide.ts | 41 | High | Logic & Correctness | Replacing the non-focused pane first activates its displaced tab and then the target, creating a phantom user activation that pollutes MRU | Add an atomic pane action that replaces and focuses with one final activation | fixed | 1 |
| 34 | src/shared/menuIdExtraction.ts | 23 | High | Logic & Correctness | The regex accepts only `app` and silently omits four real static IDs constructed with `&app`: search-genies, no-genies, reload-genies, and open-genies-folder | Accept both argument forms, classify the surfaced IDs, regenerate menu-ids.json, and add regression coverage | deferred (pre-existing) | 1 |
| 35 | src/stores/paneStore.ts | 98 | High | Logic & Correctness | `openSplit` and `setFocusedPaneTab` accept unvalidated IDs, allowing browser-primary panes and D9’s forbidden A/A state | Validate live document IDs and distinctness before mutation while preserving intentional null secondary panes; add browser-active and duplicate-ID tests | deferred (coverage/design) | 1 |
| 36 | src/stores/paneStore.ts | 125 | High | Logic & Correctness | Open-to-Side replaces a pane via separate focus and tab updates, recording the displaced tab as a transient user activation and corrupting MRU order | Add an atomic replace-and-focus action that announces only the final target; test it against the real bus and MRU store | fixed | 1 |
| 37 | src/stores/paneStoreHelpers.ts | 45 | High | Logic & Correctness | `resolveWindowSplit` promotes pane IDs even when `split.enabled` is false, so D14’s stale live primary can override the required fallback active tab | Ignore pane IDs for null or disabled splits and add a disabled-split/stale-primary regression test | deferred (coverage/design) | 1 |
| 38 | src/stores/paneStoreTypes.ts | 17 | High | Duplication | The supposedly shared pane bounds are separately hardcoded as `0.2`, `0.8`, `20`, and `80` in `SplitDivider`, allowing resizing and ARIA metadata to drift | Import these leaf constants into `SplitDivider` and derive the percentage values | deferred (pre-existing) | 1 |
| 39 | src/stores/settingsStore/shortcutDefinitions.ts | 171 | High | Logic & Correctness | Physical `Ctrl-Tab` cannot round-trip through KeyCapture on macOS because Ctrl and Meta are both serialized as `Mod`, producing Cmd-Tab; WI-TNAV2.3’s required capture test is absent | Preserve physical Ctrl on macOS and add the specified KeyCapture regression test | fixed | 1 |
| 40 | src/stores/tabActivationBus.ts | 64 | High | Logic & Correctness | The “every activating action” guard does not isolate `setActiveTab`, `closeTab`, or `detachTab`; setup `createTab` events let the test pass even if the target notification is removed | Subscribe after setup or clear the spy, then assert the exact payload and count for each action | deferred (coverage/design) | 1 |
| 41 | src/stores/tabActivationBus.ts | 84 | High | Logic & Correctness | `withActivationOrigin` accepts async callbacks despite its synchronous-only invariant, so activations after an `await` silently revert to `user` origin and pollute MRU history | Reject Promise-like callback results at the type boundary and add a compile-time regression test, or propagate origins explicitly | fixed | 1 |
| 42 | src/stores/tabMruStore.ts | 70 | High | Logic & Correctness | `removeWindow` only makes `keys()` temporarily appear empty while retaining `lists[windowLabel]`; recreating that label merges dead IDs back into MRU | Delete the stored list through a window-removal event/action and test remove-then-recreate against raw state and `keys()` | fixed | 1 |
| 43 | src/stores/tabStoreBrowserWorkspace.ts | 58 | High | Logic & Correctness | The unconditional user-origin event records browser-session restoration as viewed, leaving an unseen browser workspace in MRU so Ctrl-Tab opens restored content | Scope the synchronous browser restore transaction with `withActivationOrigin("restore")` and add a real-bus MRU restore test | deferred (coverage/design) | 1 |
| 44 | src/stores/tabStoreBrowserWorkspace.ts | 58 | High | Duplication | `createBrowserPage` now announces activation, but `BrowserPageTabs` immediately calls `setActiveTab` for the same ID, producing duplicate store writes and bus deliveries | Remove the redundant reactivation and assert exactly one event from the UI path | deferred (coverage/design) | 1 |
| 45 | src-tauri/src/menu/localized/view_menu.rs | 60 | Medium | Logic & Correctness | Close/Focus remain enabled without a split, while Sync Scroll is an always-enabled plain item; clicking it while unsplit mutates latent state and the required checkmark cannot appear | Make Sync Scroll a `CheckMenuItem`, extend menu-state sync with `splitEnabled` and `syncScroll`, disable inapplicable controls, and test transitions | deferred (coverage/design) | 1 |
| 46 | src/components/QuickOpen/useQuickOpenItems.ts | 58 | Medium | Refactoring Debt | Repeated ordered.includes calls make synchronous Quick Open construction quadratic, with no open-tab count cap | Track emitted paths in a companion Set while preserving array order | fixed | 1 |
| 47 | src/components/StatusBar/StatusBarTabStrip.tsx | 55 | Medium | Refactoring Debt | The 174-line component combines store selection, overflow, scrolling, drag/drop, split indication, browser focus, and overflow controls | Extract the browser pill and overflow controls into focused components | deferred (coverage/design) | 1 |
| 48 | src/components/StatusBar/StatusBarTabStrip.tsx | 107 | Medium | Logic & Correctness | Chevron scrolling hardcodes smooth behavior and bypasses the reduced-motion contract | Select auto under prefers-reduced-motion through a shared motion helper | fixed | 2 |
| 49 | src/components/Tabs/useTabContextMenuActions.ts | 50 | Medium | Refactoring Debt | The 242-line hook has 14 callbacks and a 25-entry memo dependency list, tightly coupling availability, effects, and schema | Split action groups into focused hooks and build the schema with a pure function | deferred (coverage/design) | 1 |
| 50 | src/components/Tabs/useTabContextMenuActions.ts | 193 | Medium | Logic & Correctness | Mutable active-tab state is read inside useMemo without a subscription, so native tab switching can leave the item visibly stale | Subscribe to eligibility inputs and include the derived boolean in memo dependencies | fixed | 2 |
| 51 | src/services/persistence/hotExit/restoreTabsHelpers.ts | 123 | Medium | Logic & Correctness | An ID skipped as a path duplicate is not added to seenIds, so a later record reusing that ID can be retained and overwrite duplicateToRetained | Record skipped IDs as aliases of the retained ID before continuing | deferred (pre-existing) | 1 |
| 52 | src/services/persistence/hotExit/restoreTabsHelpers.ts | 187 | Medium | Logic & Correctness | Restore applies only editing_enabled=false, so an explicit true override on a read-only-default format is lost after restart | Preserve a tri-state override or restore explicit true and false values against the live format default | deferred (pre-existing) | 1 |
| 53 | src/services/persistence/hotExit/restoreTabsHelpers.ts | 199 | Medium | Logic & Correctness | active_schema_id is validated against persisted format_id even though file-backed tabs use their current path-derived format, allowing a schema from the wrong format into state | Validate against the newly created tab’s live formatId | deferred (pre-existing) | 1 |
| 54 | src/services/tabs/tabDiskActions.ts | 31 | Medium | Logic & Correctness | Every manual saveToPath false path already emits detailed failure feedback, so this branch always adds a second generic error toast | Give one layer ownership of failure feedback or return a structured silent result | deferred (pre-existing) | 1 |
| 55 | src/stores/settingsStore/shortcutDefinitions.ts | 139 | Medium | Shortcuts & Patches | The new `closePane`, `focusOtherPane`, `syncPaneScroll`, and `lastUsedTab` descriptions lack locale keys, forcing English fallback in every locale and exposing internal D13 terminology | Add translated description keys, remove design-ledger jargon from user-facing copy, and test description-key completeness | deferred (coverage/design) | 1 |
| 56 | src/components/StatusBar/StatusBar.css | 196 | Low | Shortcuts & Patches | Masks and chevrons hardcode physical left/right positioning despite the plan’s logical-properties requirement | Use inline-start/inline-end positioning and direction-aware gradients | deferred (coverage/design) | 1 |
| 57 | src/components/StatusBar/scrollActiveTabIntoView.ts | 32 | Low | Dead Code | activeBrowserPageId is never read and the production caller always supplies null | Remove the field from the interface, caller, and tests | fixed | 2 |
