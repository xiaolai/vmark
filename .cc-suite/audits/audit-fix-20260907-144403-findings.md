# Audit Findings

**Run**: audit-fix 20260907-144403 | **Scope**: branch `fl/feature-ledger-fixes` vs HEAD 75e4c5d86 — every uncommitted code file with non-comment changes (144 production files, 29 five-file batches; comment-only header edits and test files excluded per the mini-audit rule) | **Audit type**: mini
**Model**: gpt-5.6-sol | **Effort**: medium (a 10-file batch at `high` stalled at the 10-minute deadline without a report; five files at medium finish in ~4 min) | **Audit threads**: 29 batches — 01a07a9c-104e-7fb1-a1fa-b8637e53e6fb, 01a07aa0-6881-7633-b7ce-7c9f0bfb6b26, 01a07aa4-b6a1-7250-b2e5-c8e9f0026cd6, 01a07aa8-f536-7dc2-bc22-ea8334ed62dc, 01a07ab2-cf81-70c0-b40e-379ef8c79ebd, 01a07ab6-4474-7621-9069-7310d71afb12, 01a07ab9-91aa-7c60-9313-fa670fd3d98e, 01a07abc-e3d2-7d72-beee-7d2f7171918a, 01a07ac1-9e36-7dd1-8465-0b2b9c39686a, 01a07ac5-67fe-7521-aace-088c71667c3e, 01a07aca-54ed-7d11-9a7d-f514edc89764, 01a07ace-7feb-7ee3-b7ab-4f7f1027103e, 01a07ad1-8fec-7401-879c-b5bad05bca00, 01a07ad5-5c8d-75d0-bc01-d38447ebbae9, 01a07ada-9b39-7cf3-9dc2-55ebbddfc7bb, 01a07ade-6234-7d03-9b99-fdc2e8d38530, 01a07ae1-259c-7cb1-af0e-d07280e39f7e, 01a07ae5-c2c7-7f62-9078-fe7d7fefd70a, 01a07aea-f3af-7b41-9da9-473b27acd35a, 01a07aef-a079-7a51-834b-d6536d9cda13, 01a07af5-fa93-7cb1-b6ef-7bcc0c26c3ec, 01a07afa-ba57-7701-83a4-955bdbb78259, 01a07afe-cdcb-7201-8d44-edacc0f66c3c, 01a07b02-858b-7c70-a323-7594cd5bc4d8, 01a07b06-dbd7-7e40-b03e-9704d3823afe, 01a07b0a-fa22-7351-a399-7d3342ff535b, 01a07b11-1ba5-7bc1-bc11-3f4d5f47a1a1, 01a07b15-24fc-7ff3-8123-80b8c5ace289, 01a07b19-7a07-7fc0-af72-918dc7adb98b
**Scope rule for fixing** (a stated deviation from "fix all"): the audit reads whole files, but this run hardens the plan's OUTPUT. A finding on a `new file` or a `changed line` (within 3 lines of a hunk the branch changed) is fixed at every severity. A finding on a `pre-existing line` the branch did not touch is fixed only when it is a High Logic & Correctness finding that reading confirms is a real defect (`open (defect check)`); refactoring-debt, duplication, dead-code and shortcut findings in untouched code are recorded as `skipped (out of plan scope: pre-existing line)` for a follow-up, so this branch does not grow a refactor of code the plan never named.
**Status values**: open | open (defect check) | fixed | not-fixed | partial | regressed | skipped (out of plan scope: pre-existing line) | skipped (user stop)

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Scope | Status | Round |
|---|------|------|----------|-----------|---------|---------------|-------|--------|-------|
| 1 | e2e/lib/rail.mjs | 54 | Medium | Logic & Correctness | Concurrent `withRailMode` calls can restore stale snapshots and overwrite each other’s setting changes | Serialize mutations per client or reject overlapping scopes. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 2 | e2e/lib/rail.mjs | 54 | High | Refactoring Debt | `withRailMode` exceeds 30 lines and combines snapshotting, mutation, restoration, and error arbitration | Extract snapshot and restoration helpers. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 3 | e2e/lib/rail.mjs | 145 | High | Refactoring Debt | `closeRailInstance` exceeds 30 lines and combines event synthesis, menu validation, action dispatch, and removal polling | Split menu opening, close-action selection, and removal verification. | changed line | fixed | 1 |
| 4 | e2e/lib/rail.mjs | 159 | High | Logic & Correctness | The global menu query can immediately match a stale or concurrently opened menu, causing the helper to close the wrong workspace | Dismiss existing menus first and associate the opened menu with the requested instance. | changed line | fixed | 2 |
| 5 | e2e/lib/rail.mjs | 164 | Medium | Shortcuts & Patches | Close is identified by a hardcoded item count and first-position assumption; harmless menu additions break teardown, while reordering can target another action | Add a stable `data-menu-action="close"` contract and select it directly. | changed line | not-fixed (rebuttal: clickRailMenuClose in e2e/lib/rail.mjs throws on a missing [data-menu-action="close"] hook — there is no item-count or first-position fallback anywhere in the file; the hook is pinned by WorkspaceRailContextMenu.test.tsx; the verdict describes code that is not in the file) [verifier: AGREE] | 2 |
| 6 | e2e/lib/rail.mjs | 188 | High | Logic & Correctness | `restoreRail` only deletes newly created instances; it cannot recreate, reorder, or otherwise restore pre-existing instances removed or mutated by a journey | Snapshot sufficient instance state and implement full restoration, or explicitly reject unsupported mutations before teardown. | changed line | partial: It rejects missing, renamed, and reordered instances, but snapshots only ID/name/active; same-name re-rooting or other state mutations remain undetected. | 3 |
| 7 | e2e/lib/workspace.mjs | 50 | High | Refactoring Debt | `openWorkspaceViaMcp` exceeds 30 lines and mixes three protocol phases, DOM interaction, and verification | Extract request, approval, and retry phases into focused helpers. | changed line | fixed | 1 |
| 8 | e2e/lib/workspace.mjs | 50 | High | Logic & Correctness | Failures after the approval prompt appears leave the dialog or one-shot grant in shared application state, contaminating subsequent journeys | Add failure cleanup in `finally`, including dialog dismissal and grant invalidation where applicable. | changed line | fixed | 3 |
| 9 | e2e/lib/workspace.mjs | 116 | High | Logic & Correctness | The helper waits for the persisted root to become falsy even though its own contract says closing a railed workspace promotes and hydrates a successor; a successor with a root causes a false timeout | Poll for removal/change of the original workspace instance or accept the promoted successor’s expected root. | changed line | fixed | 1 |
| 10 | scripts/baselineRatchetManifest.mjs | 41 | Medium | Refactoring Debt | The 326-line code module exceeds the project’s approximately 300-line limit and centralizes unrelated baseline families | Split entries into domain-specific manifest fragments and compose them in a small root manifest. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 11 | scripts/baselineRatchetManifest.mjs | 77 | High | Logic & Correctness | The comment says dropping a tracked review-schedule key is the forbidden loosening, but ordinary identity mode checks additions only; removal is silently accepted | Use the no-removal polarity and make removed tracked keys fail. | changed line | not-fixed (disputed: removal is enforced by check-review-schedule.mjs, which fails on an entry that is neither tracked nor exempt; the misleading comment was corrected) [verifier: AGREE] | 1 |
| 12 | scripts/baselineRatchetManifest.mjs | 117 | High | Logic & Correctness | `per-key-count` treats new keys as informational, so a new file containing legacy `Result<T, String>` can be added to this baseline in the same change and bypass the ratchet | Use an identity/per-key mode that fails new keys, especially for command-error and test-type baselines. | pre-existing line | fixed | 2 |
| 13 | scripts/baselineRatchetManifest.mjs | 210 | High | Logic & Correctness | Theme protection hardcodes six names; a newly added theme is only reported at the root and its individual failing-pair list remains unratcheted in later changes | Derive per-theme checks from the baseline keys or validate exact parity between theme keys and manifest checks. | pre-existing line | fixed | 2 |
| 14 | scripts/check-deleted-names.mjs | 67 | Medium | Logic & Correctness | Symbol names are interpolated into ERE patterns without escaping; valid TypeScript names containing `$`, or future unusual registry input, can alter or invalidate the regex | Escape ERE metacharacters before interpolation. | changed line | fixed | 1 |
| 15 | scripts/check-deleted-names.mjs | 87 | High | Logic & Correctness | Rust detection misses legal definitions such as `pub(in path)`, `const fn`, and `extern "C" fn`, allowing registered tombstones to reappear undetected | Parse Rust syntax or expand and fixture-test the complete supported item grammar. | changed line | fixed | 2 |
| 16 | scripts/check-deleted-names.mjs | 100 | High | Logic & Correctness | Every `git grep` failure is converted to “no matches,” including invalid regexes, bad pathspecs, missing Git, and repository errors; the safety gate fails open | Return empty only for Git’s no-match status and rethrow every operational error. | changed line | fixed | 1 |
| 17 | scripts/check-deleted-names.mjs | 115 | High | Logic & Correctness | Unknown or malformed registry kinds fall through silently, disabling the affected tombstone while the gate remains green | Validate the complete registry schema and fail on every unsupported or missing field. | changed line | fixed | 1 |
| 18 | scripts/check-deleted-names.mjs | 144 | Medium | Logic & Correctness | `--root` and `--registry` consume the next argument without checking it exists, producing an uncaught exception instead of the documented usage result | Validate option arity before incrementing and exit with code 64 on misuse. | changed line | fixed | 2 |
| 19 | scripts/check-deleted-names.mjs | 160 | Low | Shortcuts & Patches | The remediation message still tells maintainers to remove entries from `check-deleted-names.mjs`, although the registry was moved to `lib/deletedNamesRegistry.mjs` | Point the message at the actual registry file. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 20 | scripts/check-doc-joins.mjs | 55 | High | Duplication | Each join is registered twice — once as a static module and once as a path string — creating parallel sources of truth | Store one descriptor per join or derive reporting paths from module metadata. | new file | fixed | 1 |
| 21 | scripts/check-doc-joins.mjs | 90 | Medium | Logic & Correctness | `result.findings` is iterated before its array contract is validated, so malformed strings are emitted character-by-character before the contract finding appears | Validate the complete result shape before consuming either collection. | new file | fixed | 1 |
| 22 | scripts/check-doc-joins.mjs | 91 | Medium | Shortcuts & Patches | The documented contract requires `info`, but missing or non-array `info` is silently accepted or converted into a generic thrown error | Explicitly validate `info` as an array and emit a precise contract finding. | new file | fixed | 1 |
| 23 | scripts/check-doc-joins.mjs | 105 | High | Logic & Correctness | Main bypasses `loadJoins` and positionally zips two arrays without checking equal length; registry drift can assign wrong paths or omit modules | Make `main` call one canonical loader and validate uniqueness and cardinality. | new file | fixed | 1 |
| 24 | scripts/check-doc-joins.mjs | 123 | Low | Logic & Correctness | Calling `process.exit()` immediately can truncate buffered report output when stdout is piped | Set `process.exitCode` after awaiting `main` and allow Node to drain output naturally. | new file | fixed | 1 |
| 25 | scripts/check-feature-ledger-phase.sh | 66 | Medium | Logic & Correctness | `assert_grep` uses regex matching even though many callers supply literal filenames and punctuation, allowing near-matches to pass | Add a fixed-string helper using `grep -F` and reserve regex helpers for explicit ERE assertions | new file | fixed | 1 |
| 26 | scripts/check-feature-ledger-phase.sh | 82 | High | Logic & Correctness | Rust test discovery can pass when the `#[path]` text is commented out or is not followed by an active module declaration, leaving the critical test undiscovered | Parse active attributes and module declarations or query Cargo test discovery | new file | partial: Comments and missing adjacent module declarations are rejected, but an include inside a disabled enclosing `cfg` or unexpanded macro can still falsely pass. | 3 |
| 27 | scripts/check-feature-ledger-phase.sh | 84 | High | Logic & Correctness | TypeScript test discovery accepts `it(`/`test(` inside comments or strings and does not prove the test is runnable | Parse the test AST or validate against Vitest’s discovered-file inventory | new file | not-fixed: AST scanning excludes comments and strings but still counts unreachable test calls; tier ownership does not prove Vitest collection, and aliases do not preclude runtime collection validation. | 3 |
| 28 | scripts/check-feature-ledger-phase.sh | 88 | Medium | Logic & Correctness | `\S` is not portable POSIX ERE syntax, so evidence detection depends on the installed grep implementation | Replace it with `[[:space:]]`-based syntax | new file | fixed | 1 |
| 29 | scripts/check-feature-ledger-phase.sh | 97 | High | Logic & Correctness | `assert_in_static` searches the concatenation of `check:static` and `check:all`, so a command referenced only by `check:all` passes a check explicitly requiring `check:static`; substring matching can also accept a different script name | Parse the `check:static` command into exact invoked script tokens and inspect only that script | new file | fixed | 1 |
| 30 | scripts/check-feature-ledger-phase.sh | 124 | Medium | Refactoring Debt | The 347-line script exceeds the project’s approximately 300-line limit, and multiple phase functions exceed 30 lines | Move reusable assertions into a library and express phase requirements as compact declarative tables | new file | not-fixed (rebuttal: each phase body is one assertion per DoD item — a bash table has the same line count and hides the assertion kind at the call site; reusable helpers live in scripts/lib/dod-assertions.sh and the new probes went to scripts/dod-syntax.mjs) [verifier: AGREE] | 2 |
| 31 | scripts/check-feature-ledger-phase.sh | 243 | High | Logic & Correctness | The “wired or deleted” provisioning check accepts any existing file that merely lacks `allow(dead_code)`, without establishing production reachability | Check a concrete production import, module registration, or call site | new file | partial: A call site is now required, but calls inside `cfg(test)` or unreachable functions still falsely establish “production” reachability. | 3 |
| 32 | scripts/check-feature-ledger-phase.sh | 299 | High | Logic & Correctness | E2E deliverables are checked only for file existence, so empty or undiscovered journey files satisfy the phase | Validate runner discovery and require an executable journey export or registered test case | new file | fixed | 2 |
| 33 | scripts/check-feature-ledger-phase.sh | 322 | High | Logic & Correctness | The two independent `assert_any` checks can mix contradictory outcomes — for example, code extraction for one assertion and a re-verdict for the other — without completing either outcome coherently | Evaluate the extraction requirements as one atomic branch and the re-verdict requirements as the other | new file | not-fixed (disputed: (A∨R)∧(B∨R) ≡ (A∧B)∨R — a half-done extraction without a re-verdict is already red; pinned by a new test) [verifier: AGREE] | 1 |
| 34 | scripts/check-header-references.mjs | 133 | Medium | Shortcuts & Patches | `--update` catches every baseline-read error, treating malformed or unreadable baselines like a missing initial baseline and potentially overwriting them | Ignore only `ENOENT`; fail on JSON, schema, and permission errors | new file | fixed | 1 |
| 35 | scripts/check-header-references.mjs | 158 | High | Logic & Correctness | `--report` returns success when the baseline is missing, malformed, or unreadable, contradicting the documented fail-closed exit contract | Print the report if requested, then set a nonzero exit code for baseline failures | new file | fixed | 1 |
| 36 | scripts/check-keybinding-manifest.mjs | 1 | Medium | Refactoring Debt | At 899 lines, this file is three times the project guideline and combines TS parsing, Rust parsing, Markdown parsing, YAML parsing, and policy validation | Split each parser and validation leg into focused modules with independent tests | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 37 | scripts/check-keybinding-manifest.mjs | 102 | High | Duplication | `prosemirrorToTauri` is explicitly copied from the application implementation, so the validator and runtime converter can drift together undetected | Move conversion into a side-effect-free shared module or validate the gate implementation against generated runtime fixtures | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 38 | scripts/check-keybinding-manifest.mjs | 155 | High | Refactoring Debt | `scanAccelCalls` is an approximately 80-line high-cyclomatic custom Rust lexer with many coupled parser states | Extract comment, string, call-boundary, and argument-scanning helpers or use a Rust syntax parser | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 39 | scripts/check-keybinding-manifest.mjs | 401 | Medium | Logic & Correctness | `stringField` is not constrained to top-level properties and can read an earlier nested `id`, `label`, or key field as the shortcut’s own value | Track object depth or parse the TypeScript AST | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 40 | scripts/check-keybinding-manifest.mjs | 418 | High | Refactoring Debt | `splitObjectLiterals` is a greater-than-60-line high-cyclomatic lexer duplicating string/comment parsing concerns found elsewhere in the file | Extract a reusable lexical scanner or replace the text parser with the TypeScript compiler API | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 41 | scripts/check-keybinding-manifest.mjs | 543 | High | Logic & Correctness | `arrayBody` stops at the first textual `];` without balancing brackets or skipping strings/comments, allowing a partial array to be parsed while trailing shortcuts disappear silently | Locate the matching closing bracket with a comment/string-aware balanced scanner | changed line | fixed | 1 |
| 42 | scripts/check-keybinding-manifest.mjs | 609 | High | Logic & Correctness | `manifestMenuIds` collapses duplicate `menuId` values without reporting them; two shortcut definitions can target the same native command and still pass when their other fields coincide | Reject duplicate non-dynamic `menuId` values before validation | pre-existing line | fixed | 1 |
| 43 | scripts/check-keybinding-manifest.mjs | 704 | High | Logic & Correctness | Documentation validation uses a global set of accelerators, so a shortcut documented on the wrong command row still satisfies every entry using that chord | Parse rows into command-to-accelerator mappings and compare each manifest entry to its corresponding row | pre-existing line | not-fixed: Documentation remains a global accelerator set; existing label and exemption metadata could support a per-row join without restoring the banned manifest. | 3 |
| 44 | scripts/check-keybinding-manifest.mjs | 711 | Medium | Logic & Correctness | Documentation validation checks `defaultKey` and `defaultKeyOther` but never a distinct `defaultKeyMac`, allowing future macOS-specific documentation drift | Validate the effective macOS key whenever `defaultKeyMac` is present | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 45 | scripts/check-keybinding-manifest.mjs | 803 | High | Logic & Correctness | `menuLabelPairs` matches commented-out builder calls and silently keeps the first duplicate ID, allowing dead or conflicting label mappings to mask the live mapping | Strip comments and reject every duplicate ID unless identical active sites are explicitly supported | pre-existing line | fixed | 2 |
| 46 | scripts/check-mcp-docs.mjs | 42 | High | Logic & Correctness | Only the first action enum in each tool file is extracted, so additional shipped schemas in the same file are completely unchecked | Extract every action enum and reject ambiguous duplicate schemas | changed line | fixed | 1 |
| 47 | scripts/check-mcp-docs.mjs | 43 | High | Logic & Correctness | An unsupported or drifted action-schema shape returns an empty list for that file; the global nonzero check still passes when other tool files yield actions | Detect files containing action declarations that produced no parsed actions and fail closed per file | changed line | partial: Identifier-based schema drift now fails closed, but parenthesized, computed, shorthand, or spread-provided action schemas can evade both scans. | 3 |
| 48 | scripts/check-mcp-docs.mjs | 47 | Medium | Logic & Correctness | The captured valid identifier is interpolated into a regex without escaping; names containing `$` change the regex meaning and cannot be resolved reliably | Escape the identifier before constructing the regex or parse the TypeScript AST | changed line | fixed | 1 |
| 49 | scripts/check-mcp-docs.mjs | 51 | High | Logic & Correctness | Action extraction silently ignores valid string values containing digits, hyphens, uppercase letters, or other characters, potentially producing a false-green gate | Parse all string literals and validate naming separately; fail if an enum contains an unsupported member | changed line | fixed | 1 |
| 50 | scripts/check-mcp-docs.mjs | 67 | Medium | Logic & Correctness | An action passes if its code span appears anywhere in the document, including deprecation text, examples, or statements that it is unsupported | Parse the MCP reference table or action sections and require an affirmative entry for the correct tool | changed line | fixed | 3 |
| 51 | scripts/check-terminal-edge-phase.sh | 15 | Low | Logic & Correctness | Additional positional arguments are silently ignored instead of being rejected as invalid invocation | Require exactly one phase argument | changed line | fixed | 1 |
| 52 | scripts/check-terminal-edge-phase.sh | 36 | High | Logic & Correctness | Both negative grep helpers treat missing or unreadable targets as successful absence checks because grep’s error status enters the `else` branch | Check target existence/readability first and distinguish grep status 1 from status greater than 1 | pre-existing line | fixed | 1 |
| 53 | scripts/check-terminal-edge-phase.sh | 71 | High | Logic & Correctness | The negative `EDITOR` assertion uses non-portable `\s`; on grep implementations treating it literally, an actual whitespace-indented `EDITOR:` line is missed and the phase passes | Use `^[[:space:]]*EDITOR:` | pre-existing line | fixed | 1 |
| 54 | scripts/check-terminal-edge-phase.sh | 115 | Medium | Logic & Correctness | The comment promises checks for eight T10 strings, but the loop checks five and the later assertion covers only one more, leaving at least two named translation gaps unverified | Enumerate all eight expected English source values in one authoritative list | changed line | fixed | 1 |
| 55 | scripts/check-terminal-edge-phase.sh | 118 | Medium | Logic & Correctness | Translation detection depends on exact JSON spacing (`": "`), so reformatted JSON can retain English values while the check passes | Parse each locale as JSON and compare runtime values against the English locale | changed line | fixed | 1 |
| 56 | scripts/check-test-only-modules.mjs | 95 | Medium | Logic & Correctness | Graph validation ignores wildcard entry patterns, so unmatched production roots can silently produce an incomplete reachability graph | Expand every entry glob and require each pattern to match at least one file | new file | fixed | 1 |
| 57 | scripts/check-test-only-modules.mjs | 133 | High | Logic & Correctness | `runKnip` accepts JSON-looking stdout from any failed invocation, including crashes or invalid configuration, and may treat it as valid findings | Accept only Knip’s documented findings exit status and validate the complete reporter schema | new file | fixed | 2 |
| 58 | scripts/check-test-only-modules.mjs | 145 | Medium | Shortcuts & Patches | Baseline entries are not validated as unique strings, and malformed JSON/I/O errors bypass the gate’s documented controlled-failure path | Add schema validation and catch baseline read/write failures in `main`, returning exit 2 | new file | fixed | 2 |
| 59 | scripts/check-test-only-modules.mjs | 195 | Medium | Logic & Correctness | `--update` writes all current findings, including regressions, despite the stated “entries only leave” ratchet | Refuse additions against an existing baseline unless a separate explicit remeasurement option is supplied | new file | fixed | 2 |
| 60 | scripts/check-ui-phase.sh | 21 | Medium | Logic & Correctness | A failed `cd` is ignored because `errexit` is disabled, allowing checks to run against an unrelated working directory | Use `cd ... // exit 2` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 61 | scripts/check-ui-phase.sh | 23 | Low | Shortcuts & Patches | Only the first positional argument is examined; extra arguments are silently ignored | Require exactly one argument | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 62 | scripts/check-ui-phase.sh | 51 | Medium | Logic & Correctness | Package and source assertions use unrestricted substring regex matching, so comments, obsolete text, or similarly named scripts can satisfy the checks | Parse `package.json` structurally and use exact source-aware assertions | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 63 | scripts/check-ui-phase.sh | 89 | High | Logic & Correctness | Phase 0 only verifies that gate self-test files exist; broken or skipped self-tests still allow the phase to pass | Execute the relevant gate-tier test files | pre-existing line | fixed | 2 |
| 64 | scripts/check-ui-phase.sh | 117 | Low | Duplication | The six-theme list is duplicated in phases 0 and 1 and can drift | Define one shared `THEMES` array | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 65 | scripts/check-ui-phase.sh | 132 | High | Logic & Correctness | Reduced-motion behavior is treated as complete when its test file merely exists; the test is never executed | Run the targeted reduced-motion Vitest file | pre-existing line | fixed | 2 |
| 66 | scripts/check-ui-phase.sh | 169 | High | Logic & Correctness | Accessibility completion is reduced to detecting a filename; failing, empty, or skipped axe suites still pass | Discover and execute all accessibility suites, requiring at least one successful test | pre-existing line | fixed | 3 |
| 67 | scripts/check-ui-phase.sh | 169 | Medium | Logic & Correctness | Bash `globstar` is not enabled, so `**` does not recursively discover deeply nested accessibility suites | Enable `globstar` or use `find` with an exact filename pattern | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 68 | scripts/gen-feature-ledger.mjs | 80 | High | Logic & Correctness | The shared subprocess wrapper converts every failure into an empty result; `find` and `git` failures consequently become authoritative zero counts or `--` values | Return structured success/failure results and abort when required measurements fail | changed line | fixed | 1 |
| 69 | scripts/gen-feature-ledger.mjs | 103 | High | Refactoring Debt | `parseSettingsDefaults` is a formatting-dependent, high-branch line parser over TypeScript; harmless formatting or deeper nesting makes valid settings disappear | Parse the object with the TypeScript AST and split traversal into focused helpers | changed line | partial: AST recursion removes indentation and depth dependence, but nested type/parenthesis wrappers remain unhandled and the script is now 424 lines, above the project limit. | 3 |
| 70 | scripts/gen-feature-ledger.mjs | 191 | Medium | Logic & Correctness | File enumeration excludes `.js`, `.jsx`, `.mts`, and `.mjs`, although the code measurement explicitly includes JavaScript and JSX | Use one shared extension definition for code and file/test metrics | changed line | fixed | 1 |
| 71 | scripts/gen-feature-ledger.mjs | 191 | Medium | Logic & Correctness | Overlapping feature paths produce duplicate `find` results, inflating source-file, test-file, and test-line counts | Normalize and deduplicate paths and returned filenames | changed line | fixed | 1 |
| 72 | scripts/gen-feature-ledger.mjs | 198 | High | Refactoring Debt | `main` spans nearly 200 lines and combines argument parsing, validation, subprocess measurement, joins, rendering, and filesystem writes with high cyclomatic complexity | Extract argument, spine-validation, baseline-loading, per-feature measurement, and rendering functions | changed line | fixed | 1 |
| 73 | scripts/gen-feature-ledger.mjs | 201 | Low | Shortcuts & Patches | Unknown command-line arguments are silently ignored and malformed `--since` values are accepted | Implement strict argument parsing and reject unsupported or empty options | changed line | fixed | 1 |
| 74 | scripts/gen-feature-ledger.mjs | 210 | High | Logic & Correctness | Spine validation checks only path existence; an empty directory or path containing no supported source files passes and yields a misleading measured zero | Require every feature path to contribute at least one supported file and report empty matches | changed line | fixed | 2 |
| 75 | scripts/gen-feature-ledger.mjs | 239 | High | Logic & Correctness | Join validation checks only whether parsed containers are nonempty, not whether records have the expected schema; incompatible nonempty shapes can still generate all-zero columns | Validate every baseline’s container and required record fields before joining | changed line | fixed | 1 |
| 76 | scripts/gen-feature-ledger.mjs | 247 | Medium | Logic & Correctness | The generator permanently rejects a legitimately cleaned, zero-entry baseline, preventing the debt ratchets from reaching zero | Distinguish a valid empty schema from a missing or malformed payload | changed line | fixed | 1 |
| 77 | scripts/gen-feature-ledger.mjs | 266 | Low | Logic & Correctness | `split("\n").length` counts a trailing newline as an additional test line, inflating virtually every normal text file | Count newline records consistently with `wc -l` or remove one terminal empty element | changed line | fixed | 1 |
| 78 | scripts/gen-feature-ledger.mjs | 270 | High | Logic & Correctness | Coverage is reported when only one matching source file is present in a stale or partial coverage summary, misrepresenting it as feature-wide coverage | Compare coverage keys with the feature’s complete production-file set and emit `--` plus completeness metadata when coverage is partial | changed line | fixed | 1 |
| 79 | scripts/gen-feature-ledger.mjs | 329 | Medium | Logic & Correctness | User-supplied `--since` text and feature-map strings are inserted into Markdown without escaping, allowing broken tables or injected generated sections | Validate the date expression and Markdown-escape all interpolated cells and prose values | changed line | fixed | 2 |
| 80 | scripts/lib/headerReferenceTargets.mjs | 65 | High | Logic & Correctness | `statSync` follows directory symlinks, so `walkSources` and `buildTailIndex` can escape scan roots or recurse forever through a symlink cycle | Use `lstatSync`, reject directory symlinks, or track visited `realpath` values and enforce root containment | new file | fixed | 1 |
| 81 | scripts/lib/headerReferenceTargets.mjs | 145 | Medium | Shortcuts & Patches | Malformed package manifests are silently ignored, potentially converting a configuration error into misleading unresolved-dependency findings — or no finding if no dependency is referenced | Surface the manifest path and JSON parse error to the caller | new file | fixed | 1 |
| 82 | scripts/lib/headerReferenceTargets.mjs | 200 | Low | Logic & Correctness | `rel.startsWith("..")` rejects valid root-relative names such as `..config`; it is not a path-segment-aware containment check | Reject only `rel === ".."` or `rel.startsWith("../")`, preferably through a shared containment helper | new file | fixed | 1 |
| 83 | scripts/lib/headerReferenceTargets.mjs | 254 | Medium | Logic & Correctness | Repeated `super::` segments can climb above `src-tauri/src`; the resolver may then accept unrelated repository files or directories as Rust modules | Stop traversal at the Rust source root and return unresolved when another `super` would escape it | new file | fixed | 1 |
| 84 | scripts/lib/headerReferences.mjs | 62 | Medium | Logic & Correctness | The ad-hoc comment scanner still treats a block-comment-shaped template-literal line beginning with `/*` as a real file comment, contradicting its stated template-literal protection | Restrict parsing to the leading header or use a language-aware tokenizer | new file | fixed | 3 |
| 85 | scripts/lib/headerReferences.mjs | 180 | Medium | Logic & Correctness | Maintainer-doc availability is inferred from the existence of the `dev-docs` directory alone, exposing the known transient-fixture TOCTOU race | Require the stable `dev-docs/README.md` marker or snapshot availability before scanning | new file | fixed | 1 |
| 86 | scripts/lib/headerReferences.mjs | 202 | High | Logic & Correctness | Any occurrence of `@generated` in the first 20 lines — even in a string or “not generated” comment — silently excludes the entire file from the gate | Recognize an exact generated-header directive through the comment grammar | new file | fixed | 1 |
| 87 | scripts/lib/headerReferences.mjs | 205 | High | Refactoring Debt | `collectFindings` exceeds 30 lines and contains four nested traversal/decision levels over scanning, resolution, statistics, and reporting state | Extract file scanning and per-reference accounting helpers | new file | fixed | 1 |
| 88 | scripts/lib/headerReferences.mjs | 205 | Medium | Logic & Correctness | The injectable `trees` option controls only traversal; `resolutionContext` and `treeFor` still use global `TREES`, so custom-tree results are resolved against a different graph | Thread the supplied trees through context construction, indexing, dependency discovery, and tree lookup | new file | fixed | 1 |
| 89 | scripts/lib/headerReferences.mjs | 258 | Medium | Logic & Correctness | Baseline validation accepts unknown kinds and empty file/target components as long as the key contains two pipe characters | Require a nonempty file and target and validate the kind against `KINDS` | new file | fixed | 1 |
| 90 | scripts/lib/keybindingFormat.mjs | 28 | Medium | Logic & Correctness | Empty interior or leading tokens are silently discarded, so malformed shortcuts such as `Mod--Shift-A` and `-Mod-A` are normalized into apparently valid documentation | Reject empty tokens except the single terminal empty token representing the minus key | new file | fixed | 1 |
| 91 | scripts/lib/keybindingFormat.mjs | 44 | Low | Logic & Correctness | “Single letters” are uppercased only for ASCII; Unicode shortcuts such as `é` remain lowercase | Detect Unicode letters with a Unicode property expression and apply `toLocaleUpperCase` or `toUpperCase` | new file | fixed | 2 |
| 92 | scripts/lib/ptyRun.py | 25 | High | Refactoring Debt | `main` is over 60 lines, has nesting beyond three levels, and combines parsing, PTY lifecycle, input delivery, output draining, timeout enforcement, and status conversion | Extract argument validation, deadline-aware input writing, transcript draining, process termination, and exit-status conversion | new file | fixed | 1 |
| 93 | scripts/lib/ptyRun.py | 42 | Medium | Logic & Correctness | Transcript capture is unbounded, so a noisy child can consume arbitrary memory before timeout | Stream output directly or enforce a documented maximum transcript size | new file | fixed | 1 |
| 94 | scripts/lib/ptyRun.py | 43 | High | Logic & Correctness | A single blocking `os.write` occurs before the deadline starts and ignores partial writes; large input or a child that does not read stdin can hang forever or receive truncated input | Start the deadline first and loop over nonblocking writes while honoring timeout and partial-write results | new file | fixed | 1 |
| 95 | scripts/lib/ptyRun.py | 51 | High | Logic & Correctness | Any PTY read error is treated as proof the child exited, followed by blocking `waitpid` at line 82; a child that closes its slave descriptors but remains alive bypasses the timeout indefinitely | Distinguish expected PTY `EIO`, poll the child with `WNOHANG`, and retain deadline enforcement until it actually exits | new file | fixed | 1 |
| 96 | scripts/lib/ptyRun.py | 75 | High | Logic & Correctness | Timeout kills only the immediate child and does not reap it; subprocesses can survive and retain resources | Terminate the PTY child’s process group, escalate if necessary, and always call `waitpid` | new file | fixed | 2 |
| 97 | scripts/lib/ptyRun.py | 85 | Medium | Logic & Correctness | `waitstatus_to_exitcode` returns negative signal numbers, which `SystemExit` converts modulo 256 — for example SIGTERM becomes 241 instead of the conventional 143 | Convert negative results to `128 + signal`; make the fallback handle signaled and stopped statuses explicitly | new file | fixed | 1 |
| 98 | server/content/src/cliMain.ts | 55 | Medium | Refactoring Debt | `parseArgs` exceeds 30 lines and uses a growing conditional dispatcher that mixes recognition, duplicate detection, value consumption, conversion, and validation | Replace it with a declarative flag table or small per-option parsers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 99 | server/content/src/cliMain.ts | 168 | Medium | Logic & Correctness | Any occurrence of `--version` bypasses parsing, even when it is another flag’s value; `--root --version` prints the version instead of reporting a missing root value | Parse `--version` as an ordinary flag and permit the early return only after validating the complete argument structure | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 100 | server/mcp/src/index.ts | 83 | High | Duplication | Tool names and action counts are manually repeated across registration calls, comments, module prose, and `TOOL_CATEGORIES`; the comment documents that this metadata has already drifted repeatedly, while the derived count checks only category entries rather than registry identity | Define one typed tool registry containing registrar, category metadata, and action constants; derive registration, health data, counts, and descriptions from it | changed line | partial: Registration, names, categories, and counts derive from one registry, but descriptions still manually enumerate actions and can drift independently. | 3 |
| 101 | src-tauri/src/app_setup.rs | 26 | High | Logic & Correctness | Hashing hostname, OS, and architecture does not create a reliable anonymous machine identity: common hostnames collide, hostnames are guessable, and renaming the machine changes the ID; tests merely pin this flawed recipe | Generate and persist a cryptographically random installation identifier, with an explicit migration/versioning policy | pre-existing line | not-fixed: Changing the published telemetry identity requires an explicit migration/product decision; the low-entropy, rename-unstable hash remains. | 3 |
| 102 | src-tauri/src/app_setup.rs | 43 | Medium | Refactoring Debt | `setup_app` spans 86 lines and combines state construction, menus, migration cleanup, CLI argument ingestion, runtime probes, platform warnings, and event registration | Extract cohesive helpers such as `initialize_managed_state`, `queue_cli_files`, and `register_ready_listener` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 103 | src-tauri/src/app_setup.rs | 88 | Medium | Logic & Correctness | A poisoned `FILE_OPEN_STATE` mutex silently discards every CLI-supplied file, unlike the module’s other lock sites which recover from poisoning | Recover with `unwrap_or_else(/p/ p.into_inner())` and log the poisoned-state condition | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 104 | src-tauri/src/app_setup.rs | 119 | Medium | Shortcuts & Patches | Invalid ready-event payloads are silently ignored, leaving the affected window permanently unready with queued menu events potentially stranded | Log parse failures and validate that the supplied label resolves to the emitting/existing window before changing readiness state | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 105 | src-tauri/src/bin/pdf_smoke/main.rs | 83 | Medium | Logic & Correctness | The fixed 500 ms sleep neither proves that window teardown completed nor reports a leak; slow systems can exit while windows remain, while the comment claims a steady state | Poll the actual window count with a bounded deadline and fail the smoke run if it does not return to baseline | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 106 | src-tauri/src/bin/pdf_smoke/main.rs | 114 | Medium | Logic & Correctness | `render` converts `CommandError` into `String`, discarding the structured error code required to distinguish an expected validation refusal from an unrelated backend failure | Return `Result<(), CommandError>` and format the error only at the reporting boundary | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 107 | src-tauri/src/bin/pdf_smoke/main.rs | 118 | Medium | Logic & Correctness | `to_string_lossy()` can silently change a valid non-UTF-8 output path on Unix, causing the harness to render to the wrong path or fail misleadingly | Keep output paths as `Path`/`PathBuf` through an internal renderer API and reject non-UTF-8 paths explicitly if the production boundary truly requires UTF-8 | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 108 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 32 | High | Duplication | A4 geometry is copied from `scenarios.rs`, creating two independently maintained definitions for the same fixture | Define shared page specifications once in the smoke fixture module | new file | fixed | 2 |
| 109 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 49 | Medium | Logic & Correctness | Unparseable progress payloads produce only a warning and are omitted from captured state; the refused-path case can therefore pass despite emitting malformed events | Record parse errors in `Capture` and make every parse failure count as a smoke failure | new file | fixed | 1 |
| 110 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 95 | High | Duplication | The 51-line `run` function repeats the capture, operation, stop, and assertion lifecycle across cases, making listener cleanup and error handling easy to diverge | Extract a helper that owns capture lifetime and returns both the operation result and captured events | new file | fixed | 1 |
| 111 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 130 | Medium | Logic & Correctness | A PID-derived temporary directory is not guaranteed absent after a crash or PID reuse, and this case omits the explicit existence guard already used by `scenarios::bad_path` | Use a unique temporary-directory facility or fail before rendering when the directory already exists | new file | fixed | 1 |
| 112 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 133 | High | Logic & Correctness | The safety-critical “refused up front” assertion accepts any error, so an unrelated pre-render failure with no events falsely proves that an invalid destination was rejected by path validation | Preserve `CommandError` and require the exact expected `NotFound` error code in addition to an empty stage list | new file | fixed | 1 |
| 113 | src-tauri/src/content_server/bundle_manifest.rs | 27 | Medium | Shortcuts & Patches | Production bundling is hard-disabled with `None` while the comments leave the actual product decision unresolved, so packaged Knowledge Base remains unavailable and correctness depends on a later manual constant flip | Resolve the decision now: add an atomic build-and-bundle pipeline and set `Some(...)`, or explicitly remove/gate the packaged feature instead of retaining an interim sentinel | new file | not-fixed (convention: BUNDLED_CLI_RESOURCE = None is the D1 interim, a maintainer decision; bundle_manifest.test.rs gates the flip both ways) [verifier: AGREE] | 1 |
| 114 | src-tauri/src/content_server/commands.rs | 109 | Medium | Shortcuts & Patches | Stop always reports success while silently discarding kill, reap, and port-file deletion failures, potentially leaving an orphaned process or stale artifact | Return cleanup failures from explicit stop operations and at minimum log failures on best-effort shutdown paths | changed line | fixed | 2 |
| 115 | src-tauri/src/content_server/commands.rs | 124 | High | Refactoring Debt | `content_server_start` spans 117 lines and combines reuse policy, trust migration, runtime discovery, process spawning, polling, race resolution, cleanup, registration, and supervision; its high cyclomatic complexity is already hiding lifecycle races | Split it into focused runtime-resolution, bootstrap/polling, and atomic-registration helpers | pre-existing line | fixed | 1 |
| 116 | src-tauri/src/content_server/commands.rs | 130 | High | Logic & Correctness | The `get`-then-`stop_server` trust restart is a TOCTOU race: another request can replace the observed generation before `stop_server` runs, causing this request to kill the newer server | Add an atomic manager operation that removes only the observed generation or serializes restart decisions per workspace | pre-existing line | fixed | 1 |
| 117 | src-tauri/src/content_server/commands.rs | 144 | Medium | Logic & Correctness | This async command performs blocking executable discovery, filesystem operations, process spawning, and child `wait()` calls directly on an async-runtime worker | Move blocking lifecycle work into `spawn_blocking`, leaving only timed polling asynchronous | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 118 | src-tauri/src/content_server/commands.rs | 147 | Medium | Logic & Correctness | Concurrent starts share one workspace-derived port-file, allowing children to overwrite each other’s token and losing requests to delete the winning server’s file | Use a unique per-attempt port-file containing the token or generation, then transfer ownership of that exact file upon registration | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 119 | src-tauri/src/content_server/commands.rs | 165 | Medium | Shortcuts & Patches | All port-file read and JSON errors plus `Child::try_wait` errors are discarded, so permission failures, malformed output, and polling failures are misreported ten seconds later as a generic timeout | Retain and surface the last non-transient error while tolerating only expected not-found/partial-write states during bootstrap | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 120 | src-tauri/src/content_server/commands.rs | 193 | High | Logic & Correctness | Both this timeout recovery and `RegisterOutcome::Existing` return a concurrent winner without verifying that its `trusted` value matches the request, so simultaneous starts with opposite trust values can return a server enforcing the wrong CSP | Make registration trust-aware and retry/restart when the winner has incompatible trust | pre-existing line | fixed | 1 |
| 121 | src-tauri/src/content_server/manager.rs | 125 | High | Logic & Correctness | `register_or_existing` holds the global manager mutex while killing and waiting for a child; `shutdown_all` and failure cleanup repeat this pattern, allowing one slow reap to block status, stop, start, and supervision for every workspace | Decide and detach records under the mutex, then perform process and filesystem cleanup after releasing it | changed line | fixed | 1 |
| 122 | src-tauri/src/content_server/manager.rs | 129 | High | Logic & Correctness | Failed kill/reap operations are ignored and ownership is then discarded; an unkillable losing or unpollable child can remain alive after the manager forgets it, permitting multiple servers for one workspace | Preserve failed-cleanup children for retry or return a cleanup outcome, and do not discard ownership until termination is confirmed | changed line | partial: Failed children retain handles for a shutdown retry, but a replacement is immediately registered, allowing the still-running orphan and current server to coexist until exit. | 3 |
| 123 | src-tauri/src/content_server/manager.rs | 129 | High | Duplication | Kill/reap/port-file cleanup is copy-pasted across registration rejection, shutdown, poll failure, and `commands.rs`, with inconsistent logging and error propagation | Centralize managed-child teardown in one helper with an explicit cleanup result | changed line | fixed | 1 |
| 124 | src-tauri/src/content_server/mod.rs | 26 | Medium | Dead Code | Three production modules are admitted to have no production caller and use `#[allow(dead_code)]` to suppress that fact, leaving a dormant provisioning subsystem that can silently decay | Wire the upgrade path behind a concrete feature boundary or remove/archive it until it has a production caller | changed line | not-fixed (convention: the provisioning trio is WI-FL3.6's D1-blocked half) [verifier: AGREE] | 1 |
| 125 | src-tauri/src/content_server/runtime.rs | 72 | Low | Shortcuts & Patches | `node_path` is interpolated into a single-line machine-inspected log without quoting or escaping; spaces make it ambiguous and a valid Unix path containing a newline can forge additional log lines | Encode structured fields or format the path with escaped debug/JSON representation | new file | fixed | 1 |
| 126 | src-tauri/src/content_server/runtime.rs | 134 | Medium | Logic & Correctness | The startup probe detaches and discards its task handle, so cancellation or panic produces no diagnostic and the promised startup runtime line is not guaranteed before shutdown | Retain/observe the handle during startup or wrap the task body with explicit failure logging | new file | fixed | 2 |
| 127 | src-tauri/src/content_server/slidev_commands.rs | 41 | High | Shortcuts & Patches | Both Tauri commands return the forbidden legacy `Result<_, String>`, bypassing the project’s structured `CommandError` contract and command-error ratchet | Return `Result<String, CommandError>` and map missing state, transport failures, HTTP failures, and decoding failures to appropriate error categories | changed line | fixed | 1 |
| 128 | src-tauri/src/content_server/slidev_commands.rs | 42 | High | Logic & Correctness | Server metadata and bootstrap token are fetched under separate locks; a restart between calls can pair an old port with a new token and send that token to the wrong loopback listener | Add one manager method returning a generation-consistent server/token snapshot atomically | changed line | fixed | 1 |
| 129 | src-tauri/src/content_server/slidev_commands.rs | 42 | High | Duplication | Preview and export duplicate manager lookup, token lookup, base URL construction, client construction, authorization setup, request sending, and error-body handling | Extract a generation-consistent authenticated content-server client/request helper | changed line | fixed | 1 |
| 130 | src-tauri/src/content_server/slidev_commands.rs | 49 | High | Logic & Correctness | Both commands create a default reqwest client with no request timeout, so an accepted but non-responsive loopback connection can leave the Tauri command pending indefinitely | Reuse a configured client with bounded connect and total-request timeouts | changed line | fixed | 1 |
| 131 | src-tauri/src/content_server/slidev_commands.rs | 69 | Medium | Logic & Correctness | The nonce-mint response is deserialized without checking HTTP status; authentication and server failures become misleading JSON-decoding errors | Call `error_for_status` or explicitly map the status and bounded response body before deserialization | changed line | fixed | 1 |
| 132 | src-tauri/src/content_server/slidev_commands.rs | 116 | High | Logic & Correctness | The module’s only tests cover enum spelling; critical HTTP paths have no tests for absent servers/tokens, restarts, timeouts, non-2xx responses, malformed mint payloads, or transport failures | Add mock-server tests covering success and every failure/race boundary for both commands | changed line | fixed | 3 |
| 133 | src-tauri/src/content_server/spawn.rs | 92 | Low | Shortcuts & Patches | Bundled-resource resolution errors are discarded with `.ok()`, causing packaging/configuration failures to masquerade as an unprovisioned runtime | Preserve and surface the resolution error when the bundled candidate is configured | changed line | fixed | 1 |
| 134 | src-tauri/src/content_server/spawn.rs | 142 | High | Logic & Correctness | The critical process-spawning and stream-supervision path has no direct test; tests cover only CLI path resolution | Add subprocess tests covering successful spawn, spawn failure, stdout/stderr draining, and child cleanup | changed line | fixed | 2 |
| 135 | src-tauri/src/content_server/spawn.rs | 162 | High | Logic & Correctness | `BufRead::lines().map_while(Result::ok)` permanently stops draining after invalid UTF-8 or any read error; the child can then fill its pipe and deadlock, while an unbounded line can exhaust memory | Read bounded byte chunks, convert with `from_utf8_lossy`, continue or explicitly report recoverable errors, and cap buffered line length | changed line | fixed | 1 |
| 136 | src-tauri/src/content_server/spawn.rs | 183 | High | Logic & Correctness | The critical monitor loop and crash-event emission have no direct test, leaving generation races and notification behavior unverified | Test running, stale-generation, intentional-stop, unexpected-exit, and emission-failure paths with an injectable poll/event abstraction | changed line | fixed | 3 |
| 137 | src-tauri/src/dock_recent.rs | 23 | Medium | Logic & Correctness | The command returns `()` and silently degrades to a log-only no-op whenever execution is not on the main thread, making failure invisible to the caller and dependent on IPC-thread behavior | Dispatch registration explicitly onto Tauri’s main thread and return `Result<(), CommandError>` | changed line | fixed | 1 |
| 138 | src-tauri/src/dock_recent.rs | 46 | Medium | Logic & Correctness | `exists()` accepts directories and other non-file objects even though the API promises to register a document | Require an appropriate regular file using metadata and return a typed invalid-path error otherwise | changed line | fixed | 1 |
| 139 | src-tauri/src/dock_recent.rs | 54 | Medium | Logic & Correctness | Tests exercise only refusal branches; the successful AppKit registration path is completely untested | Add a main-thread macOS integration test that verifies the recent-document controller receives the URL | changed line | not-fixed: A dedicated main-thread macOS integration harness remains possible; the injected registrar never exercises `NSDocumentController`. | 3 |
| 140 | src-tauri/src/file_tree.rs | 36 | Medium | Logic & Correctness | macOS-native hidden files marked with `UF_HIDDEN` but lacking a dot prefix are reported visible | Inspect macOS file flags through `MetadataExt` and include `UF_HIDDEN` in hidden detection | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 141 | src-tauri/src/file_tree.rs | 42 | Medium | Logic & Correctness | Windows metadata failures fail open, exposing HIDDEN/SYSTEM entries as visible without logging or propagating the failure | Return a fallible result or conservatively classify the entry as hidden while logging the metadata error | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 142 | src-tauri/src/genies/commands.rs | 18 | Medium | Refactoring Debt | `get_genies_dir` exposes the legacy `Result<T, String>` command contract, losing stable machine-readable error codes | Return `Result<String, CommandError>` and map path-resolution failures to the appropriate `ErrorCode` | changed line | fixed | 1 |
| 143 | src-tauri/src/genies/commands.rs | 25 | Medium | Refactoring Debt | `list_genies` exposes the legacy `Result<T, String>` command contract | Return `Result<Vec<GenieEntry>, CommandError>` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 144 | src-tauri/src/genies/commands.rs | 25 | High | Logic & Correctness | A synchronous Tauri command performs an uncapped recursive filesystem scan, allowing a large or deeply nested genie tree to stall IPC, consume unbounded memory, or overflow the stack | Make the command async, use `spawn_blocking`, impose depth/entry limits, and traverse iteratively | pre-existing line | fixed | 2 |
| 145 | src-tauri/src/genies/commands.rs | 30 | Medium | Logic & Correctness | Scan failures are swallowed by `scan_genies_dir`, so an unreadable or partially unreadable directory is returned as a successful empty/incomplete list | Make scanning return structured errors or warnings and propagate meaningful failures through `CommandError` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 146 | src-tauri/src/genies/commands.rs | 34 | Medium | Logic & Correctness | Sorting solely by `name` is nondeterministic for equal stems because entries originate from a randomized `HashMap`; duplicate names can reorder between runs | Add deterministic tie-breakers such as category, kind, and normalized path | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 147 | src-tauri/src/genies/commands.rs | 48 | Medium | Refactoring Debt | `read_genie` exposes the legacy `Result<T, String>` command contract | Return `Result<GenieContent, CommandError>` and map blocked paths, I/O failures, and parse failures to distinct codes | pre-existing line | fixed | 1 |
| 148 | src-tauri/src/genies/commands.rs | 68 | High | Logic & Correctness | The synchronous command reads an arbitrarily large file wholly into memory, allowing a crafted genie to freeze or exhaust the application | Check metadata against a documented size limit and perform bounded reading in `spawn_blocking` | changed line | fixed | 2 |
| 149 | src-tauri/src/genies/commands.rs | 71 | Medium | Logic & Correctness | Parser dispatch uses the caller-supplied path’s extension rather than the canonicalized target, so an in-tree symlink with a different suffix is parsed using the wrong format | Derive the extension from `requested`, or reject symlinks entirely | changed line | fixed | 1 |
| 150 | src-tauri/src/genies/commands.rs | 84 | Medium | Refactoring Debt | `parse_workflow_genie` exceeds 50 lines and combines YAML parsing, metadata extraction, fallback policy, filename derivation, and object construction | Extract YAML string lookup/fallback and workflow-metadata construction helpers | changed line | fixed | 1 |
| 151 | src-tauri/src/genies/commands.rs | 96 | Low | Logic & Correctness | A whitespace-only `description` suppresses the fallback to YAML `name`, producing a visually blank picker description | Trim the description before testing emptiness and normalize the stored value | changed line | fixed | 1 |
| 152 | src-tauri/src/genies/install.rs | 107 | Medium | Logic & Correctness | Parent directories are followed without symlink validation, so a pre-existing category symlink can redirect installation outside the genies directory | Reject symlinked path components and verify each canonical parent remains beneath the canonical base | changed line | fixed | 1 |
| 153 | src-tauri/src/genies/install.rs | 113 | High | Logic & Correctness | `create_new` reserves the final pathname before content is complete; a write failure or crash leaves a truncated file that every future launch permanently treats as installed | Write and sync a temporary file in the same directory, publish it with a no-clobber atomic operation, and clean up failed writes | pre-existing line | fixed | 3 |
| 154 | src-tauri/src/genies/install.rs | 122 | Medium | Logic & Correctness | Every `AlreadyExists` error is treated as a valid installed genie, including an existing directory, symlink, socket, or other non-regular object | Inspect the existing target and skip only an acceptable regular file; return an error for other file types | changed line | fixed | 2 |
| 155 | src-tauri/src/lib.rs | 104 | Medium | Refactoring Debt | `run` spans 153 lines and combines platform policy, plugin configuration, managed state, protocol registration, debug tooling, and process startup | Extract focused builder helpers for core plugins, managed state, platform-specific single-instance setup, and debug tooling | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 156 | src-tauri/src/lib.rs | 127 | High | Logic & Correctness | The critical Linux startup path treats any nonempty `DBUS_SESSION_BUS_ADDRESS` as proof of a usable bus; malformed, stale, or unreachable addresses still register a dependency that unwraps its connection and can abort startup, while tests cover only string presence | Probe/validate the bus connection before registration or replace the plugin initialization with a fallible guard and add a Linux integration test for invalid and unreachable addresses | changed line | fixed | 4 |
| 157 | src-tauri/src/lib.rs | 240 | High | Logic & Correctness | `.base_port(9323)` does not pin port 9323: plugin v0.12 scans up to 99 subsequent ports when occupied, while the documented driver connects specifically to 9323, allowing E2E to fail or contact an unrelated listener | Make binding fail closed on exactly 9323, or publish the selected port and require the driver to discover and verify it | pre-existing line | not-fixed: The bind-and-release preflight is TOCTOU, so port 9323 can be taken before plugin binding and the plugin can still silently scan to 9324. | 3 |
| 158 | src-tauri/src/macos_menu.rs | 21 | High | Duplication | `fix_help_menu` and `fix_window_menu` duplicate main-thread acquisition, application/menu lookup, positional indexing, submenu extraction, and registration flow | Extract a shared checked submenu lookup by offset and keep only the final Help/Window registration distinct | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 159 | src-tauri/src/macos_menu.rs | 117 | Medium | Logic & Correctness | The global map is keyed only by display title, so equal titles overwrite each other; for example, a recent file and recent workspace with the same basename can force both native items to receive whichever icon was inserted last | Preserve menu identity during traversal, or key by submenu path plus title and apply icons within the corresponding native submenu | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 160 | src-tauri/src/macos_menu.rs | 124 | Medium | Shortcuts & Patches | Failures reading root or nested menu items are silently converted into an empty or partial icon map, making menu corruption indistinguishable from legitimate unmapped items | Return a `Result`, log each failed traversal with menu context, and avoid reporting successful application after partial failure | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 161 | src-tauri/src/macos_menu.rs | 149 | High | Duplication | `collect_icons_from_item` and `collect_icons_from_item_in_submenu` duplicate nearly the entire recursive `MenuItemKind` traversal and can drift independently | Replace both with one recursive collector carrying `Option<&str>` submenu context | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 162 | src-tauri/src/macos_menu.rs | 227 | Low | Refactoring Debt | `apply_menu_icons` unexpectedly repairs window traffic lights, coupling menu decoration to unrelated window-chrome behavior and repeating that workaround after every dynamic menu refresh | Move traffic-light repair to the caller that actually changes the main menu, or rename and document a higher-level chrome-refresh operation | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 163 | src-tauri/src/macos_menu_icons.rs | 206 | Medium | Logic & Correctness | Predefined-item icons are keyed by English display titles, so localized AppKit titles such as Cut/Copy/Paste do not resolve despite the module’s i18n-safety claim | Resolve predefined items by stable identifiers or item kind rather than localized title text | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 164 | src-tauri/src/mcp_bridge/bind.rs | 22 | Medium | Shortcuts & Patches | Newly introduced code returns legacy `Result<_, String>`, contrary to the project’s typed `CommandError` contract and ratchet direction | Return `Result<(TcpListener, u16), CommandError>` with stable error codes and source details | new file | fixed | 2 |
| 165 | src-tauri/src/mcp_bridge/server.rs | 1 | Medium | Refactoring Debt | The file is 486 lines, substantially exceeding the project’s ~300-line limit | Extract protocol parsing and request execution into separate modules | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 166 | src-tauri/src/mcp_bridge/server.rs | 46 | Medium | Refactoring Debt | `start_bridge` spans 83 lines and mixes binding, credential refresh, lifecycle registration, retry policy, and task spawning | Extract initialization and accept-loop helpers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 167 | src-tauri/src/mcp_bridge/server.rs | 46 | High | Logic & Correctness | The critical start/stop lifecycle lacks tests; the imported test module covers binding but not stopping during initialization or accept-loop teardown | Add deterministic lifecycle tests with injected refresh and listener seams | pre-existing line | fixed | 3 |
| 168 | src-tauri/src/mcp_bridge/server.rs | 131 | Medium | Refactoring Debt | `stop_bridge` exceeds 30 lines and combines discovery cleanup, signaling, connection invalidation, client shutdown, and pending-request failure | Split shutdown signaling and state draining into focused helpers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 169 | src-tauri/src/mcp_bridge/server.rs | 199 | Low | Shortcuts & Patches | Malformed `identify` payloads are silently ignored, leaving clients believing identification succeeded | Log or return a correlated protocol error | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 170 | src-tauri/src/mcp_bridge/server.rs | 202 | Low | Logic & Correctness | A valid identify message emits `clients-changed` even when `client_id` no longer exists and no state changed | Emit only after successfully updating a registered client | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 171 | src-tauri/src/mcp_bridge/server.rs | 248 | High | Refactoring Debt | `handle_message` spans 235 lines with numerous protocol, authorization, routing, locking, timeout, retry, and delivery branches; its cyclomatic complexity makes lifecycle errors difficult to reason about | Decompose it into envelope dispatch, Rust-side dispatch, frontend routing, and response-await helpers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 172 | src-tauri/src/mcp_bridge/server.rs | 253 | High | Logic & Correctness | Debug logging copies the first 256 characters of any raw message containing `insert`; short document content or secrets can therefore be persisted in logs despite the comment claiming otherwise | Log only parsed metadata such as type, ID, and byte counts | changed line | fixed | 1 |
| 173 | src-tauri/src/mcp_bridge/server.rs | 314 | High | Logic & Correctness | The second debug path serializes and logs the first 256 characters of mutation arguments, directly exposing inserted or replacement document content | Remove argument logging or redact all content-bearing fields | changed line | fixed | 1 |
| 174 | src-tauri/src/mcp_bridge/server.rs | 423 | Medium | Shortcuts & Patches | A universal hardcoded ten-second timeout is applied to operations with different legitimate durations, causing premature wake/retry and potentially duplicate dispatch | Define operation-aware deadlines or carry an explicit bounded deadline in the request contract | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 175 | src-tauri/src/mcp_server.rs | 10 | Medium | Dead Code | The documentation claims `mcp_server_start` can spawn a local sidecar, but no such function exists and private `MCP_SERVER` is never assigned `Some`; `local_sidecar` and its cleanup paths are consequently orphaned | Remove the obsolete local-sidecar state and documentation or restore a real owner | changed line | fixed | 1 |
| 176 | src-tauri/src/mcp_server.rs | 66 | Medium | Shortcuts & Patches | `mcp_bridge_start` is a Tauri command returning legacy `Result<_, String>` | Return `Result<McpServerStatus, CommandError>` and map failures to stable error codes | changed line | fixed | 2 |
| 177 | src-tauri/src/mcp_server.rs | 66 | Medium | Refactoring Debt | `mcp_bridge_start` exceeds 30 lines and manually coordinates multiple independent global state variables | Encapsulate bridge lifecycle in one state object with a small start method | changed line | fixed | 2 |
| 178 | src-tauri/src/mcp_server.rs | 70 | Medium | Logic & Correctness | A concurrent caller receives `running: true` with `port: None` while startup is incomplete, exposing an internally contradictory public status | Represent `Starting` explicitly or await the in-progress start result | changed line | fixed | 2 |
| 179 | src-tauri/src/mcp_server.rs | 91 | High | Logic & Correctness | Stop can race startup: if stop runs while `start_bridge` is binding or refreshing credentials, it finds no shutdown sender, returns stopped, and the original start subsequently installs its sender and launches a live bridge | Serialize start/stop under one async lifecycle mutex and revalidate generation before publishing the started state | changed line | fixed | 1 |
| 180 | src-tauri/src/mcp_server.rs | 117 | High | Logic & Correctness | If `BRIDGE_PORT.lock()` fails after the listener has started, the command returns an error without stopping the server or clearing `BRIDGE_RUNNING`, leaking a live bridge the caller believes failed | Publish lifecycle state atomically or explicitly stop and roll back on post-start bookkeeping failure | changed line | fixed | 1 |
| 181 | src-tauri/src/mcp_server.rs | 138 | Medium | Shortcuts & Patches | `mcp_bridge_stop` is a Tauri command returning legacy `Result<_, String>` | Return `Result<McpServerStatus, CommandError>` | changed line | fixed | 1 |
| 182 | src-tauri/src/mcp_server.rs | 138 | Medium | Refactoring Debt | `mcp_bridge_stop` exceeds 30 lines and duplicates lifecycle cleanup responsibilities | Move shutdown into the unified bridge-state abstraction | changed line | fixed | 1 |
| 183 | src-tauri/src/mcp_server.rs | 153 | High | Duplication | Local-sidecar termination is duplicated here and in `cleanup`, allowing lifecycle behavior and error handling to diverge | Extract one sidecar termination helper or remove the orphaned sidecar state | changed line | fixed | 1 |
| 184 | src-tauri/src/mcp_server.rs | 157 | Medium | Shortcuts & Patches | `child.kill()` errors are discarded while the command reports that the sidecar stopped | Return a `CommandError` or preserve/report a degraded shutdown status | changed line | fixed | 1 |
| 185 | src-tauri/src/mcp_server.rs | 173 | Medium | Shortcuts & Patches | `mcp_server_status` is a Tauri command returning legacy `Result<_, String>` | Return `Result<McpServerStatus, CommandError>` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 186 | src-tauri/src/mcp_server.rs | 188 | Medium | Shortcuts & Patches | `mcp_sidecar_health` is a Tauri command returning legacy `Result<_, String>` | Return `Result<McpHealthInfo, CommandError>` with stable launch, timeout, exit, and decode codes | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 187 | src-tauri/src/mcp_server.rs | 192 | Medium | Logic & Correctness | The health-check subprocess has no timeout, so a hung sidecar leaves the IPC request unresolved indefinitely | Wrap process completion in a bounded timeout and terminate the child on expiry | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 188 | src-tauri/src/mcp_server.rs | 213 | Medium | Shortcuts & Patches | `mcp_bridge_client_count` is a Tauri command returning legacy `Result<_, String>` even though its body is infallible | Return the bare value or `Result<usize, CommandError>` | changed line | fixed | 1 |
| 189 | src-tauri/src/mcp_server.rs | 221 | Medium | Shortcuts & Patches | `mcp_bridge_connected_clients` is a Tauri command returning legacy `Result<_, String>` even though its body is infallible | Return the bare value or `Result<Vec<_>, CommandError>` | changed line | fixed | 1 |
| 190 | src-tauri/src/menu/localized/file_menu.rs | 8 | Low | Dead Code | The module documentation says non-macOS omits Print and calls the backend macOS-only, contradicting the cross-platform implementation below | Update the module description to match current behavior | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 191 | src-tauri/src/menu/localized/file_menu.rs | 16 | Medium | Refactoring Debt | `build` spans 172 lines and combines common menu construction with two platform-specific tails | Extract common items and platform-tail builders or use declarative item specifications | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 192 | src-tauri/src/menu/localized/file_menu.rs | 134 | High | Duplication | The identical Print menu item is copied into both mutually exclusive platform branches | Create it once in the common sequence and keep only genuinely platform-specific tail items behind `cfg` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 193 | src-tauri/src/menu/localized/insert_menu.rs | 12 | Medium | Refactoring Debt | `build` spans 106 lines of repetitive inline construction, making ordering and accelerator changes unnecessarily error-prone | Build the simple entries from declarative menu-item specifications and retain explicit submenu/separator placement | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 194 | src-tauri/src/pdf_export/commands.rs | 22 | Medium | Refactoring Debt | `validate_output_path` exceeds 30 lines and mixes extension, absoluteness, destination-type, and parent validation | Split it into small validation helpers with distinct error construction | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 195 | src-tauri/src/pdf_export/commands.rs | 63 | Medium | Logic & Correctness | An existing parent that is a regular file is reported with the `errors.pdf.dirNotFound` localization key, falsely telling users the path is missing | Add and use a distinct `parentNotDirectory` error key | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 196 | src-tauri/src/pdf_export/commands.rs | 70 | Low | Dead Code | After rejecting non-absolute paths, the `path.parent()` fallback arm is effectively unreachable for accepted output paths | Replace the match with an explicitly required parent or document and test a platform where `None` remains possible | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 197 | src-tauri/src/pdf_export/commands.rs | 101 | Medium | Refactoring Debt | `export_pdf` exceeds 30 lines and coordinates validation, rendering, blocking post-processing, error translation, and progress publication | Extract a post-render task helper and a progress-aware orchestration function | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 198 | src-tauri/src/pdf_export/commands.rs | 119 | High | Logic & Correctness | Concurrent exports to the same `output_path` are not serialized, allowing native rendering and the two read/modify/write post-processors to race over one file | Reserve normalized output paths for the lifetime of an export or use a per-path mutex | pre-existing line | fixed | 2 |
| 199 | src-tauri/src/pdf_export/commands.rs | 139 | High | Logic & Correctness | Progress events have no job identifier and target one fixed window, so concurrent exports to different files can interleave stages and make one dialog display another job’s state | Include an export ID in every progress event and scope listeners to that ID | changed line | fixed | 2 |
| 200 | src-tauri/src/pdf_export/renderer/linux.rs | 72 | High | Duplication | `start` independently implements nearly the same hidden-window and load lifecycle as `linux_print.rs` | Extract shared window creation, load-failure guarding, navigation, and cleanup into one helper | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 201 | src-tauri/src/pdf_export/renderer/linux.rs | 72 | High | Refactoring Debt | `start` is 126 lines with nested callbacks and multiple completion paths, making the resource lifecycle and settle-once behavior difficult to audit | Decompose it into window setup, page setup, and print-operation wiring functions | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 202 | src-tauri/src/pdf_export/renderer/linux.rs | 112 | Low | Shortcuts & Patches | The load-failure handler throws away WebKit’s URI and error details | Preserve those details in diagnostics and the returned `CommandError` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 203 | src-tauri/src/pdf_export/renderer/linux.rs | 195 | Medium | Logic & Correctness | A `with_webview` failure leaves the already-created hidden render window open | Close the window in the error path before propagating `window_error` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 204 | src-tauri/src/pdf_export/renderer/linux.rs | 203 | Medium | Shortcuts & Patches | `close` silently ignores teardown failures, potentially leaving an invisible window and native webview alive with no diagnostic | Log close failures and, where feasible, add a secondary teardown path | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 205 | src-tauri/src/pdf_export/renderer/linux_print.rs | 67 | High | Duplication | `start_print` duplicates window creation, navigation, load-failure tracking, signal wiring, and cleanup from `linux.rs`, allowing lifecycle fixes to drift between export and print paths | Extract a shared Linux webview-loading lifecycle helper and supply operation-specific completion callbacks | changed line | fixed | 3 |
| 206 | src-tauri/src/pdf_export/renderer/linux_print.rs | 67 | Medium | Refactoring Debt | `start_print` spans 113 lines and mixes window construction, navigation, failure handling, dialog execution, job monitoring, and cleanup | Split construction/loading from print-operation setup and dialog-response handling | changed line | fixed | 2 |
| 207 | src-tauri/src/pdf_export/renderer/linux_print.rs | 102 | Low | Shortcuts & Patches | The load-failure callback discards the failing URI and native error, leaving only a generic error and making field failures difficult to diagnose | Capture and log or attach the URI and native error details to the `CommandError` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 208 | src-tauri/src/pdf_export/renderer/linux_print.rs | 178 | Medium | Logic & Correctness | If `with_webview` fails after the hidden window was built, the function returns an error without closing that window | Explicitly close the newly created window on `with_webview` failure before returning the error | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 209 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 34 | Low | Dead Code | The parameter comment says `page` is deliberately unused, but it is passed through and used to configure the physical paper size | Correct the comment so future changes do not mistakenly remove required geometry handling | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 210 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 67 | High | Logic & Correctness | Rendering trusts `load_html_and_wait`, whose direct contract only observes `isLoading`; a failed navigation can become “not loading” and proceed to print an error or blank page | Make the loading contract report WKNavigation failures and require confirmed successful navigation before printing | changed line | fixed | 1 |
| 211 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 96 | High | Refactoring Debt | `print_to_pdf` is 133 lines with deeply nested polling and several independent success/error exits on a critical export path | Split print configuration, operation execution, and output-completion verification into separately testable functions | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 212 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 128 | High | Logic & Correctness | Failure to remove an existing destination is ignored; the subsequent size polling can observe that stale file and report a successful export even if AppKit wrote nothing | Propagate every removal error except `NotFound`, or write to a fresh temporary destination and atomically replace the target | pre-existing line | fixed | 1 |
| 213 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 162 | High | Logic & Correctness | Completion is inferred from unchanged file size for roughly 700 ms, which is not proof that a slow or paused writer has finished and can accept a truncated PDF | Use the native operation completion result and validate the completed PDF structure before returning success | pre-existing line | fixed | 1 |
| 214 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 213 | High | Logic & Correctness | After the polling deadline, any non-empty output is accepted as success even though the code just failed to establish completion; a partial PDF is therefore reported as valid | Return a timeout/error unless the file passes a definitive completion and PDF-integrity check | pre-existing line | fixed | 1 |
| 215 | src-tauri/src/pdf_export/renderer/macos_print.rs | 80 | Medium | Logic & Correctness | `success == false` is always mapped to `Cancelled`, even though the file’s own contract states that it also represents a failed print job | Represent indeterminate failure separately, or inspect operation error state and return `CommandError` when failure can be distinguished | new file | fixed | 2 |
| 216 | src-tauri/src/pdf_export/renderer/macos_print.rs | 85 | Medium | Logic & Correctness | The delegate, webview, sink, and private document form a self-retaining cycle that is released only by the callback; a missing callback leaks all of them indefinitely | Add an operation-owned lifecycle guard or cancellation/teardown path that breaks the retain cycle if the operation is abandoned | new file | not-fixed (dispute upheld: PrintDelegateIvars.self_retain is released only in printOperationDidRun:; AppKit does not retain the delegate and always sends that selector when the sheet ends, so an app-scheduled release is a use-after-free and no NSPrintOperation API ends a sheet without it — the real-print probe now holds a Weak<PrintDelegate> and asserts it loads nothing after the callback) [verifier: AGREE] | 2 |
| 217 | src-tauri/src/pdf_export/renderer/macos_print.rs | 126 | Medium | Refactoring Debt | `print_inner` exceeds 30 lines and combines loading, print configuration, parent selection, delegate lifetime management, and modal presentation | Extract operation construction and modal-sheet presentation into focused helpers | new file | fixed | 1 |
| 218 | src-tauri/src/pdf_export/renderer/macos_print.rs | 150 | High | Logic & Correctness | The parent is selected from `keyWindow` only after synchronous document loading; focus may have moved, causing the print sheet to attach to an unrelated VMark window | Pass the invoking `WebviewWindow` or stable native window identifier through the command and retain that exact parent | new file | fixed | 2 |
| 219 | src-tauri/src/pdf_export/renderer/mod.rs | 116 | Medium | Logic & Correctness | A `spawn_blocking` join failure or panic is mapped through the same I/O error helper as a filesystem write failure, producing the wrong error category and message | Map `JoinError` to `ErrorCode::Internal` and reserve the I/O mapping for tempfile operations | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 220 | src-tauri/src/pdf_export/renderer/mod.rs | 138 | High | Duplication | `render_pdf` and `print_document` duplicate temp-file ownership, channel creation, lossy path conversion, main-thread dispatch, and timeout/error plumbing | Extract a generic dispatch-and-await helper parameterized by sink construction and platform operation | changed line | fixed | 2 |
| 221 | src-tauri/src/pdf_export/renderer/mod.rs | 138 | Medium | Refactoring Debt | `render_pdf` is 86 lines and mixes validation, storage, lifecycle ownership, dispatch, progress, and timeout policy | Decompose it into preparation, dispatch, and result-waiting functions | changed line | fixed | 2 |
| 222 | src-tauri/src/pdf_export/renderer/mod.rs | 159 | Medium | Shortcuts & Patches | `html.clone()` duplicates the entire potentially multi-megabyte document solely so its length remains available for logging | Save `html.len()` before moving `html` into `write_render_temp` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 223 | src-tauri/src/pdf_export/renderer/mod.rs | 177 | Medium | Logic & Correctness | `to_string_lossy` can change a non-UTF-8 temporary path, causing the native backend to navigate to a different or nonexistent file | Keep paths as `Path`/`PathBuf` through the platform interface and construct native file URLs directly from them | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 224 | src-tauri/src/pdf_export/renderer/mod.rs | 209 | High | Logic & Correctness | Export timeout returns an error without cancelling the native operation; it can later write to the caller’s final output path after the caller has treated the export as failed or started a retry | Render to a unique staging file, add backend cancellation/teardown, and publish to the requested path only after confirmed success | pre-existing line | fixed | 3 |
| 225 | src-tauri/src/pdf_export/renderer/mod.rs | 231 | Medium | Refactoring Debt | `print_document` is 60 lines and duplicates most of the renderer orchestration while embedding a two-phase timeout state machine | Extract the common preparation/dispatch lifecycle and isolate the print-specific shown/outcome wait | changed line | fixed | 2 |
| 226 | src-tauri/src/pdf_export/renderer/mod.rs | 244 | Medium | Logic & Correctness | The print path also converts temporary and access-directory paths with `to_string_lossy`, risking navigation failure on valid non-UTF-8 filesystem paths | Pass native path types to the platform implementations | changed line | not-fixed: On Unix, `Url::from_file_path` percent-encodes raw `OsStr` bytes, so rejecting a valid non-UTF-8 temporary path remains a functional gap. | 3 |
| 227 | src-tauri/src/pdf_export/renderer/mod.rs | 270 | High | Logic & Correctness | A pre-dialog timeout abandons only the receiver; the platform work continues and may later display a print dialog after the command has already reported failure, while retaining the hidden webview and private temp document | Add explicit cancellation and window teardown when phase one times out | changed line | fixed | 3 |
| 228 | src-tauri/src/pdf_export/renderer/outcome.rs | 80 | Medium | Logic & Correctness | `success == false` represents either cancellation or print failure, but both are reported as `Cancelled`, contradicting `PrintStatus::Cancelled`’s user-dismissal contract and potentially hiding failed jobs | Return an explicitly ambiguous status such as `Unknown`/`NotCompleted`, or inspect an additional AppKit error signal before classifying it as cancellation | new file | fixed | 2 |
| 229 | src-tauri/src/pdf_export/renderer/progress.rs | 98 | Low | Shortcuts & Patches | `emit_to` errors are silently discarded, leaving progress delivery failures completely undiagnosed | Log the error with the target window, event name, and stage | new file | fixed | 1 |
| 230 | src-tauri/src/pdf_export/renderer/progress.rs | 147 | High | Logic & Correctness | `report` releases the tracker mutex before invoking the sink, so concurrent calls can accept `Loading` then `Rendering` but emit them in reverse order; the tests cover only sequential calls despite the type being `Send + Sync` | Serialize acceptance and emission together, or assign sequence numbers under the state lock and drain emissions through a dedicated ordered dispatcher; add a concurrent regression test | new file | fixed | 1 |
| 231 | src-tauri/src/pdf_export/renderer/sink.rs | 95 | Medium | Logic & Correctness | `progress` holds the outcome mutex while invoking an arbitrary reporter closure; a closure that re-enters `settle` or `progress` deadlocks on the same non-reentrant mutex | Keep lifecycle state and ordered emission in a dedicated state machine so user-provided emission code runs without holding the outcome mutex; add a re-entrancy test | changed line | fixed | 1 |
| 232 | src-tauri/src/pdf_export/renderer/windows.rs | 72 | High | Refactoring Debt | `start` spans roughly 90 lines with nested COM callbacks and many cleanup branches, making resource ownership and settle/close behavior difficult to verify | Extract window creation, navigation validation, and one-shot callback registration into focused helpers or an explicit render-session object | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 233 | src-tauri/src/pdf_export/renderer/windows.rs | 83 | High | Logic & Correctness | The window starts navigating to `about:blank` before the `NavigationCompleted` handler is registered, and the handler neither verifies the completed URL nor enforces one-shot execution; a delayed blank event, redirect, or reload can start one or multiple PDF writes against the same output | Validate the completed navigation against `file_url`, ignore unrelated events, and atomically consume/remove the handler before starting `PrintToPdf` | changed line | partial: Navigation-ID correlation and one-shot execution fix normal ordering, but the unreadable-ID fallback can still mistake a late blank completion for the document after `Source` advances. | 3 |
| 234 | src-tauri/src/pdf_export/renderer/windows.rs | 94 | High | Logic & Correctness | If `with_webview` itself fails, the already-created hidden window is returned as an error without being closed, leaking the window and its Edge process | Replace the terminal `map_err` with an error branch that calls `close(app, &label)` before returning | pre-existing line | fixed | 1 |
| 235 | src-tauri/src/pdf_export/renderer/windows.rs | 112 | High | Logic & Correctness | The critical COM callback lifecycle — including duplicate navigation, handler ordering, setup failure, and cleanup — is not covered by this file’s tests; `windows.test.rs` tests only URL conversion | Introduce a mockable navigation/print session state machine and test duplicate, stale, failed, and out-of-order callbacks | pre-existing line | not-fixed: Pure state-machine tests do not exercise real WebView2 handler registration, COM setup failure, callback teardown, or cleanup; a live Windows host remains necessary. | 3 |
| 236 | src-tauri/src/pdf_export/renderer/windows_print.rs | 51 | High | Duplication | `start_print` duplicates the window creation, CoreWebView2 acquisition, navigation-success parsing, handler registration, navigation, error mapping, and cleanup flow from `windows.rs:72` | Extract a shared one-shot WebView2 navigation helper that yields the loaded core webview to an operation-specific callback | changed line | fixed | 1 |
| 237 | src-tauri/src/pdf_export/renderer/windows_print.rs | 51 | High | Refactoring Debt | `start_print` is an approximately 87-line nested callback function with repeated settle/close paths and high control-flow complexity | Split setup, validated navigation, and print-UI invocation into small helpers backed by a session object that owns cleanup | changed line | fixed | 2 |
| 238 | src-tauri/src/pdf_export/renderer/windows_print.rs | 58 | High | Logic & Correctness | As in the export path, the handler can receive the initial `about:blank` completion or later navigations because it does not validate the URL or run only once; this can show the print dialog for a blank page or repeatedly reopen it | Match the completed source to `file_url` and remove or atomically disable the handler after the intended navigation completes | pre-existing line | partial: The shared navigation gate prevents ordinary blank/reload duplicates, but its fail-open unreadable-ID fallback retains the same early-dialog risk. | 3 |
| 239 | src-tauri/src/pdf_export/renderer/windows_print.rs | 69 | High | Logic & Correctness | A `with_webview` failure leaves the already-created visible print window open while returning an error | Close the window in the `with_webview` error path before propagating the `CommandError` | pre-existing line | fixed | 1 |
| 240 | src-tauri/src/pdf_export/renderer/windows_print.rs | 83 | High | Logic & Correctness | The native print-dialog callback path has no colocated automated coverage for navigation failure, duplicate completion, COM-interface failure, cleanup, or shown/settle ordering | Move callback decisions into a mockable state machine and add tests for every failure and repeated-callback path | pre-existing line | not-fixed: COM failure, cleanup, and `shown()`/`settle()` ordering remain inside an unmocked callback; generic helper tests do not cover it. | 3 |
| 241 | src-tauri/src/secure_store.rs | 89 | Medium | Logic & Correctness | Every unrecognized macOS keychain status is classified as retryable I/O, including semantically different outcomes such as cancellation, interaction denial, malformed data, or entitlement failures | Explicitly map known statuses and use `Internal` as the conservative fallback | changed line | fixed | 1 |
| 242 | src-tauri/src/secure_store.rs | 98 | High | Logic & Correctness | The security-critical OSStatus downcast is tested only with synthetic errors; no platform integration test proves that the installed keyring/security-framework versions expose the real macOS error through this concrete type | Add a macOS-only integration test or injectable platform-error adapter that exercises the real boundary | changed line | fixed | 1 |
| 243 | src-tauri/src/secure_store.rs | 171 | Medium | Logic & Correctness | Key validation rejects only the empty string, allowing whitespace, control characters, unbounded lengths, and arbitrary accounts to reach platform-specific credential-store behavior | Enforce a bounded documented key grammar or restrict callers to known secret identifiers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 244 | src-tauri/src/secure_store.rs | 180 | Medium | Logic & Correctness | Synchronous credential-store operations can block the IPC delivery thread while the OS displays a keychain prompt or waits on the credential service | Make all three commands asynchronous and run keyring calls through `tokio::task::spawn_blocking` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 245 | src-tauri/src/single_instance.rs | 73 | High | Logic & Correctness | Any nonempty `DBUS_SESSION_BUS_ADDRESS`, including whitespace or malformed addresses, enables a plugin whose connection path is documented to unwrap and abort startup | Parse or probe the address before registration and disable the guard with a diagnostic when it is unusable | changed line | fixed | 4 |
| 246 | src-tauri/src/single_instance.rs | 102 | High | Logic & Correctness | The critical second-launch routing and window-surfacing path has no app-level test; tests cover only argument filtering and the environment predicate | Add a mock-runtime or platform integration test covering file forwarding, focus, minimized/hidden windows, and no-window creation | changed line | not-fixed (disputed: coverage, not a defect — the argv half is unit-tested, the surfacing half needs a live app) [verifier: DISAGREE — The critical routing and window-surfacing path remains untested; calling this merely “coverage” does not remove the defect risk.] | 1 |
| 247 | src-tauri/src/single_instance.rs | 159 | Medium | Logic & Correctness | A window can disappear after `live_labels` is captured; `get_webview_window` then returns `None` and the second launch silently surfaces nothing | Retry target selection and create a main window if no live target remains | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 248 | src-tauri/src/window_manager/commands.rs | 18 | High | Duplication | The three open-window commands repeat validate, scope-grant, URL/build, and error-mapping logic, making security behavior easy to drift | Extract shared validated-path and window-creation helpers used by every entry point | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 249 | src-tauri/src/window_manager/commands.rs | 18 | High | Logic & Correctness | None of the three critical window-creation commands has behavioral coverage; existing tests exercise only `close_window`’s not-found branch | Add mock-runtime or platform E2E coverage for successful creation, invalid inputs, multi-file handling, and builder failure | pre-existing line | not-fixed: Success, invalid-input, and multi-file tests were added, but builder failure remains untested; an injected builder seam could avoid the global-counter problem. | 3 |
| 250 | src-tauri/src/window_manager/commands.rs | 19 | High | Logic & Correctness | Validation canonicalizes a path but discards the result, then grants and opens the original path; a symlink swap between those operations can redirect the granted raw path after validation | Return the canonical path from validation and consistently grant/pass that path, or use an atomic handle-based design | pre-existing line | fixed | 4 |
| 251 | src-tauri/src/window_manager/commands.rs | 20 | Medium | Unexpected Side Effects | Filesystem scope is permanently extended before window creation, so builder failure leaves privileges granted without a corresponding window | Grant only after successful creation where possible, or revoke the grant on failure | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 252 | src-tauri/src/workflow/actions.rs | 14 | High | Refactoring Debt | `execute_action` is a 141-line, highly branched dispatcher containing validation, sandboxing, directory traversal, resource accounting, formatting, and writes | Split each action into a dedicated function and keep the dispatcher as a small routing table | changed line | fixed | 1 |
| 253 | src-tauri/src/workflow/actions.rs | 26 | High | Logic & Correctness | `read-file` checks size but not `meta.is_file()`; a FIFO or other special file inside the workspace can block the workflow indefinitely | Reject anything other than a regular file before reading and apply a cancellation-aware timeout | changed line | fixed | 2 |
| 254 | src-tauri/src/workflow/actions.rs | 26 | High | Logic & Correctness | Both read paths perform metadata size checks before a separate unbounded read, allowing concurrent file growth to bypass the 10 MB and 100 MB memory limits | Read through `take(limit + 1)` or an equivalent bounded streaming helper and enforce actual bytes consumed | changed line | fixed | 2 |
| 255 | src-tauri/src/workflow/actions.rs | 54 | Medium | Logic & Correctness | `read_dir` order is filesystem-dependent, so identical folders can produce different workflow input and output ordering across runs and platforms | Collect eligible entries and sort by a documented filename/path ordering before reading | changed line | fixed | 3 |
| 256 | src-tauri/src/workflow/actions.rs | 105 | Medium | Logic & Correctness | The total-byte counter is increased before UTF-8 reading succeeds, so unreadable or invalid-UTF-8 files consume the budget and can cause a false total-limit error | Count successfully consumed bytes, while still enforcing a separate bounded-attempt limit if needed | changed line | fixed | 1 |
| 257 | src-tauri/src/workflow/actions.rs | 128 | High | Logic & Correctness | Sandbox validation and the later directory creation/write are separate pathname operations; an existing ancestor can be replaced with an escaping symlink during the intervening awaits | Use descriptor-relative/no-follow filesystem operations or revalidate immediately before an atomic commit | pre-existing line | fixed | 4 |
| 258 | src-tauri/src/workflow/actions.rs | 137 | High | Logic & Correctness | `tokio::fs::write` truncates the destination before completing, so cancellation, disk exhaustion, or I/O failure can corrupt an existing user file | Write to a same-directory temporary file, flush it, and atomically rename it over the destination | changed line | fixed | 1 |
| 259 | src-tauri/src/workflow/commands.rs | 42 | High | Duplication | `admit_run` is an 81-line branching function with repeated manual `running.store(false)` cleanup on every rejection path | Acquire an RAII admission guard immediately after the CAS and commit/disarm it only on successful admission | changed line | fixed | 1 |
| 260 | src-tauri/src/workflow/commands.rs | 49 | High | Logic & Correctness | Engine disable and workflow admission use separate atomics: admission can read enabled, the policy can observe no run, and admission can then set `running`, producing a workflow that starts disabled without receiving cancellation | Serialize gate checking, admission, and disable/cancel transitions under one lifecycle mutex or atomic state machine | changed line | fixed | 1 |
| 261 | src-tauri/src/workflow/commands.rs | 75 | Medium | Logic & Correctness | Relative workspace roots are accepted by `is_dir()` but passed downstream unchanged; sandbox normalization can then reject otherwise valid child paths because the normalized child no longer starts with `"."` | Canonicalize the workspace during admission and pass the canonical absolute path onward | changed line | fixed | 1 |
| 262 | src-tauri/src/workflow/commands.rs | 135 | Medium | Refactoring Debt | `run_workflow` spans 124 lines and the file is 353 lines, exceeding the project’s ~300-line convention while mixing admission, snapshot planning, app-path resolution, state publication, spawning, and logging | Extract snapshot preparation and runner-launch orchestration into focused modules/functions | pre-existing line | fixed | 2 |
| 263 | src-tauri/src/workflow/commands.rs | 135 | High | Logic & Correctness | Tests cover only `admit_run`; the critical snapshot, execution-ID publication, task-spawn, panic cleanup, and completion lifecycle are untested | Add Tauri/mock-runtime integration tests for success and every failure/race boundary after admission | pre-existing line | fixed | 3 |
| 264 | src-tauri/src/workflow/commands.rs | 153 | Medium | Logic & Correctness | Caller-provided execution IDs are accepted without checking emptiness, length, grammar, or reuse, allowing ambiguous events/cancellation keys and snapshot-namespace collisions | Validate IDs once at command entry and reject duplicates, or always allocate opaque backend IDs | changed line | partial: IDs now have grammar, length, and emptiness validation, but reuse protection evicts IDs after 256 runs, allowing older event/cancellation keys to be reused. | 3 |
| 265 | src-tauri/src/workflow/commands.rs | 161 | Medium | Logic & Correctness | `app_data_dir()` is required before determining whether snapshots are needed, so even action-only workflows with no file modifications fail when app-data resolution is unavailable | Resolve app data only when snapshot files exist; keep genie-directory resolution independently optional | changed line | fixed | 1 |
| 266 | src-tauri/src/workflow/commands.rs | 190 | High | Logic & Correctness | Snapshot failure is logged and ignored, allowing destructive file writes to proceed without the recovery guarantee claimed by the workflow design | Fail closed for file-modifying workflows, or require explicit user-approved execution without a snapshot | changed line | fixed | 1 |
| 267 | src-tauri/src/workflow/commands.rs | 190 | High | Logic & Correctness | Snapshot copying is awaited before publishing `current_execution` or returning; it is unbounded by file size and `cancel_workflow` cannot match the known caller-generated ID during this potentially long operation | Publish a cancellable preparation state immediately and make bounded snapshot creation part of the managed background task | changed line | not-fixed: Snapshot preparation remains awaited so its failure can reject the invocation; publishing the ID first and using bounded, cancellable copying addresses the cited operational risks. | 3 |
| 268 | src-tauri/src/workflow/examples.rs | 33 | High | Duplication | Genie filenames duplicate the workflow’s references and can drift, allowing the test to validate unrelated assets | Resolve and validate the exact genies referenced by the parsed workflow | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 269 | src-tauri/src/workflow/examples.rs | 48 | High | Duplication | `topo_order` reimplements production topological sorting, so test and runner behavior can diverge | Expose and test the production validation/sorting path | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 270 | src-tauri/src/workflow/examples.rs | 156 | High | Shortcuts & Patches | Required parameters are inferred by scraping Rust source and error-message wording; harmless refactors can silently weaken or break this contract | Define typed action metadata or invoke the production validator directly | changed line | fixed | 1 |
| 271 | src-tauri/src/workflow/examples.rs | 190 | High | Logic & Correctness | The claimed integration coverage never executes the bundled workflow, leaving interpolation, genie resolution, action dispatch, and file output untested | Run the sample through the real runner with a fake provider and temporary workspace, then assert the saved output | changed line | fixed | 2 |
| 272 | src-tauri/src/workflow/state.rs | 37 | Medium | Logic & Correctness | Documentation promises one concurrent workflow per window, but this app-managed state has one global `running` flag and execution ID | Key execution state by window label or document that execution is intentionally app-global | changed line | fixed | 1 |
| 273 | src-tauri/src/workflow/state.rs | 119 | High | Logic & Correctness | Execution-ID matching and setting the shared cancel flag are not atomic; run A can finish and run B can reset its flag before A’s stale request stores `true`, cancelling B | Use a per-execution cancellation token or hold generation-bound state under one lock through the cancellation operation | pre-existing line | fixed | 1 |
| 274 | src-tauri/src/workflow/state.rs | 140 | High | Logic & Correctness | Checking `running` and arming cancellation are separate operations; concurrent admission can clear the cancellation after the engine is disabled, allowing a newly disabled run to proceed | Synchronize policy changes with admission and bind cancellation to the active execution generation | changed line | fixed | 1 |
| 275 | src/components/Editor/DocumentSplit/DocumentSplitContainer.tsx | 27 | Low | Refactoring Debt | The component exceeds 30 lines and combines store selection, scroll binding, focus mutation, pane sizing, and duplicated pane rendering | Extract a reusable document-pane component and focused-pane callback helper | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 276 | src/components/Editor/DocumentSplit/DocumentSplitContainer.tsx | 42 | Medium | Logic & Correctness | Scroll synchronization binds once to the current descendant; asynchronously mounted or replaced editor scrollers, including split-view mode changes absent from `reattachKey`, retain stale or fallback listeners | Bind through scroller callback refs, observe descendant replacement, or have each editor surface publish its current scroller | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 277 | src/components/Editor/DocumentSplit/SplitDivider.tsx | 27 | Low | Refactoring Debt | The component exceeds 30 lines and mixes global drag lifecycle, keyboard behavior, accessibility metadata, and rendering | Extract a generic split-resize hook | changed line | fixed | 2 |
| 278 | src/components/Editor/DocumentSplit/SplitDivider.tsx | 30 | High | Duplication | This duplicates the established resize-listener pattern but omits reentrant, blur, and unmount cleanup; closing the split or losing the window mid-drag can leak listeners and leave body selection disabled | Reuse or generalize the existing resize hook with cleanup stored in refs and an effect teardown | changed line | fixed | 1 |
| 279 | src/components/Editor/DocumentSplit/SplitDivider.tsx | 30 | Medium | Logic & Correctness | Mouse-only handling excludes touch and pen input and accepts non-primary mouse buttons as resize starts | Use Pointer Events with pointer capture and reject non-primary buttons | changed line | fixed | 1 |
| 280 | src/components/Editor/SplitPaneEditor/ValidationGutter.tsx | 60 | Low | Refactoring Debt | The component exceeds 30 lines and owns counting, live-status rendering, row interaction, translation, and badge composition | Extract summary and diagnostic-row components | changed line | fixed | 1 |
| 281 | src/components/Editor/SplitPaneEditor/ValidationGutter.tsx | 79 | High | Duplication | Error, warning, and info summaries repeat nearly identical markup and translation wiring three times | Map a typed severity descriptor array into one summary-count component | changed line | fixed | 1 |
| 282 | src/components/Editor/SplitPaneEditor/ValidationGutter.tsx | 110 | Medium | Logic & Correctness | Rows are always focusable list items even when `onJump` is absent, and actionable rows expose no button/link semantics to assistive technology | Render a button inside each list item when actionable and plain non-focusable content otherwise | changed line | fixed | 1 |
| 283 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 50 | Medium | Refactoring Debt | `WorkflowEditorPanel` spans 128 lines and mixes selection derivation, focus management, preview construction, diagnostics, and rendering | Extract scoped focus restoration and selected-step derivation into hooks/helpers | changed line | fixed | 2 |
| 284 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 89 | Medium | Logic & Correctness | Focus restoration uses global document queries and English aria-label substrings; localized labels fail, and another mounted workflow panel can receive focus | Scope queries to a panel ref and select controls through stable data attributes or refs | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 285 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 184 | Medium | Refactoring Debt | `AddJobControl` spans 74 lines and mixes state, validation, keyboard handling, and presentation | Extract validation and handlers, or move the control into its own component file | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 286 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 189 | Low | Logic & Correctness | Invalid or duplicate IDs submitted with Enter fail silently, providing no explanation of why submission did nothing | Track a validation error and expose it with visible text plus `aria-describedby` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 287 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 193 | High | Duplication | The job-ID validation expression is duplicated here and in the button-disabled condition at line 238, inviting validation drift | Extract one validated/normalized ID result or shared predicate | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 288 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 207 | Medium | Shortcuts & Patches | “Add job,” “job-id,” “Add,” and “Cancel” are hardcoded at lines 207, 216, 241, and 251 despite the surrounding localized UI | Add translation keys and render all four strings through `t` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 289 | src/components/Editor/WorkflowEditor/useActionlintDiagnostics.ts | 92 | Medium | Refactoring Debt | The 41-line hook exceeds the requested 30-line function threshold and combines subscriptions, scheduling, result ownership, and error reporting | Extract the debounced execution and outcome handling into a focused hook/helper | new file | fixed | 1 |
| 290 | src/components/Editor/WorkflowEditor/useActionlintDiagnostics.ts | 105 | Medium | Logic & Correctness | Superseded requests discard their results but do not cancel or serialize already-started actionlint processes, allowing obsolete processes to accumulate during sustained editing | Add backend cancellation or serialize executions while retaining only the latest queued text | new file | fixed | 1 |
| 291 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 69 | Medium | Refactoring Debt | The component spans 90 lines and owns binding, persistence, discard behavior, lazy loading, and layout | Extract document binding and persistence into dedicated hooks | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 292 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 81 | Medium | Logic & Correctness | The binding reads `filePath` non-reactively while the effect depends only on `workflow` and `tabId`; a path change can leave patches stashed under the old document identity | Subscribe to the hosting document’s stable identity/file path and include it in the effect dependency | changed line | fixed | 3 |
| 293 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 85 | High | Logic & Correctness | Every mounted workbench binds one global active patch queue; with two workflow panes, the last effect wins and edits from the other pane can be recorded under the wrong document | Make edit queues explicitly tab/document keyed, or scope the store per workbench instead of relying on one active binding | changed line | fixed | 3 |
| 294 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 91 | Medium | Refactoring Debt | The save callback exceeds 30 lines and combines module loading, snapshotting, serialization, disk I/O, store mutation, and notification branches | Extract a document-scoped workflow-save service returning a typed result | changed line | fixed | 1 |
| 295 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 99 | Medium | Shortcuts & Patches | The dynamic import occurs outside the `try`, so chunk-load failure bypasses the user-facing error toast and is merely logged by the caller | Move the import inside the guarded block | changed line | fixed | 1 |
| 296 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 100 | High | Logic & Correctness | Save reads whichever patch queue is currently active without verifying `boundDocumentId` against this tab, so a rebound split pane can apply another document’s patches to `tabDoc.content` | Resolve patches by this document’s stable ID and reject saves on an ownership mismatch | changed line | fixed | 1 |
| 297 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 103 | High | Logic & Correctness | `applyAndSerialize` can silently return the original YAML on parse/mutation failure, after which this handler reports success and clears all patches | Make serialization return a typed failure and never write or clear patches when application fails | changed line | fixed | 1 |
| 298 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 110 | High | Logic & Correctness | Forms remain editable while the asynchronous save runs; after awaiting, lines 112–113 overwrite newer document content and clear patches queued during the save, losing edits | Snapshot patch identity/revision, disable or version edits during save, and clear only the successfully persisted snapshot | changed line | fixed | 1 |
| 299 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 130 | Low | Dead Code | `handleDiscard` is an empty callback whose documented behavior is entirely performed elsewhere | Remove the prop/callback or move discard ownership into this handler | changed line | fixed | 1 |
| 300 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 146 | Low | Shortcuts & Patches | The editor is lazy-loaded whenever any conforming workflow tab renders, contradicting the claim that view-only users never load it; lazy loading only postpones the unconditional load | Gate mounting behind an explicit editor-open state or correct the misleading design documentation | changed line | fixed | 1 |
| 301 | src/components/FindBar/FindBar.tsx | 50 | Medium | Refactoring Debt | The 247-line component greatly exceeds the 30-line function threshold and combines subscriptions, focus/event effects, IME keyboard logic, derivation, and all markup | Split effects/keyboard behavior into hooks and extract toggle, navigation, and replacement groups | changed line | partial: Responsibilities were extracted, but `FindBar` still spans approximately 85 source lines, well above the stated 30-line function threshold. | 3 |
| 302 | src/components/FindBar/FindBar.tsx | 86 | Medium | Logic & Correctness | Query and mode changes reset `currentIndex` but retain the old `matchCount`, briefly enabling navigation/replacement against stale matches and rendering “0 of N” at line 170 | Clear match count with search-criteria changes or represent recomputation with an explicit pending state | changed line | fixed | 2 |
| 303 | src/components/FindBar/FindBar.tsx | 103 | High | Logic & Correctness | Coverage directives explicitly exclude important Escape, Tab, Shift+Tab, and replace-input keyboard branches through line 132; these are critical keyboard and accessibility paths | Remove the ignores and add focused Vitest coverage, including active composition and the post-composition grace period | pre-existing line | fixed | 1 |
| 304 | src/components/FindBar/FindBar.tsx | 135 | High | Duplication | Seven near-identical callbacks through line 161 only forward to `useUIStore.getState()`, creating repetitive maintenance points | Use stable action selectors or a small typed action-dispatch helper | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 305 | src/components/FindBar/FindBar.tsx | 177 | Medium | Logic & Correctness | Disabling regex-search only hides the toggle; an already-true `useRegex` state remains active and cannot be turned off from this UI | Force regex off when the setting becomes disabled or gate search behavior with the setting | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 306 | src/components/FindBar/FindBar.tsx | 185 | Low | Shortcuts & Patches | The regex toggle uses `ICON_MD` while adjacent case and whole-word toggles use 14px icons, producing inconsistent glyph sizing | Use the same sanctioned icon-size token for all toggle controls | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 307 | src/components/FindBar/FindBar.tsx | 210 | High | Duplication | The sanctioned 14px icon size is hardcoded repeatedly at lines 210, 236, 246, 252, 277, 286, and 291 instead of using the existing size token | Import and use `ICON_SM` consistently | changed line | fixed | 1 |
| 308 | src/components/GeniePicker/GeniePicker.tsx | 46 | High | Refactoring Debt | The 463-line component combines loading, focus restoration, filtering, grouping, history synchronization, keyboard state machines, invocation lifecycle, and rendering; its high cyclomatic complexity has already produced interacting state defects | Split state/keyboard behavior into focused hooks and extract input, list, response, and footer components | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 309 | src/components/GeniePicker/GeniePicker.tsx | 190 | High | Logic & Correctness | `handleClose()` closes and resets the picker before invocation; subsequent processing, preview, and error state changes occur while `isOpen` remains false, making the documented inline response UI unreachable | Keep the picker open during prompt invocation; close only for workflow/auto-approved completion or explicit dismissal | changed line | fixed | 1 |
| 310 | src/components/GeniePicker/GeniePicker.tsx | 202 | High | Logic & Correctness | Freeform submission has the same premature-close defect, hiding processing, preview, and invocation errors | Start the invocation without `handleClose()` and let the invocation lifecycle control dismissal | changed line | fixed | 1 |
| 311 | src/components/GeniePicker/GeniePicker.tsx | 294 | Medium | Logic & Correctness | A history-driven `filter` update does not clear `freeformConfirmed`; after confirming one prompt, cycling to another can submit the replacement on the next Enter without its required first confirmation | Reset `freeformConfirmed` whenever `promptHistory.displayValue` changes the filter | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 312 | src/components/GeniePicker/GeniePicker.tsx | 329 | Medium | Shortcuts & Patches | Coverage is explicitly suppressed for the user-visible history dropdown and ghost completion integration, leaving interaction regressions such as stale confirmation and incorrect ARIA state untested | Remove these ignores and add integration tests covering open, navigation, completion, filtering, dismissal, and confirmation reset | changed line | fixed | 2 |
| 313 | src/components/GeniePicker/GeniePicker.tsx | 359 | Medium | Logic & Correctness | Prompt-history keyboard handling runs only when no genie matches, so Ctrl/Cmd+R cannot open history in the picker’s normal initial state | Dispatch the history-toggle shortcut independently of `flatList.length`, then resolve navigation conflicts based on whether the dropdown is open | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 314 | src/components/GeniePicker/GeniePicker.tsx | 368 | Medium | Logic & Correctness | When prompt history is open, the combobox still reports `aria-expanded=false` and controls the genie list rather than the history listbox | Include history-open state in `aria-expanded` and point `aria-controls`/`aria-activedescendant` to stable IDs on the active listbox/options | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 315 | src/components/GeniePicker/PromptHistoryDropdown.tsx | 22 | Low | Refactoring Debt | The 81-line component exceeds the requested 30-line review threshold and mixes document-event management, scrolling, empty state, list rendering, and footer actions | Extract outside-dismiss/scroll behavior into hooks and the option row into a small component | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 316 | src/components/GeniePicker/PromptHistoryDropdown.tsx | 56 | Medium | Logic & Correctness | The empty-state root does not receive `listRef`; consequently the document listener sees `listRef.current === null` and never closes an empty dropdown on outside clicks | Attach the ref to both branches or render one shared referenced root | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 317 | src/components/GeniePicker/PromptHistoryDropdown.tsx | 82 | Low | Logic & Correctness | Array indexes are used as option keys, so filtering/reordering history reuses DOM identity for different prompts | Use a stable prompt-entry identifier, or the entry value if uniqueness is guaranteed | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 318 | src/components/KnowledgeBasePanel/KnowledgeBasePanel.tsx | 76 | High | Refactoring Debt | One highly branched component handles five lifecycle states, progress calculation, six toolbar actions, lazy-load recovery, and iframe rendering | Extract lifecycle views such as stopped, provisioning, error, and running into focused components | changed line | fixed | 1 |
| 319 | src/components/KnowledgeBasePanel/KnowledgeBasePanel.tsx | 118 | Medium | Logic & Correctness | Download percentage trusts unconstrained `received` and `total`, allowing negative values or output above 100%, and treats negative totals as valid divisors | Validate finite positive totals and clamp the calculated percentage to 0–100 | changed line | fixed | 1 |
| 320 | src/components/KnowledgeBasePanel/KnowledgeBaseRuntimeState.tsx | 35 | Low | Refactoring Debt | The component exceeds 30 lines and combines phase dispatch, repeated action construction, translation, and missing-runtime rendering | Extract a `StartButton` and phase-specific views or use a small exhaustive phase renderer | new file | fixed | 1 |
| 321 | src/components/KnowledgeBasePanel/runtimeState.ts | 43 | High | Logic & Correctness | The claimed wire-shape guard validates only `node` and `cli`; `{node:"ready", cli:"ready"}` passes despite missing required `nodePath`, `cliSource`, and `detail`, defeating the malformed-payload safety gate and enabling Start | Validate every required field, including nullable string fields and the exact `cliSource` union | new file | fixed | 1 |
| 322 | src/components/Sidebar/FileExplorer/FileExplorer.tsx | 94 | Medium | Refactoring Debt | FileExplorer is a roughly 289-line component and the file exceeds the project’s approximately 300-line limit, combining state, actions, tree behavior, and rendering | Extract context-menu state/wiring and file-operation callbacks into focused hooks | changed line | fixed | 1 |
| 323 | src/components/Sidebar/FileExplorer/FileExplorer.tsx | 107 | High | Logic & Correctness | Context-menu state survives workspace changes, leaving actions targeting paths from the previous workspace | Close and reset the context menu whenever rootPath or workspaceInstanceId changes | changed line | fixed | 1 |
| 324 | src/components/Sidebar/FileExplorer/FileExplorer.tsx | 130 | Medium | Logic & Correctness | The hook’s truncated result is ignored, so a bounded partial scan appears indistinguishable from a complete file tree | Consume truncated and show a persistent partial-results warning with a recovery action | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 325 | src/components/Sidebar/FileExplorer/FileExplorer.tsx | 246 | High | Duplication | Three separate Promise.resolve(...).catch(...) adapters duplicate rejection containment already claimed by runContextMenuAction and weaken its ability to await operations | Type injected operations as Promise-returning functions, pass them directly, and keep one error boundary in the dispatcher | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 326 | src/components/Sidebar/FileExplorer/useFileTree.ts | 129 | Medium | Refactoring Debt | The hook spans roughly 140 lines and owns loading, transformation, scheduling, watcher subscription, focus refresh, cancellation, and error state | Split scanning/state management and watcher scheduling into focused hooks or controllers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 327 | src/components/Sidebar/FileExplorer/useFileTree.ts | 195 | High | Logic & Correctness | A non-null root-to-root workspace switch retains and renders the previous workspace’s tree until the new scan completes, permitting interaction with stale paths | Associate nodes with their source root and return an empty/loading state whenever it does not match current rootPath | pre-existing line | fixed | 1 |
| 328 | src/components/Sidebar/FileExplorer/useFileTree.ts | 197 | Medium | Logic & Correctness | Clearing rootPath invalidates the request and clears data but never resets isLoading, leaving the hook’s loading state stale indefinitely | Call setIsLoading(false) in the null-root branch | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 329 | src/export/fontEmbedder.ts | 14 | High | Duplication | KATEX_FONTS duplicates the filenames independently repeated by getKaTeXFontFiles, creating two manifests that can drift | Define one typed KaTeX font manifest and derive both names and FontFile records from it | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 330 | src/export/fontEmbedder.ts | 63 | Low | Shortcuts & Patches | Documentation promises exponential retry backoff, but the implementation uses linear delays of one, two, and three seconds | Implement exponential delay or correct the contract to state linear backoff | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 331 | src/export/fontEmbedder.ts | 102 | High | Duplication | generateLocalFontCSS and generateEmbeddedFontCSS duplicate the complete @font-face formatter, differing only in the source URL | Extract one font-face formatter accepting a FontFile and resolved source | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 332 | src/export/htmlExport.ts | 119 | High | Logic & Correctness | Concurrent exports to the same outputPath race on identical files, and one failing export can remove or corrupt another export’s output | Serialize exports per destination or build in a unique staging directory and atomically replace the destination | pre-existing line | fixed | 4 |
| 333 | src/export/htmlExport.ts | 119 | High | Refactoring Debt | The approximately 206-line export function has high filesystem-path complexity and interleaves resolution, downloading, generation, writing, accounting, and rollback | Split it into resource, font, document-generation, and transactional-write phases with explicit result types | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 334 | src/export/htmlExport.ts | 141 | High | Logic & Correctness | createdPaths does not establish whether paths existed before export; overwritten pre-existing files are recorded as “created” and deleted if a later operation fails | Record prior existence and backups, or use transactional staging so rollback never deletes pre-existing content | changed line | fixed | 4 |
| 335 | src/export/htmlExport.ts | 145 | Medium | Logic & Correctness | Newly created outputPath, assetsPath, and imagesPath directories are never tracked or removed after failure | Track only directories proven absent before mkdir and remove them in reverse order after their contents | changed line | fixed | 4 |
| 336 | src/export/htmlExport.ts | 154 | High | Logic & Correctness | Files copied by resolveResources are absent from createdPaths, so a later failure leaves partial images behind while claiming cleanup | Have resource resolution return every created destination and include them in transactional cleanup | pre-existing line | fixed | 1 |
| 337 | src/export/htmlExport.ts | 165 | Medium | Logic & Correctness | The standalone resource report is discarded, so failures occurring only during embedding are omitted from warnings and missingCount | Merge and deduplicate diagnostics from folder and standalone resolution | changed line | fixed | 1 |
| 338 | src/export/htmlExport.ts | 175 | High | Duplication | KaTeX fonts are downloaded and embedded here even though standalone generation already includes bundled KaTeX CSS with embedded fonts | Use the bundled KaTeX asset pipeline once; only package the required local stylesheet and fonts for index.html | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 339 | src/export/htmlExport.ts | 180 | Medium | Logic & Correctness | Equal document and monospace font settings append duplicate FontFile entries, causing duplicate downloads, writes, CSS rules, and inflated size accounting | Deduplicate fonts by filename or canonical URL before downloading | changed line | fixed | 1 |
| 340 | src/export/htmlExport.ts | 266 | Medium | Logic & Correctness | Both templates retain their default includeKaTeX=true even when contentHasMath is false, adding an unnecessary CDN dependency to index.html and a large bundled payload to standalone.html | Compute hasMath once and pass includeKaTeX: hasMath to both template calls | changed line | fixed | 1 |
| 341 | src/export/htmlExport.ts | 298 | Medium | Logic & Correctness | totalSize excludes copied image resources, so the documented total exported size is systematically understated | Add sizes of successfully written image outputs, avoiding source sizes for failed copies | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 342 | src/export/useExportOperations.ts | 1 | Low | Refactoring Debt | The file is 323 lines, exceeding the project’s approximately-300-line limit | Move print-specific operations and HTML construction into a dedicated module | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 343 | src/export/useExportOperations.ts | 44 | Medium | Refactoring Debt | `exportToHtml` exceeds 30 lines and mixes validation, dialog handling, path manipulation, rendering, settings access, export, warnings, and UI reporting | Extract path selection, rendering/configuration, and result reporting into focused helpers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 344 | src/export/useExportOperations.ts | 148 | Medium | Refactoring Debt | `exportToPdfNative` exceeds 30 lines and owns the entire render/resource/window/error workflow | Extract preparation of self-contained export HTML into a shared helper | changed line | fixed | 1 |
| 345 | src/export/useExportOperations.ts | 214 | Medium | Refactoring Debt | `exportToPdfBrowser` is an 81-line orchestration function with deeply mixed DOM, resource, CSS, HTML-template, IPC, and notification responsibilities | Split source selection, document construction, and print invocation into independently testable functions | changed line | fixed | 1 |
| 346 | src/export/useExportOperations.ts | 231 | High | Logic & Correctness | `document.querySelector(".ProseMirror")` selects the first editor in the DOM, so split-document layouts can print a background pane instead of the active document | Resolve the active editor view by window and pane, or render the already-resolved active document markdown consistently | changed line | fixed | 2 |
| 347 | src/export/useExportOperations.ts | 300 | Medium | Logic & Correctness | `copyAsHtml` accepts empty/whitespace markdown and reports a successful copy, unlike both export operations | Apply the same non-empty-content validation before rendering and copying | pre-existing line | fixed | 1 |
| 348 | src/export/waitForAssets.ts | 38 | High | Logic & Correctness | `document.fonts.ready` is awaited before polling begins and is not bounded by `timeout`; a stalled font load can keep the export promise pending forever | Race font readiness against the remaining deadline or include fonts in the bounded polling loop | changed line | fixed | 1 |
| 349 | src/export/waitForAssets.ts | 99 | Medium | Logic & Correctness | Math readiness depends on English text containing `"rendering"` or `"math"`; localized loading text can be treated as ready, while terminal error text mentioning math can wait until timeout | Detect explicit lifecycle classes/data attributes rather than user-visible text | changed line | fixed | 1 |
| 350 | src/export/waitForAssets.ts | 179 | Medium | Refactoring Debt | `waitForAssets` exceeds 30 lines and nests polling, progress delivery, stabilization, timeout diagnosis, and scheduling | Extract deadline handling, warning construction, and final-frame stabilization | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 351 | src/export/waitForAssets.ts | 183 | Low | Shortcuts & Patches | `timeout` and `interval` are not validated; negative, non-finite, or zero values cause immediate timeout or pathological polling behavior | Normalize both options to finite non-negative bounds and require a positive polling interval | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 352 | src/export/waitForAssets.ts | 197 | Medium | Logic & Correctness | An exception from `onProgress` during a timer-driven poll escapes the callback and leaves the returned promise permanently pending | Catch callback failures and either ignore/log them or reject/resolve the operation deterministically | changed line | fixed | 1 |
| 353 | src/export/waitForAssets.ts | 199 | Medium | Logic & Correctness | Readiness is captured before two animation frames and never rechecked, so assets added or invalidated during layout can still produce `success: true` with stale status | Recompute stability after the settling frames and resume polling if readiness changed | changed line | fixed | 1 |
| 354 | src/export/waitForAssets.ts | 201 | Medium | Logic & Correctness | Once the code enters the double-`requestAnimationFrame` path, the configured timeout no longer applies; throttled or suspended frames can hang export indefinitely | Keep a deadline timer active through final layout stabilization and provide a non-RAF fallback | changed line | fixed | 3 |
| 355 | src/export/waitForAssets.ts | 209 | Low | Logic & Correctness | A font-only timeout returns `success: false` with an empty warnings array because no warning is generated for `fontsReady === false` | Add a specific unfinished-font warning | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 356 | src/hooks/useCommandBootstrap.ts | 131 | Medium | Refactoring Debt | The effect spans 94 lines and combines command registration, debug publication, service lifecycle, dynamic command discovery, menu mounting, readiness, and teardown | Extract a transactional bootstrap service returning one disposer | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 357 | src/hooks/useCommandBootstrap.ts | 145 | Medium | Dead Code | Development debug handles are published globally but never removed during effect cleanup, allowing commands to remain callable after bootstrap services and editor registrations are disposed | Add an ownership-aware unpublish API and remove these three handles during cleanup | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 358 | src/hooks/useCommandBootstrap.ts | 163 | High | Logic & Correctness | Startup is not transactional: if a runtime service initializer throws, previously started services and registered editor commands are left active because the effect never obtains or returns its cleanup | Wrap initialization in rollback logic and make runtime-service startup dispose already-started services on partial failure | changed line | fixed | 1 |
| 359 | src/hooks/useCommandBootstrap.ts | 197 | High | Logic & Correctness | `mountMenuCommands` can internally swallow individual listener-registration failures and still resolve, after which bootstrap reports menu commands as mounted despite a partially dead native menu | Make mounting fail atomically or return an explicit completeness result; signal readiness only for a complete installation | pre-existing line | fixed | 3 |
| 360 | src/hooks/useCommandBootstrap.ts | 214 | Medium | Logic & Correctness | The one-shot readiness latch is never reset on a completed unmount, so a later remount in the same webview can appear ready before its replacement listeners exist | Couple readiness state to the listener lifecycle or reset it during cleanup | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 361 | src/hooks/useContentServer.ts | 89 | Medium | Logic & Correctness | Unknown or missing output extensions silently select PDF while preserving the incompatible filename, potentially writing PDF data to a `.docx` or extensionless path | Reject unsupported extensions or normalize the output path to `.pdf` | changed line | fixed | 1 |
| 362 | src/hooks/useContentServer.ts | 106 | High | Refactoring Debt | `startServer` exceeds 30 lines and has substantial branching/nesting across budget management, trust reconciliation, server state, nonce acquisition, and errors | Extract a serialized start/reconcile operation with explicit result states | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 363 | src/hooks/useContentServer.ts | 108 | High | Logic & Correctness | A start captures `rootPath` once but commits results after multiple awaits without verifying that the workspace is still current; switching workspaces can publish the old server’s URL into the new workspace UI and leave the old child running | Attach a workspace/generation token to the operation and discard or stop stale results | pre-existing line | not-fixed: stale UI commits are guarded, but safely releasing the old child requires backend leasing because servers are shared by root across windows. | 3 |
| 364 | src/hooks/useContentServer.ts | 125 | High | Logic & Correctness | Concurrent manual starts, crash restarts, and trust reconciliations are not serialized, so completions can arrive out of order and overwrite newer running/error/iframe state | Use a single-flight queue or monotonically increasing operation generation and commit only the latest result | changed line | fixed | 1 |
| 365 | src/hooks/useContentServer.ts | 151 | High | Logic & Correctness | `stop` can race a new `start`; after the new start succeeds, the older stop still executes `store.stop()` and hides the running server | Serialize lifecycle operations or guard the final state transition with the operation generation | changed line | fixed | 1 |
| 366 | src/hooks/useContentServer.ts | 157 | High | Shortcuts & Patches | Stop failures are discarded and the store is unconditionally marked stopped, leaving a potentially live child process represented as stopped | Surface the `CommandError` and retain/refresh running state unless backend stop is confirmed | changed line | fixed | 1 |
| 367 | src/hooks/useContentServer.ts | 165 | High | Shortcuts & Patches | The fixed three-second stop-intent window is timing-dependent; a delayed legitimate exit can be misclassified as a crash and automatically restart a server the user stopped | Have the backend identify intentional exits or track a root-scoped stop generation until explicit acknowledgement | changed line | fixed | 4 |
| 368 | src/hooks/useContentServer.ts | 165 | Low | Dead Code | The stop-guard timer is not retained or cancelled on unmount, so lifecycle-owned work survives disposal | Store the timer ID in a ref and clear it in effect cleanup | changed line | fixed | 1 |
| 369 | src/hooks/useContentServer.ts | 228 | Medium | Logic & Correctness | Restart attempts are reset only by a manual start, not after a successful supervised recovery, so three unrelated crashes over an arbitrarily long session exhaust a budget documented as “consecutive” | Reset attempts after a confirmed stable start or rename and document the policy as a lifetime/manual-session budget | pre-existing line | fixed | 1 |
| 370 | src/hooks/useContentServer.ts | 243 | High | Logic & Correctness | Rapid trust changes launch unawaited overlapping reconciliations, amplifying the out-of-order state and stale-CSP races in `startServer` | Debounce/coalesce trust changes and await them through the serialized lifecycle controller | changed line | not-fixed (rebuttal: the subscriber gates on status === "running" so no trust-driven start overlaps the initial start; a flip during the auth-URL await launches a successor whose generation retires the earlier start at its next checkpoint (tests at useContentServer.test.ts); the residual is same-root IPC ordering in the Rust manager) [verifier: AGREE] | 2 |
| 371 | src/hooks/useContentServer.ts | 255 | Medium | Logic & Correctness | `intentionalStopRef` is a global boolean rather than workspace-scoped; switching roots during its three-second lifetime can suppress a genuine crash for the new workspace | Store the stopped workspace root and operation generation, and suppress only a matching exit | changed line | fixed | 1 |
| 372 | src/hooks/useContentServer.ts | 270 | Medium | Incomplete Error Handling | Failure to install the crash listener is only written to a debug logger even though supervision is now disabled and the UI remains unaware | Put the store into a degraded/error state or expose supervision health to the user | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 373 | src/hooks/useGenieInvocation.ts | 48 | Medium | Refactoring Debt | `runWorkflowGenie` exceeds 50 lines and combines provider resolution, workspace validation, execution registration, IPC, rollback, recents, and presentation errors | Split provider construction, execution registration, and workflow dispatch/rollback into focused functions | changed line | fixed | 1 |
| 374 | src/hooks/useGenieInvocation.ts | 85 | High | Logic & Correctness | Concurrent workflow invocations overwrite the shared execution ID; an earlier failure can then clear a newer execution at line 96 | Acquire a workflow lock or make rollback conditional on the store still owning this execution ID | pre-existing line | fixed | 1 |
| 375 | src/hooks/useGenieInvocation.ts | 129 | High | Logic & Correctness | `cancel()` removes the listener and resets frontend state but does not cancel the running Rust/provider request, allowing continued resource use and background emissions | Add a request-ID-based backend cancellation command and await or safely dispatch it before releasing local ownership | pre-existing line | partial: Request-ID cancellation reaches active providers, but cancellation after `invoke` is sent and before Rust registers the token remains a documented race that can run uncancelled. | 3 |
| 376 | src/hooks/useGenieInvocation.ts | 139 | High | Logic & Correctness | Unmount cleanup unconditionally cancels the global invocation store; because production has multiple hook consumers, one instance can cancel an invocation owned by another | Track ownership by request ID and cancel only when this hook owns the active invocation | changed line | not-fixed (disputed: both consumers are window-lifetime mounts, so unmount is window teardown) [verifier: AGREE] | 1 |
| 377 | src/hooks/useGenieInvocation.ts | 144 | High | Duplication | `invokeGenie` and `invokeFreeform` duplicate precondition checks, extraction, empty-content reporting, prompt construction, and stream dispatch | Extract a shared prompt-invocation pipeline with strategy callbacks for template construction and context radius | changed line | fixed | 1 |
| 378 | src/hooks/useGenieInvocation.ts | 172 | Medium | Logic & Correctness | A genie is added to recents before `runGenieStream` validates provider configuration or acquires its lock, so rejected and never-started invocations pollute history | Have the runner return a started/success status and add the recent entry only after successful lock acquisition | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 379 | src/hooks/useMcpAutoStart.ts | 26 | Medium | Logic & Correctness | A component-local ref does not enforce the documented “once per app session” guarantee; remounting creates a fresh ref and invokes start again | Use module-level/session ownership or rely on a centralized MCP lifecycle store with idempotent startup | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 380 | src/hooks/useMcpAutoStart.ts | 32 | Medium | Shortcuts & Patches | The attempt is permanently marked complete before startup succeeds, while failure is only sent to a development log, leaving transient startup failures invisible and unrecoverable | Mark success after the command resolves and expose failure/retry through shared MCP state or user-visible status | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 381 | src/hooks/useMcpServer.ts | 73 | Medium | Logic & Correctness | `refresh()` is an asynchronous operation but never updates `loading`, contradicting the result contract and allowing controls to act while status is unresolved | Include refresh in reference-counted or operation-ID-based loading state | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 382 | src/hooks/useMcpServer.ts | 87 | High | Logic & Correctness | Start, stop, and refresh have no sequencing or stale-response guard; overlapping calls or external events can let an older completion overwrite newer server state, and one completion clears `loading` while another remains pending | Serialize mutations or attach monotonically increasing operation IDs and ignore stale completions | changed line | fixed | 3 |
| 383 | src/hooks/useMcpServer.ts | 87 | High | Duplication | `start` and `stop` duplicate the complete loading/error/invoke/status/finally workflow | Extract a shared bridge-operation helper parameterized by command name | changed line | fixed | 1 |
| 384 | src/hooks/useMcpServer.ts | 121 | High | Logic & Correctness | Initial status fetching begins before event subscriptions are installed, creating a window where a start/stop transition can occur after the returned snapshot but before listeners become active | Install both listeners first, then refresh after `Promise.all`, with an unmount guard | changed line | fixed | 1 |
| 385 | src/hooks/useMcpServer.ts | 140 | Low | Logic & Correctness | A stopped event updates running state but retains any prior error, whereas a started event clears it, producing inconsistent stale-error behavior | Define a consistent event policy and clear or refresh the error on authoritative stop events | changed line | fixed | 1 |
| 386 | src/hooks/usePromptHistory.ts | 65 | Medium | Logic & Correctness | Ghost text and dropdown entries read Zustand through `getState()` inside memoization, so history changes alone do not trigger a render and open suggestions can remain stale | Subscribe with a selector such as `usePromptHistoryStore(s => s.entries)` and derive both views from that value | changed line | fixed | 1 |
| 387 | src/hooks/usePromptHistory.ts | 80 | Medium | Logic & Correctness | Changing the dropdown filter does not reset or clamp `dropdownSelectedIndex`; shrinking results can leave selection out of range and Enter silently closes without selecting | Reset selection to zero on filter changes or clamp it whenever entries change | changed line | fixed | 3 |
| 388 | src/hooks/usePromptHistory.ts | 141 | Medium | Logic & Correctness | The documented “Escape clears ghost text” behavior is absent when the dropdown is closed | Handle Escape in normal mode and maintain a suppression state that resets when the draft changes | pre-existing line | fixed | 2 |
| 389 | src/hooks/usePromptHistory.ts | 141 | High | Refactoring Debt | `handleKeyDown` spans roughly 90 lines with several interaction modes and deeply nested branches, giving it high cyclomatic complexity and making precedence bugs likely | Split IME, dropdown, ghost-text, and cycling handling into small ordered handlers returning whether they consumed the event | pre-existing line | fixed | 1 |
| 390 | src/hooks/usePromptHistory.ts | 157 | Low | Logic & Correctness | ArrowDown on an empty dropdown calculates `Math.min(1, -1)` and stores `-1` as the selected index | Ignore navigation when there are no entries and clamp within `[0, length - 1]` only for nonempty lists | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 391 | src/lib/browser/agent/recorderShimSensitivity.src.js | 24 | High | Logic & Correctness | Sensitive text controls are recognized only through ASCII identifier tokens and selected attributes; common OTP/security fields identified solely by visible labels, `aria-labelledby`, placeholders, or localized text are classified non-sensitive | Reuse the recorder’s accessible-name calculation and add tested normalization for common verification/security terminology and localized labels | changed line | fixed | 3 |
| 392 | src/lib/browser/agent/recorderShimSensitivity.src.js | 37 | Low | Shortcuts & Patches | The no-WeakMap compatibility fallback writes enumerable VMark-specific properties onto page-owned elements, exposing implementation details and permitting page code to alter classification | Require WeakMap on supported engines or keep fallback state in a private bounded collection without mutating page elements | pre-existing line | fixed | 1 |
| 393 | src/lib/browser/agent/recorderShimSensitivity.src.js | 52 | Medium | Shortcuts & Patches | Broad empty catches make classifier and marking failures silently fail open as non-sensitive, which is unsafe at a sensitivity boundary | Preserve page isolation but record an internal failure signal and conservatively classify the affected field as sensitive | changed line | fixed | 4 |
| 394 | src/lib/browser/agent/recorderShimSensitivity.src.js | 100 | High | Logic & Correctness | The permanent observer tracks only `type`; removal or laundering of `autocomplete`, `name`, `id`, or `aria-label` before focus/input loses an otherwise-sensitive classification | Observe every sensitivity-defining attribute and permanently mark targets whose old or new attribute state is sensitive | changed line | fixed | 1 |
| 395 | src/lib/browser/url.ts | 176 | High | Logic & Correctness | SPA-style fragments such as `#/callback?access_token=secret` are parsed as one parameter name, so credentials remain persisted | Parse fragment routes and their nested query separately before rebuilding the fragment | changed line | fixed | 1 |
| 396 | src/lib/browser/url.ts | 177 | High | Logic & Correctness | Credential-name matching misses common camelCase keys such as `accessToken`, `refreshToken`, `sessionId`, and `apiKey` | Normalize camelCase boundaries or compare canonicalized parameter names against an explicit set | changed line | fixed | 1 |
| 397 | src/lib/browser/url.ts | 185 | High | Logic & Correctness | Parse failures return the original URL unchanged, allowing malformed credential-bearing input to be written to disk despite this function being the persistence redactor | Fail closed by returning a non-secret placeholder, origin-only value, or `null` | changed line | fixed | 2 |
| 398 | src/lib/browser/url.ts | 194 | Medium | Logic & Correctness | Credential path detection examines encoded segments without decoding them, so forms such as `password%2Dreset` bypass the flow-word check | Safely decode each segment before classification and treat decode failures conservatively | changed line | fixed | 1 |
| 399 | src/lib/formats/adapters/htmlTrust.ts | 81 | Medium | Logic & Correctness | `token` is interpolated directly into a URL path, so `/`, `?`, `#`, percent escapes, or Unicode can change URL structure or prevent protocol lookup | Encode the token as one path segment with `encodeURIComponent`, or constrain it with a validated opaque-token type | changed line | fixed | 1 |
| 400 | src/lib/formats/registry.ts | 23 | Medium | Dead Code | `always-when-registered` remains in the public adapter contract while an empty allow-list makes every use fail registration | Remove the unsupported union member and validation branch until needed, or register the actual supported IDs | changed line | fixed (maintainer decision 2026-09-08: drop — the whole sidePanelKeepAlive field went, not just the dead member: no adapter declared it and no code read any of its three values, so ALWAYS_KEEP_ALIVE_ALLOW_LIST and the registration check went with it) | 3 |
| 401 | src/lib/formats/registry.ts | 52 | High | Refactoring Debt | `registerFormat` spans 122 lines with numerous independent validation branches, duplicate checks, normalization, and state mutation, creating high cyclomatic complexity | Extract focused validators and a normalization function, then keep registration as validate-normalize-commit | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 402 | src/lib/formats/registry.ts | 98 | High | Duplication | The WYSIWYG-component requirement is checked again at line 132; the latter branch is unreachable and carries a conflicting error message | Keep one authoritative validation with one error message | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 403 | src/lib/formats/registry.ts | 168 | High | Logic & Correctness | The registry stores the caller-owned mutable config while indexing its original ID and normalized extensions; later mutation can desynchronize `formats`, `byId`, and `byExt`, and listed extensions remain unnormalized | Store an immutable normalized snapshot and freeze or deeply copy invariant-bearing fields | pre-existing line | fixed | 2 |
| 404 | src/lib/formats/registry.ts | 218 | High | Logic & Correctness | A pathed unknown file falls back to Markdown when `txt` is unavailable, directly violating the documented “NEVER markdown” contract and potentially granting WYSIWYG behavior to an unknown format | Require the plain-text format for pathed files and throw a clear bootstrap error if it is absent | pre-existing line | fixed | 2 |
| 405 | src/lib/lintEngine/ruleMeta.ts | 18 | High | Duplication | Rule IDs and severities are deliberately redeclared separately from their emitters, leaving multiple sources of truth whose consistency depends on a bespoke synchronization test | Export metadata from each rule definition and derive both the rule collection and documentation table from those definitions | new file | partial: Severity is centralized, but every rule ID remains duplicated between `RULE_ROWS` and `ruleEmission("ID")` call sites, still requiring synchronization tests. | 3 |
| 406 | src/lib/lintEngine/ruleMeta.ts | 51 | Medium | Logic & Correctness | Titles are hardcoded English strings even though the comments state they are rendered in the diagnostics UI, producing untranslated UI in non-English locales | Store translation keys or localize titles at the UI boundary | new file | fixed (maintainer decision 2026-09-08: localize, keep id visible — t("lint.rule.<ID>") across all ten editor.json bundles, localized at the RuleBadge boundary so ruleMeta.ts stays dependency-free for the docs gate; the id remains the pill text and the translated title rides in the tooltip and sr-only suffix) | 3 |
| 407 | src/lib/lintEngine/ruleMeta.ts | 51 | Low | Logic & Correctness | `readonly RuleMeta[]` protects only array operations at compile time; exported element objects remain mutable and `BY_ID` shares those references | Use `Readonly<RuleMeta>` entries and freeze the exported data, or expose a lookup API without exporting mutable objects | new file | fixed | 1 |
| 408 | src/pages/settings/AdvancedSettings.tsx | 12 | High | Duplication | Imports `isMacPlatform` from the shortcut matcher despite the canonical platform utility already defining it, leaving two implementations with different behavior | Import from `@/utils/platform` and remove the duplicate helper when callers are migrated | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 409 | src/pages/settings/AdvancedSettings.tsx | 14 | Medium | Refactoring Debt | The 191-line component mixes six independent settings sections and extensive feature-gating logic | Extract focused group components such as browser, workflow, performance, and macOS settings | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 410 | src/pages/settings/AdvancedSettings.tsx | 106 | High | Logic & Correctness | The macOS conditional also hides all embedded-browser settings on Windows and Linux, although only quarantine handling is macOS-specific | Close the macOS group after the quarantine row and place browser settings in a separate cross-platform group | pre-existing line | not-fixed (disputed: deliberate — the embedded browser is macOS-only (website/guide/browser.md; browserCommands.ts gates on isMacPlatform) and AdvancedSettings.nonMac.test.tsx pins hiding the whole group off macOS) [verifier: AGREE] | 1 |
| 411 | src/pages/settings/AppearanceSettings.tsx | 28 | Medium | Refactoring Debt | `ThemeSwatchRow` is a 75-line component combining platform filtering, theme lookup, presentation, and selection behavior | Extract the individual swatch and move theme filtering into a small helper | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 412 | src/pages/settings/AppearanceSettings.tsx | 104 | Medium | Refactoring Debt | `AppearanceSettings` exceeds 80 lines and owns theme, window, and focus-mode sections | Extract each settings group into a focused component | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 413 | src/pages/settings/AppearanceSettings.tsx | 106 | Medium | Refactoring Debt | Subscribes to the entire `appearance` object, violating the project’s selector convention and rerendering the whole page for every appearance-field update | Subscribe to each rendered field with narrow selectors | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 414 | src/pages/settings/AppearanceSettings.tsx | 110 | Low | Shortcuts & Patches | `??` fallbacks are repeated for required, deep-merged settings (`followSystemAppearance`, `showFilenameInTitlebar`, and `focusModeDim`), masking broken state instead of enforcing the store contract | Remove the fallbacks or normalize legacy data exclusively at the persistence boundary | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 415 | src/pages/settings/EditorSettings.tsx | 69 | Medium | Logic & Correctness | `TypographyConfig` permits every `AppearanceSettings` key and uses an unrelated `isNumeric` flag, so future entries can type-check while writing a string or number to an incompatible setting | Use a discriminated union that binds each permitted key to its exact option and value type | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 416 | src/pages/settings/EditorSettings.tsx | 74 | Low | Dead Code | `optionLabelKey` is declared but never populated or read | Remove the property | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 417 | src/pages/settings/EditorSettings.tsx | 77 | Medium | Refactoring Debt | The 224-line component combines option construction, compatibility normalization, dependent-setting logic, and five UI sections | Extract typography, behavior, display, and large-file groups and isolate dependent auto-pair state | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 418 | src/pages/settings/EditorSettings.tsx | 79 | Medium | Refactoring Debt | Lines 79–82 subscribe to four complete Zustand sections, contrary to the required narrow-selector pattern | Select only the individual fields rendered by this page | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 419 | src/pages/settings/EditorSettings.tsx | 125 | Medium | Shortcuts & Patches | Lines 125–130 hardcode English CJK-spacing descriptions in an otherwise localized settings page | Add translation keys for the qualitative labels while retaining the numeric `em` values | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 420 | src/pages/settings/ShortcutsSettings.tsx | 28 | High | Refactoring Debt | The 262-line component has high complexity across filtering, importing, exporting, confirmation, category rendering, row rendering, capture state, and modal orchestration | Extract import/export controls, shortcut lists, and row components; move filtering into a dedicated hook/helper | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 421 | src/pages/settings/ShortcutsSettings.tsx | 98 | Medium | Logic & Correctness | `FileReader` has no `onerror` or `onabort` handling, so filesystem/read failures produce no user feedback | Handle both events and surface a localized error | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 422 | src/pages/settings/ShortcutsSettings.tsx | 100 | High | Logic & Correctness | A partially invalid import is applied before failure is reported; valid entries replace the current map while invalid entries are discarded, potentially wiping bindings despite an error message | Validate the complete document first and apply atomically, or explicitly preview and confirm a partial import | pre-existing line | fixed | 3 |
| 423 | src/pages/settings/ShortcutsSettings.tsx | 102 | Low | Shortcuts & Patches | Uses a hardcoded English browser `alert`, bypassing localization and the application’s dialog/error UI | Use translated text and the established application dialog/toast service | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 424 | src/pages/settings/ShortcutsSettings.tsx | 109 | Medium | Refactoring Debt | The 66-line nested row renderer is recreated on every render and mixes data lookup, conditional controls, styling, and inline SVG markup | Extract a typed `ShortcutRow` component and pass resolved values and callbacks | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 425 | src/pages/settings/ShortcutsSettings.tsx | 282 | High | Logic & Correctness | Conflict detection receives the shortcut’s existing binding, not the newly captured chord; the modal therefore shows a stale or missing conflict warning and can silently create collisions | Move conflict calculation into `KeyCapture` using `capturedKey`, or report captured-key changes to the parent | pre-existing line | fixed | 1 |
| 426 | src/plugins/actions/actionDefinitionsBlocks.ts | 14 | High | Duplication | Every entry repeats its object key as `id`, repeats category strings, and repeats identical support flags; `Record<string, ActionDefinition>` does not enforce key/id equality, so the copies can drift silently | Introduce a typed category/default-support builder that derives `id` from the key and preserves exhaustive `ActionId` checking | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 427 | src/plugins/actions/actionDefinitionsBlocks.ts | 18 | Medium | Shortcuts & Patches | User-facing action labels are hardcoded in English throughout the registry, bypassing the application’s localization system | Store translation keys or action IDs and resolve localized labels at the presentation boundary | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 428 | src/plugins/actions/menuMapping.ts | 16 | Medium | Logic & Correctness | `Record<MenuEventId, ...>` falsely guarantees a mapping for every `menu:${string}` value, so unknown or omitted events type-check but return `undefined` at runtime | Define a closed union of supported menu events, or use `Partial<Record<...>>` and require callers to handle missing mappings | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 429 | src/plugins/actions/types.ts | 183 | Medium | Logic & Correctness | `ActionDefinition` does not correlate `id` with `defaultParams`; non-heading actions may receive heading parameters while `setHeading` may omit them | Define `ActionDefinition` as a discriminated union keyed by `ActionId`, requiring heading parameters only for `setHeading` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 430 | src/plugins/syntaxReveal/marks.ts | 26 | High | Refactoring Debt | `findAnyMarkRangeAtCursor` exceeds 30 lines and nests iteration and conditionals more than three levels, obscuring its boundary and tie-breaking behavior | Extract candidate collection and deterministic candidate selection into small pure helpers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 431 | src/plugins/syntaxReveal/marks.ts | 39 | Medium | Logic & Correctness | Both ends of a text node are treated as inclusive, so a cursor at a boundary can match both the preceding and following nodes and select a mark from the wrong side | Resolve the cursor-associated node using ProseMirror position semantics, or use half-open ranges with an explicit end-of-parent fallback | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 432 | src/plugins/toolbarActions/adapterActions.ts | 26 | High | Duplication | The action vocabulary is manually repeated across this array, `_ACTION_IDS`, menu mappings, applicability rules, and safety lists; adding `insertToc` required synchronized edits in several registries and can drift silently | Introduce one typed action metadata registry and derive IDs, mappings, applicability, and safety sets from it | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 433 | src/plugins/toolbarActions/sourceAdapter.ts | 45 | Low | Dead Code | Orphaned “Re-export formatCJKCurrentBlock” comment describes an export that does not exist | Remove the stale comment or restore the intended explicit export | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 434 | src/plugins/toolbarActions/sourceAdapter.ts | 67 | High | Refactoring Debt | `performSourceToolbarAction` is a 187-line, highly cyclomatic dispatcher; adding or reviewing an action requires navigating roughly 80 switch branches and synchronized category logic | Split category dispatchers or use typed action-to-handler maps while retaining exhaustive action coverage | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 435 | src/plugins/toolbarActions/sourceBlockPlacement.ts | 58 | Medium | Refactoring Debt | `insertBlockText` exceeds 50 lines and combines structure detection, separator policy, prefix transformation, insertion-range calculation, dispatch, and focus side effects | Extract a pure placement planner returning `{ changes, anchor }`, leaving dispatch/focus in the wrapper | changed line | fixed | 1 |
| 436 | src/plugins/toolbarActions/sourceBlockPlacement.ts | 130 | Low | Logic & Correctness | `mapOffsetThroughPrefix` claims to clamp offsets past the template end but returns `mapped - 1`; for an empty prefixed template this can be before the actual body end | Clamp the input to `[0, text.length]` or return `indentBlock(text, prefix).length` after exhausting the lines | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 437 | src/plugins/toolbarActions/sourceBlockPlacement.ts | 152 | High | Duplication | `continuationPrefix` independently reimplements Markdown quote/list parsing and has already drifted from the shared grammar: it accepts unlimited quote indentation, Unicode whitespace, and ordered markers longer than CommonMark’s nine-digit limit | Reuse a shared parsed-line/container representation with the same CommonMark limits as `lineContent.ts` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 438 | src/plugins/toolbarActions/sourceBlockPlacement.ts | 226 | Medium | Logic & Correctness | The `^\\s*(?:>\\s?)*\\s*` wrapper treats NBSP and four-or-more-space indented code as ordinary indentation; inserting a list marker after that prefix can leave the line as indented code or place the marker after non-Markdown whitespace while still returning success | Parse only valid Markdown container indentation and either normalize indented-code input or reject the action | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 439 | src/plugins/toolbarActions/sourceInsertActions.ts | 54 | Low | Dead Code | Consecutive JSDoc blocks leave the code-block documentation detached from `insertCodeBlock`; the following `blockBodyForWrapping` declaration receives the nearest block instead | Move the code-block JSDoc directly above `insertCodeBlock` and keep only the wrapping-helper documentation above `blockBodyForWrapping` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 440 | src/plugins/toolbarActions/sourceInsertActions.ts | 101 | High | Logic & Correctness | Block conversion relies on a line-based span that merges adjacent Markdown blocks unless separated by a blank line; a caret in `paragraph\n# heading`, a thematic break, quote, or HTML block can therefore convert unrelated neighboring blocks together | Resolve the enclosing top-level block from CodeMirror’s Markdown syntax tree before replacing text | pre-existing line | partial: Heading, thematic-break, fence, and quote boundaries improved, but the implementation remains line-based and explicitly leaves list and HTML boundaries unresolved. | 3 |
| 441 | src/plugins/toolbarActions/sourceInsertActions.ts | 158 | High | Duplication | Diagram insertion routes arbitrary selected text through fixed three-backtick builders even though this file separately implements adaptive fence sizing for code blocks; a selection containing ``` prematurely closes the generated diagram fence | Centralize fenced-block construction and choose a delimiter longer than every backtick run in the selected/default content | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 442 | src/plugins/toolbarActions/sourceInsertActions.ts | 186 | Medium | Refactoring Debt | `handleBuildInsert` exceeds 45 lines and combines selection expansion, destructive range replacement, markup normalization, quote reconstruction, cursor remapping, and dispatch | Extract pure helpers for range/body resolution and quoted-result remapping, then keep orchestration concise | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 443 | src/plugins/toolbarActions/sourceInsertActions.ts | 203 | High | Logic & Correctness | Selection-consuming alert/details/math/diagram actions use the same incomplete block-span heuristic, so a partial selection can replace additional adjacent Markdown blocks that the user never selected | Use syntax-tree block boundaries and add coverage for adjacent headings, quotes, lists, thematic breaks, and HTML blocks without blank separators | pre-existing line | partial: Selection-consuming actions use the same incomplete heuristic and can still absorb adjacent list or HTML blocks. | 3 |
| 444 | src/plugins/toolbarActions/sourceInsertActions.ts | 260 | High | Logic & Correctness | Unfencing replaces the opening delimiter line through the closer with only interior lines; for a list-contained fence such as `- ````, this deletes the list marker and changes or destroys the enclosing list structure | Preserve the opener’s container/list prefix and restore it onto the first body line when removing the fence | pre-existing line | fixed | 4 |
| 445 | src/plugins/toolbarActions/wysiwygAdapter.ts | 74 | Medium | Logic & Correctness | Multi-selection helpers are treated as successful when they return `true`, but their implementations return `true` without propagating underlying Tiptap command failures; this suppresses accurate failure reporting at lines 74, 136–151, and 175–181 | Make the helpers return actual command outcomes and have this dispatcher propagate them | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 446 | src/plugins/toolbarActions/wysiwygAdapter.ts | 87 | High | Refactoring Debt | `performWysiwygToolbarAction` is a 207-line, extremely high-cyclomatic-complexity dispatcher; additions can easily break unrelated routing or omit required policy handling | Split routing into typed category maps/dispatchers and enforce exhaustive action coverage | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 447 | src/plugins/toolbarActions/wysiwygAdapter.ts | 87 | Medium | Logic & Correctness | Accepting arbitrary `string` values makes the switch non-exhaustive; newly added or misspelled action IDs silently return `false` | Accept the project’s `AdapterAction`/`ActionId` union and add a compile-time exhaustive check | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 448 | src/plugins/toolbarActions/wysiwygAdapter.ts | 99 | High | Duplication | Inline-mark, list, and blockquote cases repeat nearly identical dispatch and fallback structures, increasing drift risk across aliases and multi-selection behavior | Replace repeated cases with typed action-to-handler maps and one shared multi-selection routing helper | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 449 | src/plugins/toolbarActions/wysiwygAdapterBlockInsert.ts | 60 | Medium | Logic & Correctness | The insertion chain’s `.run()` result is discarded and the function always returns `true`, falsely reporting success when schema constraints or selection placement reject insertion | Return the result of `.run()` directly | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 450 | src/plugins/toolbarContext/types.ts | 15 | Medium | Refactoring Debt | `CursorContext` permits contradictory states such as `hasSelection: true` without `selectionInfo`, WYSIWYG with source-only block math, and headings containing both source and WYSIWYG coordinates | Model selection and surface-specific context as discriminated unions | changed line | not-fixed (convention: smallest-targeted-fix — the union rewrite is a cross-file contract change (tiptapContext.ts builds the context field-by-field with a non-literal hasSelection; enableRules, linkPopupUtils, multiSelectionContext consume it); the branch only rewrote this file's header) [verifier: AGREE] | 1 |
| 451 | src/plugins/toolbarContext/types.ts | 136 | Low | Logic & Correctness | Heading level is typed as unrestricted `number` although the documented domain is exactly 0–6, allowing invalid values to escape compile-time checking | Use `0 / 1 / 2 / 3 / 4 / 5 / 6` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 452 | src/services/commands/registerAllCommands.ts | 4 | Low | Dead Code | Documentation says the function registers thirteen groups and that every non-editor registration is a second-call no-op, but it now registers fifteen groups and owner-based registrars replace their batches | Update the contract comment to reflect the actual count and replacement semantics | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 453 | src/services/commands/registerAllCommands.ts | 29 | High | Logic & Correctness | Registration is a non-atomic sequence with no rollback; if any later registrar throws, earlier commands remain installed and subsequent first-command guards can preserve a permanently partial registry | Register all groups through an atomic owner-based batch or collect disposers and roll back in a `catch` | changed line | fixed | 3 |
| 454 | src/services/commands/registerAllCommands.ts | 37 | Medium | Logic & Correctness | `registerClaimCommands` only uses a module-local `registered` latch, so hot-module reload resets the latch while the command bus survives and can make this bootstrap throw on duplicate registrations | Make claim registration bus-aware or owner-based like the other HMR-safe registrars | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 455 | src/services/commands/tabCommands.ts | 33 | Medium | Refactoring Debt | `buildTabCommandSpecs` spans roughly 90 lines, obscuring individual command behavior and increasing merge risk | Split lifecycle, navigation, and status-bar specifications into focused builders. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 456 | src/services/commands/tabCommands.ts | 98 | Medium | Logic & Correctness | The close operation is detached, so `executeCommand` reports completion before dirty prompts and tab cleanup finish; subsequent commands can observe stale state | Make `run` async and await `closeTabWithDirtyCheck`, retaining the error handler. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 457 | src/services/commands/viewCommands.ts | 31 | High | Duplication | The locally duplicated font default/range (`18`, `12–32`) conflicts with the settings store’s canonical `8–48` range; at size 40, “zoom in” decreases to 32, and at size 10, “zoom out” increases to 12 | Export and reuse one canonical default, bounds, and step policy from the settings domain. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 458 | src/services/commands/viewCommands.ts | 39 | Medium | Refactoring Debt | `registerViewCommands` spans about 230 lines, making registration integrity and per-command behavior difficult to review | Build focused command-spec groups and register them as one declarative batch. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 459 | src/services/commands/viewCommands.ts | 40 | High | Logic & Correctness | The first-ID sentinel cannot detect foreign collisions or partial registration; if a later registration throws, retry exits because the first command remains, permanently leaving an incomplete command set | Use `registerCommands` with an owner token and atomic preflight, as `tabCommands.ts` does. | pre-existing line | fixed | 3 |
| 460 | src/services/commands/windowCommands.ts | 39 | Medium | Logic & Correctness | Focusing the current window is outside the per-window error handling; if it closes during the operation, the entire command rejects after partially rearranging windows | Catch and log the final focus failure or re-resolve and validate the current window before focusing. | new file | fixed | 1 |
| 461 | src/services/commands/windowCommands.ts | 44 | Low | Logic & Correctness | `hasCommand` silently accepts an identically named command from another registrar, potentially retaining the wrong implementation | Register under an owner token so foreign collisions fail explicitly while HMR replacement remains safe. | new file | fixed | 1 |
| 462 | src/services/commands/workspaceCommands.ts | 46 | Low | Shortcuts & Patches | The compatibility re-export preserves an obsolete import path and obscures the guard constant’s actual ownership | Migrate consumers to `openWorkspaceByPath.ts` and remove the re-export. | changed line | fixed | 1 |
| 463 | src/services/commands/workspaceCommands.ts | 63 | Medium | Refactoring Debt | `registerWorkspaceCommands` exceeds 70 lines and mixes dialog handling, transition coordination, persistence, rail resolution, dirty-tab closure, and registration | Extract open and close handlers, then register an atomic specification batch. | changed line | fixed | 1 |
| 464 | src/services/commands/workspaceCommands.ts | 65 | High | Logic & Correctness | The first-command guard can hide a foreign collision and makes a failed second registration unrecoverable because `workspace.openFolder` remains registered | Replace the sentinel and sequential registrations with owner-based atomic `registerCommands`. | pre-existing line | fixed | 1 |
| 465 | src/services/commands/workspaceCommands.ts | 98 | Medium | Logic & Correctness | `openWorkspaceByPath` reports failure by returning `false`, but the result is ignored; its never-throw contract means the surrounding `catch` cannot handle ordinary open failures, leaving users with only a debug log | Check the returned boolean and show a localized failure notification. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 466 | src/services/contentServer/client.ts | 96 | Low | Shortcuts & Patches | `openKbInBrowser` returns a single-use authentication URL after launching it even though production callers need only completion; this unnecessarily exposes a capability URL and invites accidental reuse | Return `Promise<void>` and keep the nonce-scoped URL internal. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 467 | src/services/contentServer/client.ts | 102 | Medium | Logic & Correctness | The graph response is parsed without schema validation and returned as `unknown`; malformed JSON or a valid but incorrect JSON shape fails late or propagates unchecked data | Validate the parsed value against the graph contract and throw a contextual command error. | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 468 | src/services/editor/activeSelectionText.ts | 40 | Medium | Logic & Correctness | `sourceMode` identifies the display mode, not the focused pane; split mode leaves it false, so a retained WYSIWYG selection wins even when the user is working in Source | Prefer the editor reporting focus (`editor.isFocused`/`view.hasFocus`) or track the focused split pane explicitly | new file | fixed | 1 |
| 469 | src/services/editor/activeSelectionText.ts | 42 | Medium | Logic & Correctness | String truthiness conflates “no selection” with a non-collapsed selection whose textual representation is empty, such as an atom/node selection, allowing an unrelated fallback selection to win | Return structured selection state such as `{ hasSelection, text }` and fallback only when `hasSelection` is false | new file | fixed | 1 |
| 470 | src/services/editor/activeSelectionText.ts | 43 | Medium | Logic & Correctness | Fallback does not verify `activeWysiwygTabId` and `activeSourceTabId`, so transient or stale editor registrations can return selection text from another tab | Require matching active tab IDs before crossing surfaces | new file | fixed | 1 |
| 471 | src/services/editor/extractContext.ts | 27 | Low | Refactoring Debt | `extractSurroundingContext` spans roughly 68 lines and combines validation, block lookup, traversal, serialization, and formatting | Extract range validation, indexed child metadata, and directional collection helpers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 472 | src/services/editor/extractContext.ts | 32 | Medium | Logic & Correctness | Inputs are not validated: fractional/NaN radii produce inconsistent counts, while reversed or out-of-bounds ranges are silently mapped to arbitrary blocks | Validate a finite non-negative integer radius and enforce `0 <= from <= to <= doc.content.size` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 473 | src/services/editor/extractContext.ts | 62 | Medium | Shortcuts & Patches | Invalid ranges fall back to the final child, concealing upstream defects and attaching misleading end-of-document context | Reject invalid ranges or return empty context instead of silently selecting the last block | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 474 | src/services/editor/extractContext.ts | 68 | Medium | Logic & Correctness | Empty siblings do not count toward `radius` because termination uses `beforeParts.length`/`afterParts.length`; the function can therefore include blocks farther away than the documented sibling radius | Count visited siblings separately from emitted non-empty strings | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 475 | src/services/editor/extractContext.ts | 68 | High | Duplication | The forward and backward loops duplicate range construction, serialization, trimming, and filtering; both also repeatedly call the linear `childPos`, making extraction quadratic on large documents | Precompute child offsets once and use one shared block-serialization helper | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 476 | src/services/featureFlags/workflowFeatureFlag.ts | 4 | Low | Dead Code | The module comment still claims it provides a YAML-surface fallback, but that export was removed and the file now contains only the engine reader | Remove the stale fallback claim or restore the documented API if it remains required | changed line | fixed | 1 |
| 477 | src/services/navigation/restoreWorkspaceTabs.ts | 38 | High | Duplication | `stillReplaceable` reimplements the clean-untitled-document predicate independently of the existing replaceable-tab policy, creating a policy-drift seam | Extract a shared per-tab replaceability predicate and reuse it from both checks | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 478 | src/services/navigation/restoreWorkspaceTabs.ts | 56 | Low | Refactoring Debt | `restoreOnePath` exceeds 30 lines and still combines media dispatch, I/O, stale-dedup handling, creation, ingestion, rollback, and reporting | Split reading and tab creation/ingestion into focused helpers with explicit result types | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 479 | src/services/navigation/restoreWorkspaceTabs.ts | 62 | Medium | Logic & Correctness | `tryOpenMediaFile` returns whether the path is media, not whether a tab was created; a deduplicated media open is therefore counted as newly created and can trigger blank-tab cleanup incorrectly | Use its `onTabCreated` callback or return a creation result that distinguishes new and existing tabs | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 480 | src/services/navigation/restoreWorkspaceTabs.ts | 84 | High | Logic & Correctness | Restored text tabs bypass `applyFileOwnershipAfterOpen`; unlike normal text and media opens, they are neither assigned to the correct workspace instance nor checked for cross-window writable conflicts, risking multiple writable copies | Apply file ownership immediately after successful ingestion and include that operation in rollback handling | pre-existing line | fixed | 1 |
| 481 | src/services/navigation/restoreWorkspaceTabs.ts | 103 | Low | Refactoring Debt | `restoreWorkspaceTabs` exceeds 30 lines and combines validation, restoration, stale blank-tab cleanup, and MRU repair | Extract path validation/restoration and post-restore reconciliation phases | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 482 | src/services/persistence/splitLayoutPersistence.ts | 53 | Medium | Logic & Correctness | Parsed layouts accept out-of-range/non-finite fractions, empty paths, and identical pane paths, allowing malformed localStorage data into restoration | Validate `Number.isFinite`, clamp/range, non-empty paths, and distinct normalized paths | changed line | fixed | 3 |
| 483 | src/services/persistence/splitLayoutPersistence.ts | 87 | Medium | Logic & Correctness | Legacy migration and cleanup consider only the current raw path spelling; on Windows, a legacy key saved under different casing can be missed or later resurrect stale state | Discover legacy keys by normalized root identity and remove every matching legacy spelling | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 484 | src/services/workspaces/workspaceSession.ts | 68 | High | Refactoring Debt | `persistRailWorkspaceSessions` is an 82-line, high-complexity function combining state capture, ownership partitioning, root grouping, layout selection, migration, IPC, and error handling | Extract pure root-group/session-snapshot helpers and a focused per-root persistence operation | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 485 | src/services/workspaces/workspaceSession.ts | 114 | High | Logic & Correctness | Split state is written before validating or successfully writing the workspace config; malformed disks or IPC failure leave layout and restored-tab records from different snapshots | Validate first and save the split only after the config write succeeds, or implement rollback/transaction semantics | pre-existing line | not-fixed (disputed: the split layout is per-machine UI state validated at restore — restoreSplitLayout skips unless both pane files are open — so a layout newer than the tab list cannot produce a wrong split; ordering it after the write would only lose it on a transient write failure) [verifier: AGREE] | 1 |
| 486 | src/services/workspaces/workspaceSession.ts | 117 | High | Logic & Correctness | The per-root read-modify-write sequence is non-atomic, so concurrent window closes for the same root can read the same config and overwrite each other with nondeterministic session state | Move the update into one serialized backend command or lock/version the root config update | pre-existing line | not-fixed (disputed: the on-disk config holds ONE tab list per root by design, so sequential closes of two windows on one root overwrite each other too (last window wins, as the rail-off path always did); the race only makes the winner nondeterministic — a per-root lock/backend merge is a data-model change, not a defect fix) [verifier: AGREE] | 1 |
| 487 | src/services/workspaces/workspaceSession.ts | 140 | High | Duplication | Rail and legacy branches independently assemble tab paths, session records, split layouts, and config writes; their behavior has already diverged around disk refresh and config repair | Share a session-snapshot/config-patch abstraction and keep only ownership selection mode-specific | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 488 | src/shared/menuIdExtraction.ts | 34 | High | Shortcuts & Patches | The Rust contract relies on a regex tied to one call spelling and ordinary string literals; comments, raw strings, renamed handle variables, constants, or formatting changes can create false positives or silently omit real IDs | Parse Rust syntax with a proper parser or generate a typed menu-ID manifest from Rust | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 489 | src/stores/paneStore.ts | 99 | High | Logic & Correctness | `openSplit`, `setFocusedPaneTab`, and `showTabInPane` accept arbitrary IDs without verifying window membership, document kind, or whether the tab already occupies the opposite pane; activation reconciliation then preserves the invalid duplicate | Centralize pane assignment validation and focus an existing pane instead of duplicating its tab | pre-existing line | not-fixed: the three public actions still accept invalid or duplicate tab IDs and can directly corrupt pane invariants; safe current callers do not make those APIs safe. | 3 |
| 490 | src/stores/paneStoreTypes.ts | 39 | Medium | Unexpected Side Effects | `DEFAULT_SPLIT` is a mutable object returned directly for windows without state, so accidental consumer mutation corrupts the default for every window without a Zustand update | Make `WindowSplit` readonly and freeze the default, or return a fresh default object | changed line | fixed | 1 |
| 491 | src/stores/recentsStore.ts | 36 | Medium | Logic & Correctness | Removing the previously exported `RecentWorkspace` alias is an unnecessary public API break, even though internal references are currently absent | Restore `export type RecentWorkspace = RecentEntry` or deprecate it before removal | changed line | not-fixed (convention: knip runs in check:static and fails on an unused export; the app has no external consumers of the alias, which is why the branch removed it) [verifier: AGREE] | 1 |
| 492 | src/stores/settingsStore.ts | 163 | Medium | Refactoring Debt | The 45-line `merge` callback combines root validation, six migrations, reconciliation, element sanitization, and default augmentation | Extract a named, independently testable settings-hydration function | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 493 | src/stores/settingsStore.ts | 164 | Medium | Logic & Correctness | The root persisted value is cast to an object without validation; an array or string can contribute numeric/unknown keys to live Zustand state because unknown keys pass through reconciliation | Accept the value only when `isPlainObject(persistedState)`, otherwise use `{}` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 494 | src/stores/settingsStore.ts | 176 | Low | Logic & Correctness | Hydration mutates the supplied persisted object and its nested sections in place, creating a surprising side effect for callers and tests retaining that object | Clone the raw persisted value before applying migrations, or make migrations return new objects | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 495 | src/stores/settingsStore.ts | 179 | High | Duplication | Migration orchestration is a manually maintained sequence of calls; adding a migration without updating this list silently leaves it inactive | Export a single ordered migration pipeline from `migrations.ts` | changed line | fixed | 1 |
| 496 | src/stores/settingsStore.ts | 204 | Medium | Logic & Correctness | Unconditionally unioning defaults into persisted `customLinkProtocols` resurrects any default protocol the user deliberately removed on every hydration | Store built-in protocols separately, or apply default augmentation only during an explicitly versioned migration | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 497 | src/stores/settingsStore/defaults.ts | 18 | Medium | Logic & Correctness | The exported canonical defaults object remains mutable; any direct consumer can corrupt future `resetSettings()` results despite the store’s defensive cloning | Export deeply frozen defaults or expose a `createInitialState()` factory returning a fresh value | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 498 | src/stores/settingsStore/defaults.ts | 191 | Low | Refactoring Debt | `ObjectSections` treats every object-valued top-level property — including future arrays — as an updatable settings section and can exclude nullable object sections | Derive the union from an explicit section map or restrict it to plain-record-shaped properties | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 499 | src/stores/settingsStore/migrations.ts | 1 | High | Duplication | `isPlainObject` duplicates the shared implementation already exported by `persistGuards.ts`, allowing trust-boundary behavior to drift | Import and reuse `isPlainObject` from `persistGuards.ts` | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 500 | src/stores/settingsStore/migrations.ts | 11 | High | Duplication | The retired-field migrations repeat the same inspect-object/check-key/delete pattern across `inputGate`, `autoHideStatusBar`, `port`, and `workflowViewer` | Introduce a small typed `removeLegacyField` helper while retaining named migration wrappers | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 501 | src/stores/settingsStore/migrations.ts | 25 | Medium | Logic & Correctness | A corrupt non-boolean `advanced.workspaceRailMode` returns before deletion, so the retired unknown field survives sanitization and is persisted indefinitely | Migrate only valid booleans, but delete the legacy key regardless of its value type | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 502 | src/stores/settingsStore/shortcutDefinitions.ts | 5 | High | Duplication | Shortcut identity and bindings are manually duplicated across this registry, Rust menu definitions, and documentation, creating three sources that can drift | Generate downstream menu/docs artifacts from one canonical manifest, with validation at generation boundaries | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 503 | src/stores/settingsStore/shortcutDefinitions.ts | 33 | Medium | Logic & Correctness | Shortcut IDs are unrestricted strings, so misspelled or duplicate IDs are not rejected by TypeScript and can silently overwrite map entries downstream | Derive a literal `ShortcutId` union from a readonly registry and enforce uniqueness when constructing it | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 504 | src/stores/settingsStore/shortcutDefinitions.ts | 45 | Low | Logic & Correctness | The canonical shortcut registry and derived category order are exported as mutable arrays, allowing consumers to alter global shortcut metadata at runtime | Export readonly arrays and readonly definitions | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 505 | src/stores/workspaceStore.ts | 88 | Medium | Logic & Correctness | `openWorkspace` accepts empty or whitespace-only root paths, creating contradictory workspace state that `bootstrapConfig` subsequently treats as closed | Validate and normalize the root path before updating state | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 506 | src/stores/workspaceStore.ts | 91 | High | Logic & Correctness | Config ingestion retains caller-owned nested `identity`, `ai`, and `sessionTabs` objects; later external mutation can silently alter persisted state or workspace trust without notifying subscribers | Normalize or clone every mutable nested field at the store boundary | pre-existing line | fixed | 1 |
| 507 | src/stores/workspaceStore.ts | 114 | High | Duplication | Config actions repeatedly implement get/null-check/spread/set logic, and their inconsistent cloning already creates different ownership guarantees | Introduce one internal functional config updater that consistently clones and validates the resulting configuration | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 508 | src/stores/workspaceStore.ts | 138 | Low | Logic & Correctness | `addExcludedFolder` accepts empty strings and multi-segment paths even though exclusion matching only recognizes individual path segments, leaving inert configuration entries | Reject blank values and separators, or explicitly support normalized relative-path patterns | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 509 | src/stores/workspaceStore.ts | 207 | Medium | Logic & Correctness | Paths outside the workspace can be reported as excluded when their absolute path contains an excluded segment such as `node_modules` | Verify that the candidate is within `rootPath` before applying workspace-relative exclusions | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 510 | src/types/aiGenies.ts | 25 | Medium | Logic & Correctness | `context` is documented as an integer from 0–2 but typed as any number, permitting negative, fractional, `NaN`, and arbitrarily large values | Type it as `0 / 1 / 2` and retain runtime validation for untrusted data | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 511 | src/types/aiGenies.ts | 39 | Low | Shortcuts & Patches | The optional `kind` backward-compatibility shim prevents exhaustive discrimination and has no stated migration/removal boundary | Normalize legacy entries at ingestion and make `kind` required internally | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 512 | src/types/aiGenies.ts | 81 | Medium | Logic & Correctness | `AiResponseChunk` permits contradictory states such as an error with `done: false`, or completion carrying both content and an error | Replace it with a discriminated union for data, completion, and failure events | pre-existing line | skipped (out of plan scope: pre-existing line) | - |
| 513 | src/hooks/useContentServer.ts | 1 | High | Logic & Correctness | Observed during iteration 2 (not in the Codex audit): nothing resets `contentServerStore` when the workspace switches while a server is already RUNNING — `reset()` has no non-test caller, the hook has no `rootPath` effect, and `KnowledgeBasePanel.tsx` reads no workspace state — so the Knowledge Base panel keeps the previous root's URL; the generation fix (#363) covers only a start in flight | Reset the store (or re-reconcile against the new root) when the workspace root changes while `status === "running"`, with a test | pre-existing line | fixed | 1 |
| 514 | src/services/commands/clipboardCommands.ts | 58 | Medium | Logic & Correctness | Class of #453, observed by the W2-src fixer: ten command modules keep a module-level `registered` flag as their idempotency guard, so after the #453 batch rollback (any later registrar throwing) every already-registered module still has `registered === true` and the remount retry skips its commands (`clipboardCommands` additionally set the flag BEFORE registering, so its own throw also latched it) | Delete the flag and let the bus be the only guard (`hasCommand(sentinel)`, which the rollback clears); pin with one table-driven test over all ten modules — own-throw retry, rolled-back-by-a-later-registrar retry, and idempotence on a populated bus | pre-existing line | fixed | 1 |
| 515 | src-tauri/src/ai_provider/cli_path_guard.rs | 58 | High | Logic & Correctness | Found while testing #375 (not in the Codex audit): `validate_cli_path` rejected every provider outside `KNOWN_CLI_PROVIDERS` BEFORE its `None` short-circuit, and `run_ai_prompt` routes every provider through it, so every REST-provider genie invocation failed with "Unknown CLI provider: <name>" since commit 9d1859fcc (2026-07-23) | Short-circuit on `None` first; an explicit path for an unknown provider is still refused; two regression tests | pre-existing line | fixed | 2 |
