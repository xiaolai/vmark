# Audit Findings

**Run**: audit-fix 20260818-084500 | **Scope**: uncommitted changes (11 production files) | **Audit type**: mini
**Model**: gpt-5.6-sol | **Effort**: high | **Audit threads**: 11 per-file jobs (see audit-results.jsonl)
**Status values**: open | fixed | not-fixed | partial | regressed | skipped (severity filter) | skipped (user stop) | skipped (out of scope)

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | src/utils/platform.ts | 19 | High | Logic | `/win/i` matches any string containing "win" — including `"Darwin"`, which `getRuntimePlatform()` would then classify as Windows and case-fold paths for | Anchor the pattern; add "Darwin" to the test matrix | fixed (round 1) | 1-2 |
| 2 | src/utils/platform.ts | 13 | High | Duplication | Platform detection is not centralized: `shortcutMatch.ts:48`, `FilesImagesSettings.tsx:33`, `terminalSpawnEnv.ts:31`, `sourceWorkflowGoto.ts:90` each implement their own check | Move all detection here and update callers | skipped (out of scope) — repo-wide migration of 4 unrelated call sites | - |
| 3 | src/shell/shellChrome.ts | 32 | High | Duplication | `30px` is restated in `index.css` `:root` and twice as `var(--workspace-rail-width, 30px)` fallbacks in `app-shell.css` | Drop the dead fallbacks or generate both from one source | skipped (out of scope) — the :root default is documented policy (rule 31); the two app-shell.css fallbacks are inert | - |
| 4 | src/shell/AppShell.tsx | 34 | High | Duplication | `CHROME_HEIGHT = 40` duplicates `.title-bar { height: 40px }`; the test reuses the TS constant so it cannot see CSS drift | Publish the height as a CSS var and have title-bar.css consume it | fixed (round 2) | 1-2 |
| 5 | src/shell/AppShell.tsx | 78 | Medium | Logic | `chrome ? H : 0` coerces a `ReactNode`: `chrome={0}` renders content with no padding; `chrome={true}`/`[]` renders nothing but reserves 40px | Narrow `chrome` to `ReactElement \| null` | fixed (round 1) | 1-2 |
| 6 | src/shell/AppShell.tsx | 56 | Low | Logic | `sidebarWidth`'s doc says the 0 default applies "when sidebar is null"; it applies whenever the prop is omitted, so a sidebar can render collapsed | Correct the doc (or require the pair) | fixed (round 1) | 1-2 |
| 7 | src/hooks/useWindowTitle.ts | 47,57 | High | Duplication | filename-or-fallback is written twice, and `formatDocumentTitle` reimplements `getFileNameWithoutExtension()` with different trailing-dot behaviour | Extract a local helper; use the existing util | fixed (round 1) | 1-2 |
| 8 | src/App.tsx | 169 | High | Logic | The platform→chrome wiring has no test at the composition root. **An inverted condition would pass every existing test** | Render `MainLayout` with the platform mocked; assert the chrome slot and CSS vars both ways | fixed (round 1) | 1-2 |
| 9 | src/App.tsx | 61 | High | Logic | Root error boundary is a one-way latch — no retry/reload path, untested | Add recovery UI + tests | partial (round 2) — now TESTED (catches + reports a render failure, App.chrome.test.tsx); still a one-way latch, recovery UI not added | 2 |
| 10 | src/App.tsx | 220 | High | Duplication | `FeatureErrorBoundary`→`Suspense fallback={null}` repeated for Terminal, Settings, PDF Export | Extract `LazyFeatureBoundary` | skipped (out of scope) — App.tsx refactor | - |
| 11 | src/App.tsx | 151 | Medium | Refactoring Debt | `MainLayout` ~100 lines; App.tsx at 297 of the 300-line limit | Extract `ShellSidebar` / `EditorSurface` / `AppOverlays` | skipped (out of scope) — App.tsx refactor; file is at 298/300, flagged for follow-up | - |
| 12 | src/App.tsx | 80 | Low | Shortcuts | Error-fallback styling hardcodes 40/16px, 8px radius, font families, 14px | Move to CSS / tokens | skipped (out of scope) — pre-existing error-fallback styling | - |
| 13 | src/pages/settings/AppearanceSettings.tsx | 31 | Medium | Logic | Off macOS a persisted mac-only theme is filtered out, leaving no swatch selected though `coerceThemeId` resolves one at runtime | Coerce before comparing | skipped (out of scope) — pre-existing AppearanceSettings behaviour | - |
| 14 | src/pages/settings/AppearanceSettings.tsx | 35 | Medium | Logic | Theme swatches expose no `aria-pressed` / radiogroup semantics — selection is visual only | Add `aria-pressed` or radio semantics + a11y test | skipped (out of scope) — pre-existing a11y gap, worth a follow-up | - |
| 15 | src/pages/settings/AppearanceSettings.tsx | 44 | Medium | Shortcuts | `ring-gray-400 dark:ring-gray-500` hardcodes Tailwind palette and uses `dark:` instead of `.dark-theme` | Use tokens | skipped (out of scope) — pre-existing Tailwind palette usage | - |
| 16 | src/pages/settings/AppearanceSettings.tsx | 81 | High | Duplication | Light/dark theme blocks are near-identical markup | Extract a labelled theme-row component | skipped (out of scope) — AppearanceSettings refactor | - |
| 17 | src/pages/settings/AppearanceSettings.tsx | 81 | Medium | Logic | Custom light/dark sections skip `SearchableSection`, so settings search cannot find "Light theme" | Wrap in `SearchableSection` + regression test | skipped (out of scope) — pre-existing settings-search gap | - |
| 18 | src/components/Sidebar/Sidebar.tsx | 127 | High | Logic | Destructive history-clear orchestration untested (cancel, failure, double-click) | Add component tests | skipped (out of scope) — pre-existing untested Sidebar feature | - |
| 19 | src/components/Sidebar/Sidebar.tsx | 131 | Medium | Shortcuts | An `ask()` rejection escapes the `void`-discarded async handler → unhandled rejection | Catch, log, keep the `finally` reset | skipped (out of scope) — REAL pre-existing unhandled rejection, worth a follow-up | - |
| 20 | src/components/Sidebar/Sidebar.tsx | 49 | High | Duplication | View taxonomy split across 5 parallel maps that can drift | One config per sidebar kind | skipped (out of scope) — Sidebar refactor | - |
| 21 | src/components/Sidebar/Sidebar.tsx | 106 | Medium | Logic | Two unchecked casts to `BrowserSidebarView`; the context type does not guarantee it | Make `SidebarContext` a discriminated union | skipped (out of scope) — pre-existing unchecked casts | - |
| 22 | src/components/Sidebar/Sidebar.tsx | 82 | High | Refactoring Debt | ~180-line render function, high branch count | Extract headers, routers, and a history-clear hook | skipped (out of scope) — Sidebar refactor | - |
| 23 | src/components/Browser/BrowserChrome.tsx | 56 | High | Logic | `activePageId` is trusted without checking it names a live browser page; an unknown id binds the omnibox to a nonexistent page | Validate against `view.browserPages`, return null otherwise | fixed (round 2) | 1-2 |
| 24 | src/components/Browser/BrowserWorkspaceSurface.tsx | 33 | High | Logic | The pane-aware path is **unreachable**: `paneStore` admits only `kind === "document"` tabs, so no pane ever holds a browser page. Activating a browser during split view mounts no browser surface at all | Mount the workspace above the split container; drop the pane override | skipped (out of scope) — pre-existing browser/split-view architecture; confirmed the pane path is unreachable | - |
| 25 | src/components/Browser/BrowserWorkspaceSurface.tsx | 34 | High | Duplication | Surface and its `BrowserChrome` child each subscribe to tabs/activeTab and run `getBrowserWorkspaceView` | Compute once, pass down | partial — the surface now passes a validated id, but BrowserChrome still self-resolves for the title-bar placement | - |
| 26 | src-tauri/src/window_manager/document_windows.rs | 112 | High | Logic | Module doc and `lib.rs` both promise "windows start hidden, shown after `ready`" — there is no `.visible(false)` and `mark_window_ready` never shows a window. `lib.rs` drops `StateFlags::VISIBLE` to protect a mechanism that does not exist | Implement it, or delete the false contract | fixed (round 1) — docs corrected, feature NOT implemented | 1-2 |
| 27 | src-tauri/src/window_manager/document_windows.rs | 114 | High | Duplication | `.min_inner_size(800.0, 600.0)` restates `MIN_WIDTH`/`MIN_HEIGHT`, which are themselves used as the DEFAULT size | Name the two pairs separately and use the constants | fixed (round 1) | 1-2 |
| 28 | src-tauri/src/window_manager/document_windows.rs | 104 | High | Logic | The shared builder path is untested — no assertion that title/geometry/platform flags reach the Tauri builder | Extract a spec or use Tauri's mock runtime | skipped (out of scope) — needs Tauri mock-runtime test infrastructure | - |
| 29 | src-tauri/src/window_manager/document_windows.rs | 140 | High | Duplication | Counter/label/cascade logic repeated across three entry points | Centralize allocation | skipped (out of scope) — Rust refactor | - |
| 30 | src-tauri/src/window_manager/document_windows.rs | 39 | High | Duplication | `build_window_url` and `build_window_url_with_files` duplicate query assembly | Share a finalizer | skipped (out of scope) — Rust refactor | - |
| 31 | src-tauri/src/window_manager/document_windows.rs | 180 | Medium | Shortcuts | A malformed preallocated label silently becomes cascade index 0 via `unwrap_or(0)` | Pass a typed identity or fail loudly | skipped (out of scope) — pre-existing silent unwrap_or(0) | - |
| 32 | src-tauri/src/window_manager/document_windows.rs | 70 | Low | Shortcuts | `unwrap_or_default()` turns an (infallible) serialization failure into `files=` with invalid JSON | `expect` with the invariant stated | skipped (out of scope) — pre-existing infallible-serialization fallback | - |
| 33 | src-tauri/src/window_manager/document_windows.test.rs | 197 | High | Duplication | `initial_title_is_never_blank_off_macos` is subsumed by the exact-equality test above it, and its comment's claim to be name-independent is false — it passes `"VMark"` too | Delete or parameterize | fixed (round 1) | 1-2 |

**Clean**: `src/components/WorkspaceRail/index.ts` — no findings.
