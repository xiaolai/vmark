# Audit Findings

**Run**: audit-fix 20260815-113544 | **Scope**: this branch — production files of the trusted-HTML-preview feature, the browser default flip, and the preview-deferral change | **Audit type**: mini (5-dim)
**Model**: gpt-5.6-sol | **Effort**: high | **Sandbox**: read-only (audit) / workspace-write (fix) | **Fixer**: Claude
**Status values**: open | fixed | not-fixed | partial | regressed | deferred (with reason) | rejected (with reason)

Findings are merged and de-duplicated across the 11 per-file audit jobs; several files reported the
same underlying defect, and those are one row here.

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | src-tauri/src/trusted_html/protocol.rs | 63 | High | Logic | `frame-ancestors 'self'` blocks the cross-origin app parent, so the trusted iframe never loads. **VERIFIED by probe in WebKit**: a frame served with `frame-ancestors 'self'` is refused when embedded from a different origin. | Drop the directive; isolation is carried by the opaque-origin sandbox, not by this | **fixed** round 1 — directive removed; regression test asserts it stays absent | 1 |
| 2 | src/lib/formats/adapters/HtmlPreview.tsx | 52 | High | Logic | CSP-meta injection uses `/<head([\s>])/` so an attributed `<head lang="en">` becomes `<head <meta …>lang="en">` — malformed, CSP may not apply. Pre-existing (copied verbatim from the old adapter), now in my file | Replace the whole opening tag via `/<head\b[^>]*>/i` + `$&`; test an attributed head | **fixed** round 1 — whole opening tag replaced; tests for attributed and bare `<head>` | 1 |
| 3 | src/components/Editor/SplitPaneEditor/SplitPaneEditor.tsx | 123,287 | High | Logic | The preview receives DEFERRED content, but `HtmlPreview` **publishes that value** on Enable/Reload — so editing then immediately clicking Reload can publish and execute the previous document. Introduced by my deferral change | Give the preview both a deferred render value and the authoritative content for actions | **fixed** round 1 — `PreviewRendererProps.liveContent` added; actions use it, rendering uses `content` | 1 |
| 4 | src-tauri/src/trusted_html/mod.rs | 22 | High | Dead Code | Module doc states grants end "on explicit revoke, on preview unmount, and on `beforeunload`". Unmount and `beforeunload` revocation were never implemented — I removed them and did not correct the doc | Make the doc describe what the code does | **fixed** round 1 — doc now states the two real cases and records the removed overclaim | 1 |
| 5 | src/services/trustedHtml/trustedHtmlBridge.ts | 64 | High | Dead Code | `revokeAllTrustedHtml` has no production caller; `trusted_html_revoke_all` and `htmlTrustStore.tokens()` likewise. Also unsafe as documented — the command is process-global and would revoke other windows' grants | Remove the orphaned global sweep, or make grants window-scoped and wire real teardown | **fixed** round 1 — global sweep removed from Rust, registry and bridge; superseded by #23's window scoping | 1 |
| 6 | src/lib/formats/adapters/HtmlPreview.tsx | 119 | High | Logic | Whitespace-only content returns before the trust bar renders, so clearing a trusted document removes **Revoke** while the grant stays live | Keep the trust bar mounted; show the empty placeholder in place of the iframe only | **fixed** round 1 — trust bar stays mounted; placeholder replaces the frame only | 1 |
| 7 | src/lib/formats/adapters/HtmlPreview.tsx | 155 | High | Logic | A stored token executes immediately on remount, so toggling view mode or reopening the tab re-runs scripts without Reload — contradicting the "never re-runs itself" contract I documented and stated publicly on #1273 | Separate authorization from active execution; require an explicit run after remount | **partial** round 1 — the empty-content churn (#6) is gone; remount still re-runs the SAME document, which is the per-session grant semantic. Narrowed and documented, not silently dropped | 1 |
| 8 | src/lib/formats/adapters/HtmlTrustBar.tsx | 56 | High | Logic | Confirmation resets only when `trusted` changes. Switching files while both are untrusted leaves the prompt open, so confirming authorizes the **new** file — and can bypass the `canTrust` gate for an unsaved one | Reset confirmation on document identity, not just on trust; block confirm when `!canTrust` | **fixed** round 1 — confirmation scoped to `documentKey` + trust; `canTrust` re-checked at confirm time | 1 |
| 9 | src/lib/formats/adapters/HtmlPreview.tsx | 103 | High | Logic | Reload failures set `error`, but the trusted branch of the trust bar never renders it — a failed publish is silent and leaves stale content on screen | Render the error in both branches; test a rejected publish | **fixed** round 1 — error rendered in the trusted branch too | 1 |
| 10 | src/lib/formats/adapters/htmlTrust.ts | 47 | High | Logic | The URL hardcodes the macOS/Linux custom-protocol form. Windows/Android serve custom protocols as `http://<scheme>.localhost/…`, which neither this URL nor the app `frame-src` covers | Use Tauri's platform-aware conversion and widen `frame-src`; or state the platform limit explicitly | **not-fixed** — deliberate. Correct fix needs `convertFileSrc`'s real output shape, unverifiable without a Windows build; guessing a security-relevant URL is how #1 shipped. Stated in code and in `formats.md` | 1 |
| 11 | src/lib/formats/adapters/html.tsx + src/locales/*/editor.json | - | Medium | Logic | The validator's English fallback messages were reworded for trusted mode, but the gutter may prefer per-`ruleId` locale strings that still say "sandboxed preview will block" | Verify how the gutter resolves these; update all locales if it translates by ruleId | **fixed** round 1 — confirmed the gutter translates by `ruleId`; all 10 locales updated | 1 |
| 12 | src/lib/formats/adapters/HtmlPreview.tsx / trustedHtmlBridge.ts / htmlTrustStore.ts | 81/35/51 | High | Logic | No single-flight guard on Enable/Reload. Rapid confirmations mint multiple backend grants; the store overwrites the token so the earlier one is orphaned but still occupies a `MAX_GRANTS` slot | Guard with a pending state; revoke superseded tokens | **fixed** round 1 — `busy` ref single-flight guard; test double-clicks Enable | 1 |
| 13 | src/services/trustedHtml/trustedHtmlBridge.ts | 36 | Medium | Logic | A malformed-token response is rejected only after the backend grant exists, leaking that grant | Best-effort revoke the returned string before throwing | **fixed** round 1 — malformed token best-effort revoked before throwing | 1 |
| 14 | src/lib/formats/adapters/htmlTrust.ts | 43 | Medium | Logic | `TRUSTED_ALLOW = ""` declares no directives rather than explicitly denying; the test asserts only the string, not denial | Assert the capability behaviour in the WebKit tier rather than the constant | **fixed** round 1 — kept `""` (a cross-origin frame delegates nothing by default); behaviour asserted in the WebKit tier rather than the constant | 1 |
| 15 | src/services/trustedHtml/trustedHtmlBridge.ts | 19 | Medium | Refactoring | A `services/` module imports its token contract from a `lib/formats/adapters/` rendering module — reverses the ADR-013 tier direction | Move the token contract somewhere both may depend on | **fixed** round 1 — token contract moved into the bridge, its only consumer | 1 |
| 16 | src-tauri/src/trusted_html/state.rs | 85 | Medium | Shortcuts | `revoke`/`revoke_all` convert mutex poisoning into apparent success; the commands then report success without revoking | Propagate a typed error | deferred — poisoning requires a panic mid-mutation; the registry now refuses rather than reports success where it can | - |
| 17 | src-tauri/src/trusted_html/state.rs | 37 | High | Logic | `MAX_GRANTS` × `MAX_DOC_BYTES` permits ~1 GiB retained | Track aggregate bytes under the mutex | **fixed** round 2 — `MAX_TOTAL_BYTES` (64 MiB) enforced atomically across grant/publish/revoke, accounted as a delta so a shrink is allowed at a full budget | 2 |
| 18 | src-tauri/src/trusted_html/* + htmlTrust.ts + tauri.conf.json | - | High | Duplication | The scheme string and token length are independently restated in Rust, TypeScript and JSON; the test "verifying" the scheme compares the TS constant to another literal | Add a cross-layer parity gate like `lint:ipc-contract` | **fixed** round 2 — `scripts/check-trusted-html-contract.mjs` + self-test, wired into `check:static` as `lint:trusted-html` | 2 |
| 19 | src/lib/formats/adapters/html.tsx | 40,43 | Medium | Duplication | Three near-identical validator branches; line-splitting defeats multiline attributes and every column is reported as 1 | Table-drive the rules; scan whole source with offsets | **fixed** round 2 — table-driven rules, whole-source scan with real line/column, multiline attributes, every occurrence, document order | 2 |
| 20 | src/components/Editor/SplitPaneEditor/SplitPaneEditor.tsx | 117,153-162 | High | Logic | Schema detection and diagnostics use current content while the renderer gets deferred content — the preview props are not one coherent snapshot | Derive renderer, content and preview diagnostics from one snapshot | **fixed** round 2 — all three now come from `usePreviewModel` | 2 |
| 21 | src/components/Editor/SplitPaneEditor/SplitPaneEditor.tsx | 56-297 | High | Refactoring | ~240-line component owning store selection, schema routing, validation, view modes, resize, IPC and layout; at the 300-line gate | Extract `usePreviewModel`, `useSplitViewMode`, `useResizeFraction` | **fixed** round 2 — `usePreviewModel` extracted; SplitPaneEditor 297 → 271 lines | 2 |
| 22 | src/lib/formats/adapters/HtmlPreview.tsx | 57 | Medium | Refactoring | Component mixes sanitization, transition state, async lifecycle, error handling and both renderers | Extract an execution-state hook | deferred — follows from #3/#7/#12; revisit after those land | - |
| 23 | src-tauri/src/trusted_html/state.rs, commands.rs | 46,23 | High | Logic | Grants carry no window owner, so a destroyed webview leaves them resident until process exit | Key grants by window label; revoke on `WindowEvent::Destroyed` | **fixed** round 2 — grants carry an owner label; `revoke_window` wired to `WindowEvent::Destroyed` beside the browser teardown | 2 |
| 24 | src-tauri/src/trusted_html/protocol.test.rs + webkit test | - | High | Testing | Nothing exercised protocol registration, app `frame-src`, response CSP and script execution TOGETHER — which is why #1 shipped green | Run the real thing in the app | **fixed** round 3 — driven end to end in `pnpm tauri:dev` via Tauri MCP. Verified live: safe preview `sandbox=""`; enable → confirm → frame at `vmark-trusted://doc/<token>?run=1` with `sandbox="allow-scripts" allow=""`; document EXECUTED; `origin=null`, parent document/globals blocked, `__TAURI_INTERNALS__` absent, localStorage blocked, `fetch` blocked, Web Audio available; Reload re-ran at `?run=2`; Revoke returned to the sandboxed frame; closing the window logged `revoked 1 trusted-HTML grant(s) for window 'doc-0'`. NEW FINDING: `window.webkit.messageHandlers` IS reachable from the sandboxed frame, but a well-formed IPC post is rejected with `missing field __TAURI_INVOKE_KEY__` and the command never runs — now recorded in `protocol.rs`. | 3 |
