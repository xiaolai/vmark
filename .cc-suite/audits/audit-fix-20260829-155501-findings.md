# Audit Findings — Round 2

**Run**: audit-fix 20260829-155501 | **Scope**: uncommitted working tree (447 files; round-1 fix surface + fresh half) | **Audit type**: mini
**Model**: codex gpt-5 thread 01a04c77-903b-7f73-a370-f6fd2f1a45c3 (fix-surface half) + Claude workflow wf_3244e30a-3a3 (fresh half: tsx-adoption, locale-copy, docs-vs-code)
**Status values**: open | fixed | not-fixed | partial | regressed | skipped

| # | Location | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|----------|----------|-----------|---------|---------------|--------|-------|
| 1 | dev-docs/design-system.md:65-68 (Cursor policy) vs src/styles/button-shared.css:47 | Medium | doc-vs-code contradiction | design-system.md D7 states 'cursor: default on buttons and controls', and the plan's D7 decision (dev-docs/plans/20260829-ui-consistency.md:127) says the primitives '.vm-btn, .vm-icon-btn, .vm-menu declare cursor: default' — but the canonical text button `.vm-btn` ships `cursor: pointer` (button-sha | Either change `.vm-btn` to `cursor: default` per D7 (and verify no visual QA regression), or amend design-system.md's D7 paragraph to record `.vm-btn` as a deli | fixed | 1 |
| 2 | .claude/rules/33-focus-indicators.md:32-33 vs src/styles/icon-button-shared.css:88,171 and dev-docs/design-system.md:59 | Medium | doc-vs-code contradiction | Rule 33 §1 (rewritten this session) mandates the focus bar use `--accent-primary`: 'Use --accent-primary, not --primary-color … focus rules standardise on the accent name.' Every shipped focus indicator uses `--primary-color`: the flat bars in icon-button-shared.css (lines 88, 171), `.vm-btn:focus-v | Pick one token name. Either switch the four primitives' focus rules to `--accent-primary` (zero visual change today), or delete rule 33's 'Use --accent-primary, | fixed | 2 |
| 3 | .claude/rules/30-ui-consistency.md:22 (also AGENTS.md:602) | Medium | stale doc contradicting current standard | Rule 30's Quick-Rules summary still says 'Popup buttons: Focus = U-shaped underline via ::after, not rings' — the exact pattern rule 33 §1 (updated in this same change set) retired: D4 makes the flat 2px bar 'THE one focus shape' and says the open-U variant 'survives only in not-yet-migrated CSS'. R | Change rule 30:22 to 'Focus = flat 2px bar via ::after (D4, rule 33 §1)' and update AGENTS.md:602's 'U-shaped underline' to match. | fixed | 1 |
| 4 | .claude/rules/32-component-patterns.md:208 | Medium | doc instructs gate-forbidden pattern | The 'Popup Inputs' copy-paste snippet declares `font-family: var(--font-sans)`. The shipped rule is the opposite: popup-shared.css:41 uses `--font-ui`, and the C5 check of lint:ui-consistency forbids `--font-sans` outside document selectors (R3: chrome never uses the reading font). A reader copying  | Change line 208 to `font-family: var(--font-ui);` (and ideally note R3 beside it). | fixed | 1 |
| 5 | .claude/rules/32-component-patterns.md:16-21 | Low | arrived-but-still-'arriving' | Six table rows still say 'arriving in WI-UI3.1 / WI-UI3.3 / WI-UI3.4' for `.vm-overlay`+`.vm-overlay__panel`, `.vm-menu`, `.vm-input`, `.vm-panel`, `.vm-chip`, `.vm-switch` — all of which exist in this working tree (src/styles/overlay-shared.css, input-shared.css, panel-shared.css) and are already c | Delete the '(arriving in WI-x)' parentheticals from the six rows. | fixed | 1 |
| 6 | .claude/rules/32-component-patterns.md:9 and :18 | Low | incomplete variant list | The canonical-components table lists `.vm-btn (+ --primary, --plain, --danger)` — omitting `--cta` (button-shared.css:99) and `--compact` (button-shared.css:74), both of which rule 32's own prose discusses ('.vm-btn--cta exists because…') — and `.vm-input (+ --field, --bare)` omits `--mono` (input-s | Add `--cta`, `--compact` to the `.vm-btn` row and `--mono` to the `.vm-input` row. | fixed | 1 |
| 7 | .claude/rules/32-component-patterns.md:172 | Low | nonexistent class documented | The Popup/Dialog Surface base-pattern snippet documents `.popup-container--vertical { flex-direction: column; gap: 6px; }` — no such class exists anywhere in src (repo-wide grep returns nothing; it did not exist at HEAD either). The same snippet also shows `padding: var(--popup-padding)` while the s | Delete the `.popup-container--vertical` block from the snippet (or re-add the class if a vertical popup variant is wanted), and update the padding line to match | fixed | 2 |
| 8 | .claude/rules/31-design-tokens.md:54-59 | Low | stale-on-arrival prose | The 'Font roles (R3, arriving with WI-UI2.1)' paragraph — added in this change set — ends: 'Until --font-ui is declared, the 32 chrome --font-sans sites are frozen in ui-consistency-baseline.json C5.' Both halves are already false in the same working tree: `--font-ui` IS declared (src/styles/index.c | Drop '(arriving with WI-UI2.1)' and replace the last sentence with the present-tense fact: `--font-ui` is declared and C5 enforces it at zero baseline. | fixed | 1 |
| 9 | .claude/rules/31-design-tokens.md:335 | Low | count no longer matches code | 'All four are written by shellChromeVars(), not by CSS' sits under a Layout table this same diff extended with `--bar-height`, `--shell-card-inset` and `--shell-card-radius` — all also written by shellChromeVars(). The function now returns SEVEN vars (src/shell/shellChrome.ts:161-177: bar-height, ra | Reword to 'Every row below except --sidebar-bg is written by shellChromeVars()' or name the seven vars explicitly. | fixed | 1 |
| 10 | .claude/rules/31-design-tokens.md:407-409 | Low | broken markdown table | Three token rows — `--outline-width`, `--settings-nav-width` (ADDED this session), `--table-border-color` — sit directly after the macos-private-api prose paragraph with no table header, orphaned from the Layout Tokens table dozens of lines above. They do not render as a table (raw pipe text in any  | Move the three rows up into the Layout Tokens table (before the shellChromeVars prose), where they belong. | fixed | 1 |
| 11 | .claude/rules/33-focus-indicators.md:70 | Low | stale count | 'Nine such rules exist; grep -rn "focus: caret-only" src is the audit' — the change set added six new caret-only markers (overlay-shared.css ×2, input-shared.css, FindBar.css, StatusBar.css, Sidebar.css, FileExplorer.css, TerminalSearchBar.css, genie-picker.css among them) without updating the count | Update the count (or replace the literal number with 'run the grep for the current list', which cannot go stale). | fixed | 1 |
| 12 | src/styles/icon-button-shared.css:13 | Low | header names nonexistent modifier | The header says 'Sizes: base/`--md` 26 (--icon-size-md)…', implying a `.vm-icon-btn--md` modifier exists alongside `--sm`/`--lg`. No `--md` selector is declared in this file or anywhere in src (repo-wide grep: zero hits). A reader writing `vm-icon-btn--md` gets a silent no-op class (harmless only be | Change to 'Sizes: base 26 (--icon-size-md), --sm 24 …' — drop the `--md` name, or add the modifier as an explicit alias of the base. | fixed | 1 |
| 13 | src/styles/input-shared.css:14 vs :55 | Low | header contradicts own declarations | The header claims 'Sizes: fields read at --font-size-sm', but the form-field variant it just described (`.vm-input--field`, line 48) overrides to `font-size: var(--font-size-xs)` (11px) at line 55; only the bottom-border base `.vm-input` is `--font-size-sm` (12px). The one variant the sentence most  | Correct the header: base `.vm-input` at --font-size-sm; `--field` at --font-size-xs (or align the code if the size split is unintended). | fixed | 1 |
| 14 | .claude/rules/35-copy-conventions.md:4,12 vs scripts/check-i18n-keys.ts:689-735 | Low | enforcement overstated | Rule 35 says 'The casing REGISTER comes from the key pattern … checkCopyConventions enforces it against scripts/i18n-copy-baseline.json', and its table asserts running copy (*.label, *.description, *.empty, *.placeholder, toast.*) is Sentence case. The implementation enforces only HALF the register: | Either add a sentence-case check for SENTENCE_KEY values in checkCopyConventions (baseline the current violations), or reword rule 35 to state that only the chr | fixed | 1 |
| 15 | src/locales/en/settings.json:203 | Medium | copy-accuracy | Introduced this session: the ellipsis sweep rewrote language.ellipsisNormalization.description from the accurate ". . . -> ..." to ". . . → …", changing the EXAMPLE OUTPUT, not just the arrow. The formatter (src/lib/cjkFormatter/rules/universal.ts:27-51 normalizeEllipsis) produces ASCII "..." in Lat | Restore the accurate example, keeping the arrow convention: ". . . → ..." (or describe the context-dependent output, e.g. ". . . → ... (…… between CJK)"). | fixed | 3 |
| 16 | src/locales/en/settings.json:196 | Medium | copy-accuracy | Introduced this session: the trailing-period sweep (Q3, no trailing period on descriptions) deleted a SEMANTIC period from language.consecutivePunctuation.description — "Limit repeated !?." became "Limit repeated !?". The final "." was the third member of the mark set, not a sentence period: limitCo | Reword so the period is unambiguous, e.g. "Limit repeated ！？。" (use the actual fullwidth marks — they are also more accurate, since the rule touches only fullwi | fixed | 1 |
| 17 | src/locales/ja/settings.json:147 (also zh-CN:147, zh-TW:147, fr:147, ko:147) | Medium | locale-consistency | The rewritten language.cjkFormatting.hint quotes a menu path that does not match those locales' actual menus (src-tauri/locales/*.yml). ja hint: "フォーマット → CJK → 選択範囲をフォーマット" but the ja menu is 書式 → CJK → 選択範囲を書式設定 (top menu AND item name both wrong). zh-CN hint: "格式 → CJK → 格式化所选内容" but the menu is  | In each locale's hint, quote the exact labels from that locale's src-tauri yml: menu.format, menu.format.cjk, menu.format.cjk.selection. Consider a test that de | fixed | 2 |
| 18 | src/locales/en/common.json:170 | Medium | copy-accuracy | The title-case sweep force-converted full sentences under *.title keys into pseudo-title-case: browser.error.title became "This Page Could not Be Loaded." — broken casing ("not" lowered by the gate's STOP_WORDS while "Be" is capped; every style guide caps "Not" as an adverb) plus a trailing period o | Revert these sentence-shaped values to sentence case ("This page could not be loaded.", "Session not fully saved") and baseline them in scripts/i18n-copy-baseli | fixed | 1 |
| 19 | src/locales/en/settings.json:150-159 | Medium | copy-accuracy | Pre-existing but retouched by this session's arrow sweep without repair: four en fullwidth/spacing examples are no-ops because the fullwidth characters were lost at some point (verified by codepoints). fullwidthAlphanumeric (:150) "1 2 3 → 123, A → A" — both sides ASCII; should be １２３ → 123, Ａ → A ( | Restore real fullwidth characters in the en examples so each shows an actual before→after (１２３ → 123; , → ，; () → （）; 中文English → 中文 English), and fix zh-CN's r | fixed | 2 |
| 20 | src/locales/en/editor.json:319-321 | Low | a11y-copy | New keys splitPane.errorCount/warningCount/infoCount ("{{count}} errors" etc., consumed as aria-labels in ValidationGutter.tsx:62/70/78 with { count }) have no _one/_other plural forms, unlike every other count key in the repo (shortcuts.resultCount_one, toast.lintFoundIssues_one). Screen readers he | Split each into _one/_other forms in en and all 9 siblings ("{{count}} error"/"{{count}} errors"), matching the house pattern. | fixed | 1 |
| 21 | src/locales/zh-CN/settings.json:161 (same in all 9 non-en locales) | Low | locale-consistency | The ASCII-arrow-to-→ sweep was applied to exactly ONE sibling key (language.cjkParenthesisSpacing.description) in every non-en locale while its ~10 neighboring descriptions in the same CJK Formatting settings group kept "->" (e.g. zh-CN lines 150-205: fullwidthAlphanumeric, cjkEnglishSpacing, curren | Finish the arrow sweep across the sibling locales' description examples (mechanical: replace " -> " with " → " in the language.* description values of the 9 loc | fixed | 1 |
| 22 | src/locales/ja/dialog.json:254 (also de:254, ko, es) | Low | locale-consistency | The new dialog keys added this session use a different quotation style than the existing keys in the same locale file: ja deleteFile.message uses Western “{{name}}” while ja's existing unsavedChanges.single uses native 「{{title}}」; de's new key uses „{{name}}“ while its existing keys use straight \" | Normalize each locale's dialog quoting to that locale's convention (ja 「」, de „“, fr « », es «» or “”) — at minimum make the new delete/confirm keys match the s | fixed | 1 |
| 23 | src/components/Sidebar/FileExplorer/ContextMenu.test.tsx:24 | Low | test-blind-spot | The test overrides the global react-i18next mock with a local dictionary that still carries pre-sweep copy: "Move to..." (:24) and "Open Terminal Here (max 5 sessions)" (:34), while production locales now say "Move to…" and "Open Terminal Here (Max 5 Sessions)". Same class: MediaPopupView.test.ts:10 | Drop the local t() dictionary in ContextMenu.test.tsx in favor of the global setup mock that resolves real en bundles; update the mock-DOM placeholder literals  | fixed | 1 |
| 24 | src/locales/en/settings.json:448 | Low | locale-consistency | terminal.copyOnSelect.label alone was flipped to sentence case ("Copy on select") while every sibling label in the same Terminal settings group stays Title Case ("Cursor Blink", "WebGL Renderer", "Panel Size", "Mac Option as Meta"…), and the website docs still document the Title Case form (website/g | Either revert this one label to "Copy on Select" to match its siblings, or sweep the whole terminal group (and the two website tables) to sentence case in one p | fixed | 1 |
| 25 | src/plugins/tableOfContents/TocNodeView.ts:96 | Low | i18n | Pre-existing, adjacent to a changed key: the ToC block's empty state is hardcoded English (emptyLi.textContent = "No headings") and never goes through i18n, so all 9 non-en locales show English there — and it now also diverges from the swept sidebar equivalent (sidebar outline.noHeadings became "No  | Key the string through the plugin i18n surface (i18n.t via the editor namespace, as terminalMessages.ts does) with translations in all 10 locales. | fixed | 1 |
| 26 | src/components/Editor/FormatSurface.tsx:70-71 | Medium | i18n-correctness | SurfaceLoadFailure resolves the format display name in the wrong i18n namespace: `useTranslation("editor")` + `t(`format.${formatId}`)`, but format names live only in the common namespace (src/locales/en/common.json: "format.markdown", "format.txt", ...) — editor.json has no format.* keys. Every oth | Resolve via the common namespace — e.g. thread `formatConfig.nameI18nKey` into SurfaceLoadFailure and use `localizedFormatName(nameI18nKey, formatId)` from src/ | fixed | 1 |
| 27 | src/pages/settings/KeyCapture.tsx:62 and src/pages/settings/McpConfigPreviewDialog.tsx:104 | Medium | design-consistency | Two blocking modal dialogs had their backdrop changed from `bg-black/50` to `bg-[var(--hover-bg-strong)]` — the FINDER tint (rgba(0,0,0,0.08) light; rgba(255,255,255,0.12) dark). The same change set's own canonical rule says modals get the darker scrim: overlay-shared.css:36-41 (`.vm-overlay--center | Move both onto the shared shell: `className="vm-overlay vm-overlay--center ..."` (dropping the hand-rolled fixed/inset/z/bg utilities), or at minimum use the -- | fixed | 1 |
| 28 | src/pages/settings/layout.tsx:32-35,49-50,64-68 | Low | behavior-change | SettingRow's new `<label htmlFor={controlId}>` includes "button" in LABELABLE_TAGS and treats EVERY custom-component child as labelable, and cloneElement stamps `id: controlId` onto the sole child unconditionally. For rows whose sole child is an action Button — IntegrationsSettings.tsx ~line 296 ("D | Scope the htmlFor wiring to value controls: set htmlFor only when the child is Toggle/Select/FieldInput (e.g. check `children.type` against the primitive compon | fixed | 1 |
| 29 | src/pages/settings/buttons.tsx:122,132,136 (CopyButton), plus vm-spinner sizing at AboutSettings.tsx:79,106 and UpdateAvailableCard.tsx:184,200 | Low | dead-code | CopyButton's `size` prop and the `iconSize` ternary (`w-3.5 h-3.5` vs `w-3 h-3`) are dead: `.vm-icon-btn svg` in icon-button-shared.css:96-103 sets width/height to 14px UNLAYERED, and Tailwind v4 utilities are layered, so the unlayered rule always wins regardless of order — both branches render 14px | Delete the `size` prop and `iconSize` ternary from CopyButton (or make xs real via `.vm-icon-btn` variants), and drop the no-op `w-3 h-3`/`w-4 h-4` utilities fr | fixed | 1 |
| 30 | src/components/Editor/FormatSurface.tsx:19-20 | Low | doc-rot | The file header's "Key decisions" still states: "The error state reuses `editor:preview.failedToLoad` rather than adding a string: it says exactly this, in all ten locales, today." — but this change replaced that with a NEW key (`preview.surfaceFailed` + `dialog:errorBoundary.tryAgain` on a Retry bu | Rewrite the bullet to record the WI-UI4.4 decision: a format-naming `preview.surfaceFailed` message plus an in-place Retry that bumps the lazy key. | fixed | 1 |
| 31 | src/components/shared/TabStripButton.tsx:4-6 | Low | doc-rot | The header claims TabStripButton is "the one add/close button for the three tab strips — the document strip (StatusBar), the browser page tabs, and the terminal tab bar", but the terminal tab bar was not adopted: TerminalTabBar.tsx:168-191 renders raw `<button className="vm-icon-btn vm-icon-btn--sm" | Either adopt TabStripButton for TerminalTabBar's add button (its close/swap/restart are icon actions, not strip close buttons) or correct the header to name the | fixed | 1 |
| 32 | src/pages/settings/McpConfigPreviewDialog.tsx:104 | Low | dead-class | `justify-content` in the backdrop className is not a Tailwind utility (typo for `justify-center`) — it compiles to nothing, so the flex container never centers horizontally; the dialog only appears centered because the panel carries `mx-auto` (line 113). The typo predates this diff but the line was  | Replace `justify-content` with `justify-center` (or drop it and keep mx-auto as the one centering mechanism). | fixed | 1 |
| 33 | scripts/lib/uiConsistencyCss.mjs:324 | Medium | gate-soundness | :focus inside :not() counts as a positive focus state — universal-toolbar.css:71 marks universal-toolbar-btn covered via its hover paint | blank :not() groups before testing; add the real selector as a fixture | fixed | 1 |
| 34 | scripts/lib/designTokenChecks.mjs:38 | Medium | gate-soundness | nearest-preceding keyframe lookup does not prove enclosure — a step-shaped rule after a keyframes block inherits the wrong prefix | compute balanced @keyframes brace ranges; prefix only rules inside them | fixed | 2 |
| 35 | scripts/lib/designTokenChecks.mjs:52 | Medium | gate-soundness | identical repeated declarations share one ID while the caller compares sets — duplicates invisible | append an occurrence ordinal to duplicate IDs | fixed | 1 |
| 36 | scripts/lib/designTokenChecks.mjs:15 | Low | gate-soundness | KEYFRAME_STEP_RE rejects valid .5% steps, leaving them unprefixed | accept \d*\.\d+% percentages | fixed | 1 |
| 37 | scripts/check-i18n-keys.ts:619 | Low | gate-soundness | toast classification by identifier spelling — local fn named toast false-positives; import {toast as notify} evades | resolve sonner/imeToast import bindings (alias-aware) | fixed | 2 |
| 38 | scripts/check-i18n-copy.test.ts:7 | Low | test-integrity | no behavioral test for bare-toast detection or fail-closed missing baseline | add fixture tests for both fixes | fixed | 1 |
| 39 | scripts/check-keybinding-manifest.mjs:839 | Medium | gate-soundness | missing label pairs silently skipped; half-floor passes with 54 unchecked | explicit reasoned unpaired allowlist; fail on any other unpaired id | fixed | 1 |
| 40 | scripts/check-keybinding-manifest.mjs:846 | Medium | gate-soundness | LABEL_EXEMPT accepts ANY non-equal drift — an exempt label can change to anything | record each exemption's expected menu label; fail on mismatch | fixed | 2 |
| 41 | scripts/check-keybinding-manifest.test.mjs:16 | Low | test-integrity | gate test covers only the happy tree; failure branches untested behaviorally | behavioral tests where the script permits | partial | 1 | 
| 42 | src/services/navigation/fileOpen.ts:144 | Medium | error-handling | errorMessage() pre-stringifies before errorDetail — typed rejections still render [object Object] | pass the raw error | fixed | 1 |
| 43 | src/services/navigation/executeOpenDecision.ts:98 | Medium | error-handling | replaceResult.error pre-stringified, defeating errorDetail's normalization | pass raw | fixed | 1 |
| 44 | src/export/pandocExport.ts:105 | Medium | error-handling | catch pre-stringifies before errorDetail | pass raw error; drop errorMessage import | fixed | 1 |
| 45 | src/services/ime/imeToast.test.ts:183 | Low | test-integrity | errorDetail has no unit tests (typed CommandError, record, empty detail, pin) | add direct wrapper tests | fixed | 1 |
| 46 | src/components/Terminal/TerminalTabBar.css:89 | Low | consistency | .terminal-tab:hover (later, equal specificity) repaints the focused rename box hover-grey | exclude .terminal-tab-rename from the hover selector | fixed | 1 |
| 47 | src/plugins/imagePreview/ImagePreviewView.ts:79 | Low | consistency | inline display:block overrides .popup-container's flex — gap/alignment declarations inert | show with display:flex | fixed | 1 |
| 48 | src/components/StatusBar/StatusBar.a11y.test.tsx:87 | Low | test-integrity | state-name test only checks aria-label nonempty and equal to title | assert formatMcpTooltip's state-bearing value | fixed | 1 |
| 49 | src/pages/settings/{KeyCapture,McpConfigPreviewDialog}.test.tsx | new | Medium | coverage | (verification sweep) check:all coverage ratchet tripped by hairline drops (functions 93.44% vs 93.46%, statements 94.20% vs 94.21%) after the two audit rounds' code changes | covered the two 0%-covered modals touched by finding 27 (9 behavioral tests: shell adoption, chord capture, Escape/backdrop cancel, confirm wiring) — thresholds untouched | fixed | 3 |
