# Audit Findings — audit-fix round 2 20260908

**Run**: audit-fix round 2 20260908 | **Scope**: the 198 production files this branch's round-1 fixes changed | **Audit type**: mini
**Model**: gpt-5.6-sol | **Effort**: xhigh | **Audit threads**: 57 batches
**Status values**: open | open (defect check) | fixed | not-fixed | partial | regressed | skipped (out of plan scope: pre-existing line)

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Scope | Status | Round |
|---|------|------|----------|-----------|---------|---------------|-------|--------|-------|
| 1 | e2e/lib/rail.mjs | 54 | Medium | Refactoring Debt | The 377-line module contains several orchestration functions exceeding 30 lines and mixes settings, DOM interaction, validation, and teardown | Split settings lifecycle, menu interaction, and restoration into focused helpers | pre-existing line | not-fixed (rebuttal: every function is small and documented; the one genuinely long path, restoreRail, is a linear collect-failures loop with each branch explained. shippedRailModeDefault was split out and e2e/lib/rail.test.mjs added, so the untestable part is now unit-tested) | 3 |
| 2 | e2e/lib/rail.mjs | 60 | High | Logic & Correctness | `setRailMode` runs before the `try`; a partial write followed by an event or transport failure bypasses restoration and leaves user state changed | Put the mutation inside the protected `try/finally` and always attempt restoration after a set attempt | changed line | fixed | 1 |
| 3 | e2e/lib/rail.mjs | 77 | Medium | Shortcuts & Patches | Restoring an originally absent preference hardcodes today’s default `false`, so a future default change restores the wrong live state | Obtain the default from the application’s settings source or support deleting and rehydrating the live key | pre-existing line | fixed | 3 |
| 4 | e2e/lib/rail.mjs | 114 | High | Duplication | Rail-instance selector construction is duplicated here and at line 156, risking divergent escaping or hook changes | Centralize DOM lookup/selector generation in one helper | pre-existing line | fixed | 1 |
| 5 | e2e/lib/rail.mjs | 297 | Medium | Logic & Correctness | Restoration stops on the first failed close, leaving later journey-created instances and prior activation unrestored | Attempt all safe cleanup operations, collect failures, then throw an aggregate teardown error | changed line | fixed | 2 |
| 6 | e2e/lib/rail.mjs | 320 | Low | Logic & Correctness | `isPlaceholder` blindly calls `startsWith`; a missing `data-instance-id` produces an opaque TypeError instead of a contract failure | Validate instance IDs while constructing the rail snapshot and throw a descriptive error | changed line | fixed | 3 |
| 7 | e2e/lib/rail.mjs | 343 | High | Logic & Correctness | A pre-existing entry’s disambiguated display name can change merely because a same-named workspace was added; this is incorrectly diagnosed as a re-root or rename even after cleanup restores it | Compare stable workspace identity/root data, or defer the name comparison until journey-created entries are removed | changed line | fixed (duplicate of #4, already closed by the same change: unsupportedMutations now runs against the FINAL rail after cleanup, so a display name that changed only while a same-named journey workspace was open is restored before the diagnosis reads it; the throw is gated on the identity comparison, and the diagnosis only explains it) | 1 |
| 8 | e2e/lib/workspace.mjs | 40 | Medium | Shortcuts & Patches | Approval actions are selected through styling classes, repeating the selector-staleness failure documented immediately above | Add stable `data-approval-action` hooks and select by behavior rather than visual class | changed line | fixed | 3 |
| 9 | e2e/lib/workspace.mjs | 63 | High | Logic & Correctness | `dismissApprovalPrompt` denies only one prompt and waits for no overlay; if another queued prompt becomes visible, it times out and leaves that prompt behind | Drain prompts deterministically or dismiss the request by stable prompt ID | changed line | fixed | 1 |
| 10 | e2e/lib/workspace.mjs | 76 | Medium | Logic & Correctness | Approval text and persisted root are compared to the uncanonicalized input path, although the product canonicalizes workspace paths; symlinks, relative segments, or trailing separators can make a correct open fail | Canonicalize once before requesting approval and use the canonical path throughout | changed line | fixed | 2 |
| 11 | e2e/lib/workspace.mjs | 164 | Medium | Logic & Correctness | Store liveness is proven by `pending.length > 0`, so an HMR-parallel store containing an unrelated stale prompt can be mistaken for the live store | Match the pending prompt’s path, window, client, and preferably request ID | changed line | fixed | 2 |
| 12 | e2e/lib/workspace.mjs | 180 | High | Logic & Correctness | Grant identities are serialized with ` | `; valid paths or client IDs containing that character can collide and leave a newly minted grant live / Compare structured tuples or serialize arrays with JSON | changed line | fixed | 1 |
| 13 | e2e/lib/workspace.mjs | 181 | High | Logic & Correctness | Cleanup consumes every grant added after the snapshot, including grants concurrently approved by another flow | Record the target prompt’s exact key and revoke only its corresponding one-shot | changed line | not-fixed (rebuttal: journeys run sequentially against one client and the grant is minted by the approve this helper just clicked, so there is no concurrent flow; narrowing by folderPath would MISS the grant, since the store keys on canonicalPath — a symlink-resolved value the helper never sees — and a missed grant stays live for 5 minutes, strictly worse) | 1 |
| 14 | e2e/lib/workspace.mjs | 220 | Medium | Logic & Correctness | Any failed dismissal is reported as “no approval dialog was left open,” including selector failure or timeout with a dialog still present | Re-query the overlay and report distinct dismissed, absent, and cleanup-failed states | changed line | not-fixed (rebuttal: dismissApprovalPrompt already re-queries the overlay — if (!clicked) break then remaining: (await getApprovalPrompt(client)) !== null — so a selector failure or a still-open dialog reports STILL OPEN; "no approval dialog was left open" is reachable only when the overlay is genuinely absent) | 2 |
| 15 | e2e/lib/workspace.mjs | 254 | High | Logic & Correctness | The close wait succeeds when only the persisted root changes; the documented regression that nulled the root while leaving the same active rail instance would therefore pass | When a rail instance was active, require that exact instance ID to disappear from the rail | changed line | fixed | 1 |
| 16 | scripts/check-command-error-ratchet.mjs | 33 | High | Shortcuts & Patches | The acknowledged type-alias limitation lets a new `Result<T, String>` command bypass the ratchet simply by returning an alias | Parse Rust with `syn` or reject/resolve aliases whose expansion contains the legacy result type | pre-existing line | fixed | 3 |
| 17 | scripts/check-command-error-ratchet.mjs | 70 | High | Refactoring Debt | `stripCommentsAndStrings` is an 89-line, highly branched handwritten Rust lexer whose correctness controls the gate | Replace it with a parser/tokenizer or split it into independently testable lexical-state handlers | pre-existing line | fixed | 3 |
| 18 | scripts/check-command-error-ratchet.mjs | 113 | Medium | Logic & Correctness | Raw-string recognition examines only 16 characters, while Rust permits substantially more hash delimiters; such strings are not stripped correctly | Scan the delimiter without an arbitrary substring cap | pre-existing line | fixed | 3 |
| 19 | scripts/check-command-error-ratchet.mjs | 184 | High | Logic & Correctness | `returnTypeOf` includes a trailing `where` clause in the extracted type, causing valid `Result<T, String> where ...` signatures to evade detection | Parse and terminate the return type at a depth-zero `where` token | pre-existing line | fixed | 1 |
| 20 | scripts/check-command-error-ratchet.mjs | 276 | Medium | Shortcuts & Patches | Typed command-name discovery uses an unexplained 400-character window; long intervening attributes silently hide commands | Parse the declaration following the attribute without a fixed-distance cutoff | pre-existing line | fixed | 3 |
| 21 | scripts/check-command-error-ratchet.mjs | 298 | High | Refactoring Debt | The 139-line AST routine performs binding collection, command resolution, scope tracking, suppression handling, and diagnostics inside a 652-line file | Extract dedicated symbol-resolution, scope, invocation, and diagnostic passes into separate modules | pre-existing line | not-fixed (rebuttal: findStringifiedTypedErrors is a single AST walk over per-file closure state — constStrings, ambiguous, bindingCallbacks, markers, found — and splitting it into four passes would thread that state through four signatures; its branches are already pinned individually by 15 cases, and the length that WAS a hazard, the duplicated lexer, is gone) | 3 |
| 22 | scripts/check-command-error-ratchet.mjs | 310 | High | Logic & Correctness | The supposed constant resolver collects `let`/`var` declarations and uses one file-global name map, ignoring reassignment, lexical scope, declaration order, and shadowing | Resolve only immutable declarations through scope-aware TypeScript symbols | pre-existing line | fixed | 1 |
| 23 | scripts/check-command-error-ratchet.mjs | 345 | High | Logic & Correctness | Finding any typed invocation causes every caught `String(error)` anywhere in the file to be treated as that invocation’s rejection, producing both false positives and misleading command attribution | Trace each invocation into its own rejection handler or conservatively inspect only directly associated handlers | pre-existing line | not-fixed (rebuttal: the file-level INVOKE-handler association is a documented deliberate limitation with a designed escape hatch — rule 50 §10 states it and // command-error-ok: <reason> scopes an exemption to one line; per-invocation rejection tracing needs dataflow the gate explicitly does not do) | 1 |
| 24 | scripts/check-command-error-ratchet.mjs | 350 | High | Logic & Correctness | Any identifier or property method named `invoke` is treated as Tauri’s API, so unrelated `object.invoke()` calls can activate the gate | Resolve the callee to the imported Tauri `invoke` binding | changed line | fixed | 1 |
| 25 | scripts/check-command-error-ratchet.mjs | 385 | Medium | Logic & Correctness | Rejection bindings are recognized only in `catch` clauses and `.catch(...)`; `.then(success, onRejected)` and equivalent handlers are missed | Cover all supported Promise rejection-handler forms through AST analysis | changed line | fixed | 2 |
| 26 | scripts/check-command-error-ratchet.mjs | 439 | Medium | Logic & Correctness | `scriptKindFor` classifies `.mts` and `.cts` as JavaScript despite the scanner explicitly including them as TypeScript | Switch on the actual extension and map `.ts/.mts/.cts` and JSX variants correctly | pre-existing line | fixed | 3 |
| 27 | scripts/check-command-error-ratchet.mjs | 459 | High | Duplication | Rust files are walked, read, stripped, and analyzed once for legacy counts and again for typed command names | Perform one crate scan that returns both results | pre-existing line | fixed | 3 |
| 28 | scripts/check-command-error-ratchet.mjs | 556 | Medium | Logic & Correctness | `--root` or `--baseline` without a value silently falls back to defaults instead of reporting misuse | Validate that value-taking flags have a following non-flag argument and use the documented usage exit code | pre-existing line | fixed | 3 |
| 29 | scripts/check-command-error-ratchet.mjs | 570 | Medium | Refactoring Debt | The 82-line `main` mixes argument handling, scanning, baseline writes, comparison, formatting, and process termination | Separate CLI parsing, analysis, reporting, and exit-code selection | pre-existing line | not-fixed (rebuttal: each of main's four report branches is asserted through a real subprocess run in the self-test; extracting a printer would test the same strings one layer further from what a user sees) | 3 |
| 30 | scripts/check-deleted-names.mjs | 45 | High | Logic & Correctness | The acknowledged line-based scan misses ordinary multiline export clauses, allowing a deleted symbol to return without failing CI | Use the TypeScript AST for exported-symbol detection | changed line | fixed | 1 |
| 31 | scripts/check-deleted-names.mjs | 73 | Medium | Logic & Correctness | The ASCII-only boundary treats Unicode identifier characters as delimiters, so prefixes such as `deletedNameé` can falsely match the tombstone | Use parser-derived identifiers or Unicode identifier boundaries | changed line | not-fixed (rebuttal: EDGE = [^A-Za-z0-9_$] can only OVER-match — deletedNameé is a false POSITIVE, the gate's documented fail-closed direction; no spelling of a registry name produces a MISS, and a POSIX class is locale-dependent in git grep) | 2 |
| 32 | scripts/check-deleted-names.mjs | 89 | High | Logic & Correctness | The export grammar misses standard forms including `export const enum`, `export namespace/module`, `export import`, and `export type { ... }` | Replace the partial regex inventory with TypeScript AST export inspection | changed line | fixed | 1 |
| 33 | scripts/check-deleted-names.mjs | 109 | Medium | Logic & Correctness | Rust macro invocations that generate a deleted item are invisible to the item regex, leaving a documented enforcement bypass | Inspect expanded Rust metadata or explicitly detect/reject relevant macro-generated definitions | changed line | not-fixed (rebuttal: the header states this limitation explicitly — "not a Rust parser: an item produced by a macro is outside it"; the suggested fix needs cargo expand, and AGENTS.md records that check:all never invokes cargo at all) | 2 |
| 34 | scripts/check-deleted-names.mjs | 125 | Medium | Logic & Correctness | Plain `git grep` ignores untracked files, so a locally reintroduced symbol can pass the gate until it is staged or committed | Include non-ignored untracked files or scan the filesystem using Git’s tracked-plus-untracked inventory | changed line | fixed | 2 |
| 35 | scripts/check-deleted-names.mjs | 130 | Medium | Logic & Correctness | Raw grep treats matching comments, block-comment contents, and multiline strings as definitions, causing false CI failures | Parse source syntax and inspect declaration/export nodes only | changed line | not-fixed (rebuttal: the header lists this as KNOWN LIMITATION 2, "false POSITIVE, i.e. it fails closed", and defers the TS-program pass to "if this tripwire ever has to be authoritative") | 2 |
| 36 | scripts/check-deleted-names.mjs | 171 | Medium | Logic & Correctness | `existsSync` returns false for a broken symlink, allowing a deleted path to reappear in Git as such a symlink without detection | Use `lstatSync`/`lstat` or Git’s path inventory | changed line | fixed | 2 |
| 37 | scripts/check-deleted-names.mjs | 193 | Medium | Refactoring Debt | The 56-line `main` combines parsing, registry I/O, evaluation, diagnostics, and exits | Extract pure argument parsing and registry loading/reporting helpers | changed line | not-fixed (rebuttal: every branch of main — exit 64 for a valueless flag, 64 for an unknown arg, 2 for an unreadable registry, 2 for a bad entry, 1 for a reappearance — is already exercised as a subprocess with its exit code and message asserted; extracting a pure parseArgs adds no coverage) | 3 |
| 38 | scripts/check-deleted-names.mjs | 238 | Low | Shortcuts & Patches | Failure guidance tells maintainers to remove entries from `scripts/check-deleted-names.mjs`, but the registry lives in `scripts/lib/deletedNamesRegistry.mjs` | Correct the diagnostic to name the actual registry file | changed line | fixed | 3 |
| 39 | scripts/check-feature-ledger-phase.sh | 38 | Medium | Logic & Correctness | Argument parsing accepts multiple positional phases and silently uses the last one; unknown options are also treated as phases, so the wrong audit phase can run. | Reject unknown flags and require exactly one positional phase. | new file | fixed | 2 |
| 40 | scripts/check-feature-ledger-phase.sh | 76 | High | Logic & Correctness | provisioning_wired scans every top-level Rust file without proving that the containing module is compiled or reachable from production; an orphan file containing the call passes. | Traverse the Rust module graph or verify reachability through a compiled test. | new file | fixed | 1 |
| 41 | scripts/check-feature-ledger-phase.sh | 145 | Medium | Logic & Correctness | The assertion claims that Mod+1 through Mod+5 are documented but checks only Mod+1. | Verify every chord or parse and validate the documented range. | new file | fixed | 2 |
| 42 | scripts/check-feature-ledger-phase.sh | 148 | Medium | Logic & Correctness | Removing the old outline-and-polish name satisfies an assertion claiming that the replacement bundled sample is documented. | Positively assert the current sample name or derive it from the resource inventory. | new file | fixed | 2 |
| 43 | scripts/check-feature-ledger-phase.sh | 158 | Medium | Logic & Correctness | Separate document-wide greps do not prove that the command-palette section contains the shortcut; the chord may occur elsewhere. | Parse the section and search only within its boundaries. | new file | fixed | 2 |
| 44 | scripts/check-feature-ledger-phase.sh | 193 | High | Logic & Correctness | Multiple Phase 3 wiring assertions rely on textual presence at lines 193, 196, 199, 208, 215, and 221; definitions, imports, comments, or dead code can satisfy them without a reachable feature. | Replace them with syntax-aware call/reachability checks and behavioral tests. | new file | fixed | 1 |
| 45 | scripts/check-feature-ledger-phase.sh | 237 | Medium | Logic & Correctness | The tool-count assertion rejects only three historical phrases; any other incorrect count passes. | Derive the counts from registered tools and compare them with parsed header claims. | new file | fixed | 2 |
| 46 | scripts/check-feature-ledger-phase.sh | 242 | High | Logic & Correctness | Phase 5 claims tests are discovered, but assert_test_file only detects a test case or immediate Rust include and does not prove Vitest/Cargo runner inclusion. | Query the actual test-tier partition/Cargo module graph or run a discovery-only command. | new file | not-fixed (rebuttal for the vitest half: dod-syntax.mjs's header delegates collection to check-scripts-parity.test.mjs, which asserts repo-wide that every test file is matched by exactly one tier; restating it would be a second copy of one policy. The cargo half WAS strengthened: assert_test_file now also requires the including module to be declared beside it) | 1 |
| 47 | scripts/check-feature-ledger-phase.sh | 263 | High | Logic & Correctness | Finding an environment-variable lookup does not prove that the DBus check guards registration or executes in the required order. | Add a behavioral unit test and a syntax-aware control-flow assertion. | new file | fixed | 1 |
| 48 | scripts/check-feature-ledger-phase.sh | 264 | High | Logic & Correctness | Merely finding PdfProgress in each platform renderer does not prove progress is emitted on success, failure, cancellation, and early-return paths. | Add platform renderer tests that observe emitted progress across every exit path. | new file | fixed | 1 |
| 49 | scripts/check-feature-ledger-phase.sh | 272 | High | Logic & Correctness | The presence of security_framework does not prove keychain permission denials are classified correctly. | Exercise representative denial codes and assert the resulting CommandError classification. | new file | fixed | 1 |
| 50 | scripts/check-feature-ledger-phase.sh | 279 | High | Logic & Correctness | The two independent assert_any calls allow mixed outcomes: one can pass via the Cargo feature and the other via a re-verdict, falsely declaring completion. | Implement one compound assertion requiring either both feature conditions or one valid re-verdict. | new file | not-fixed (rebuttal: the two assert_any calls are conjoined by the phase, so the condition is (A or R) and (B or R), which distributes to (A and B) or R — a half-done extraction with no re-verdict cannot pass; check-feature-ledger-phase.test.mjs already pins that case and the compound rewrite was reverted as a no-op) | 1 |
| 51 | scripts/check-header-references.mjs | 68 | Low | Logic & Correctness | --root= resolves an empty value to the current working directory instead of rejecting malformed input. | Reject an empty substring before calling path.resolve. | new file | fixed | 3 |
| 52 | scripts/check-header-references.mjs | 120 | Medium | Refactoring Debt | main spans 67 lines and combines scanning, reporting, baseline mutation, error policy, and process termination. | Split it into scan, update, compare, and CLI-exit functions returning explicit results. | new file | not-fixed (rebuttal: main is two mutually exclusive modes with no shared rule and no drift channel; both are covered end-to-end by subprocess tests including the fail-closed paths) | 3 |
| 53 | scripts/check-header-references.mjs | 159 | Low | Shortcuts & Patches | Baseline updates overwrite the destination directly and do not handle write failures, allowing interruption or disk errors to leave a truncated baseline. | Write to a sibling temporary file, fsync if appropriate, rename atomically, and report errors cleanly. | new file | fixed | 3 |
| 54 | scripts/check-keybinding-manifest.mjs | 1 | Medium | Refactoring Debt | The 930-line file exceeds the project’s approximately 300-line limit and mixes four parsers, policy tables, validation, and CLI side effects. | Split frontend, Rust, Markdown, and label parsing into independently tested modules. | pre-existing line | not-fixed (rebuttal: the piece with real drift risk, prosemirrorToTauri, was extracted under #57 and is now parity-tested; the remaining parsers are pinned by mutation-verified end-to-end assertions, and scripts/ is outside lint:file-size's ROOTS) | 3 |
| 55 | scripts/check-keybinding-manifest.mjs | 55 | High | Logic & Correctness | search-genies is excluded as dynamically bound without verifying that its dynamic registration still exists, leaving that critical shortcut path unchecked. | Add a syntax-aware assertion for the dynamic binding and validate exemption liveness. | changed line | fixed | 1 |
| 56 | scripts/check-keybinding-manifest.mjs | 76 | Medium | Shortcuts & Patches | Interior heading shortcuts are permanently exempted without checking that the documented range still exists or still covers those headings. | Parse the range row and verify its endpoints and covered IDs. | pre-existing line | fixed | 3 |
| 57 | scripts/check-keybinding-manifest.mjs | 104 | High | Duplication | prosemirrorToTauri is copied from runtime TypeScript and depends on a manual “keep in sync” instruction, permitting the gate and application to diverge. | Move conversion semantics into one side-effect-free shared module or a generated contract consumed by both. | pre-existing line | fixed | 3 |
| 58 | scripts/check-keybinding-manifest.mjs | 157 | High | Refactoring Debt | scanAccelCalls is an 82-line, deeply branched lexer/parser with high cyclomatic complexity. | Decompose tokenization, call-boundary detection, argument parsing, and shape validation. | pre-existing line | fixed | 3 |
| 59 | scripts/check-keybinding-manifest.mjs | 167 | High | Duplication | scanAccelCalls reimplements Rust comment/string lexing despite importing rustCode, and unlike that lexer it misses nested block comments, raw strings, byte strings, and character literals. | Use one shared Rust tokenizer that emits structured string tokens and code spans. | pre-existing line | fixed | 3 |
| 60 | scripts/check-keybinding-manifest.mjs | 223 | High | Logic & Correctness | Four string literals plus the word macos are treated as the expected conditional without validating cfg! structure, polarity, branch order, or else association. | Parse the exact if cfg!(target_os = "macos") {…} else {…} expression. | pre-existing line | fixed | 1 |
| 61 | scripts/check-keybinding-manifest.mjs | 349 | High | Logic & Correctness | Documentation is reduced to a global accelerator set, including inline code outside the table; swapped command shortcuts or stray prose mentions can therefore pass. | Parse table rows into command-to-accelerator mappings and compare each manifest entry with its own row. | pre-existing line | not-fixed (out of reach: mapping each command to its own docs row needs a manifest-label to docs-Action join, and the table's Action cells are free prose with two different column orders; that is a new cross-surface join the finding did not scope) | 1 |
| 62 | scripts/check-keybinding-manifest.mjs | 403 | High | Logic & Correctness | stringField searches raw object text and can take a field from a comment, string value, or nested object instead of the top-level property. | Parse the TypeScript AST and inspect top-level PropertyAssignment nodes. | changed line | fixed | 1 |
| 63 | scripts/check-keybinding-manifest.mjs | 420 | High | Refactoring Debt | splitObjectLiterals is a 62-line multi-state lexer with high cyclomatic complexity and duplicated literal/comment handling. | Reuse the TypeScript AST or extract and unit-test a common tokenizer. | changed line | not-fixed (rebuttal: splitObjectLiterals does not exist — parseObjectLiterals walks the TypeScript AST, and the self-test already asserts not.toContain("function splitObjectLiterals")) | 3 |
| 64 | scripts/check-keybinding-manifest.mjs | 492 | High | Logic & Correctness | parseObjectLiterals silently ignores top-level spreads, identifiers, conditional expressions, and factory calls because it validates only object literals it happened to find. | Parse every array element and fail on any unsupported element shape. | changed line | fixed | 1 |
| 65 | scripts/check-keybinding-manifest.mjs | 545 | Medium | Logic & Correctness | The declaration regex runs over raw source and can anchor on a declaration-shaped comment or string before the real array. | Locate the declaration through the TypeScript/Rust token stream. | pre-existing line | fixed | 3 |
| 66 | scripts/check-keybinding-manifest.mjs | 581 | Low | Dead Code | The defs.length zero check is unreachable because zero definitions necessarily produce a zero manifest and line 580 exits first. | Check defs.length before deriving/checking manifest. | changed line | fixed | 3 |
| 67 | scripts/check-keybinding-manifest.mjs | 586 | Medium | Logic & Correctness | arrayBody applies arrayLiteralEnd to Rust, although that helper does not understand nested Rust block comments or raw strings and can choose the wrong closing bracket. | Use the shared Rust lexer or a Rust-aware balanced-array scanner. | changed line | not-fixed (rebuttal: arrayBody passes lang: rel.endsWith(".rs") ? "rust" : "ts", and arrayLiteralEnd's Rust branch runs rustCode, which handles nested block comments and every raw-string form; this exact claim was already fixed as round-2 #136/#138/#139/#140 and is recorded in that module's header) | 2 |
| 68 | scripts/check-keybinding-manifest.mjs | 592 | High | Logic & Correctness | Rust contract tuples are regexed from raw array text; commented/string-contained tuples can count, while unsupported live tuple shapes are silently omitted. | Parse tokenized Rust tuples and assert that every array element was consumed exactly once. | changed line | fixed | 1 |
| 69 | scripts/check-keybinding-manifest.mjs | 670 | Medium | Logic & Correctness | Contract comparison is only manifest-to-Rust; extra stale entries in DEFAULT_ACCELERATORS or PLATFORM_ACCELERATORS are never rejected. | Add reverse checks from both Rust maps to the manifest. | pre-existing line | fixed | 3 |
| 70 | scripts/check-keybinding-manifest.mjs | 721 | Medium | Logic & Correctness | Documentation checks use defaultKey and defaultKeyOther but never defaultKeyMac; an empty defaultKey also skips documentation even when the macOS override is non-empty. | Validate every non-empty effective platform key, including defaultKeyMac. | pre-existing line | fixed | 3 |
| 71 | scripts/check-keybinding-manifest.mjs | 824 | Medium | Logic & Correctness | Keeping all Rust string contents and applying a regex can match builder-like text embedded inside a raw string, without proving a real with_id call. | Tokenize the call expression and consume literal tokens only from actual arguments. | changed line | fixed | 2 |
| 72 | scripts/check-keybinding-manifest.mjs | 832 | Medium | Logic & Correctness | en.yml is parsed with a line regex that requires exactly two-space, double-quoted scalars, does not decode YAML escapes, and silently overwrites duplicate keys. | Use the project YAML parser and reject duplicate or non-string menu labels explicitly. | changed line | fixed | 2 |
| 73 | scripts/check-mcp-docs.mjs | 88 | High | Logic & Correctness | `stringLiterals` silently ignores spread or non-literal array elements when at least one literal exists, allowing shipped actions to escape documentation checks | Validate every array element and recursively resolve spreads, preferably via the TypeScript AST | changed line | fixed | 1 |
| 74 | scripts/check-mcp-docs.mjs | 88 | Medium | Logic & Correctness | The comment stripper is not string-aware and extracted escape sequences are not decoded, so valid action strings containing comment markers or escapes are misread | Obtain literal runtime values from AST string-literal nodes | changed line | not-fixed (rebuttal: stringLiterals fails CLOSED — its residue check throws on any element text it cannot read, and an undecoded escape yields a name the docs cannot match, reported as undocumented; both claims require an action name containing // or a backslash escape, and no action does) | 2 |
| 75 | scripts/check-mcp-docs.mjs | 118 | High | Logic & Correctness | `declaredArray` scans raw source, so a commented-out or string-contained declaration can shadow the real imported array and produce an incorrect action set | Parse declarations with the TypeScript AST and ignore non-code tokens | changed line | fixed | 1 |
| 76 | scripts/check-mcp-docs.mjs | 121 | High | Refactoring Debt | `extractActions` exceeds 30 lines and combines discovery, validation, import resolution, extraction, and deduplication with high branch complexity | Split it into schema discovery, named-array resolution, and element-validation functions | changed line | not-fixed (rebuttal: declaredActionSchemas, declaredArray and stringLiterals are already extracted with documented reasons; what remains is one loop, and the sibling-import path it names is directly tested with a readSibling fake) | 3 |
| 77 | scripts/check-mcp-docs.mjs | 161 | High | Logic & Correctness | `toolName` scans raw source and returns only the first textual `registerTool` match; comments, strings, or multiple registrations can misassociate actions with the wrong tool | Parse registrations structurally and associate each schema with its owning registration | changed line | fixed | 1 |
| 78 | scripts/check-mcp-docs.mjs | 197 | Medium | Logic & Correctness | Repeated documentation sections for the same tool overwrite earlier sections, causing valid entries in the earlier section to disappear | Append repeated sections or reject duplicate tool headings explicitly | changed line | fixed | 2 |
| 79 | scripts/check-terminal-edge-phase.sh | 47 | High | Logic & Correctness | DoD assertions search raw text, so comments, dead code, unused symbols, and `it.skip` titles can satisfy critical behavior and security checks without executable coverage | Use structural parsers and run the specifically named tests for behavioral requirements | pre-existing line | fixed | 1 |
| 80 | scripts/check-terminal-edge-phase.sh | 47 | High | Duplication | `assert_grep`, `assert_re`, `assert_no_grep`, and `assert_no_re` duplicate matching, status handling, and diagnostics | Introduce one matcher helper parameterized by fixed/regex mode and expected presence | pre-existing line | fixed | 3 |
| 81 | scripts/check-terminal-edge-phase.sh | 96 | Medium | Logic & Correctness | Translation validation accepts an empty localized string and does not require the English reference key to exist as a string | Require non-empty strings in both bundles and compare normalized values | changed line | fixed | 2 |
| 82 | scripts/check-terminal-edge-phase.sh | 229 | Medium | Shortcuts & Patches | Phase 3 declares an unwired dependency — and its unused-dependency exemption — a successful deliverable, institutionalizing dead dependency debt | Wire and behaviorally test serialization, or remove the dependency until implementation | changed line | not-fixed (rebuttal: serializeAddonDependency.test.ts already asserts the dependency stays unimported, stays exact-pinned, and that its knip exemption exists, so the exemption cannot outlive its reason; wiring or removing it is a product decision touching package.json and knip.json, neither in this group) | 3 |
| 83 | scripts/check-test-only-modules.mjs | 72 | High | Logic & Correctness | The broad `test[A-Z]` filename heuristic hides legitimate production modules such as `testConnection.ts`, creating false-negative dead-code results | Restrict exclusions to explicit test directories/suffixes or an audited allowlist | new file | fixed | 1 |
| 84 | scripts/check-test-only-modules.mjs | 109 | High | Logic & Correctness | An `issues[]` record with no `files` property is silently accepted; a partial reporter-schema change can therefore omit findings while other records keep the run apparently valid | Require every record to match an explicitly supported schema or reject the entire report | new file | fixed | 1 |
| 85 | scripts/check-test-only-modules.mjs | 132 | High | Logic & Correctness | The hand-written glob parser can loop forever on an unmatched `{` and classifies bracket patterns as globs while matching brackets literally | Replace it with the same established glob implementation Knip uses, or fully validate before conversion | new file | fixed | 1 |
| 86 | scripts/check-test-only-modules.mjs | 216 | High | Duplication | Knip JSON is parsed and validated in `runKnip`, then parsed again in `measure` | Return the validated file list from `runKnip` and consume it directly | new file | fixed | 3 |
| 87 | scripts/check-test-only-modules.mjs | 237 | Medium | Logic & Correctness | Baseline updates overwrite the target directly, so interruption, disk failure, or concurrent updates can leave a truncated baseline | Write to a same-directory temporary file and atomically rename it | new file | fixed | 2 |
| 88 | scripts/check-test-only-modules.mjs | 278 | High | Refactoring Debt | `main` exceeds 30 lines and mixes argument parsing, measurement, baseline policy, mutation, reporting, and exit-code selection | Extract argument parsing, update handling, and comparison reporting into focused functions | new file | not-fixed (rebuttal: main is glue over eight separately exported and separately tested functions — measure, readBaseline, writeBaseline, compareWithBaseline, updateDecision, assertGraphDefinition — which is the decomposition asked for, already done) | 3 |
| 89 | scripts/check-test-only-modules.mjs | 326 | Medium | Refactoring Debt | The 326-line file exceeds the project’s approximate 300-line limit | Move glob/config validation or baseline management into a focused module | new file | not-fixed (rebuttal: scripts/ is outside check-file-size.mjs's ROOTS, and the module is one cohesive gate whose seams are already exported and tested) | 3 |
| 90 | scripts/check-ui-phase.sh | 26 | Medium | Logic & Correctness | Failure to change into the repository root is ignored, allowing assertions to run against the caller’s working directory | Use `cd "$(dirname "$0")/.." // exit 64` | pre-existing line | fixed | 3 |
| 91 | scripts/check-ui-phase.sh | 28 | Medium | Logic & Correctness | Arguments after the phase number are silently ignored despite the documented bad-invocation exit code | Require exactly one argument and reject extras with exit 64 | pre-existing line | fixed | 3 |
| 92 | scripts/check-ui-phase.sh | 56 | High | Logic & Correctness | Raw grep assertions can be satisfied by comments, stale strings, or unreachable code, producing false-positive phase completion | Replace semantic checks with structural inspection or executable gate assertions | pre-existing line | not-fixed (rebuttal: these assertions are registration/presence checks in JSON and CSS, each paired in the same phase with an assert_cmd that RUNS the gate or an assert_test_run that runs the test — reducedMotionGlobal.test.ts reads index.css at runtime and check-ui-consistency requires the --font-ui bridge structurally, so a comment-only match still fails the phase) | 1 |
| 93 | scripts/check-ui-phase.sh | 61 | Medium | Shortcuts & Patches | `assert_cmd` discards all command output, leaving failures without actionable diagnostics | Capture output and include a bounded failure excerpt | pre-existing line | fixed | 3 |
| 94 | scripts/check-ui-phase.sh | 74 | High | Refactoring Debt | `assert_test_run` combines discovery, process execution, temporary-file management, JSON parsing, suite validation, and reporting with high cyclomatic complexity | Extract the JSON validator into a dedicated module and split discovery from execution | changed line | fixed | 3 |
| 95 | scripts/check-ui-phase.sh | 79 | Low | Shortcuts & Patches | Temporary reports are cleaned only on the normal path and leak when the process is interrupted | Register an EXIT/INT/TERM cleanup trap | changed line | fixed | 3 |
| 96 | scripts/check-ui-phase.sh | 80 | High | Logic & Correctness | The Vitest process exit status is discarded; a run with green-looking partial JSON but a non-test runtime or reporter failure can be reported as successful | Capture the command status and require both exit zero and a valid successful report | changed line | fixed | 1 |
| 97 | scripts/check-ui-phase.sh | 88 | High | Logic & Correctness | Suite validation compares only counts and only rejects too few suites, so extra or wrong suites can mask an omitted required suite | Compare canonical expected and reported file-path sets for exact equality | changed line | fixed | 1 |
| 98 | scripts/check-ui-phase.sh | 159 | High | Duplication | The hardcoded theme list is repeated in phases 0 and 1, allowing future theme coverage to drift between phases | Define one shared `THEMES` array and iterate it everywhere | changed line | fixed | 3 |
| 99 | scripts/check-window-creation-thread.mjs | 54 | High | Shortcuts & Patches | Detection depends on one exact `WebviewWindowBuilder::new` substring; aliases, turbofish syntax, and alternative constructors can evade it while existing seeds keep `seedCount` nonzero | Use Rust syntax/symbol-aware detection or explicitly forbid and test every alternative construction form | changed line | fixed | 3 |
| 100 | scripts/check-window-creation-thread.mjs | 67 | High | Duplication | `stripComments` duplicates the repository’s Rust lexer but supports fewer literal forms, creating two drifting implementations of the same parsing policy | Reuse `scripts/lib/rustSource.mjs` or extract one shared lexer | changed line | not-fixed (rebuttal: stripComments is literally (src) => rustCode(src) — it already IS the shared lexer, with the reasoning in its own docblock) | 3 |
| 101 | scripts/check-window-creation-thread.mjs | 100 | High | Refactoring Debt | `parseFns` is a high-complexity 44-line hand-written parser combining function discovery, body balancing, attribute association, visibility inference, and metadata construction | Split these concerns into lexer-backed helpers or use a Rust parser | changed line | not-fixed (rebuttal: the one hazardous concern, attribute binding, was extracted to attributeBlock under #105; signatureEnd, the raw-identifier regex and the brace balance are each a named helper or one documented line, each pinned by a fixture test) | 3 |
| 102 | scripts/check-window-creation-thread.mjs | 103 | High | Logic & Correctness | The function-name regex only recognizes ASCII identifiers and partially parses raw or Unicode identifiers, breaking identities and transitive-call matching | Tokenize Rust identifiers, including `r#name` and Unicode XID forms | changed line | fixed | 1 |
| 103 | scripts/check-window-creation-thread.mjs | 110 | High | Logic & Correctness | `indexOf("{")` does not verify that the brace belongs to the matched function; bodyless trait or extern declarations consume a later item’s body | Scan the signature to its first top-level `{` or `;`, and skip declarations ending in `;` | changed line | fixed | 1 |
| 104 | scripts/check-window-creation-thread.mjs | 115 | High | Logic & Correctness | The brace balancer counts braces retained inside ordinary strings and does not recognize raw strings or character literals, so valid literals can truncate or extend a function body and hide call edges | Balance braces over source with comments and all Rust literals blanked while preserving offsets | changed line | fixed | 1 |
| 105 | scripts/check-window-creation-thread.mjs | 123 | Medium | Shortcuts & Patches | The arbitrary 600-byte attribute lookbehind silently stops recognizing commands with a longer valid contiguous attribute block | Walk backward token-wise through contiguous attributes without a byte limit | changed line | fixed | 3 |
| 106 | scripts/check-window-creation-thread.mjs | 138 | Medium | Logic & Correctness | `pub(super)` and `pub(in …)` are treated as crate-wide visibility, creating false edges between unrelated modules | Resolve the module tree and enforce the actual restricted visibility scope | pre-existing line | not-fixed (rebuttal: pub(super) IS visible to sibling modules, so not treating it as file-scoped is correct; the imprecision only ADDS candidate edges, which can add a finding but never hide one, and the gate is zero-tolerance and green) | 3 |
| 107 | scripts/check-window-creation-thread.mjs | 139 | High | Logic & Correctness | Function identity is only `file::name`; same-named methods or nested functions in one file collide and share reachability | Include source offset and owning module/impl path in the identity | pre-existing line | fixed | 1 |
| 108 | scripts/check-window-creation-thread.mjs | 169 | High | Logic & Correctness | Name-based reachability deliberately ignores cross-file receiver calls and cannot resolve renamed imports or function values, so a synchronous command can reach window creation without detection | Build the graph from Rust syntax and resolved imports/types, or constrain window creation behind a uniquely gated API | changed line | not-fixed (rebuttal: the header records the measurement — bare-name resolution reports 15 findings, 8 false; the visibility rule brings it to 7, all real — and the alternative the finding names, a Rust syntax/import graph, is the architectural change it concedes) | 1 |
| 109 | scripts/check-window-creation-thread.mjs | 205 | High | Logic & Correctness | The escape marker is searched in raw body text, so a string containing `// window-thread-ok: reason` suppresses a real violation | Accept markers only from lexer-identified comment tokens | changed line | fixed | 1 |
| 110 | scripts/check-window-creation-thread.mjs | 211 | Medium | Refactoring Debt | `main` spans 58 lines and mixes discovery, parsing, policy evaluation, diagnostics, and process termination | Extract source discovery and result-reporting helpers | pre-existing line | not-fixed (rebuttal: main is a linear sequence of guard-report-exit blocks, and every one is driven by a subprocess test — vacuous pass, seed-zero, alias refusal, bare marker, violation, real crate) | 3 |
| 111 | scripts/dod-syntax.mjs | 114 | Low | Logic & Correctness | `RegExp.test` is called without resetting `lastIndex`, so exported callers passing a global or sticky regex receive state-dependent results across files | Reset `re.lastIndex` before each test or clone the regex without stateful flags | new file | fixed | 3 |
| 112 | scripts/dod-syntax.mjs | 162 | High | Logic & Correctness | A title alone is counted as a runnable test; `it("placeholder")` or `test("x", undefined)` can satisfy the DoD despite lacking a callable handler | Require a valid handler, accounting separately for `each` factory calls | new file | fixed | 1 |
| 113 | scripts/dod-syntax.mjs | 162 | High | Logic & Correctness | Calls are matched solely by the identifier text `it` or `test`, so a shadowed local function can produce a false pass | Resolve bindings to the test framework import/global and reject shadowed roots | new file | fixed | 1 |
| 114 | scripts/dod-syntax.mjs | 169 | High | Logic & Correctness | Unconditional AST traversal counts tests inside never-called functions, dead branches, and skipped test callbacks even though module evaluation never registers them | Restrict traversal to module/suite registration contexts and propagate inert state through skipped cases | new file | not-fixed (rebuttal: the header records that the obvious structural rule was measured against every test file in the repo and refuses a real one — markdownPipeline/__tests__/performance.test.ts nests under the alias describePerf; skipped suites and cases ARE propagated) | 1 |
| 115 | scripts/dod-syntax.mjs | 179 | High | Logic & Correctness | Journey validation inspects only a binding’s initializer; subsequent assignment or object mutation can replace `name` or `run` while the checker still passes | Require an immutable, unmodified binding or perform symbol/write analysis | new file | fixed | 1 |
| 116 | scripts/dod-syntax.mjs | 192 | High | Logic & Correctness | `propertyNamed` returns the first explicit property and ignores later duplicates, spreads, and computed overrides, while JavaScript uses the final runtime value | Evaluate properties in source order or fail closed on duplicates, spreads, and computed keys affecting `name` or `run` | new file | fixed | 1 |
| 117 | scripts/dod-syntax.mjs | 242 | Medium | Refactoring Debt | `main` exceeds 30 lines and combines four command parsers, execution, diagnostics, and exit-code policy | Extract one handler per subcommand and dispatch through a command map | new file | fixed | 3 |
| 118 | scripts/dod-syntax.mjs | 269 | High | Logic & Correctness | `ts-has-test-case` never checks parse diagnostics, allowing a syntactically invalid file recovered by the TypeScript parser to pass | Reject any source file with `parseDiagnostics` before counting cases | new file | fixed | 1 |
| 119 | scripts/gen-feature-ledger.mjs | 1 | Medium | Refactoring Debt | The file is 424 lines, exceeding the project’s approximately 300-line code-file limit | Split schema/default verification, filesystem measurement, coverage joining, and CLI orchestration into focused modules | changed line | not-fixed (rebuttal: no rule in this file is written twice any more — the two that were, the test policy and the enumeration, are fixed under other rows — and every branch is covered by the 48-case self-test; scripts/ is outside check-file-size.mjs's ROOTS) | 3 |
| 120 | scripts/gen-feature-ledger.mjs | 101 | Medium | Shortcuts & Patches | `readJson` conflates a missing file with a JSON `null` value and lets malformed JSON escape without source-specific diagnostics | Distinguish missing, null, and parse failure and report the offending path | changed line | fixed | 3 |
| 121 | scripts/gen-feature-ledger.mjs | 135 | Medium | Logic & Correctness | Settings defaults are inspected without checking TypeScript parse diagnostics, so parser recovery can verify a partial malformed initializer | Refuse defaults containing parse diagnostics | changed line | fixed | 2 |
| 122 | scripts/gen-feature-ledger.mjs | 147 | High | Logic & Correctness | Spreads, shorthand properties, and computed properties are skipped even when they override an earlier literal key, allowing the verifier to approve a default different from the runtime value | Mark affected subtrees unverifiable or resolve object composition and final property precedence | changed line | fixed | 1 |
| 123 | scripts/gen-feature-ledger.mjs | 194 | High | Logic & Correctness | The feature-map shape is not validated; an empty `features` array produces an authoritative empty ledger, and empty `paths` arrays reach platform-dependent tool defaults or failures | Validate a schema requiring nonempty features, unique names, and nonempty normalized paths | changed line | fixed | 1 |
| 124 | scripts/gen-feature-ledger.mjs | 200 | Medium | Logic & Correctness | `root` is used for `existsSync`, but `listFiles` searches relative to process cwd, so exported helpers can validate one tree and inspect another | Resolve all measurement paths against `root` or run subprocesses with `cwd: root` | changed line | fixed | 2 |
| 125 | scripts/gen-feature-ledger.mjs | 201 | High | Duplication | Every path is enumerated twice during validation and then the feature is enumerated again during measurement | Build one file inventory per normalized feature and derive all-file, code-file, and test-file views from it | changed line | fixed | 3 |
| 126 | scripts/gen-feature-ledger.mjs | 233 | Medium | Logic & Correctness | Normalization does not collapse `..`, repeated separators, or `.`; equivalent or nested paths can survive and be counted more than once | Use `path.normalize`, reject absolute/root-escaping paths, and deduplicate canonical forms | changed line | fixed | 2 |
| 127 | scripts/gen-feature-ledger.mjs | 241 | High | Duplication | Test classification is duplicated and already inconsistent: `isTest` recognizes `/test/`, but the tokei exclusions do not, so some test code contributes to both test lines and production code lines | Define one shared test-file policy and measure code from the resulting explicit source-file set | changed line | fixed | 3 |
| 128 | scripts/gen-feature-ledger.mjs | 257 | Medium | Logic & Correctness | Newline-delimited `find` output misparses valid filenames containing newlines, corrupting counts and subsequent reads | Use NUL-delimited output and split on `\0` | changed line | fixed | 2 |
| 129 | scripts/gen-feature-ledger.mjs | 277 | High | Refactoring Debt | `joinSources` is a high-cyclomatic, densely packed validator for four unrelated formats, making omissions and divergent validation rules difficult to see | Extract one typed validator per baseline format | changed line | not-fixed (rebuttal: four independent shape validators with no shared rule between them, each already covered by joinSources cases in the self-test; splitting them removes no drift channel) | 3 |
| 130 | scripts/gen-feature-ledger.mjs | 281 | Medium | Logic & Correctness | Baseline count validation accepts negative and fractional numbers and accepts empty path keys, despite treating them as measured counts and identities | Require nonnegative safe integers and nonempty normalized path keys | changed line | fixed | 2 |
| 131 | scripts/gen-feature-ledger.mjs | 323 | Medium | Logic & Correctness | Absolute coverage paths are relativized with a raw string-prefix check; sibling paths sharing the root prefix can be sliced as though they were inside the repository | Use `path.relative` and reject results outside the root | changed line | fixed | 2 |
| 132 | scripts/gen-feature-ledger.mjs | 327 | High | Logic & Correctness | A coverage entry counts as “seen” even when `lines` or its numeric fields are absent; defaults of zero can therefore yield a falsely complete percentage | Validate each coverage record before incrementing `seen`, and fail closed on malformed metrics | changed line | fixed | 1 |
| 133 | scripts/gen-feature-ledger.mjs | 375 | Medium | Refactoring Debt | `main` exceeds 30 lines and coordinates parsing, validation, four data joins, measurement, sorting, rendering, and filesystem writes | Extract loading/validation, measurement, and output stages | changed line | not-fixed (rebuttal: same as #29 — main is CLI orchestration whose every refusal path is asserted by exit code and message through the real generator) | 3 |
| 134 | scripts/gen-feature-ledger.mjs | 405 | High | Logic & Correctness | Any existing coverage summary is trusted without commit/tree provenance, so a stale summary from older code can be reported as current coverage when filenames still match | Record and verify the source tree hash for coverage, or require regeneration when inputs are newer | changed line | fixed | 1 |
| 135 | scripts/gen-feature-ledger.mjs | 417 | Medium | Logic & Correctness | The generated ledger is overwritten directly; interruption or disk failure can leave a truncated artifact | Write to a sibling temporary file, fsync if needed, then atomically rename | changed line | fixed | 2 |
| 136 | scripts/lib/arrayLiteralEnd.mjs | 26 | High | Logic & Correctness | JavaScript regex literals are not recognized, so brackets or comment-like text inside a regex alter depth or trigger comment skipping | Use the TypeScript scanner for TS/JS lexical tokens | new file | fixed | 1 |
| 137 | scripts/lib/arrayLiteralEnd.mjs | 27 | Low | Logic & Correctness | Line comments terminate only on `\n`; valid lone `\r`, U+2028, and U+2029 terminators make the scanner discard the rest of the file | Recognize all ECMAScript line terminators | new file | not-fixed (rebuttal: arrayLiteralEnd scans no comments — TS goes through ts.createSourceFile, which honours every ECMAScript line terminator, and Rust through rustCode, where \n is the only terminator Rust's grammar defines) | 3 |
| 138 | scripts/lib/arrayLiteralEnd.mjs | 30 | High | Logic & Correctness | Rust block comments can nest, but `indexOf("*/")` stops at the first close, exposing brackets still inside the outer comment | Track nested block-comment depth | new file | fixed | 1 |
| 139 | scripts/lib/arrayLiteralEnd.mjs | 34 | High | Logic & Correctness | Template literals are treated as flat quoted strings; nested templates inside `${…}` can expose a bracket as the outer array terminator | Track template text and interpolation states or delegate to the TypeScript scanner | new file | fixed | 1 |
| 140 | scripts/lib/arrayLiteralEnd.mjs | 34 | High | Logic & Correctness | Rust raw strings may contain unescaped quotes, but the ordinary-string loop stops at the first quote and can expose brackets still inside the raw literal | Recognize raw and raw-byte string delimiters with their hash counts | new file | fixed | 1 |
| 141 | scripts/lib/docJoins/lintTable.mjs | 71 | Medium | Logic & Correctness | Bare-prose detection excludes supported named keys such as Enter, Escape, and PageDown, allowing stale unquoted chords to escape checking | Build the expression from the same named-key vocabulary used by `chordTokens` | new file | fixed | 2 |
| 142 | scripts/lib/docJoins/lintTable.mjs | 86 | Medium | Logic & Correctness | Table parsing treats every pipe preceded by a backslash as escaped, mishandling even backslash runs and escaped terminal pipes | Parse cells with a state machine that counts consecutive backslashes and distinguishes outer delimiters | new file | fixed | 2 |
| 143 | scripts/lib/docJoins/lintTable.mjs | 99 | Medium | Logic & Correctness | A delimiter row can have a different column count from the header and still be accepted as a valid table | Require the delimiter and header rows to have identical cell counts | new file | fixed | 2 |
| 144 | scripts/lib/docJoins/lintTable.mjs | 109 | High | Refactoring Debt | `joinRuleTable` exceeds 30 lines and has high cyclomatic complexity across parsing, validation, duplicate detection, lookup, and completeness checks | Extract row parsing and per-row validation into focused helpers | new file | not-fixed (rebuttal: joinRuleTable is one pass over rows; findTable, splitCells, leadingTitle and checkedRuleMeta are already separate, and both directions of the join are pinned by dedicated cases) | 3 |
| 145 | scripts/lib/docJoins/lintTable.mjs | 153 | Medium | Logic & Correctness | Hyphen-separated tokens are not trimmed even though the prose regex accepts surrounding spaces, so `Cmd - Shift - L` is silently ignored | Trim tokens returned by `keyTokens` or use one shared separator parser | new file | fixed | 2 |
| 146 | scripts/lib/docJoins/lintTable.mjs | 170 | High | Duplication | `docsChord` reimplements formatter normalization and has already drifted from `prosemirrorToDocs`, notably for Unicode code-point keys | Export and reuse a shared token formatter or canonical chord normalizer | new file | fixed | 3 |
| 147 | scripts/lib/docJoins/lintTable.mjs | 177 | Medium | Logic & Correctness | Fence handling toggles on any backtick or tilde marker without tracking marker type or length, so content can prematurely close a fence and produce false findings | Record the opening marker and require a compatible closing fence | new file | fixed | 2 |
| 148 | scripts/lib/docJoins/lintTable.mjs | 194 | High | Refactoring Debt | `joinTriggers` exceeds 30 lines and has high cyclomatic complexity spanning registry validation, row matching, binding checks, and document-wide scans | Split registry lookup, trigger-row validation, and stale-chord scanning into separate helpers | new file | not-fixed (rebuttal: joinTriggers's three stages already delegate to findTable, chordsInDoc and chordTokens; the remaining body is the join itself, and every one of its findings has its own test) | 3 |
| 149 | scripts/lib/docJoins/lintTable.mjs | 208 | Medium | Logic & Correctness | Trigger rows are not checked for exactly two cells or unique actions, so malformed or duplicate rows can satisfy the join | Validate every row’s arity and reject duplicate required actions before matching | new file | fixed | 2 |
| 150 | scripts/lib/docJoins/lintTable.mjs | 239 | Medium | Logic & Correctness | Rule metadata validation does not reject duplicate IDs; `Map` overwrites one entry and the completeness check then considers both covered | Validate ID uniqueness before constructing `byId` | new file | fixed | 2 |
| 151 | scripts/lib/docJoins/lintTable.mjs | 249 | Medium | Logic & Correctness | Shortcut validation checks only that the value is an array, allowing malformed or duplicate definitions and making `find` select an arbitrary duplicate | Validate required fields and uniqueness for the three consumed shortcut IDs | new file | fixed | 2 |
| 152 | scripts/lib/dod-assertions.sh | 23 | High | Duplication | The grep assertion family duplicates status handling across multiple wrappers and `probe`, causing error semantics to diverge | Centralize grep execution and explicitly distinguish match, no-match, and execution-error statuses | new file | fixed | 3 |
| 153 | scripts/lib/dod-assertions.sh | 26 | High | Logic & Correctness | Negative grep assertions report success when grep exits with an error, including unreadable files and invalid regular expressions | Branch explicitly on exit statuses 0, 1, and greater than 1, failing on errors | new file | fixed | 1 |
| 154 | scripts/lib/dod-assertions.sh | 61 | Medium | Logic & Correctness | Rust test discovery hardcodes the owning module to the sibling non-test file, rejecting valid test files included from `mod.rs` or another module | Accept the owning module explicitly or search candidate module files with the syntax-aware probe | new file | fixed | 2 |
| 155 | scripts/lib/dod-assertions.sh | 78 | High | Logic & Correctness | Work-item and decision IDs are interpolated into regular expressions unescaped, so an ID containing a dot can match a different identifier | Escape the identifier before regex construction or parse the fixed prefix separately | new file | fixed | 1 |
| 156 | scripts/lib/dod-assertions.sh | 94 | Low | Shortcuts & Patches | JSON and schema errors are discarded and reduced to the misleading message that the baseline has `?` entries | Capture and report the parser error while still failing closed | new file | fixed | 3 |
| 157 | scripts/lib/dod-assertions.sh | 100 | Medium | Shortcuts & Patches | `assert_exec` depends on an undocumented global `EXEC`; under nounset mode a missing initialization aborts the checker | Pass execution mode explicitly or use a validated default such as `${EXEC:-0}` | new file | fixed | 3 |
| 158 | scripts/lib/dod-assertions.sh | 101 | Medium | Shortcuts & Patches | Executed gate output is discarded completely, leaving only the command text when a gate fails | Capture output and include a bounded diagnostic tail in the failure report | new file | fixed | 3 |
| 159 | scripts/lib/dod-assertions.sh | 104 | Medium | Shortcuts & Patches | The probe serialization has no escaping for its pipe delimiter, so common fixed strings or paths containing a pipe are misparsed | Pass kind, pattern, and path as separate arguments or define an escaped structured encoding | new file | fixed | 3 |
| 160 | scripts/lib/dod-assertions.sh | 110 | High | Logic & Correctness | A `nogrep` probe converts grep execution errors into success through shell negation, allowing `assert_any` to pass unverifiable evidence | Inspect grep’s exit status and accept only the explicit no-match status | new file | fixed | 1 |
| 161 | scripts/lib/featureLedgerRender.mjs | 22 | Medium | Logic & Correctness | Line flattening handles LF and CRLF but not a lone carriage return, which CommonMark treats as a line ending and can use to break generated structure | Replace all CRLF, LF, and lone CR sequences | new file | fixed | 2 |
| 162 | scripts/lib/featureLedgerRender.mjs | 22 | Medium | Logic & Correctness | Plain-cell escaping omits ampersands, so entity-like input such as `&copy;` does not render as the original text despite the function’s contract | Encode ampersands as `&amp;` before applying other escapes | new file | fixed | 2 |
| 163 | scripts/lib/featureLedgerRender.mjs | 45 | Medium | Logic & Correctness | Coverage values are not validated for finiteness or range, allowing `NaN%`, `Infinity%`, or impossible percentages into the ledger | Validate the coverage shape and require a finite percentage from 0 through 100 | new file | fixed | 2 |
| 164 | scripts/lib/featureLedgerRender.mjs | 51 | Medium | Logic & Correctness | A measured feature with zero test lines renders `--` instead of `0.00`, directly contradicting the document’s distinction between unmeasured and zero | Check for nullish values and zero code explicitly rather than using truthiness | new file | fixed | 2 |
| 165 | scripts/lib/featureLedgerRender.mjs | 54 | Medium | Logic & Correctness | `bigFiles`, `mocks`, `dep`, and `coup` use truthy fallback, converting missing or `NaN` measurements into a claimed zero | Validate numeric fields and use nullish handling that preserves unmeasured state | new file | fixed | 2 |
| 166 | scripts/lib/featureLedgerRender.mjs | 54 | Medium | Logic & Correctness | Most row fields are interpolated without escaping or type validation, so an unexpected string containing a pipe or newline corrupts the generated table | Validate the complete row schema and escape every rendered field | new file | fixed | 2 |
| 167 | scripts/lib/featureLedgerRender.mjs | 58 | Low | Refactoring Debt | `renderLedger` exceeds 30 lines and couples dynamic interpolation to a large static document template | Extract stable sections into named constants or small rendering helpers | new file | not-fixed (rebuttal: renderLedger is one template literal; hoisting its prose into constants would put the document's text further from the table it describes and removes no hazard — the Low finding names no defect) | 3 |
| 168 | scripts/lib/featureLedgerRender.mjs | 70 | Low | Logic & Correctness | `defaultsRel` is inserted into inline code delimiters directly at two locations, so a backtick in the supplied path breaks the generated Markdown | Render it through `codeSpan` in both locations | new file | fixed | 3 |
| 169 | scripts/lib/headerReferenceTargets.mjs | 52 | High | Logic & Correctness | The source walker excludes shell files even though scripts such as `dod-assertions.sh` contain header references, silently leaving those references unaudited | Add `.sh` and any other supported source extensions to the scan policy | new file | not-fixed (out of reach: adding .sh to SOURCE_EXTENSIONS was measured 2026-09-08 and immediately reports TWELVE "# Plan: dev-docs/plans/*.md" headers whose plan file is absent from this tree while a sibling worktree holds them; all maintainer-local, so CI never sees them and only this machine goes red. Landing it needs a maintainer call — retire the 12 headers as "Origin: ... (retired)" or restore the plans — since appending to the identity baseline is forbidden by its own header. Recorded at the declaration) | 1 |
| 170 | scripts/lib/headerReferenceTargets.mjs | 134 | High | Logic & Correctness | Extension variants are appended even when the target already has an extension, so a missing `foo.js` can falsely resolve through `foo.js.ts` | Generate extension candidates only when `posix.extname(spec)` is empty | new file | fixed | 1 |
| 171 | scripts/lib/headerReferenceTargets.mjs | 134 | High | Duplication | Extension-candidate construction is duplicated here and in `pathExistsAt`, allowing resolution semantics and bugs to drift | Centralize candidate generation in one helper used by both location and tail resolution | new file | fixed | 3 |
| 172 | scripts/lib/headerReferenceTargets.mjs | 175 | Medium | Logic & Correctness | Glob traversal treats `.` and `..` as literal directory entries, so anchored globs such as `./foo/*.ts` and `../foo/*.ts` cannot resolve | Normalize the static relative prefix into the base before traversing wildcard segments | new file | fixed | 2 |
| 173 | scripts/lib/headerReferenceTargets.mjs | 179 | Medium | Logic & Correctness | Terminal glob matches count directories as hits even though the documented contract requires at least one matching file | Require terminal frontier entries to be files unless directory matching is explicitly requested | new file | fixed | 2 |
| 174 | scripts/lib/headerReferenceTargets.mjs | 223 | Medium | Logic & Correctness | A single mutable `via` value makes brace-expansion provenance order-dependent and can hide that one alternative used suffix fallback | Accumulate resolution routes and return a set or deterministic strongest classification | new file | fixed | 2 |
| 175 | scripts/lib/headerReferenceTargets.mjs | 233 | Medium | Logic & Correctness | Dependency resolution requires an exact manifest key and rejects valid package subpaths such as `@scope/package/subpath` | Extract the root package name from the module specifier before consulting dependency names | new file | fixed | 2 |
| 176 | scripts/lib/headerReferenceTargets.mjs | 250 | Medium | Refactoring Debt | `resolveRustModuleTarget` exceeds 30 lines and combines parsing, qualifier traversal, boundary enforcement, candidate generation, and lookup | Extract qualifier-to-base resolution and module-candidate generation | new file | not-fixed (rebuttal: ~40 lines of one linear flow — qualifier to bases, then one lookup; the candidate list .rs//mod.rs/dir appears once so there is nothing to drift, and every qualifier branch is already driven by a test) | 3 |
| 177 | scripts/lib/headerReferenceTargets.mjs | 251 | Medium | Logic & Correctness | Empty or prefix-only Rust targets such as `crate::` produce an empty relative path and can resolve merely because the base directory exists | Reject targets with no module segment after consuming qualifiers | new file | fixed | 2 |
| 178 | scripts/lib/headerReferences.mjs | 1 | Medium | Refactoring Debt | The 354-line module exceeds the project’s ~300-line limit and combines tokenization, reference resolution, statistics, and baseline handling. | Split grammar/scanning and baseline operations into focused modules. | new file | not-fixed (rebuttal: scripts/ is outside lint:file-size and the module's seams are already exported and individually tested; two real defects inside it were fixed instead, #182 and #183) | 3 |
| 179 | scripts/lib/headerReferences.mjs | 100 | High | Logic & Correctness | Skipping TypeScript parsing whenever no backtick exists leaves multiline JSX text and backslash-continued string literals unblanked; comment-shaped text — including `@generated` — can create false references or silently skip an entire file. | Always parse supported JS/TS/JSX sources before scanning comments. | new file | fixed | 1 |
| 180 | scripts/lib/headerReferences.mjs | 103 | High | Logic & Correctness | `[...source]` indexes Unicode code points, while TypeScript AST offsets use UTF-16 code units; an emoji or astral CJK character before a literal shifts every blanking range and can expose literal text as fake comments. | Use `source.split("")` or another UTF-16-offset-preserving representation. | new file | fixed | 1 |
| 181 | scripts/lib/headerReferences.mjs | 125 | Medium | Logic & Correctness | The boolean block-comment state closes at the first `*/`, but Rust block comments nest; references after an inner close are incorrectly ignored while still inside the outer comment. | Track Rust block-comment depth or consume structured comment spans from the Rust scanner. | new file | fixed | 2 |
| 182 | scripts/lib/headerReferences.mjs | 149 | Low | Logic & Correctness | Wrapper removal precedes punctuation removal, so combined forms such as `"foo.rs",` retain the closing quote and fail resolution. | Strip punctuation and balanced wrappers in a loop or parse the complete wrapped token first. | new file | fixed | 3 |
| 183 | scripts/lib/headerReferences.mjs | 177 | Low | Logic & Correctness | Exact directory targets such as `dev-docs` or `../dev-docs` are not classified as maintainer-local because only paths containing a following slash match. | Match normalized path segments, including `t === "dev-docs"`. | new file | fixed | 3 |
| 184 | scripts/lib/headerReferences.mjs | 199 | High | Logic & Correctness | The `#[path] mod` regex runs over raw sibling source, so declarations inside comments or string examples can create phantom module aliases and allow an invalid `@module` header to pass. | Extract actual Rust attribute/module tokens with a comment-and-literal-aware parser. | new file | fixed | 1 |
| 185 | scripts/lib/headerReferences.mjs | 202 | Medium | Logic & Correctness | Mounted-module discovery examines only sibling `.rs` files and requires the attribute value to equal the basename; valid mounts such as `#[path = "sub/foo.rs"]`, `./foo.rs`, raw-string paths, or ancestor declarations are missed. | Build a normalized repository-wide mount index using Rust’s path-relative rules. | new file | not-fixed (rebuttal: all 281 #[path] mounts in this crate are bare sibling basenames — zero contain a separator, zero are raw strings — and a missed mount produces a LOUD false finding on @module, not a silent pass) | 2 |
| 186 | scripts/lib/keybindingFormat.mjs | 36 | Medium | Logic & Correctness | Every final empty split token becomes the minus key, so malformed `Mod-` is accepted as `Mod + -`; direct `keyTokens("")` likewise returns `["-"]`. | Accept minus only for the exact `-` or `--` terminal encoding and reject lone trailing separators and empty input. | new file | fixed | 2 |
| 187 | scripts/lib/ptyRun.py | 45 | Low | Logic & Correctness | `--timeout` accepts zero, negative, NaN, and infinite values, producing immediate or permanently unbounded behavior contrary to a finite timeout contract. | Use an argparse validator requiring a finite positive number. | new file | fixed | 3 |
| 188 | scripts/lib/ptyRun.py | 119 | Medium | Logic & Correctness | Receiving any output immediately continues the loop before reaping the child; a completed child with sustained buffered or descendant output can be reported as timed out. | Check `waitpid` on every iteration after handling I/O, including iterations that read data. | new file | fixed (no RED receipt: the fix is correct in shape — the reap can no longer be starved by a continue — but no input was found where old and new disagree, since the reader outpaces any writer and a gap always appears; the added test is a regression guard and its comment says so) | 2 |
| 189 | scripts/lib/ptyRun.py | 188 | Medium | Shortcuts & Patches | Exceptional paths from `select`, PTY I/O, or transcript writes only close the master FD; they do not terminate and reap the spawned process group, potentially leaving descendants running. | Track whether the child was reaped and call process-group cleanup in the exception/finally path before closing the FD. | new file | fixed | 3 |
| 190 | scripts/lib/rustSource.mjs | 45 | High | Refactoring Debt | `rustCode` is roughly 75 lines with a highly branched handwritten lexer, exceeding the 30-line function guideline and concentrating substantial cyclomatic complexity in one routine. | Split comment, raw-string, quoted-string, and character consumers into small cursor helpers. | new file | fixed | 3 |
| 191 | scripts/lib/rustSource.mjs | 83 | Medium | Logic & Correctness | Raw C strings (`cr"…"`, `cr#"…"#`) are unsupported, so comment-shaped contents inside valid modern Rust literals can be treated as code. | Recognize `cr` alongside `r` and `br`, with correct identifier-boundary handling. | new file | fixed | 2 |
| 192 | scripts/lib/rustSource.mjs | 84 | Medium | Logic & Correctness | Raw-string header detection examines only 16 UTF-16 units, despite claiming support for any hash count; valid delimiters above 14 hashes for `r` or 13 for `br` are mis-tokenized. | Scan hashes until the opening quote, subject to Rust’s delimiter limit, without a fixed slice. | new file | fixed | 2 |
| 193 | scripts/lib/rustSource.mjs | 110 | Low | Logic & Correctness | Unescaped character detection assumes exactly one UTF-16 unit, so valid astral Unicode scalar literals such as `'😀'` are mistaken for lifetimes and remain unblanked. | Consume one Unicode code point before checking for the closing apostrophe. | new file | fixed | 3 |
| 194 | server/mcp/src/cli.ts | 20 | High | Duplication | `VERSION` is manually duplicated across five files and synchronized by `sed`, creating a documented drift risk. | Generate or import one authoritative version value during build. | pre-existing line | fixed | 3 |
| 195 | server/mcp/src/cli.ts | 31 | Medium | Logic & Correctness | `process.exit()` immediately follows buffered console writes here and at lines 97/106, so piped version or health-check output can be truncated. | Set `process.exitCode` and return, or await a completed stream write before exiting. | pre-existing line | fixed | 3 |
| 196 | server/mcp/src/cli.ts | 45 | Medium | Refactoring Debt | `runHealthCheck` spans 64 lines, mixing mock construction, validation, serialization, and process termination. | Extract bridge construction, validation, and result rendering into focused functions. | pre-existing line | not-fixed (rebuttal: the function moved to server/mcp/src/utils/healthCheck.ts, required by #195's growth pushing cli.ts over the 300-line gate, and its branches are now driven end-to-end; mock construction/validation/rendering were not split, each being a handful of lines with no drift surface) | 3 |
| 197 | server/mcp/src/cli.ts | 64 | Medium | Logic & Correctness | The health check validates only total count and property truthiness; wrong names or compensating zero/two-tool registrations can pass. | Compare exact registry names, reject duplicates, and verify each registration contributes exactly its declared tool. | pre-existing line | fixed | 3 |
| 198 | server/mcp/src/cli.ts | 133 | High | Logic & Correctness | A missing or invalid explicit `--port` is silently ignored, causing fallback discovery and potentially connecting to a different VMark instance than requested. | Fail fast on missing, invalid, or conflicting `--port` arguments. | changed line | fixed | 1 |
| 199 | server/mcp/src/cli.ts | 167 | Medium | Refactoring Debt | `main` spans 88 lines and owns configuration, lifecycle, SDK registration, bridge connection, and transport startup. | Extract bridge construction, tool registration, and startup orchestration. | pre-existing line | not-fixed (rebuttal: main is one startup sequence — parse, bridge, shutdown, register, serve — each step already a call into a tested module, and its ORDERING is what matters, e.g. shutdown triggers registered before any await and transport before bridge.connect(); splitting would hide the order) | 3 |
| 200 | server/mcp/src/cli.ts | 244 | High | Logic & Correctness | MCP stdio startup waits for the VMark WebSocket connection first; an unreachable stale endpoint can delay initialization by the bridge’s full timeout and make the MCP client reject startup. | Start the stdio transport immediately and initiate the bridge connection concurrently with scoped error handling. | pre-existing line | fixed | 1 |
| 201 | server/mcp/src/cli.ts | 257 | Medium | Shortcuts & Patches | The global `unhandledRejection` handler logs and continues for every rejection, including non-recoverable programming or transport failures, leaving potentially corrupted state alive. | Catch expected reconnect failures locally and terminate gracefully on genuinely unhandled rejections. | pre-existing line | not-fixed (rebuttal: the handler exists so a long-lived sidecar survives a transient reconnect/transport rejection; terminating on any rejection would drop the MCP session on a benign one, and only the local catch can distinguish them — main() already has one, and uncaughtException remains the fatal path) | 3 |
| 202 | server/mcp/src/index.ts | 22 | Medium | Dead Code | The shebang and documented `node dist/index.js` invocation imply an executable entry point, but this module only exports definitions and exits without starting a server. | Remove the executable documentation/shebang or delegate direct execution to the CLI entry point. | pre-existing line | fixed | 3 |
| 203 | server/mcp/src/index.ts | 108 | High | Duplication | Registry descriptions manually repeat action names at lines 109, 139, and 147, preserving the same drift channel the registry refactor claims to remove. | Generate action-list wording from each entry’s `actions`, leaving only non-enumerative explanatory prose manual. | changed line | fixed | 3 |
| 204 | server/mcp/src/index.ts | 179 | Medium | Logic & Correctness | Normal server creation does not verify unique declared names or that each register function registered its matching name; the underlying map can silently overwrite collisions. | Validate registry-name uniqueness and each registration’s before/after map delta during construction. | changed line | not-fixed (rebuttal: toolContract.test.ts asserts the exact nine-name identity list AND tools().length === EXPECTED_TOOL_COUNT, and cli.ts re-checks the count at --health-check; a name collision drops the map to 8 and fails both) | 2 |
| 205 | server/mcp/src/tools/browser.ts | 42 | Medium | Refactoring Debt | `registerBrowserTool` spans 160 lines and combines extensive documentation, schema construction, and dispatch wiring. | Extract the description and input schema into named constants or dedicated modules. | changed line | not-fixed (rebuttal: the 160 lines are a description string and a Zod schema — declarative data; moving them to constants in the same 204-line file relocates lines without removing a hazard) | 3 |
| 206 | server/mcp/src/tools/browser.ts | 79 | Medium | Logic & Correctness | The composite schema makes every action-specific argument optional and permits irrelevant combinations, so invalid calls are advertised as schema-valid and fail only after dispatch. | Model action-specific contracts explicitly or add a shared conditional-validation layer before dispatch. | changed line | not-fixed (rebuttal: ToolDefinition.inputSchema is a flat ToolShape, which cannot express an action-discriminated union; the "conditional-validation layer before dispatch" already exists as runBrowserAction's per-action table plus readOptionalId/scriptSchema — inherent to the composite-tool design, ADR-5) | 2 |
| 207 | server/mcp/src/tools/browser.ts | 81 | High | Duplication | `tabId`, `timeoutMs`, optional-ID handling, and the dispatch wrapper are duplicated nearly verbatim in `browserRead.ts`. | Extract shared browser registration fields and a validated dispatch wrapper. | changed line | not-fixed (out of reach: deduplicating with browserRead.ts requires editing that file, which belongs to another group; the shared home already exists as browserArgs.ts, which both import, so a one-sided extraction would leave the duplication and risk a concurrent-edit collision) | 3 |
| 208 | server/mcp/src/tools/browser.ts | 122 | Medium | Logic & Correctness | Approval-facing payloads including style maps, class arrays, workflow source/inputs, and typed text have no size or item-count bounds, unlike scripts. | Add shared byte-length and collection-size limits at the public schema and defensive handler layers. | pre-existing line | fixed | 3 |
| 209 | server/mcp/src/tools/browser.ts | 178 | Medium | Logic & Correctness | `url` accepts any string although the advertised contract promises an HTTP(S) destination. | Use a schema refinement that parses the URL and restricts its protocol to HTTP or HTTPS. | pre-existing line | fixed | 3 |
| 210 | server/mcp/src/tools/browser.ts | 198 | Low | Refactoring Debt | The dispatch return is over-indented, obscuring the otherwise flat handler flow. | Align the return with the preceding statements. | pre-existing line | fixed | 3 |
| 211 | server/mcp/src/tools/browserRead.ts | 45 | Medium | Refactoring Debt | `registerBrowserReadTool` spans 92 lines and combines documentation, schema definition, and dispatch. | Extract the description and schema into focused constants/modules. | changed line | not-fixed (rebuttal: a declarative registerTool call — metadata, schema, one dispatch line; hoisting the description string a few lines up changes no behaviour and removes no drift channel) | 3 |
| 212 | server/mcp/src/tools/browserRead.ts | 79 | Medium | Logic & Correctness | As in `browser`, the flat all-optional schema accepts missing required arguments and irrelevant cross-action fields as valid MCP requests. | Introduce action-aware validation or separate action-specific input contracts. | changed line | not-fixed (rebuttal: same flat ToolShape constraint and same per-action dispatch validation as browser; browserRead shares browserArgs/browserDispatch) | 2 |
| 213 | server/mcp/src/tools/browserRead.ts | 85 | Medium | Logic & Correctness | `fields` is an unrestricted record despite documenting the exact `{attributes, box, styles}` shape, so malformed keys and value types pass public validation. | Replace it with a strict object containing optional booleans and a bounded string array. | pre-existing line | not-fixed (rebuttal: the strict fields shape has ONE enforcing declaration, readFields in services/mcpBridge/v2/browserQuery.ts, which is tested; the bridge contract deliberately declares fields as unknown-optional, and a third strict copy in the tool schema would reject a field the app later accepts) | 3 |
| 214 | server/mcp/src/tools/browserRead.ts | 89 | Medium | Logic & Correctness | `ref` and `role` accept whitespace and are not normalized; the imported dispatcher checks trimmed truthiness but forwards the original value. | Apply `.trim().min(1)` in the schema and forward normalized values defensively. | pre-existing line | fixed | 3 |
| 215 | server/mcp/src/tools/browserRead.ts | 105 | Medium | Logic & Correctness | `urlContains` accepts empty strings and `?`/`#` even though the description says such impossible needles are refused; the imported handler does not enforce that promise. | Add non-empty and query/fragment exclusion refinements. | pre-existing line | not-fixed (rebuttal: the ?/# refusal IS enforced and tested — browserWaitForPoll.ts refuses a needle that can never match the redacted URL, pinned as A-06; a zod refinement in the sidecar would be a second copy of the app's rule) | 3 |
| 216 | server/mcp/src/tools/browserRead.ts | 113 | Medium | Logic & Correctness | `runId` accepts whitespace-only identifiers; the imported handler rejects only the exact empty string and forwards whitespace. | Reuse the trimmed non-empty identifier schema and defensive ID reader. | pre-existing line | fixed | 3 |
| 217 | server/mcp/src/tools/coherence.ts | 28 | Medium | Refactoring Debt | `registerCoherenceTool` spans 62 lines and combines metadata, schema construction, validation, dispatch, and response formatting. | Extract the tool definition and handler into focused constants/functions. | changed line | not-fixed (rebuttal: same as #211 — declarative registration, no hazard) | 3 |
| 218 | server/mcp/src/tools/coherence.ts | 51 | High | Duplication | `workspace_root` validation is duplicated at coherenceResolve.ts:55-85; both versions accept whitespace-only and relative paths despite promising an absolute path. | Introduce and reuse schema/runtime guards for a nonblank absolute path while preserving legal trailing spaces. | changed line | not-fixed (out of reach: the duplicate half is server/mcp/src/tools/coherenceResolve.ts, in a disjoint group, and the shared requiredPathSchema/readWorkspaceRoot helper belongs in toolArgs.ts; tightening only coherence.ts would leave the read path strict and the non-undoable WRITE path lax, deepening the divergence) | 3 |
| 219 | server/mcp/src/tools/coherence.ts | 85 | Medium | Logic & Correctness | Oversized `claims` and `contexts` responses receive the generic recovery advice to narrow by tab or part, but this tool exposes no filter or pagination; `edges` similarly suggests mutating entries merely to reveal later results. | Add cursor/limit filtering and action-specific, non-mutating recovery instructions. | pre-existing line | fixed | 3 |
| 220 | server/mcp/src/tools/coherenceResolve.ts | 30 | Medium | Refactoring Debt | `registerCoherenceResolveTool` spans 69 lines and mixes declarative metadata with validation and mutation dispatch. | Extract the definition and handler into separate units. | changed line | not-fixed (rebuttal: the 69 lines are metadata plus the DEFENSIVE guard layer, which exists precisely because callTool runs no schema validation before a non-undoable audit-logged write; splitting metadata from the guard separates the write from the thing that protects it) | 3 |
| 221 | server/mcp/src/tools/coherenceResolve.ts | 59 | High | Logic & Correctness | `txf`, `input`, and `resolution` are optional even though every resolve request requires them; the handler forwards `undefined`, making malformed destructive requests valid according to the advertised schema. | Make all three fields required, require a nonblank `txf`, and defensively validate them before dispatch. | changed line | fixed | 1 |
| 222 | server/mcp/src/tools/coherenceResolve.ts | 70 | High | Logic & Correctness | `reason` is optional and may be blank for `resolution: "waive"`, contradicting the contract and allowing an unauditable waiver attempt. | Reject missing or whitespace-only reasons when resolution is `waive`. | changed line | fixed | 1 |
| 223 | server/mcp/src/tools/coherenceResolve.ts | 76 | High | Duplication | The valid action is hardcoded again instead of being derived from `COHERENCE_RESOLVE_ACTIONS`, creating a second source of truth that can drift. | Build the error from `COHERENCE_RESOLVE_ACTIONS.join(', ')`. | pre-existing line | fixed | 3 |
| 224 | server/mcp/src/tools/document.ts | 35 | Medium | Refactoring Debt | `registerDocumentTool` spans 157 lines and combines extensive documentation, schemas, argument normalization, dispatch, and error recovery. | Extract the definition, output schema, and action handlers into focused units. | changed line | not-fixed (rebuttal: description plus input and output schemas are declarative, and the output schema's shape is a decision argued at length in its own comment; the handler underneath is three short action branches) | 3 |
| 225 | server/mcp/src/tools/document.ts | 102 | Medium | Shortcuts & Patches | The output schema deliberately allows `{}` and cannot enforce required fields, valid `save_skipped` values, or mutual exclusion with `save_error`; it therefore provides almost no contract validation. | Split read/write/transform into separate tools or add an action discriminant and validate action-specific result invariants. | pre-existing line | not-fixed (rebuttal: the file carries a measured decision — the SDK turns a failed output validation into an McpError, so a stricter schema converts a COMPLETED disk write into a protocol failure; "{} validates" is the stated accepted trade and the row re-litigates it without new evidence) | 3 |
| 226 | server/mcp/src/tools/document.ts | 142 | High | Logic & Correctness | A supplied non-string `expected_revision` is silently converted to `undefined`, turning a guarded write or transform into an unconditional mutation for direct `callTool` callers. | Distinguish omission from invalid input and reject every supplied value that is not a nonblank string. | changed line | fixed | 1 |
| 227 | server/mcp/src/tools/document.ts | 161 | High | Logic & Correctness | Any invalid `save` value is silently treated as the default `true`; for example, a direct caller passing `"false"` causes an unintended disk write. | Reject supplied non-boolean values and forward validated booleans explicitly. | pre-existing line | fixed | 1 |
| 228 | server/mcp/src/tools/document.ts | 187 | High | Duplication | The allowed actions are hardcoded in the error text despite `DOCUMENT_ACTIONS` being the exported source of truth. | Generate the message with `DOCUMENT_ACTIONS.join(', ')`. | pre-existing line | fixed | 3 |
| 229 | server/mcp/src/tools/selection.ts | 27 | Medium | Refactoring Debt | `registerSelectionTool` spans 127 lines and combines metadata, schemas, parsing, dispatch, and stale-error handling. | Extract the definition and separate `get`/`set` handlers. | changed line | not-fixed (rebuttal: declarative registration; see #211) | 3 |
| 230 | server/mcp/src/tools/selection.ts | 45 | High | Logic & Correctness | `set` targets the selection at execution time rather than the range returned by `get`; cursor movement does not change the document revision, so a valid revision can still replace the wrong text. | Return a selection token or range from `get` and require `set` to atomically verify and replace that exact selection. | pre-existing line | not-fixed (out of reach: an atomic verify-and-replace needs a selection TOKEN in the bridge protocol and a matching webview handler in src/, outside this group's scope; the tool description already states the call-time semantics) | 1 |
| 231 | server/mcp/src/tools/selection.ts | 55 | High | Logic & Correctness | `expected_revision` is optional, and lines 117-120 silently discard invalid supplied values, despite the documentation saying the token must be passed back; both paths bypass stale-write protection. | Require a validated revision for `set`, or expose an explicit, separately named unconditional operation. | changed line | fixed | 1 |
| 232 | server/mcp/src/tools/selection.ts | 74 | Medium | Shortcuts & Patches | The output schema accepts `{}` and weakly types ranges, modes, kinds, revisions, and replacement counts, so missing, negative, fractional, reversed, or unknown values still satisfy the declared contract. | Use action-specific schemas or a discriminant; require fields and enforce enums, integer/nonnegative counts, and `from <= to`. | pre-existing line | not-fixed (rebuttal: re-litigates a decision argued in the code at selection.ts — the SDK turns a failed OUTPUT validation into an McpError, so a payload the schema failed to anticipate would report an already-committed selection replacement as a protocol failure; every field optional is the deliberate trade) | 3 |
| 233 | server/mcp/src/tools/selection.ts | 149 | High | Duplication | The action list is hardcoded in the error message instead of using `SELECTION_ACTIONS`, leaving duplicated contract data. | Generate the message from `SELECTION_ACTIONS.join(', ')`. | pre-existing line | fixed | 3 |
| 234 | server/mcp/src/tools/session.ts | 59 | Medium | Logic & Correctness | The “stable” output schema makes both top-level fields optional and accepts arbitrary window/capability values, allowing malformed successful responses to pass validation | Require the stable core fields with passthrough object schemas and model truncation as a separate envelope | pre-existing line | not-fixed (rebuttal: the remedy would break truncated responses — TRUNCATION_OUTPUT_SHAPE is spread into the SAME output schema and structuredJsonResult replaces structuredContent with the envelope alone, so a required windows fails validation on every truncated response; a separate envelope is not expressible since the SDK's outputSchema is a raw object shape, not a union) | 3 |
| 235 | server/mcp/src/tools/session.ts | 84 | Medium | Shortcuts & Patches | `get_state` can exceed the output limit but has no pagination or filtering, making omitted windows and tabs permanently inaccessible without asking the user to close them | Add cursor-based pagination or window/tab filters | pre-existing line | not-fixed (out of reach: pagination for session.get_state is a bridge-protocol change spanning operationSchemas.ts, the generated contracts on both sides, the app-side session handler and the docs, none in this group; RECOVERY.sessionGetState already states the limitation honestly) | 3 |
| 236 | server/mcp/src/tools/workflow.ts | 52 | Medium | Logic & Correctness | The mutating `patches` contract is optional and accepts arbitrary records, so the schema does not enforce any documented IRPatch discriminant or required fields | Define the eight patch variants as a Zod discriminated union and require a non-empty array for `apply_patch` | changed line | not-fixed (rebuttal: the IRPatch discriminator IS validated by the handler that owns the union — services/mcpBridge/v2/workflow.ts validates it client-side and returns INVALID_PATCH; restating eight variants in the sidecar would be a third declaration of a shape lib/ghaWorkflow/save/mutators.ts owns, which AGENTS.md's wire-contract section forbids) | 2 |
| 237 | server/mcp/src/tools/workflow.ts | 75 | High | Logic & Correctness | A blank or non-string `expected_revision` is accepted or silently converted to `undefined`, disabling optimistic concurrency for direct `callTool` callers | Reject every supplied value that is not a valid non-empty revision string | pre-existing line | fixed | 1 |
| 238 | server/mcp/src/tools/workspace.ts | 98 | High | Refactoring Debt | The approximately 115-line handler combines shared parsing, eight actions, dispatch, approval handling, and response serialization, producing high cyclomatic complexity | Extract action-specific handlers with their own argument validation and keep registration declarative | pre-existing line | not-fixed (rebuttal: a flat switch with one ~10-line case per action, each a guard plus one bridge send; cyclomatic complexity per branch is 1-2, and eight extracted functions with one caller each would add indirection and no drift protection) | 3 |
| 239 | server/mcp/src/tools/workspace.ts | 103 | High | Duplication | `tabId` and `windowLabel` are validated globally and then revalidated in required-ID branches; this also lets irrelevant arguments reject unrelated actions | Parse arguments inside each action handler exactly once | pre-existing line | not-fixed (rebuttal: not a duplicate — readOptionalId permits absence, readRequiredId does not, and close/switch_tab need the stricter one; refusing a request carrying a malformed identifier is the module header's thesis, since a blank id reads as "the focused target" downstream) | 3 |
| 240 | server/mcp/src/tools/workspace.ts | 109 | Medium | Logic & Correctness | A direct caller supplying a non-string `kind` has it silently discarded, causing `new` to create the default Markdown tab instead of rejecting invalid input | Explicitly reject a supplied value unless it is `markdown` or `yaml-workflow` | pre-existing line | fixed | 3 |
| 241 | server/mcp/src/tools/workspace.ts | 151 | Medium | Logic & Correctness | The documented `{needsApproval: true}` result is converted into an unstructured prose error, discarding the approval envelope and forcing clients to interpret text | Return a structured error result containing `needsApproval` and `folderPath`, or correct the advertised contract | pre-existing line | fixed | 3 |
| 242 | src-tauri/src/ai_provider/cancel.rs | 70 | High | Logic & Correctness | Cancellation is lost if it arrives before registration: `cancel` returns a successful no-op and a subsequent registration receives a fresh uncancelled token | Register at the command boundary before cancellation can race, or retain bounded cancellation tombstones | new file | fixed | 1 |
| 243 | src-tauri/src/ai_provider/cancel.rs | 123 | High | Shortcuts & Patches | The new helper returns legacy `Result<(), String>`, extending the untyped error surface contrary to the project’s `CommandError` migration rule | Migrate provider dispatch errors to a typed error and return `Result<(), CommandError>` | new file | not-fixed (rebuttal: dispatch_registered is a pub(super) helper, not a command, and the ratchet's unit is the command signature; migrating its only caller needs ai_provider/mod.rs, dispatch.rs and the frontend String(error) sites, all outside this group) | 3 |
| 244 | src-tauri/src/ai_provider/cancel.rs | 124 | High | Logic & Correctness | The critical cleanup claim for a command future dropped mid-dispatch is untested; existing coverage exercises normal return and provider cancellation only | Abort a task while dispatch is pending, then assert the registry entry is removed and the ID can be reused | new file | fixed | 1 |
| 245 | src-tauri/src/ai_provider/cancel.rs | 135 | Medium | Logic & Correctness | `request_id` accepts empty, whitespace, arbitrarily long, and newline-containing values, producing misleading no-op success and permitting malformed log entries | Validate the expected UUID/non-empty bounded format and return `CommandError::invalid_input` on failure | new file | not-fixed (rebuttal: cancel.rs's header already argues the no-op — cancel is a desired-state operation and the pre-cancel memory is bounded by PRE_CANCELLED_CAP; the id is crypto.randomUUID() from our own webview) | 2 |
| 246 | src-tauri/src/automation_port.rs | 27 | Medium | Logic & Correctness | The bind-and-release probe has a TOCTOU race: another process can claim port 9323 before the plugin binds, causing the plugin to scan to another port and defeating the promised fail-closed behavior | Bind through an API that transfers the reserved listener to the plugin, disable fallback scanning, or verify the plugin’s effective port and abort if it is not 9323 | new file | not-fixed (rebuttal: automation_port.rs states this window is the one thing the probe cannot close and why the alternative, a silent scan, is worse; debug builds only) | 2 |
| 247 | src-tauri/src/automation_port.rs | 36 | Low | Logic & Correctness | The free-port test drops an ephemeral listener twice and assumes nobody reclaims the port between operations, making the test inherently flaky under concurrent socket activity | Test an atomic reservation API, or retry only when a competing binder demonstrably won the race | new file | fixed | 3 |
| 248 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 97 | Medium | Shortcuts & Patches | New code introduces `Result<(), String>` and later erases `CommandError`, contrary to the typed backend error convention | Make `check_sequence` generic over a displayable error or retain `CommandError` through the assertion path | new file | not-fixed (rebuttal: check_sequence is a transcript printer in a smoke BINARY, not a Tauri command, and nothing is erased — run maps the CommandError to a format! carrying both code and message) | 3 |
| 249 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 127 | Medium | Refactoring Debt | The 37-line `run` function exceeds the requested function-size threshold and coordinates three independent smoke cases | Extract renderer-progress and command-progress case helpers | new file | not-fixed (rebuttal: run is three independent captured cases in sequence with no shared state, each already owning its counter — the property scenarios.rs's header exists to protect — so extracting adds three names for one call each) | 3 |
| 250 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 147 | High | Logic & Correctness | This calls `export_pdf`, but the smoke app built in `main.rs:68-70` never manages `ExportGate`; `export_pdf` therefore returns `Internal` before rendering and this critical command-progress case can never pass | Register `ExportGate::default()` in the smoke builder or provide a correctly initialized command-test application | new file | fixed | 1 |
| 251 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 166 | High | Duplication | `refused` duplicates the missing-parent setup, renderer invocation, `NotFound` classification, and diagnostics from `scenarios.rs:118-159` | Centralize the missing-path fixture and refusal assertion, with an optional progress-capture assertion | new file | fixed | 3 |
| 252 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 166 | Medium | Refactoring Debt | The 48-line `refused` function combines fixture creation, capture orchestration, error classification, stage validation, and reporting | Split fixture construction and result validation into focused helpers | new file | fixed | 3 |
| 253 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 170 | Low | Logic & Correctness | A PID-only scratch name can collide with a stale directory after PID reuse, producing an unrelated persistent smoke failure; `scenarios.rs:129` repeats the same weakness | Keep a unique temporary directory alive and target a guaranteed-missing child directory beneath it | new file | fixed | 3 |
| 254 | src-tauri/src/bin/pdf_smoke/progress_case.rs | 196 | Low | Logic & Correctness | A `NotFound` result accompanied by leaked stages enters this arm and prints `code=NotFound (expected NotFound)`, obscuring the actual failure | Separate wrong-error-code and nonempty-stage branches and report each violated condition explicitly | new file | fixed | 3 |
| 255 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 33 | High | Logic & Correctness | A5 is excluded from the claimed size-by-orientation matrix and is exercised only in portrait, leaving the A5 landscape path untested | Add A5 to `SIZES` and remove or repurpose the redundant portrait-only case | changed line | not-fixed (rebuttal: the matrix's claim is scoped to "every size the dialog offers" and src/export/pdfOptions.ts offers exactly a4/letter/a3/legal — A5 is not reachable through the dialog, and landscape is already proven as a width/height swap on all four) | 1 |
| 256 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 69 | Medium | Refactoring Debt | The 42-line `pagination` function bundles three unrelated artifact scenarios and exceeds the requested function-size threshold | Extract basic-pagination, A5-geometry, and large-document helpers | pre-existing line | not-fixed (rebuttal: pagination is the three cases the geometry matrix does NOT cover, 6-10 lines each and enumerated by name in main.rs's case list; three pub fns would put three names in the coordinator for cases whose only shared property is "not in the matrix") | 3 |
| 257 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 72 | High | Logic & Correctness | The “pagination” fixture creates content taller than one A4 page, but `check` only validates the PDF header and first MediaBox; it merely prints an approximate page count and never asserts pagination occurred | Parse the PDF page tree and require the expected page count | changed line | fixed | 1 |
| 258 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 83 | High | Duplication | A4’s expected rounded dimensions are hardcoded here and again at lines 104, 216, and 217 despite introducing `A4` as the single geometry source | Derive verification dimensions from `PageSpec` through a shared helper | changed line | fixed | 3 |
| 259 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 118 | Medium | Refactoring Debt | The 42-line `bad_path` function exceeds the requested function-size threshold and mixes fixture setup, execution, classification, and reporting | Reuse a focused missing-path fixture and refusal-result helper | pre-existing line | fixed | 3 |
| 260 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 162 | Medium | Refactoring Debt | The 43-line `sequential` function combines export iteration, asynchronous window settling, leak detection, and transcript reporting | Extract a reusable window-baseline waiter and a separate sequential-render loop | pre-existing line | not-fixed (rebuttal: the window-baseline waiter is already extracted as windows_returned_to; what remains is a 20-iteration loop plus the transcript decision, and the header records that separating the count from the transcript is what produced the false "PASS 20 exports") | 3 |
| 261 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 186 | High | Logic & Correctness | Window cleanup only waits and fails when `after > before`; closing an existing window, or closing one baseline window while leaking a replacement, can leave `after <= before` and incorrectly pass | Snapshot window labels, wait for exact set equality, and fail on added, removed, or replaced windows | changed line | fixed | 1 |
| 262 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 198 | Low | Logic & Correctness | An interrupted sequence prints a second `FAIL` line without incrementing `failures`, so the final count disagrees with the transcript | Either count this as a separate failed assertion or emit it as context without another `FAIL` marker | changed line | fixed | 3 |
| 263 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 207 | High | Logic & Correctness | The concurrent scenario performs no post-run window-baseline assertion, so a leak specific to concurrent rendering passes unnoticed because this is the final window-producing scenario | Apply the exact window-label baseline check around the concurrent case | changed line | fixed | 1 |
| 264 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 212 | High | Logic & Correctness | A single `tokio::join!` does not guarantee both renders reach label allocation or output setup simultaneously, so the critical collision race may never be exercised | Add a test synchronization hook/barrier at the contested phase or run synchronized repeated rounds | changed line | fixed (partly: three synchronized rounds instead of one; repetition raises the chance of landing in the collision window but does not make it deterministic — that needs a barrier inside pdf_export/renderer) | 1 |
| 265 | src-tauri/src/bin/pdf_smoke/scenarios.rs | 222 | High | Logic & Correctness | Each output is checked only for its own two-character sentinel; an output containing both documents passes despite cross-contamination | Use long unique sentinels and assert both presence of the correct sentinel and absence of the other sentinel | pre-existing line | fixed | 1 |
| 266 | src-tauri/src/bounded_read.rs | 74 | Low | Logic & Correctness | After `open` fails, the code re-reads the pathname to classify the error. A concurrent rename or symlink swap can therefore misclassify the original I/O failure using a different object, contradicting the handle-based design. | On Windows, open directories with `FILE_FLAG_BACKUP_SEMANTICS` and classify the returned handle; otherwise preserve the original open error without another path lookup. | new file | not-fixed (rebuttal: the re-lookup never feeds a read — the handle-based guarantee is about what is READ and both branches refuse — and open_regular already logs the original error at the site; the proposed FILE_FLAG_BACKUP_SEMANTICS remedy would make directories openable on a security-sensitive read path) | 3 |
| 267 | src-tauri/src/bounded_read.rs | 99 | High | Logic & Correctness | The critical post-`fstat` growth defense is untested. The existing oversized-file test exits at line 95 because its reported length already exceeds the limit, so it never exercises `take(limit + 1)` detecting a file that grows after metadata inspection. | Extract the bounded-stream logic into an injectable helper or add synchronization that appends after metadata capture, then assert `TooLarge` from the byte-count branch. | new file | fixed | 1 |
| 268 | src-tauri/src/canonical_path.rs | 33 | High | Logic & Correctness | Every non-UNC `\\?\` path is stripped indiscriminately. Volume-GUID paths become relative, while names containing trailing dots/spaces or reserved device names can resolve differently without verbatim semantics, so later operations may target a different object than the canonicalized one. | Strip only validated drive-letter and UNC forms whose components are safe under normal Win32 normalization; preserve or reject other namespace paths and test Volume GUID and verbatim-only filenames. | new file | fixed | 1 |
| 269 | src-tauri/src/command_registry.rs | 14 | Low | Refactoring Debt | `#[macro_export]` exposes this implementation-only registry publicly, but its expansion uses call-site-relative private module paths and is not a usable downstream API. | Keep the macro crate-private using a module-level `macro_rules!` plus `pub(crate) use`, or make every path hygienic with `$crate::` if public export is intentional. | pre-existing line | not-fixed (out of reach: removing the #[macro_export] means macro_rules! plus pub(crate) use, which changes the invocation path in lib.rs, another fixer's file this round; $crate:: hygiene alone would not remove the public export, which is the finding) | 3 |
| 270 | src-tauri/src/content_server/cleanup.rs | 116 | Low | Logic & Correctness | `is_clean` ignores `orphan`; because `CleanupOutcome` exposes mutable public fields, a state containing a live child handle but no recorded errors is incorrectly reported as clean. | Include `self.orphan.is_none()` and restrict field construction through invariant-preserving constructors. | new file | fixed | 3 |
| 271 | src-tauri/src/content_server/cleanup.rs | 172 | High | Logic & Correctness | Cleanup removes a stable per-workspace port-file pathname after the manager record and lock have already been released. A concurrent start can recreate that same pathname while the old child is being reaped, after which old cleanup deletes the new generation’s file, causing startup timeout or leaving a registered server without its rendezvous file. | Use generation/token-specific port-file paths or a per-workspace lifecycle state that prevents replacement until unlinking completes; add a barrier-controlled concurrent stop/start regression test. | new file | not-fixed (rebuttal: after #277/#323 every remaining port-file removal is by the record that owned the path at registration, and the file has no consumer once await_port has read it; the residual needs an old teardown in a sub-100ms gap and its worst outcome is a retryable timeout START_ATTEMPTS absorbs. Closing it fully needs the token in cleanup::Detached — a file split, not a targeted fix) | 1 |
| 272 | src-tauri/src/content_server/client.rs | 68 | Medium | Logic & Correctness | `auth_url` inserts `nonce` and `next` directly into a query string; reserved characters can corrupt or inject parameters, and the documented relative-path invariant is unenforced | Construct the URL with `Url::query_pairs_mut` and reject non-relative `next` values | new file | not-fixed (rebuttal: both call sites pass a literal — None in http.rs, "/slidev/" in slidev_commands.rs — and the nonce comes from our own server's /__mint; no caller-controlled input reaches the query string) | 2 |
| 273 | src-tauri/src/content_server/client.rs | 122 | Medium | Logic & Correctness | Non-success responses are read without a size limit and copied verbatim into the error, allowing a broken loopback server to cause excessive allocation and oversized IPC/log messages | Read a capped body and truncate/sanitize it before constructing the error | new file | fixed | 2 |
| 274 | src-tauri/src/content_server/client.rs | 135 | Medium | Logic & Correctness | Every JSON-body error is classified as `Internal`; a timeout or transport failure while reading the body therefore becomes non-retryable and contradicts `transport_failure` | Classify timeout/network errors first and reserve `Internal` for completed but malformed JSON | new file | fixed | 2 |
| 275 | src-tauri/src/content_server/client.rs | 142 | Low | Logic & Correctness | An empty nonce is accepted as valid and returned to callers, deferring the failure to a misleading authentication error | Validate the nonce as non-empty and in the server’s expected token format | new file | fixed | 3 |
| 276 | src-tauri/src/content_server/client.rs | 154 | High | Logic & Correctness | Formatting the full `reqwest::Error` can include request URLs containing nonce or session query credentials, leaking them into serialized errors or logs | Strip URLs with `without_url()` or emit sanitized transport details only | new file | fixed | 1 |
| 277 | src-tauri/src/content_server/commands.rs | 59 | High | Logic & Correctness | Every concurrent start for one workspace uses the same port-file path, so children can overwrite one another’s handoff and one attempt can delete another attempt’s file | Give every spawn a unique token-scoped port file and retain only the registered winner’s path | changed line | fixed | 1 |
| 278 | src-tauri/src/content_server/commands.rs | 69 | Medium | Shortcuts & Patches | The fixed two-attempt retry merely masks the shared-port-file race; additional competing starts can exhaust it and produce a spurious conflict | Serialize starts per canonical workspace or remove the shared-file race instead of relying on a fixed retry count | changed line | not-fixed (rebuttal: the retry's purpose is documented at the code site, the suggested "serialize per canonical workspace" contradicts commands.rs's explicit "Deliberately NOT canonicalized" decision, and exhausting it yields a conflict telling the user to start again — not data loss) | 3 |
| 279 | src-tauri/src/content_server/commands.rs | 74 | High | Logic & Correctness | The directly linked tests never execute the real start command or its spawn, port-wait, retry, cancellation, and concurrency paths | Extract an injectable lifecycle helper and add deterministic tests covering exit, timeout, cancellation, shutdown, and concurrent trust changes | changed line | fixed | 1 |
| 280 | src-tauri/src/content_server/commands.rs | 77 | High | Logic & Correctness | `workspace_root` is neither validated nor canonicalized before being used as a trust identity, manager key, hash input, and server root; empty, relative, symlinked, and equivalent spellings can address unintended or duplicate workspaces with different trust states | Require an absolute existing directory, canonicalize it once, and use the canonical identity in every command | changed line | fixed | 1 |
| 281 | src-tauri/src/content_server/commands.rs | 81 | High | Logic & Correctness | `start_once` owns an unregistered raw `Child` across an await; cancellation or application exit during that interval drops the handle without killing the child, allowing the Node server to outlive VMark | Use a kill-on-drop pending-child guard or register pending children before the first await and drain them during shutdown | changed line | not-fixed (rebuttal: future-drop is unreachable — Tauri's respond_async spawns and discards the JoinHandle, so nothing can abort a command future; the reachable half needs the manager to own a spawned-but-unregistered child, past start.rs's size headroom) | 1 |
| 282 | src-tauri/src/content_server/commands.rs | 101 | Medium | Logic & Correctness | An async Tauri command performs synchronous `Child::kill`/`wait` and filesystem cleanup inline, potentially blocking an async-runtime worker indefinitely | Move teardown to `spawn_blocking` and use a bounded reap strategy | changed line | not-fixed (rebuttal: cleanup.rs argues the reap is prompt — a delivered SIGKILL then waitpid — and a REFUSED kill deliberately uses try_wait, never wait (#122); the blocking claim the finding rests on is what that header removed) | 2 |
| 283 | src-tauri/src/content_server/commands.rs | 109 | High | Logic & Correctness | `stop` removes the registry entry before cleanup; if cleanup fails, a second stop returns success even though an orphan child or stale port file may remain | Retain a per-workspace failed-teardown state and retry or continue reporting the failure until cleanup succeeds | changed line | not-fixed (rebuttal: deliberate and pinned — commands.rs's header states the record leaves the registry either way and a second stop is quiet, while the failing attempt DOES report; erroring on every later stop would make a workspace permanently unstoppable in the UI) | 1 |
| 284 | src-tauri/src/content_server/http.rs | 46 | High | Logic & Correctness | The security-critical nonce-to-session graph handshake has no local test seam or direct tests for malformed statuses, headers, query strings, or body-read failures | Extract the handshake into a helper accepting `ServerClient` and test all redirect and parsing failure cases with a mock server | pre-existing line | fixed | 1 |
| 285 | src-tauri/src/content_server/http.rs | 55 | High | Logic & Correctness | The authentication response status is never checked; any response carrying a `Location` header, including an error response, is treated as a successful authentication redirect | Require the documented `302 Found` status and surface other statuses through typed refusal handling | changed line | fixed | 1 |
| 286 | src-tauri/src/content_server/http.rs | 56 | High | Duplication | The `/__auth?t=` request is assembled separately from `ServerClient::auth_url`, recreating precisely the URL-construction logic the client abstraction was introduced to centralize | Add an authenticated-handshake request method to `ServerClient` and use it in both callers | changed line | fixed | 3 |
| 287 | src-tauri/src/content_server/http.rs | 63 | High | Logic & Correctness | `split("s=").nth(1)` is not query parsing: it can match `s=` inside another parameter, retains trailing parameters, performs no decoding, and accepts an empty token | Parse `Location` as a URL, retrieve exactly the `s` query parameter, validate it, and encode it when building the graph request | changed line | fixed | 1 |
| 288 | src-tauri/src/content_server/http.rs | 75 | High | Duplication | Successful-body reading bypasses the shared transport mapper, so a body timeout becomes `Network` instead of `Timeout`, loses operation context, and may expose the session-bearing URL | Add `send_ok_text`/`read_text` to `ServerClient` with sanitized, consistent error classification | changed line | not-fixed (rebuttal: that line already maps through body_failure, which routes timeout/connect/request kinds to transport_failure and strips the URL with without_url() — exactly what #274/#276 did) | 3 |
| 289 | src-tauri/src/content_server/manager.rs | 116 | Low | Logic & Correctness | The monotonic generation counter uses unchecked addition; overflow panics in debug builds and wraps to a previously used generation in release builds | Use `checked_add` and return a typed internal error, or use a collision-safe generation strategy | changed line | not-fixed (rebuttal: next_generation is u64; at one registration per microsecond overflow is ~584,000 years away) | 3 |
| 290 | src-tauri/src/content_server/manager.rs | 177 | High | Refactoring Debt | `register_or_existing` exceeds 30 lines, nests lifecycle, trust, ownership, replacement, and cleanup decisions four levels deep, and has high cyclomatic complexity | Compute a small transition enum under the lock, then handle each transition in separate cleanup/response helpers | changed line | not-fixed (rebuttal: it already has the shape the finding proposes — the transition is decided under the lock and the loser's cleanup runs after release; the nesting is one if/else-if/else over three named outcomes) | 3 |
| 291 | src-tauri/src/content_server/manager.rs | 186 | High | Logic & Correctness | After the registration lock is released, an opposite-trust concurrent start can replace and kill the newly registered child before its caller constructs the handle; the first command can return a dead port and monitor the replacement’s generation | Serialize the complete start operation per workspace and add a barrier-driven concurrent-start test | changed line | fixed | 1 |
| 292 | src-tauri/src/content_server/manager.rs | 223 | High | Logic & Correctness | Cleanup failure for a losing or displaced child is discarded; during a trust downgrade the old relaxed-CSP server can remain alive while registration still reports success | Propagate `CleanupOutcome` through `RegisterOutcome` and do not report a completed trust transition until the displaced child is confirmed dead | changed line | not-fixed (rebuttal: cleanup.rs's header states the policy — best-effort paths log and move on, the explicit stop refuses success over it; the failure is logged twice and the handle retained for the quit retry, and nothing addresses the displaced child's port after the swap) | 1 |
| 293 | src-tauri/src/content_server/manager.rs | 223 | Medium | Logic & Correctness | Registration performs synchronous kill-and-wait cleanup before returning to its async caller, which can block an executor worker despite releasing the manager mutex | Return the detached loser to the async lifecycle layer and perform bounded teardown via `spawn_blocking` | changed line | not-fixed (rebuttal: same path as #282 — register_or_existing releases the lock and then runs the one prompt teardown) | 2 |
| 294 | src-tauri/src/content_server/manager.rs | 224 | High | Logic & Correctness | Orphan handoff races with `shutdown_all`: shutdown can set `shutting_down` and drain the orphan vector before `retain_orphan` pushes this child, leaving it unowned when the process exits | Make orphan admission shutdown-aware and atomically choose between queueing and immediate final cleanup | changed line | fixed | 1 |
| 295 | src-tauri/src/content_server/manager_poll.rs | 29 | Medium | Refactoring Debt | poll_current_child is over 30 lines and nests registry, child, try_wait, and result matches four levels deep, obscuring lifecycle transitions | Extract child polling and registry-removal decisions into small typed helpers | new file | not-fixed (rebuttal: 30 lines with the failure path already extracted as handle_poll_failure; the nesting IS the decision — generation match, child present, try_wait — each arm 1-3 lines and separately tested) | 3 |
| 296 | src-tauri/src/content_server/manager_poll.rs | 35 | Medium | Shortcuts & Patches | Production code treats a missing child handle as Running solely to support metadata-only tests, allowing an impossible production state to masquerade as healthy forever | Make Managed.child non-optional in production and use a test-specific abstraction or fixture | new file | not-fixed (out of reach: Managed.child is declared and constructed in content_server/manager.rs, another fixer's file; making it non-optional plus a test fixture is a change there) | 3 |
| 297 | src-tauri/src/content_server/manager_poll.rs | 56 | High | Logic & Correctness | Cleanup occurs after deregistration and lock release, so a concurrent replacement can register the same root and port-file path before the detached cleanup unlinks it, deleting the new server's port file | Serialize per-root cleanup and registration, or make port-file ownership generation-aware with atomic conditional removal | new file | not-fixed (rebuttal: same mechanism as #271 — the record removed owned the path at registration and the port file has no reader after await_port returns) | 1 |
| 298 | src-tauri/src/content_server/manager_poll.rs | 56 | High | Logic & Correctness | The cleanup outcome is discarded; after repeated poll errors, a failed kill/reap can leave the child alive as an orphan while the caller is told Exited(None) | Add an Unmanageable/Unknown state and propagate cleanup failure instead of reporting a confirmed exit | new file | not-fixed (rebuttal: the child-alive-but-unpollable premise has no mechanism — Child::try_wait fails only with ECHILD (child gone) or EINVAL (impossible for a live handle); the Exited(None) verdict is documented and the handle is retained as an orphan) | 1 |
| 299 | src-tauri/src/content_server/manager_poll.rs | 78 | Medium | Logic & Correctness | User-controlled workspace roots and error details are interpolated unescaped, so Unix paths containing newlines can forge additional log entries | Emit structured fields or debug-quote and escape root and detail | new file | not-fixed (rebuttal: the workspace root is a directory the user picked in their own folder picker, written to their own log; the crate interpolates roots unquoted at dozens of sites, so escaping two would be inconsistent cosmetics) | 2 |
| 300 | src-tauri/src/content_server/manager_teardown.rs | 31 | Medium | Logic & Correctness | The orphan warning interpolates the workspace root without escaping, permitting newline-based log forgery | Use structured logging or escaped/debug formatting for root | new file | not-fixed (rebuttal: as #299) | 2 |
| 301 | src-tauri/src/content_server/manager_teardown.rs | 34 | High | Logic & Correctness | retain_orphan can push an orphan after shutdown_all has set shutting_down and drained the orphan list, allowing a teardown racing application exit to leave a child behind permanently | Track in-flight teardowns and make shutdown wait for their retention decisions before its final orphan drain | new file | fixed | 1 |
| 302 | src-tauri/src/content_server/manager_teardown.rs | 62 | High | Logic & Correctness | shutdown_all performs synchronous, sequential kill-and-wait cleanup without a deadline; one child whose wait never completes blocks cleanup of every later child and can hang the critical quit path | Use bounded termination and ensure each child cannot prevent the remaining shutdown work | new file | not-fixed (rebuttal: the only blocking wait() follows a DELIVERED SIGKILL, which is uncatchable and reaps promptly; a REFUSED kill is followed by non-blocking try_wait — cleanup.rs's header records this as the #122 reason) | 1 |
| 303 | src-tauri/src/content_server/mod.rs | 17 | High | Shortcuts & Patches | The module explicitly leaves the bundled CLI unset while the provisioning path has no production caller, so a normal packaged installation has no operational content-server runtime | Wire one runtime source end-to-end before release, or gate/remove the unavailable feature from packaged builds | changed line | not-fixed (out of reach: whether a packaged build ships a content-server runtime is a release/product decision — BUNDLED_CLI_RESOURCE is None deliberately, stated in mod.rs and spawn.rs — needing a maintainer ruling and release infrastructure, not a code change) | 3 |
| 304 | src-tauri/src/content_server/mod.rs | 33 | Medium | Dead Code | provision, signature, and swap are admitted production-orphan modules hidden by blanket module-wide allow(dead_code), which also suppresses newly introduced accidental dead code | Feature-gate or remove the dormant upgrade path and scope any unavoidable allowance to individual intentional items | changed line | fixed | 3 |
| 305 | src-tauri/src/content_server/runtime.rs | 133 | Medium | Logic & Correctness | Both runtime probes launch an unbounded blocking resolver; a hung login shell can leave the command pending indefinitely, repeated requests can accumulate blocking tasks, and aborting the observer cannot cancel an already-running blocking task | Give subprocess resolution a timeout and cancellation strategy, and coalesce concurrent probes | new file | not-fixed (rebuttal: login_shell_path() is OnceLock-cached and bounded by a 5s timeout, and the rest of the probe is which plus is_file(); the "unbounded blocking resolver" does not exist) | 2 |
| 306 | src-tauri/src/content_server/runtime.rs | 196 | High | Logic & Correctness | keep_probe unconditionally overwrites every ProbeSlot state, so repeated startup calls detach the previous observer and an immediate shutdown can settle Unstarted before this assignment resurrects the slot as Running | Reserve and transition the slot atomically before spawning, accepting only Unstarted -> Running and rejecting or aborting later installs | new file | not-fixed (rebuttal: log_runtime_state has one caller at setup and settle_probe runs only from quit::finalize_quit; no ordering lets keep_probe overwrite a Settled or Running slot) | 1 |
| 307 | src-tauri/src/content_server/runtime.rs | 213 | High | Logic & Correctness | is_finished is a racy status check rather than observing the JoinHandle result: the observer can log between the check and abort, producing contradictory lines, and a panicked/cancelled observer is classified as Finished | Have the observer record successful logging through the same synchronized state machine, or asynchronously join and inspect its result during settlement | new file | not-fixed (rebuttal: the verdict is a LOG classification made once at quit and both lines the window can produce are true; awaiting the JoinHandle would make the synchronous quit path async for a diagnostic) | 1 |
| 308 | src-tauri/src/content_server/slidev_commands.rs | 57 | High | Logic & Correctness | Preview startup and nonce minting are separate requests against mutable workspace-wide Slidev state; concurrent previews can return a URL for a deck replaced by another request, while mint failure leaves a partially started preview | Make preview startup and nonce creation one server-side atomic operation or serialize preview requests per workspace | changed line | not-fixed (rebuttal: the mutable state is the Node server's workspace-wide Slidev process in server/content/, outside this scope; serializing the two Rust calls would not make the returned URL name the caller's deck, since the deck is decided by the last POST) | 1 |
| 309 | src-tauri/src/content_server/slidev_commands.rs | 75 | Medium | Logic & Correctness | Export treats every 2xx response as success, discards the returned payload, and reports the caller-supplied path even if the server returned a different destination or malformed response | Deserialize and validate the export response, then return its confirmed output path | changed line | fixed | 2 |
| 310 | src-tauri/src/content_server/slidev_commands.rs | 99 | Medium | Shortcuts & Patches | PREVIEW_TIMEOUT is applied independently to preview and nonce requests, so the command can run for nearly 240 seconds despite a nominal 120-second timeout | Apply one end-to-end deadline and pass the remaining duration to each request | changed line | fixed | 3 |
| 311 | src-tauri/src/content_server/spawn.rs | 54 | High | Logic & Correctness | The critical Node-discovery path has no deterministic coverage in the linked test module, including non-zero status, empty output, malformed output, and lookup failure | Inject the lookup command/output and add tests for every result branch | changed line | fixed | 1 |
| 312 | src-tauri/src/content_server/spawn.rs | 55 | Medium | Logic & Correctness | Command::output has no timeout, so a stalled executable lookup or filesystem-backed PATH entry can block server startup indefinitely | Use the existing bounded process-capture abstraction or another kill-and-reap timeout | pre-existing line | fixed | 3 |
| 313 | src-tauri/src/content_server/spawn.rs | 63 | Medium | Logic & Correctness | Node paths are decoded with from_utf8_lossy, while environment paths are also trimmed at line 124; both operations can corrupt valid filesystem paths | Preserve paths as OsString/PathBuf and only use lossless string conversion where Node arguments require it | changed line | not-fixed (rebuttal: spawn.rs names the non-UTF-8 branch as pinned and spawn.test.rs asserts it; the env-override trim is the documented "blank counts as unset" rule) | 2 |
| 314 | src-tauri/src/content_server/spawn.rs | 198 | High | Logic & Correctness | thread::spawn can panic after the child has started; unwinding then drops the Child without terminating it, potentially orphaning the server process | Use thread::Builder::spawn, return failures, and kill/reap the child if either drain thread cannot be created | changed line | fixed | 1 |
| 315 | src-tauri/src/content_server/spawn.rs | 214 | Medium | Logic & Correctness | Splitting long output at an arbitrary byte boundary can divide a valid UTF-8 code point and replace both halves with replacement characters, corrupting Unicode/CJK logs | Carry an incomplete UTF-8 suffix into the next chunk or decode incrementally | changed line | fixed | 2 |
| 316 | src-tauri/src/content_server/start.rs | 109 | High | Logic & Correctness | The critical start lifecycle has no direct test module; adjacent tests cover only start_decision and server_args, leaving spawn, port polling, cleanup, registration, retry, and concurrency behavior untested | Add injected child/filesystem/manager seams and tests for every StartOutcome and race branch | new file | fixed | 1 |
| 317 | src-tauri/src/content_server/start.rs | 109 | Medium | Refactoring Debt | start_once exceeds 30 lines and coordinates ownership, cleanup, registration, supervision, and error mapping in one branch-heavy function | Extract port-wait failure handling and registration finalization into focused helpers | new file | not-fixed (rebuttal: a four-step pipeline whose every error path is already a named helper — spawn_for, await_port, lost_the_port_file, reuse_or_displace — and the ordering the header documents IS the function) | 3 |
| 318 | src-tauri/src/content_server/start.rs | 115 | High | Logic & Correctness | An existing same-trust registration is reused without checking its child; a crash between supervisor polls causes start to return a dead server as Ready | Atomically poll/validate the observed generation before reuse and spawn again when it has exited | new file | fixed | 1 |
| 319 | src-tauri/src/content_server/start.rs | 118 | High | Logic & Correctness | This async lifecycle performs synchronous executable lookup, filesystem operations, process spawning, killing, and waiting on the async runtime thread | Move blocking lifecycle work to spawn_blocking or use asynchronous process/filesystem APIs | new file | not-fixed (rebuttal: the slow part, login_shell_path(), is a process-wide OnceLock already warmed at setup, so what remains synchronous is one which exec plus a few is_file() stats; and the reap follows a delivered SIGKILL) | 1 |
| 320 | src-tauri/src/content_server/start.rs | 183 | High | Logic & Correctness | A trust-changing restart proceeds even when cleanup failed and retained the old server as a still-running orphan, so the previous CSP/trust policy can remain reachable | Abort the trust transition unless the displaced child is confirmed dead, and surface the cleanup failure | new file | not-fixed (rebuttal: same as #292 — logged twice and retained for the quit retry, and after the swap nothing addresses the displaced child's port; refusing the transition would leave the workspace unserved to punish a SIGKILL the OS refused) | 1 |
| 321 | src-tauri/src/content_server/start.rs | 195 | Medium | Logic & Correctness | Every Node/CLI resolution failure is classified as not-found and every spawn failure as internal, misclassifying permission, app-data, resource-resolution, and I/O failures | Return typed resolver errors and map each cause to the appropriate CommandError code | new file | fixed (spawn failures keep the OS class; the resolver half is NOT changed — "missing node/cli is not-found" is argued at start.rs, and typing the two internal cases needs a Result<_, String> refactor across three files for a code nothing branches on) | 2 |
| 322 | src-tauri/src/content_server/start.rs | 201 | Medium | Shortcuts & Patches | Failure to remove a stale port file is ignored, so startup continues toward a misleading timeout instead of reporting the actual filesystem error | Inspect CleanupOutcome and return CommandError::io before spawning when stale-file removal fails | new file | not-fixed (rebuttal: spawn_for deletes nothing — the stale-file removal described was removed deliberately (#277/#323, stated ten lines above the cited line) because clearing the shared path erased a concurrent start's readiness record) | 3 |
| 323 | src-tauri/src/content_server/start.rs | 201 | High | Logic & Correctness | All concurrent attempts use one deterministic port-file path and delete it without verifying its token; one attempt can erase another child’s newly written readiness record, including the deletion at line 264 | Give each attempt a unique port file or conditionally unlink only a record carrying that attempt’s token | new file | fixed | 1 |
| 324 | src-tauri/src/content_server/start.rs | 203 | Low | Logic & Correctness | CLI and port-file PathBuf values are converted with to_string_lossy, so valid non-UTF-8 paths can be changed before reaching Node | Pass OsStr/OsString arguments through the command builder without lossy conversion | new file | not-fixed (out of reach: passing OsStr through needs spawn_server's &[&str] signature in content_server/spawn.rs, another fixer's file) | 3 |
| 325 | src-tauri/src/content_server/start.rs | 225 | High | Logic & Correctness | await_port checks the file before child liveness, so a child that wrote its port and immediately exited is registered and returned as Ready | Check try_wait before accepting readiness and ideally confirm the loopback service responds | new file | not-fixed (rebuttal: a liveness check before accepting cannot establish liveness — the child can die one instruction later — and the supervisor covers a child dying at any moment, deregistering within MONITOR_INTERVAL and emitting content-server:exited) | 1 |
| 326 | src-tauri/src/content_server/start.rs | 226 | Low | Refactoring Debt | Port-file parsing nests four control-flow levels, obscuring the relationship between malformed files, token mismatch, child exit, and retry | Extract a parse_matching_port helper and use guard clauses | new file | fixed | 3 |
| 327 | src-tauri/src/content_server/start.rs | 229 | Low | Logic & Correctness | A matching record with port 0 is accepted even though it cannot identify a usable bound server | Reject zero and any other invalid readiness values as a server-contract error | new file | fixed | 3 |
| 328 | src-tauri/src/content_server/start.rs | 234 | Medium | Logic & Correctness | Child::try_wait errors are silently treated as “still running” until the generic port timeout, losing the real process error and delaying failure by ten seconds | Represent poll errors in PortWait and return a correctly classified CommandError | new file | fixed | 2 |
| 329 | src-tauri/src/content_server/supervisor.rs | 32 | High | Logic & Correctness | Exit events omit the server generation; a delayed event from an old generation can arrive after a replacement is registered and make the frontend restart or penalize the healthy server | Include generation in the event and have consumers ignore events not matching their active generation | new file | not-fixed (rebuttal: supervise calls on_exit only when the generation was CURRENT, so a stale event needs a full replacement between the poll and delivery, and the frontend's response reuses the running server rather than restarting; a wire generation would also change useContentServer.ts, outside this group) | 1 |
| 330 | src-tauri/src/content_server/supervisor.rs | 37 | Medium | Logic & Correctness | code: None conflates signal termination with repeated liveness-poll failure, preventing the frontend from distinguishing a confirmed exit from an unpollable child that may remain alive | Add a typed exit reason and preserve poll-failure details in the event | new file | not-fixed (rebuttal: both paths run the identical teardown through d.cleanup(root) and produce the identical ChildState::Exited, and manager_poll.rs already logs the un-pollable case distinctly; a typed reason on the wire would add a field with no consumer) | 2 |
| 331 | src-tauri/src/content_server/supervisor.rs | 56 | High | Logic & Correctness | thread::spawn can panic after registration, leaving a live server with no supervisor and no recovery notification | Use thread::Builder::spawn returning Result and roll back/clean up the registration if supervision cannot start | new file | fixed | 1 |
| 332 | src-tauri/src/content_server/supervisor.rs | 67 | Medium | Shortcuts & Patches | Emit failure is logged and permanently discarded, so the only signal driving the documented frontend restart policy can be lost | Maintain durable observable crash state or add a bounded retry/fallback notification path | new file | not-fixed (rebuttal: documented at report_exit — the thread has nobody to tell and must not unwind; the crash is also observable without the event, since poll_current_child removes the registration and its port file BEFORE the emit) | 3 |
| 333 | src-tauri/src/content_server/supervisor.rs | 94 | Low | Logic & Correctness | The loop sleeps before its first poll, leaving immediately exited children registered and reported as running for the full monitor interval | Poll immediately, then sleep only after a Running result | new file | fixed | 3 |
| 334 | src-tauri/src/dock_recent.rs | 48 | High | Logic & Correctness | The command returns success once work is queued, even if the file changes before execution or registration is skipped; the real main-thread AppKit path is untested | Await a oneshot result from the main-thread closure, propagate typed failures, and add a macOS integration test | changed line | not-fixed (rebuttal: dock_recent.rs states the decision — awaiting a oneshot from the main thread would let a best-effort Dock nicety stall the IPC thread, and a Recent Documents URL that changed after queueing has no consequence) | 1 |
| 335 | src-tauri/src/dock_recent.rs | 54 | Medium | Logic & Correctness | Every metadata failure is reported as `not-found`, including permission and general I/O errors | Match `ErrorKind` and emit `NotFound`, `PermissionDenied`, or `Io` accurately | changed line | fixed | 2 |
| 336 | src-tauri/src/fs_scope.rs | 43 | High | Logic & Correctness | The asset-protocol grant, required for inline media, has no corresponding assertion in the module’s tests | Test `asset_protocol_scope().is_allowed()` for both single-file and recursive grants | changed line | fixed | 1 |
| 337 | src-tauri/src/fs_scope.rs | 65 | Medium | Logic & Correctness | Scope failures are swallowed, partial grants are retained, and the command returns no indication that access will later fail | Return `Result<(), CommandError>` with per-scope failure details and handle partial success explicitly | changed line | not-fixed (rebuttal: fs_scope.rs already carries BOTH forms — best-effort allow_fs_read* and asserting grant_fs_read, whose verdict is is_allowed; the workspace grant's best-effort posture is argued in the header) | 2 |
| 338 | src-tauri/src/fs_scope.rs | 65 | High | Logic & Correctness | The recursive-grant behavior exists specifically for Windows drive-letter paths, but all runtime scope tests are excluded on Windows | Add a native Windows integration test or a platform-level scope/path-matching test | changed line | not-fixed (out of reach: needs a native Windows integration test — tauri::test::MockRuntime does not exist there and this crate only cross-compiles for Windows) | 1 |
| 339 | src-tauri/src/fs_scope.rs | 86 | High | Logic & Correctness | Any invoker can grant recursive filesystem and asset-protocol access to an arbitrary frontend-supplied path, bypassing the static scope boundary | Require a backend-validated workspace/user-selection capability before expanding scopes | changed line | not-fixed (rebuttal: a caller that can invoke allow_workspace_access already runs at the app's privilege — the trust-boundary verdict in rule 60 §12; the picker/recents/restore funnel is frontend-side, so a backend-issued capability cannot exist without a redesign) | 1 |
| 340 | src-tauri/src/genies/commands.rs | 59 | Medium | Logic & Correctness | An inaccessible directory, failed directory entry, or bounded scan truncation is silently converted into a successful empty/partial list | Make scanning return a typed result including I/O and truncation status, then propagate it | changed line | fixed | 2 |
| 341 | src-tauri/src/genies/commands.rs | 63 | Medium | Logic & Correctness | Sorting only by `name` leaves equal-name entries in randomized `HashMap` iteration order | Add deterministic category and path tie-breakers | changed line | fixed | 2 |
| 342 | src-tauri/src/genies/commands.rs | 87 | Medium | Refactoring Debt | `read_genie_in` exceeds 30 lines and combines path resolution, authorization, bounded I/O, decoding, format selection, and parsing | Extract focused path-validation, loading, and parsing helpers | changed line | not-fixed (rebuttal: ~55 lines, mostly the numbered rationale per refusal; the ordering — canonicalize, containment, open-and-read through ONE handle, format from the canonical target — is the TOCTOU guarantee) | 3 |
| 343 | src-tauri/src/genies/commands.rs | 89 | High | Logic & Correctness | Containment is checked by canonicalizing a path before reopening it; replacing the canonical target or an ancestor with a symlink can race the check and read outside the genies directory | Open relative to a secured directory handle with no-follow/beneath semantics | changed line | not-fixed (rebuttal: read_genie_in canonicalizes and then opens the CANONICAL path, so every static symlink is caught; the residual needs an attacker executing concurrently inside the user's own app-data genies/, who can read the target directly) | 1 |
| 344 | src-tauri/src/genies/commands.rs | 89 | Medium | Logic & Correctness | All canonicalization failures are labeled `not-found`, while a file disappearing during the subsequent open becomes `io`, contradicting the documented error contract | Centralize filesystem-error mapping and classify each `ErrorKind` consistently | changed line | fixed | 2 |
| 345 | src-tauri/src/genies/commands.rs | 132 | Medium | Logic & Correctness | Every extension other than YAML defaults to the Markdown parser, so `.txt` and other unlisted files can be accepted as genies | Accept only `.md`, `.yml`, and `.yaml`; reject every other extension | changed line | fixed | 2 |
| 346 | src-tauri/src/genies/commands.rs | 146 | Medium | Logic & Correctness | Any syntactically valid YAML — including scalars and sequences — is labeled a workflow genie without structural validation | Require a mapping and validate required workflow fields with the production workflow validator | changed line | fixed | 2 |
| 347 | src-tauri/src/genies/install.rs | 148 | High | Logic & Correctness | The parent is canonicalized and checked, then reopened by pathname; a concurrent symlink swap can redirect temporary-file creation and publication outside the base | Retain secured directory handles and publish relative to them with no-follow semantics | changed line | not-fixed (rebuttal: same privilege boundary, and the content written is a bundled public genie template — redirecting the write leaks nothing an attacker at that privilege lacks) | 1 |
| 348 | src-tauri/src/genies/install.rs | 159 | Medium | Logic & Correctness | `symlink_metadata` is called once as `is_ok()` and again in `already_installed`, losing non-`NotFound` errors and creating a disappearance/type-change race | Use one exhaustive metadata match and publish only on `ErrorKind::NotFound` | changed line | fixed | 2 |
| 349 | src-tauri/src/genies/install.rs | 185 | Medium | Shortcuts & Patches | Any regular file counts as installed without checking readability or validity, so legacy truncated, corrupt, or unreadable defaults remain silently broken forever | Validate existing files and surface corruption; use version/checksum metadata to repair known bundled corruption without overwriting genuine edits | changed line | not-fixed (rebuttal: genies are user-editable, so content divergence from the bundle is indistinguishable from a genuine edit without version metadata the format does not carry; the truncation source was removed at the mechanism level by #153's no-clobber publish) | 3 |
| 350 | src-tauri/src/genies/scanning.rs | 52 | High | Refactoring Debt | walk_bounded spans 46 lines and combines traversal, limits, error policy, logging, classification, and dispatch with deep branching | Extract entry processing and return a typed traversal outcome to reduce complexity | changed line | not-fixed (rebuttal: one bounded worklist loop; every branch — symlink, depth, entry cap, non-UTF-8, non-regular — is documented in place and observable through the truncated flag scanning.test.rs asserts on) | 3 |
| 351 | src-tauri/src/genies/scanning.rs | 62 | Medium | Shortcuts & Patches | read_dir failures and per-entry errors are silently discarded, making permission or transient I/O failures indistinguishable from an empty directory | Return a structured scan outcome or Result and preserve/log encountered I/O errors | changed line | fixed | 3 |
| 352 | src-tauri/src/genies/scanning.rs | 91 | High | Logic & Correctness | Any non-directory, non-symlink node with a supported extension is accepted, including FIFOs, sockets, and devices; opening a listed FIFO later can block indefinitely | Require ft.is_file() before classification and test FIFO/socket rejection on Unix | changed line | fixed | 1 |
| 353 | src-tauri/src/genies/scanning.rs | 116 | Medium | Logic & Correctness | char::is_control does not remove Unicode bidi formatting characters such as RTL overrides, so filenames can still render misleading menu labels; sanitization can also produce an empty label | Reject bidi-format characters and skip or replace names that become empty | changed line | fixed | 2 |
| 354 | src-tauri/src/genies/scanning.rs | 128 | Medium | Logic & Correctness | Both production callers here and at line 153 discard the truncation boolean, so callers receive incomplete results without knowing the scan hit a depth or entry limit | Propagate a ScanOutcome containing entries and truncation/error status | changed line | not-fixed (rebuttal: every reason the walk is incomplete is now LOGGED where it happens, so "callers receive incomplete results without knowing" is false for the log; surfacing it to the picker is a list_genies wire-shape change owned by the src/ tier) | 2 |
| 355 | src-tauri/src/genies/scanning.rs | 131 | Medium | Logic & Correctness | to_string_lossy is used for keys, categories, and executable paths; distinct non-UTF-8 Unix paths can collapse to the same replacement string and overwrite each other, while the emitted path cannot reopen the original file | Reject and report non-UTF-8 paths before insertion, or preserve an opaque filesystem-safe identifier | changed line | fixed | 2 |
| 356 | src-tauri/src/genies/scanning.rs | 160 | Low | Logic & Correctness | Sorting only by title leaves equal-title entries in arbitrary filesystem traversal order | Add deterministic category and path tie-breakers | changed line | not-fixed (rebuttal: scan_genies_with_titles already breaks a title tie on path, audit #341) | 3 |
| 357 | src-tauri/src/lib.rs | 109 | High | Logic & Correctness | The critical startup composition and conditional plugin/state wiring have no attached construction test; lib.test.rs only exercises three re-exports | Extract a builder-construction function and add supported-platform MockRuntime/startup-smoke assertions for managed state, plugins, protocol, and handlers | changed line | fixed | 1 |
| 358 | src-tauri/src/lib.rs | 109 | Medium | Refactoring Debt | run is roughly 178 lines and owns platform policy, plugin registration, state injection, protocol handling, debug automation, build failure handling, and event-loop startup | Split platform plugins, common plugins/state, trusted protocol, and debug automation into focused builder helpers | changed line | fixed | 3 |
| 359 | src-tauri/src/lib.rs | 137 | Medium | Logic & Correctness | When the Linux D-Bus gate is false, the app starts with no single-instance protection, reintroducing concurrent processes sharing mutable profile/session data | Add a filesystem-lock fallback or fail the second launch without relying exclusively on D-Bus | changed line | not-fixed (out of reach: a second single-instance IPC channel on the one platform this repo neither builds nor runs; a lock that merely refuses the second launch would DROP the file the user double-clicked) | 2 |
| 360 | src-tauri/src/lib.rs | 258 | Medium | Logic & Correctness | The automation-port probe releases port 9323 before the plugin binds it, so another process can win the race and the plugin can silently slide to another port despite the stated fixed-port guarantee | Use a strict-bind plugin API, transfer an already-bound listener, or verify the plugin's actual bound port and abort on mismatch | pre-existing line | not-fixed (rebuttal: automation_port.rs states the residual explicitly and the vendored tauri-plugin-mcp-bridge 0.12.0 exposes neither a fail-closed bind nor its bound port — it only logs it) | 3 |
| 361 | src-tauri/src/mcp_bridge/accept_loop.rs | 37 | Medium | Refactoring Debt | accept_loop exceeds 30 lines and mixes cancellation, acceptance, retry policy, logging, admission, and cleanup | Extract retry/backoff handling into a small state object or helper | new file | not-fixed (rebuttal: the retry arm's correctness is re-arming the SAME &mut shutdown inside the backoff (#362); a backoff state object would either take the receiver or lose the re-arm) | 3 |
| 362 | src-tauri/src/mcp_bridge/accept_loop.rs | 45 | Medium | Logic & Correctness | tokio::select! is unbiased, so if shutdown and accept are simultaneously ready, a new connection may be admitted after shutdown was signaled | Add biased; with shutdown first or explicitly recheck shutdown before admission | new file | fixed | 2 |
| 363 | src-tauri/src/mcp_bridge/accept_loop.rs | 56 | High | Logic & Correctness | The persistent accept-error path is critical recovery logic but the attached tests never produce an accept error, so retry counting, threshold exit, backoff, and on_exit behavior are unverified | Inject an accept operation or listener abstraction and test transient errors, 30 consecutive errors, counter reset, and shutdown during backoff | new file | fixed | 1 |
| 364 | src-tauri/src/mcp_bridge/accept_loop.rs | 70 | Medium | Logic & Correctness | Error backoff is awaited outside the select, preventing shutdown from being observed for up to one second | Select between shutdown and the backoff timer | new file | not-fixed (rebuttal: the backoff already re-arms the shutdown arm — accept_loop.rs says so; the finding describes a version that no longer exists) | 2 |
| 365 | src-tauri/src/mcp_bridge/accept_loop.rs | 80 | Medium | Logic & Correctness | on_exit is not guaranteed “on every exit path”: dropping/aborting the future or a panic in admit bypasses this line and can leave lifecycle state marked active | Install an RAII exit guard before entering the loop | new file | not-fixed (rebuttal: ExitGuard is exactly the RAII guard asked for, declared FIRST so it drops last, documented at accept_loop.rs) | 2 |
| 366 | src-tauri/src/mcp_bridge/bind.rs | 31 | Medium | Shortcuts & Patches | Both failure paths create raw English CommandError values without an i18n key or structured stage/address detail, weakening the typed error contract at the command boundary | Use localized_error! and attach machine-readable bind/local_addr context with with_detail | new file | fixed | 3 |
| 367 | src-tauri/src/mcp_bridge/lifecycle.rs | 29 | Medium | Logic & Correctness | Independent `running` and `port` fields permit invalid states; `publish` and `abort_start` can transition without checking the current phase or generation, while `snapshot` silently masks `(false, Some(_))` as stopped | Replace the fields with a generation-carrying `Stopped`/`Starting`/`Running` enum and validate each transition | new file | not-fixed (rebuttal: every writer of running/port runs under BridgeLifecycle::serialize including loop_exit_handler, so (false, Some(_)) is unreachable; abort_start and on_loop_exit are already generation-guarded) | 2 |
| 368 | src-tauri/src/mcp_bridge/lifecycle.rs | 91 | Low | Logic & Correctness | Both generation increments use unchecked `+= 1`; overflow panics in debug builds and wraps in release, potentially making a stale generation current again | Use checked increment with an explicit terminal error, or a non-repeating epoch strategy | new file | not-fixed (rebuttal: u64 monotonic counters, so 2^64 starts is unreachable — the same reasoning rule 50 §10 already applies to NEXT_REQUEST_ID) | 3 |
| 369 | src-tauri/src/mcp_bridge/lifecycle.rs | 123 | High | Logic & Correctness | A current accept-loop exit only clears lifecycle bookkeeping; it does not invalidate admissions, drain authenticated clients or pending requests, or clear the shutdown slot, so an unexpected listener failure can report `Stopped` while old clients remain operational; this critical path is untested | Route current-loop exit through one idempotent full-teardown operation and add a test with active clients and pending requests | new file | fixed | 1 |
| 370 | src-tauri/src/mcp_bridge/managed.rs | 48 | High | Duplication | `connection_generation` duplicates the lifecycle generation as a second bridge-epoch source of truth; the counters advance on different paths and can drift, notably when the accept loop exits unexpectedly | Use one shared bridge epoch for listener ownership and connection admission, updated by a single lifecycle transition | pre-existing line | not-fixed (rebuttal: rule 50 §10 settles this symbol, and they are not interchangeable — bump_connection_generation runs UNDER the tables lock try_register_client re-checks, while mark_stopped takes only the lifecycle mutex, so merging loses the atomicity the admission refusal depends on) | 3 |
| 371 | src-tauri/src/mcp_bridge/managed.rs | 139 | Medium | Logic & Correctness | `HashMap::insert` silently replaces an existing client with the same ID; the replaced connection’s cleanup can then remove the replacement, leaving the new connection untracked | Use `Entry::Vacant`, reject duplicate IDs, and make ID allocation overflow-safe | pre-existing line | not-fixed (rebuttal: ids come from BridgeState::next_client_id, a u64 bumped under the same lock that inserts, so a duplicate needs 2^64 connections in one process) | 3 |
| 372 | src-tauri/src/mcp_bridge/server.rs | 23 | Medium | Refactoring Debt | The mutable request-ID counter remains process-global despite `McpBridgeState` claiming ownership of bridge runtime state, weakening per-app test isolation and lifecycle encapsulation | Move the counter into `McpBridgeState` and expose a collision-safe allocator | pre-existing line | not-fixed (rebuttal: rule 50 §10 names this exact symbol as a sanctioned static, "monotonic id counters"; it moved with its function into routed_request.rs and the doc now states the reason) | 3 |
| 373 | src-tauri/src/mcp_bridge/server.rs | 46 | Low | Refactoring Debt | `stop_bridge` exceeds 30 lines and mixes discovery cleanup, loop signaling, admission invalidation, client shutdown, and pending-request rejection | Extract narrowly named teardown helpers while preserving the generation-before-drain ordering | changed line | not-fixed (rebuttal: 15 statements, and the generation-before-drain ordering under one lock is the whole function) | 3 |
| 374 | src-tauri/src/mcp_bridge/server.rs | 48 | Medium | Shortcuts & Patches | Port-file removal is best-effort and `stop_bridge` cannot report failure, so callers report a successful stop while a stale discovery file may remain | Return `Result<(), CommandError>` from teardown and propagate non-`NotFound` removal failures | changed line | not-fixed (out of reach: remove_port_file is infallible by signature in mcp_bridge/token_file.rs, another fixer's file; making the teardown fallible also changes the app-exit path in lib.rs) | 3 |
| 375 | src-tauri/src/mcp_bridge/server.rs | 114 | Medium | Logic & Correctness | Identification silently discards malformed payloads, accepts unvalidated display strings, and emits `clients-changed` even when the client was absent or its identity did not change | Validate and bound identity fields, log/reply on malformed input, and emit only after an actual state change | pre-existing line | fixed | 3 |
| 376 | src-tauri/src/mcp_bridge/server.rs | 163 | High | Refactoring Debt | `handle_message` spans 232 lines with protocol parsing, authentication-state lookup, native dispatch, write serialization, pending registration, routing, timeout recovery, and delivery, producing high cyclomatic complexity | Split it into typed envelope dispatch and dedicated identify, native-request, routed-request, registration, and response-wait stages | pre-existing line | fixed | 3 |
| 377 | src-tauri/src/mcp_bridge/server.rs | 176 | Medium | Shortcuts & Patches | The privacy comment says message text is never logged, but the code logs complete client-controlled message types and IDs; these can contain secrets, newlines, or arbitrarily misleading log content | Log bounded escaped values or hashes, and enforce protocol-level length and character constraints | changed line | fixed | 3 |
| 378 | src-tauri/src/mcp_bridge/server.rs | 191 | High | Duplication | The client-sender lookup is copied almost verbatim again at lines 213–218, creating two subtly different missing-client policies | Extract one lookup helper or perform one lookup before dispatching all non-identify envelopes | pre-existing line | fixed | 3 |
| 379 | src-tauri/src/mcp_bridge/server.rs | 284 | High | Logic & Correctness | A stop can drain clients after the sender is cloned at lines 213–218 but before pending registration; this handler then recreates pending state and emits the old request after shutdown, potentially executing a document mutation; existing stop tests do not exercise this critical interleaving | Capture the admission epoch with the client and atomically revalidate both epoch and membership during registration, with cancellation-aware dispatch and a deterministic race test | changed line | fixed | 1 |
| 380 | src-tauri/src/mcp_bridge/server.rs | 336 | High | Duplication | The 10-second request timeout is duplicated in `wake_retry.rs`, while logs separately hardcode the derived 20-second total; policy changes can therefore desynchronize behavior and diagnostics | Define shared timeout constants or a request-timeout policy used by both attempts and their messages | pre-existing line | fixed | 3 |
| 381 | src-tauri/src/mcp_bridge/server.rs | 398 | Medium | Refactoring Debt | The file is 398 lines, materially exceeding the project’s approximately 300-line limit despite already being described as split | Move identification/parsing and routed-request orchestration into focused modules | pre-existing line | fixed | 3 |
| 382 | src-tauri/src/mcp_bridge/start.rs | 72 | Low | Refactoring Debt | `start_bridge_with` spans 48 lines and mixes cancellable preparation with irreversible publication and task startup, making rollback invariants difficult to maintain | Separate cancellable preparation from a no-await commit phase and an accept-loop runner | new file | not-fixed (rebuttal: the header argues the exact alternative proposed and rejects it — "the awaits that follow it are covered by a rollback guard rather than by reordering (audit #383)", and PublishedPort is that guard) | 3 |
| 383 | src-tauri/src/mcp_bridge/start.rs | 84 | High | Logic & Correctness | The discovery token is published before awaited refresh and shutdown-slot operations; cancellation after publication drops the listener but leaves a valid-looking stale `port:token` file | Complete awaited preparation before publication and add a rollback guard that removes the file unless startup commits | new file | fixed | 1 |
| 384 | src-tauri/src/mcp_bridge/start.rs | 88 | High | Logic & Correctness | A panicked refresh task is merely logged and startup continues with the previous credential snapshot, potentially retaining a revoked client principal | Treat `JoinError` as startup failure or explicitly replace the registry with a fail-closed empty snapshot | new file | fixed | 1 |
| 385 | src-tauri/src/mcp_bridge/start.rs | 106 | High | Logic & Correctness | The accept-loop `JoinHandle` is discarded, and `spawn_logged` catches panics outside `accept_loop`; a panic therefore bypasses `on_exit`, leaving lifecycle state and the token file marked live | Store the handle and wrap the loop so exit cleanup runs after normal completion or unwind | new file | fixed | 1 |
| 386 | src-tauri/src/mcp_bridge/token_file.rs | 79 | Low | Shortcuts & Patches | `remove_port_file` returns nothing and reduces cleanup failures to logs, so callers report a clean stop even when the discovery credential remains on disk | Return a typed cleanup result while still completing listener shutdown | changed line | not-fixed (out of reach: plumbing a result needs stop_bridge (returns ()) and mcp_bridge_stop, both another fixer's files this round; the residual is bounded — the leftover file names a closed port behind a bumped connection generation, and the next start replaces it atomically) | 3 |
| 387 | src-tauri/src/mcp_bridge/token_file.rs | 109 | Low | Refactoring Debt | `write_secured` exceeds 30 lines and combines staging, durability, permission enforcement, and platform-specific replacement | Extract a platform replacement helper and a staged-file preparation helper | pre-existing line | fixed | 3 |
| 388 | src-tauri/src/mcp_bridge/token_file.rs | 110 | Medium | Logic & Correctness | The secret is written and synced before the explicit mode enforcement at line 123, so staging-time confidentiality still depends on `NamedTempFile` defaults — the dependency the module claims to remove | Enforce and verify permissions immediately after creation, before writing, then verify again before publication | pre-existing line | fixed | 3 |
| 389 | src-tauri/src/mcp_bridge/token_file.rs | 135 | Medium | Logic & Correctness | On Windows every initial persist failure triggers deletion of the existing target and retry, regardless of error kind, breaking the documented atomic guarantee and potentially turning a transient error into data loss | Retry only the specific collision error or use an atomic Windows replacement API while preserving the original failure | pre-existing line | fixed | 3 |
| 390 | src-tauri/src/mcp_bridge/token_file.rs | 185 | High | Logic & Correctness | Unix verification checks only `0o777` mode bits; extended ACL grants, notably macOS ACL entries, can permit another principal while this check reports the token protected | Remove or inspect extended ACLs and verify the effective owner-only access policy | pre-existing line | not-fixed (out of reach: reading macOS ACLs needs acl_get_file, which the libc crate does not bind — raw FFI or a new dependency, and chmod cannot strip an inherited ACE) | 1 |
| 391 | src-tauri/src/mcp_bridge/token_file.rs | 199 | High | Logic & Correctness | Windows permission enforcement and verification are unconditional no-ops, leaving a full-authority token dependent on whatever inherited DACL happens to exist | Create the directory and file with an owner-only DACL and verify the resulting ACL before publication | pre-existing line | not-fixed (out of reach: needs SetNamedSecurityInfoW and a Windows DACL round-trip; nothing here can RUN a Windows test, and untested security code in a token file is worse than a stated gap) | 1 |
| 392 | src-tauri/src/mcp_server.rs | 90 | High | Logic & Correctness | The lifecycle claim is manually aborted only when `start_bridge` returns `Err`; cancellation or panic during the await leaves the bridge permanently in `Starting` | Use an RAII start-claim guard that aborts on drop until explicitly committed | changed line | fixed | 1 |
| 393 | src-tauri/src/mcp_server.rs | 117 | High | Logic & Correctness | Unexpected accept-loop exit only clears lifecycle state and removes the port file; authenticated clients and pending requests remain active, and no stopped event reaches the frontend | Run generation-guarded full teardown on unexpected exit, including client/pending drainage and status notification | changed line | fixed | 1 |
| 394 | src-tauri/src/mcp_server.rs | 149 | High | Logic & Correctness | Shutdown reports `Stopped` without awaiting accept-loop termination; the old loop can accept after the connection-generation bump, capture the new generation, and register after the drain | Store and await the accept-loop handle before bumping/draining, or close admission atomically before signaling | changed line | fixed | 1 |
| 395 | src-tauri/src/mcp_server.rs | 168 | Medium | Logic & Correctness | The sidecar health process has no timeout and `.output()` buffers unbounded stdout/stderr, so a wedged or noisy sidecar can hang the command or exhaust memory | Apply a deadline, kill the child on expiry, and cap streamed output | changed line | fixed | 2 |
| 396 | src-tauri/src/mcp_server.rs | 217 | High | Shortcuts & Patches | The file’s tests cover only `BridgePhase` projection; critical command orchestration, cancellation, unexpected exit, and stop/admission races are untested | Add mock-runtime command tests with controlled cancellation and accept-loop race hooks | changed line | fixed | 3 |
| 397 | src-tauri/src/pdf_export/commands.rs | 24 | Low | Refactoring Debt | `validate_output_path` spans 52 lines and mixes lexical validation, filesystem inspection, and error translation | Extract extension, absolute-path, destination, and parent validators | pre-existing line | not-fixed (rebuttal: validate_output_path is a linear checklist of five guards, each with its own error code and its own reason at the site; four extracted validators would add four names and four call sites for the same sequence) | 3 |
| 398 | src-tauri/src/pdf_export/commands.rs | 68 | Low | Logic & Correctness | An existing parent that is a regular file is reported with the `dirNotFound` localization key even though it exists | Add a distinct `parentNotDirectory` error key | pre-existing line | fixed | 3 |
| 399 | src-tauri/src/pdf_export/commands.rs | 106 | Low | Refactoring Debt | `export_pdf` spans 58 lines and combines validation, admission, rendering, blocking work, warning construction, and progress publication | Extract cancellation-safe export execution and post-processing helpers | changed line | not-fixed (rebuttal: post_process is already extracted; what remains is six linear steps and the gate slot is bound to a local precisely so every exit releases it) | 3 |
| 400 | src-tauri/src/pdf_export/commands.rs | 146 | High | Logic & Correctness | Dropping the command future releases `ExportSlot`, but an already-running native render or `spawn_blocking` post-process continues detached; another export can then overlap progress or mutate the same output | Use an owned permit retained by every background/native operation until actual settlement, and add cancellation tests | pre-existing line | not-fixed (rebuttal: Tauri spawns command futures detached (respond_async, handle discarded) and nothing wraps export_pdf in a timeout/select!, so the future is not dropped before it returns and the ExportSlot is released only then) | 1 |
| 401 | src-tauri/src/pdf_export/renderer/linux.rs | 63 | High | Shortcuts & Patches | The English `"Print to File"` display name is hardcoded, but GTK can register this virtual printer under a localized name; export can therefore fail with “Printer not found” on non-English Linux desktops. | Enumerate GTK printers and select the available virtual PDF/file printer by capability, returning a typed unsupported error if none exists. | pre-existing line | not-fixed (out of reach: gtk 0.18.2 and gtk-sys 0.18.2 expose no GtkPrinter or gtk_enumerate_printers, so selecting by capability needs hand-written libgtk-3 FFI on the one platform this project can neither run nor compile; residual stated at FILE_PRINTER) | 3 |
| 402 | src-tauri/src/pdf_export/renderer/linux.rs | 79 | Medium | Refactoring Debt | `start` exceeds 30 lines and embeds window navigation, timeout arbitration, GTK configuration, signal wiring, and job launch in one large closure. | Extract page-setup creation and print-operation configuration into focused helpers. | pre-existing line | not-fixed (rebuttal: length only, and Linux is not compilable here — an unverifiable mechanical extraction for a cosmetic gain is a claim, not a fix) | 3 |
| 403 | src-tauri/src/pdf_export/renderer/linux.rs | 121 | High | Logic & Correctness | `PageSpec` values reach GTK without `PageSpec::validate()`; direct callers of public `render_pdf` bypass command-layer validation and can supply NaN, negative, or oversized native geometry. | Validate at the public renderer boundary, with defensive validation here if this backend remains independently callable. | changed line | fixed | 1 |
| 404 | src-tauri/src/pdf_export/renderer/linux.rs | 157 | High | Logic & Correctness | The critical `failed`→`finished` double-signal arbitration and cleanup path has no local test seam or Linux-specific test module. | Extract signal-outcome arbitration into a pure tested component and add a Linux integration test covering failure followed by finished. | changed line | not-fixed (out of reach: a Linux/GTK integration test — nothing here compiles Linux; the arbitration itself is RenderSink::settle's settle-once guarantee, pinned platform-neutrally) | 1 |
| 405 | src-tauri/src/pdf_export/renderer/linux.rs | 181 | Medium | Logic & Correctness | `on_finished()` is evaluated before `settle()` discovers that `failed` already settled the sink; despite the comment, completion-side effects still execute after failure. | Atomically gate callback evaluation on an unsettled sink, or require a side-effect-free completion value and keep progress reporting separately guarded. | changed line | not-fixed (rebuttal: the only side effect in either on_finished is sink.progress(Finishing), and RenderSink::progress gates on the outcome being pending; linux.rs says so and the sink drops it after a failed settle) | 2 |
| 406 | src-tauri/src/pdf_export/renderer/linux.rs | 189 | Medium | Shortcuts & Patches | Window-close failures are silently discarded, allowing a hidden webview or WebKit process leak with no diagnostic evidence. | Log close failures with the window label; propagate them where settlement has not already occurred. | pre-existing line | fixed | 3 |
| 407 | src-tauri/src/pdf_export/renderer/linux_nav.rs | 43 | Medium | Refactoring Debt | `linux_nav` imports shared primitives from `linux`, while `linux` imports `navigate_once`, creating a bidirectional module dependency and placing navigation infrastructure in the export-specific module. | Move the label, URL conversion, close, and window-error helpers into `linux_nav` or a neutral Linux support module. | new file | not-fixed (rebuttal: intra-crate sibling modules referencing each other is not a hazard in Rust — one compilation unit, no initialization order — and the split is the documented file-size answer; the remedy also requires editing another fixer's file) | 3 |
| 408 | src-tauri/src/pdf_export/renderer/linux_nav.rs | 53 | Medium | Refactoring Debt | `navigate_once` exceeds 30 lines and combines window construction, teardown registration, two signal state machines, navigation, and synchronous attachment-error cleanup. | Split window creation, handler attachment, and navigation-state arbitration into focused helpers. | new file | not-fixed (rebuttal: the header enumerates four properties that hold BECAUSE the handler wiring, the two flags and the navigation are in one scope, the bootstrap-Started gate in particular) | 3 |
| 409 | src-tauri/src/pdf_export/renderer/linux_nav.rs | 53 | High | Logic & Correctness | The exactly-once/failure-before-finished lifecycle is critical native asynchronous behavior but has no pure state abstraction or co-located tests, leaving event-order races unprotected. | Extract a navigation state machine and test blank bootstrap, target success, cancellation, failure-then-finished, repeated finished, attachment failure, and timeout teardown sequences. | new file | not-fixed (out of reach: no Linux toolchain here; the concrete ordering defect it points at is fixed under #410) | 1 |
| 410 | src-tauri/src/pdf_export/renderer/linux_nav.rs | 62 | High | Logic & Correctness | The builder initiates an `about:blank` navigation, then line 108 initiates the file navigation, while handlers accept any failure or `Finished`; completion or cancellation from the initial navigation can settle the export incorrectly or run `on_loaded` against the blank page. | Correlate events with the requested file URL/navigation generation and explicitly ignore the bootstrap navigation and its cancellation. | new file | fixed | 1 |
| 411 | src-tauri/src/pdf_export/renderer/linux_nav.rs | 85 | Medium | Shortcuts & Patches | The load-failure handler discards the failing URI, load stage, and GLib error, reducing distinct permission, missing-file, and decoding failures to an undiagnosable generic error. | Capture the callback arguments and attach sanitized structured detail to the typed localized error. | new file | fixed | 3 |
| 412 | src-tauri/src/pdf_export/renderer/linux_print.rs | 68 | Low | Dead Code | `_read_access_dir` is accepted but never used, obscuring whether Linux intentionally ignores the access boundary. | Remove it from this platform helper or document why Linux does not require it. | changed line | fixed | 3 |
| 413 | src-tauri/src/pdf_export/renderer/linux_print.rs | 76 | Medium | Refactoring Debt | `start_print` spans 59 lines and combines navigation callback construction, signal lifecycle, timeout claiming, dialog presentation, and outcome settlement. | Extract the loaded-document/dialog handler into a named function. | changed line | not-fixed (rebuttal: start_print is navigate_once plus one closure whose body is the dialog protocol in the order it must happen — settle_from_signals, claim(), shown(), run_dialog — and the header calls that ordering the contract) | 3 |
| 414 | src-tauri/src/pdf_export/renderer/linux_print.rs | 116 | High | Logic & Correctness | The critical Print/Cancel and `failed`/`finished` lifecycle is coupled directly to concrete GTK objects and has no local test seam; signal-ordering or cleanup regressions cannot be tested deterministically. | Introduce a print-operation adapter and test cancel, success, failure-before-finished, and missing-signal paths. | changed line | not-fixed (out of reach: no Linux toolchain here) | 1 |
| 415 | src-tauri/src/pdf_export/renderer/macos.rs | 7 | Low | Shortcuts & Patches | The module documentation says `configure_print_info` never sets a page size, contradicting lines 240–242. | Update the documentation to describe export-specific paper sizing versus interactive defaults. | pre-existing line | fixed | 3 |
| 416 | src-tauri/src/pdf_export/renderer/macos.rs | 55 | High | Duplication | `did_fail` and `did_fail_provisional` duplicate failure logging and state mutation, allowing the two navigation-failure paths to drift. | Route both delegate callbacks through one `record_failure` helper. | changed line | fixed | 3 |
| 417 | src-tauri/src/pdf_export/renderer/macos.rs | 114 | Medium | Shortcuts & Patches | The off-screen viewport is hardcoded to 800×600 independently of `PageSpec`; responsive CSS, viewport units, and print scaling can therefore vary unexpectedly across paper sizes. | Derive or explicitly normalize the render viewport from the target print geometry and cover it with layout tests. | pre-existing line | not-fixed (rebuttal: the window exists because printOperationWithPrintInfo requires one, documented at create_offscreen_webview; WebKit re-lays out for the print page box set on NSPrintInfo, and changing the frame unmeasured would move every existing export's layout) | 3 |
| 418 | src-tauri/src/pdf_export/renderer/macos.rs | 148 | Medium | Refactoring Debt | `load_html_and_wait` spans 61 lines and combines delegate lifetime, URL construction, navigation, polling, detachment, error mapping, and render settling. | Split navigation setup, deadline waiting, and readiness verification into focused helpers. | changed line | not-fixed (rebuttal: the function's correctness is the delegate's lifetime spanning the whole body — set, poll, detach — and splitting extends that borrow across a boundary or detaches early) | 3 |
| 419 | src-tauri/src/pdf_export/renderer/macos.rs | 205 | High | Logic & Correctness | A successful top-level navigation followed by a fixed 200 ms delay does not guarantee web fonts, images, or asynchronous layout are ready, so valid documents can be printed with fallback fonts or incomplete content. | Wait for an explicit readiness condition such as `document.fonts.ready`, image completion, and animation-frame settlement under a bounded deadline. | changed line | not-fixed (rebuttal: every asset in the export document is an inlined data URI (katexFontEmbed/fontEmbedder), which is why the HTML must reach the webview as a FILE per ADR-PDF4, so no fetch can outlive didFinishNavigation; the 200ms tick is layout settle time and pdf-smoke measures real output on all three backends) | 1 |
| 420 | src-tauri/src/pdf_export/renderer/macos.rs | 226 | High | Logic & Correctness | Silent exports copy mutable global `sharedPrintInfo` and normalize only selected fields; persisted orientation, scaling, or centering can leak into output, including double-orienting dimensions already swapped in `PageSpec`. | Use dedicated export print information or reset every layout-affecting property when `page` is `Some`; retain shared settings only for the interactive dialog. | changed line | fixed | 1 |
| 421 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 31 | Low | Dead Code | `_app` is passed into `render_on_main_thread` but unused. | Remove it from this platform helper or document the interface constraint requiring it. | changed line | fixed | 3 |
| 422 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 35 | Low | Shortcuts & Patches | The comment says `page` is deliberately unused even though it controls `NSPrintInfo` paper size at lines 82 and 118. | Correct the comment to match the implementation. | pre-existing line | fixed | 3 |
| 423 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 50 | Medium | Refactoring Debt | `render_inner` spans 48 lines and mixes thread validation, progress state, native-object creation, loading, timeout claiming, rendering, and completion reporting. | Extract loading/claiming and rendering/progress transitions into separate helpers. | changed line | not-fixed (rebuttal: render_inner is linear and its progress points are the documented three; splitting separates the claim from the print it guards) | 3 |
| 424 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 106 | Medium | Refactoring Debt | `print_to_pdf` spans 51 lines and combines print-info construction, Objective-C dictionary mutation, stale-output handling, operation construction, and job execution. | Introduce a focused save-job configuration helper and keep this function as orchestration. | pre-existing line | not-fixed (rebuttal: print_to_pdf is NSPrintInfo configuration followed by one call, and the save-job configuration the finding proposes extracting is already run_save_job in macos_save_job.rs) | 3 |
| 425 | src-tauri/src/pdf_export/renderer/macos_ops.rs | 155 | High | Logic & Correctness | `run_save_job` may return its 60-second timeout while AppKit is still writing; `render_inner` then drops the off-screen source and settlement deletes the staging file, allowing a late native writer to fail or recreate an orphaned file. | Cancel and await native termination, or transfer the webview, operation, and output cleanup to callback-owned state until the late completion arrives. | changed line | fixed | 1 |
| 426 | src-tauri/src/pdf_export/renderer/macos_print.rs | 71 | High | Duplication | The self-retaining Objective-C delegate lifecycle duplicates `SaveJobDelegate`, including the retain cycle and autoreleased callback release; fixes to this unsafe ownership pattern must be maintained twice. | Centralize the modal-operation delegate lifecycle or share a rigorously tested retain/release helper. | new file | not-fixed (out of reach: the second copy of the self-retain lifecycle is macos_save_job.rs, another fixer's file; a helper applied to one of the two adds indirection without removing the drift) | 3 |
| 427 | src-tauri/src/pdf_export/renderer/macos_print.rs | 147 | Medium | Refactoring Debt | `print_inner` spans 41 lines and combines native setup, parent acquisition, reentrant loading, window-liveness validation, timeout claiming, and presentation. | Extract post-load parent validation and claimed presentation into a separate function. | new file | not-fixed (rebuttal: 15 statements whose ORDER is the documented correctness sequence — resolve+retain parent, load, re-check by identity, claim, present (#218/#428/#227)) | 3 |
| 428 | src-tauri/src/pdf_export/renderer/macos_print.rs | 168 | High | Logic & Correctness | After the nested run loop, the code checks only whether any Tauri window has the same label; if the original closes and a replacement reuses that label, `parent` still points to the old closed `NSWindow` while the check passes. | Re-resolve the native window after loading and compare its identity with the retained parent, or use the newly resolved window. | new file | fixed | 1 |
| 429 | src-tauri/src/pdf_export/renderer/macos_print.rs | 243 | High | Logic & Correctness | An unlabelled call with no key window attaches the sheet to the deliberately hidden render window, making the dialog invisible while `shown()` moves the caller into an unbounded wait. | Use an app-modal panel, make a suitable parent visible, or fail before claiming when no displayable parent exists. | new file | not-fixed (rebuttal: the one production caller always passes Some(window.label()), so choose_parent(false, ...) is reachable only from a direct/harness call; the fallback is deliberate, documented, and pinned) | 1 |
| 430 | src-tauri/src/pdf_export/renderer/macos_print.rs | 260 | High | Logic & Correctness | The actual sheet, cancellation, parent-close, and callback-liveness paths are untested; the local native probe hides the panel and covers only a successful save job, while this production path relies on the callback as its sole settlement and release mechanism. | Add a native integration test that exercises the production presentation function, cancellation, parent closure, and callback absence/cleanup. | new file | not-fixed (out of reach: presenting a real sheet needs a UI session and a human to dismiss it; macos_print.test.rs already runs a real NSPrintOperation with the panel hidden and measures the delegate release) | 1 |
| 431 | src-tauri/src/pdf_export/renderer/macos_save_job.rs | 90 | Low | Refactoring Debt | `run_save_job` spans 54 lines and mixes Objective-C dispatch, run-loop polling, timeout policy, logging, PDF verification, and error mapping. | Extract modal-operation startup and completion waiting/result mapping into focused helpers. | new file | not-fixed (rebuttal: the abandon flag's correctness argument is stated at the site — the callback only ever runs inside run_loop_tick and the loop above has just left one — and it holds only because the poll loop and the flag set are in one scope) | 3 |
| 432 | src-tauri/src/pdf_export/renderer/macos_save_job.rs | 139 | High | Logic & Correctness | The timeout returns while AppKit may still own an active print operation. The self-retained delegate has no late-output cleanup and leaks permanently if the callback never arrives; late native work can also outlive the staging cleanup and offscreen objects. | Cancel the operation and await confirmed termination, or make the retained completion object own the required native lifetimes and delete any output arriving after timeout. | new file | fixed | 1 |
| 433 | src-tauri/src/pdf_export/renderer/macos_save_job.rs | 165 | Medium | Shortcuts & Patches | `unwrap_or(0)` converts every open/read failure — including permission and transient I/O errors — into “missing or empty,” discarding the actual cause. | Handle `File::open` and reading explicitly, preserving the I/O error and its detail in `CommandError`. | new file | fixed | 3 |
| 434 | src-tauri/src/pdf_export/renderer/macos_save_job.rs | 173 | Medium | Logic & Correctness | Any file beginning with `%PDF-` is accepted, including a five-byte or otherwise truncated/corrupt file, despite the function claiming to verify a PDF. | Validate the completed file with a PDF parser or a structural integrity check stronger than the header alone. | new file | fixed | 2 |
| 435 | src-tauri/src/pdf_export/renderer/mod.rs | 218 | High | Logic & Correctness | `print_document` delegates to a wait that becomes unbounded when a backend has claimed the sink but never reaches `shown()` or settlement — for example, if the native presentation call hangs after claiming. This violates the documented “bounded until shown” contract. | Model `presenting` separately from `shown`; allow the deadline to abandon and tear down a claimed-but-not-shown operation, while keeping only genuinely shown dialogs unbounded. | changed line | not-fixed (rebuttal: shown() follows a successful claim() on the same thread in every backend with one native call between, so no path claims and never shows; treating a claim as the dialog being about to appear is the documented #227 decision) | 1 |
| 436 | src-tauri/src/pdf_export/renderer/navigation.rs | 141 | High | Logic & Correctness | Whenever either navigation ID is unavailable, classification falls back to the webview’s current `Source`, even though the file documents that `Source` can already point to the document during a late `about:blank` completion. If `Source` is also unavailable, every successful completion is accepted. Both paths can silently print a blank or unrelated page. | Treat missing identity as an explicit indeterminate failure, or retain another event-specific identity; never classify an unidentifiable successful event as the document. | new file | not-fixed (rebuttal: deliberate and documented — navigation.rs's header says Source is kept only as the fallback for an id unreadable on one side, and with nothing readable the event is taken as the document's so a broken getter fails loud rather than hangs) | 1 |
| 437 | src-tauri/src/pdf_export/renderer/progress.rs | 190 | Medium | Refactoring Debt | `report` is a 35-line state machine combining admission, ordering, drain ownership, and delivery | Extract locked enqueue/admission and queue-draining helpers | new file | not-fixed (rebuttal: the drain-ownership handoff is one lock-discipline invariant, and splitting it moves the decision away from the state it reads; ~28 code lines) | 3 |
| 438 | src-tauri/src/pdf_export/renderer/progress.rs | 228 | High | Logic & Correctness | `close()` does not cancel queued stages; a concurrent drainer can invoke the progress sink after settlement | Serialize closing with dispatch using a queue barrier/finalizer, and add a concurrent close-versus-report test | new file | fixed | 1 |
| 439 | src-tauri/src/pdf_export/renderer/shell.rs | 37 | High | Logic & Correctness | The attached tests cover only `utf8_path`; private-document creation, cancellation cleanup, dispatch failure, and ownership transfer are untested critical paths | Add focused tests for successful creation, write/join failures, cancellation, and `run_on_main_thread` rejection | new file | fixed | 1 |
| 440 | src-tauri/src/pdf_export/renderer/shell.rs | 58 | Medium | Logic & Correctness | `.map_err(err)??` classifies a Tokio `JoinError` such as a task panic as an I/O failure | Map `JoinError` to `ErrorCode::Internal` separately from filesystem errors | new file | fixed | 2 |
| 441 | src-tauri/src/pdf_export/renderer/shell.rs | 103 | Medium | Refactoring Debt | The 43-line `dispatch` function combines path validation, file creation, channel construction, ownership transfer, and event-loop scheduling | Extract preparation and scheduling into independently testable helpers | new file | not-fixed (rebuttal: ~15 statements whose ORDER is the guarantee (#226) — the temp dir is checked before anything is written, and the sink owns the file before the second utf8_path, so a refusal is cleaned by its Drop guard) | 3 |
| 442 | src-tauri/src/pdf_export/renderer/shell.rs | 126 | Medium | Logic & Correctness | After `keep()`, a panic in `build_sink` leaks the private HTML because no cleanup owner exists yet | Retain a cleanup guard until sink construction succeeds, then transfer/disarm it | new file | not-fixed (rebuttal: between keep() and the sink taking ownership there is no fallible step — build_sink is RenderSink::with_progress/for_dialog, an Arc::new — and the one fallible step after it, utf8_path, is already covered by the sink's Drop) | 2 |
| 443 | src-tauri/src/pdf_export/renderer/sink.rs | 176 | High | Logic & Correctness | `claim` drops the outcome mutex before its CAS, allowing `settle` to win between the check and CAS while `claim` still returns true and permits irreversible UI/printing work | Make settlement and phase transition one linearizable operation, preferably under one lifecycle mutex | changed line | fixed | 1 |
| 444 | src-tauri/src/pdf_export/renderer/sink.rs | 192 | High | Logic & Correctness | `abandon` likewise separates its settled check from its CAS; concurrent settlement can deliver success, preserve staging, and then have abandonment incorrectly return `Marked` | Hold the lifecycle lock through the transition or combine sender and phase in one locked enum; add settle-versus-abandon contention tests | changed line | fixed | 1 |
| 445 | src-tauri/src/pdf_export/renderer/sink.rs | 244 | High | Logic & Correctness | A successful oneshot `send` only proves the receiver existed at that instant, not that it consumed the result; cancellation immediately afterward leaves the staging PDF permanently orphaned | Transfer an RAII staging guard through the channel or require an explicit publish acknowledgement before relinquishing cleanup ownership | changed line | not-fixed (rebuttal: same as #400 — the receiver's future is never dropped between the send and the read, and an undelivered Ok already removes the staging file in settle) | 1 |
| 446 | src-tauri/src/pdf_export/renderer/staging.rs | 58 | Medium | Shortcuts & Patches | Every rename failure uses `errors.pdf.staleOutputNotRemoved`, including missing source, permission, and other publish failures unrelated to stale-output removal | Introduce a general publish-failed localization key and preserve the OS detail | new file | fixed | 3 |
| 447 | src-tauri/src/pdf_export/renderer/teardown.rs | 40 | High | Logic & Correctness | A timeout can call `run()` before delayed main-thread window construction calls `arm()`; `run()` forgets the request, so the subsequently armed window can leak indefinitely if navigation never completes | Model teardown as explicit Unarmed/Armed/Run/Disarmed states and immediately execute a closer armed after Run | new file | fixed | 1 |
| 448 | src-tauri/src/pdf_export/renderer/teardown.rs | 41 | Medium | Logic & Correctness | A second `arm()` silently drops the previous closer in release builds because the invariant is enforced only by `debug_assert!`, potentially leaking the first window | Reject duplicate arming in all builds or execute the displaced closer | new file | fixed | 2 |
| 449 | src-tauri/src/pdf_export/renderer/wait.rs | 74 | High | Logic & Correctness | `settle_render` and `await_dialog` are not cancellation-safe; dropping either future bypasses `abandon()` and teardown, allowing background rendering or a ghost dialog after the command is gone | Add an RAII wait guard that applies the appropriate abandonment/teardown policy unless disarmed on normal completion | new file | not-fixed (rebuttal: same as #400 — neither settle_render nor await_dialog can be dropped; were one dropped, every platform settle path closes its own window and settle discards the staging file on an undelivered Ok) | 1 |
| 450 | src-tauri/src/pdf_export/renderer/wait.rs | 75 | High | Logic & Correctness | The success branch calls `publish()`, whose `std::fs::rename` implementation cannot replace an existing destination on Windows, so exporting over an existing PDF fails | Implement platform-correct atomic replacement, such as `MoveFileExW`/`ReplaceFileW`, with a safe nonexistent-target fallback | new file | not-fixed (rebuttal: FALSE PREMISE — Rust's std::fs::rename on Windows is MoveFileExW with MOVEFILE_REPLACE_EXISTING and DOES replace an existing destination; atomic_replace.rs records that a remove-then-retry built on the opposite premise was itself a data-loss defect, audit 20260906 B1) | 1 |
| 451 | src-tauri/src/pdf_export/renderer/wait.rs | 75 | Medium | Logic & Correctness | The advertised end-to-end timeout excludes the synchronous `publish()` operation; a slow or stalled network filesystem can block the async task indefinitely after rendering succeeds | Move publication to controlled blocking work and define an explicit bounded publication policy that preserves late-write safety | new file | fixed | 2 |
| 452 | src-tauri/src/pdf_export/renderer/wait.rs | 92 | Low | Shortcuts & Patches | `bound.as_secs()` truncates fractional bounds, causing sub-second timeouts to report zero seconds; the same defect recurs at line 121 | Round up, report milliseconds, or constrain the API to whole-second bounds | new file | fixed | 3 |
| 453 | src-tauri/src/pdf_export/renderer/wait.rs | 124 | High | Logic & Correctness | A platform that has merely claimed but never shown the dialog is waited on forever, contradicting the stated “bounded until shown” policy and permitting a permanently hung command | Track Claimed and Shown separately; only switch to unbounded waiting after the shown signal is confirmed | new file | not-fixed (rebuttal: same as #435 — no backend claims without reaching shown() on the same thread) | 1 |
| 454 | src-tauri/src/pdf_export/renderer/windows.rs | 98 | High | Duplication | Error settlement and window closing duplicate `RenderWindow::fail`, contradicting the claimed single cleanup path and allowing lifecycle behavior to drift between callers | Make `OnLoaded` return `Result` or expose a shared lifecycle context so `windows_nav` performs all failure cleanup | changed line | fixed | 3 |
| 455 | src-tauri/src/pdf_export/renderer/windows.rs | 107 | High | Logic & Correctness | The critical COM print path — settings, callback outcomes, progress ordering, and cleanup — is untested; the direct test module covers only URL conversion | Extract a mockable WebView2 adapter and test every setup error, callback error, refusal, success, timeout, and cleanup path | pre-existing line | not-fixed (out of reach: no Windows runtime here, and tauri::test's MockRuntime does not exist on Windows at all — Cargo.toml scopes tauri's test feature to cfg(not(target_os = "windows"))) | 1 |
| 456 | src-tauri/src/pdf_export/renderer/windows.rs | 107 | Medium | Refactoring Debt | `print_to_pdf` spans over 50 lines and places settings creation, callback translation, progress, teardown, and dispatch inside one broad unsafe block | Extract settings construction and completion handling, and restrict unsafe blocks to individual COM calls | pre-existing line | fixed | 3 |
| 457 | src-tauri/src/pdf_export/renderer/windows.rs | 143 | Medium | Logic & Correctness | A successful callback emits `Finishing` even after a mid-print timeout because claimed abandonment is not recorded and the progress reporter remains open, producing stale progress after failure | Record waiter abandonment independently of claim state and suppress/close progress when the render wait expires | changed line | fixed | 2 |
| 458 | src-tauri/src/pdf_export/renderer/windows.rs | 169 | Medium | Shortcuts & Patches | Window-close failures are discarded even though cleanup is required to prevent hidden-window and Edge-process leaks | Log the exact label and close error and retain or retry teardown where safe | changed line | fixed | 3 |
| 459 | src-tauri/src/pdf_export/renderer/windows_nav.rs | 54 | Medium | Refactoring Debt | The shared navigation module imports helpers and constants from one of its clients while that client imports `navigate_once`, creating a circular and misleading module dependency | Move shared Windows renderer primitives into `windows_nav` or a dedicated `windows_common` module | new file | not-fixed (out of reach: breaking the cycle needs a windows_common module declared in renderer/mod.rs and imported by windows_print.rs, both outside this group; a re-export through windows.rs would leave windows_print importing primitives from a module that no longer defines them) | 3 |
| 460 | src-tauri/src/pdf_export/renderer/windows_nav.rs | 140 | High | Logic & Correctness | The critical COM wiring — handler registration order, `with_webview` scheduling, event extraction, and terminal cleanup — has no direct test; only the separate pure classifier is tested | Introduce an injectable navigation adapter or Windows integration harness covering registration failures, event ordering, timeout races, and cleanup | new file | not-fixed (out of reach: same as #455; the pure classifier it contrasts with is navigation.rs, already compiled and tested on every platform for exactly this reason) | 1 |
| 461 | src-tauri/src/pdf_export/renderer/windows_nav.rs | 161 | High | Logic & Correctness | The completion handler registered on `core` captures `core.clone()`, while registration tokens are discarded; this creates a COM reference cycle that can retain the webview, handler, and sink after close | Use the callback’s sender instead of capturing `core`, or retain tokens and unregister both handlers on every terminal path | new file | fixed | 1 |
| 462 | src-tauri/src/pdf_export/renderer/windows_nav.rs | 176 | High | Logic & Correctness | Navigation metadata errors are collapsed into `None`; if the document’s starting URI cannot be read, a late `about:blank` completion can be matched using the already-advanced document source and silently print a blank PDF | Propagate getter failures as explicit `CommandError`s or require a positive document identity before acting | new file | not-fixed (rebuttal: same as #436 — the Source fallback for an unreadable id is the documented decision in navigation.rs) | 1 |
| 463 | src-tauri/src/pdf_export/renderer/windows_nav.rs | 187 | Medium | Refactoring Debt | `completion_handler` exceeds 30 lines and combines fallible COM extraction, navigation classification, claim arbitration, error construction, and side effects | Extract a `read_completion` result and a separate terminal-step executor | new file | not-fixed (rebuttal: a match over four documented steps, each 1-3 statements, with the COM reads already extracted; the Option::take one-shot must stay inside the closure that owns it) | 3 |
| 464 | src-tauri/src/pdf_export/renderer/windows_print.rs | 69 | Low | Shortcuts & Patches | The user-visible `"VMark Print"` window title is hardcoded in English despite the localized application surface. | Resolve the title through the existing localization mechanism. | changed line | fixed | 3 |
| 465 | src-tauri/src/pdf_export/renderer/windows_print.rs | 71 | High | Logic & Correctness | The critical Windows-only COM success, failure, and cleanup path has no injectable seam or targeted platform test; shared navigation/sink tests cannot exercise `ShowPrintUI`. | Wrap the COM operation behind an injectable adapter and test success, cast failure, API failure, and close behavior on Windows. | changed line | not-fixed (out of reach: same as #455) | 1 |
| 466 | src-tauri/src/pdf_export/renderer/windows_print.rs | 87 | Medium | Logic & Correctness | `sink.shown()` fires even when the COM cast or `ShowPrintUI` fails, falsely reporting that a dialog appeared. | Call `shown()` only in the success branch; close and settle directly on failure. | changed line | not-fixed (rebuttal: settle() drops the shown sender, so within(bound, shown_rx) returns Some whether shown() fired or the channel closed; no observer can distinguish the two and await_dialog reads the outcome either way) | 2 |
| 467 | src-tauri/src/secure_store.rs | 86 | Low | Shortcuts & Patches | macOS security statuses are copied as raw integers, coupling classification to manually maintained constants. | Import the platform binding constants where available and test classification against those bindings. | changed line | not-fixed (rebuttal: security-framework-sys 2.17.0 exports only 2 of the 4 constants — errSecUserCanceled and errSecInteractionNotAllowed are absent — so importing would leave the table half-bound; OSStatus values are frozen ABI, each already cited to SecBase.h, and a test already pins all four) | 3 |
| 468 | src-tauri/src/secure_store.rs | 158 | High | Logic & Correctness | The critical native credential-store path is tested only with the in-memory mock, leaving real persistence, prompting, platform limits, denial mapping, and concurrent access unverified. | Add opt-in target-specific integration tests using isolated disposable service/account names. | pre-existing line | not-fixed (rebuttal: the real keychain cannot be exercised headlessly — a locked login keychain or an ACL prompt blocks — and an ignored test nobody runs is not coverage; the keyring mock store is the boundary that can be asserted) | 1 |
| 469 | src-tauri/src/secure_store.rs | 184 | Medium | Logic & Correctness | Validation rejects only an empty key; whitespace, control/NUL characters, excessive lengths, and keys outside the intended namespace reach platform backends and become inconsistent `internal` errors. | Define and enforce a portable key grammar and length limit, returning `invalid-input`. | pre-existing line | fixed | 3 |
| 470 | src-tauri/src/secure_store.rs | 192 | High | Logic & Correctness | All three synchronous Tauri commands perform blocking OS credential-store operations inline; a locked/unresponsive keychain, Secret Service, or prompt can block the IPC/UI thread. | Make the commands async and execute keyring calls through `spawn_blocking`. | changed line | fixed | 1 |
| 471 | src-tauri/src/session_bus.rs | 66 | Low | Dead Code | The module-wide off-Linux `allow(dead_code)` suppresses warnings for every current and future orphaned item merely to retain tests. | Compile the module/functions under `cfg(any(target_os = "linux", test))` or narrowly annotate only the test-only entry point. | new file | fixed | 3 |
| 472 | src-tauri/src/session_bus.rs | 83 | Medium | Logic & Correctness | An unset/non-UTF-8 address always disables the guard even though zbus can use the standard `XDG_RUNTIME_DIR/bus` fallback, unnecessarily permitting multiple instances in valid sessions. | Evaluate the exact zbus fallback without I/O and register when it parses safely. | new file | not-fixed (rebuttal: session_bus.rs engages both cases — unset is "not evidence of a bus", and non-UTF-8 is explicitly noted as stricter-than-zbus, the safe direction; reproducing zbus's fallback means rebuilding its /run/user/<uid> construction on a platform this repo cannot run) | 2 |
| 473 | src-tauri/src/session_bus.rs | 103 | High | Duplication | The module reimplements zbus address parsing, option grammar, GUID parsing, and transport selection; correctness depends on remaining exactly synchronized with a third-party parser. | Invoke the pinned zbus parser directly, then apply only VMark’s local-transport policy to the parsed address. | new file | not-fixed (rebuttal: the gate's contract is "stricter than zbus, never laxer" per its header, which delegating to zbus's parser cannot express; and vmark has no zbus dependency, so it would add a Linux-only crate to answer a question needing one-way strictness only) | 3 |
| 474 | src-tauri/src/session_bus.rs | 171 | High | Logic & Correctness | Any syntactically accepted Unix socket enables plugin registration even when the endpoint is stale or is a non-D-Bus listener; downstream connection/authentication may block or fail silently, leaving the destructive multi-instance state without a warning. | Use a plugin/API that exposes setup failure and bounded connection behavior, and test the complete registration path. | new file | not-fixed (rebuttal + out of reach: session_bus.rs already states that a parseable-but-dead address is the plugin's own catch-all and nothing this side can observe; exposing it needs an upstream tauri-plugin-single-instance change) | 1 |
| 475 | src-tauri/src/single_instance.rs | 104 | High | Logic & Correctness | The production plugin callback and its real `route_file_opens` wiring are not exercised end-to-end on Windows or Linux; tests replace the forwarder and Windows mock-runtime coverage is disabled. | Add platform E2E coverage that launches a second process and verifies forwarding, window surfacing, and single-process preservation. | changed line | not-fixed (out of reach: needs a Windows/Linux E2E launching a second process — no Windows mock runtime, and nothing here compiles or runs Linux) | 1 |
| 476 | src-tauri/src/single_instance.rs | 139 | High | Logic & Correctness | Argument filtering performs synchronous `is_file()` metadata I/O from the documented main-event-loop callback; a UNC path, disconnected mount, or slow filesystem can freeze the running app. | Perform only lexical filtering in the callback and move filesystem validation/routing to a worker. | changed line | fixed | 1 |
| 477 | src-tauri/src/single_instance.rs | 147 | Medium | Refactoring Debt | `surface_a_window` exceeds 40 lines and combines state selection, race-prone lookup, creation, native window manipulation, and logging. | Split target selection from idempotent creation/reveal behavior. | changed line | fixed | 3 |
| 478 | src-tauri/src/single_instance.rs | 169 | Medium | Logic & Correctness | Main-window creation is check-then-create; if another path creates `main` concurrently, the loser only logs `WindowLabelAlreadyExists` and does not reveal the winning window. | Treat an existing-label error idempotently by looking up and surfacing the winner. | pre-existing line | fixed | 3 |
| 479 | src-tauri/src/single_instance.rs | 175 | Medium | Logic & Correctness | A selected window can close between the window snapshot and `get_webview_window`; the function then silently returns, making the second launch appear ignored. | Retry selection against a fresh snapshot and create a document window if no fallback remains. | pre-existing line | fixed | 3 |
| 480 | src-tauri/src/single_instance.rs | 180 | High | Duplication | The show/unminimize/focus sequence duplicates the same native-window reveal logic in `finder_open_delivery`, including separate error handling that can drift. | Extract one shared `reveal_document_window` helper and reuse it in both paths. | pre-existing line | fixed | 3 |
| 481 | src-tauri/src/window_manager/commands.rs | 58 | High | Logic & Correctness | `allow_fs_read` suppresses filesystem- and asset-scope failures, so the command can report success while the new window cannot read its file | Add a strict `Result`-returning grant operation and propagate failures as `CommandError`. | changed line | fixed | 1 |
| 482 | src-tauri/src/window_manager/commands.rs | 58 | High | Logic & Correctness | Grants are not rolled back if subsequent window construction fails, leaving access widened after a failed open; this critical failure path is untested | Use a reference-counted scope lease that revokes newly added grants unless window creation succeeds. | changed line | not-fixed (rebuttal: tauri_plugin_fs::Scope is additive-only — there is no revoke, and forbid_file would permanently blacklist a file the user may legitimately open later; the widening also grants nothing the successful path would not have granted) | 1 |
| 483 | src-tauri/src/window_manager/commands.rs | 108 | High | Logic & Correctness | The batch size and aggregate path length are unbounded, while every path is synchronously canonicalized before constructing an arbitrarily large URL | Reject excessive counts/bytes and run filesystem validation on a blocking worker. | changed line | not-fixed (rebuttal: open_workspace_with_files_in_new_window is #[tauri::command(async)], so it already runs on a worker rather than the IPC thread — the audit missed the attribute; the unbounded batch is self-DoS from inside the trust boundary) | 1 |
| 484 | src-tauri/src/window_manager/commands.rs | 129 | Low | Shortcuts & Patches | A frontend-controlled label is interpolated directly into logs, allowing newline/control-character log injection | Log the label using escaped debug formatting or structured fields. | pre-existing line | fixed | 3 |
| 485 | src-tauri/src/window_manager/document_windows.rs | 56 | High | Duplication | `build_window_url` and `build_window_url_with_files` duplicate query collection and final URL assembly | Introduce one query-builder helper used by both functions. | pre-existing line | fixed | 3 |
| 486 | src-tauri/src/window_manager/document_windows.rs | 120 | Medium | Refactoring Debt | `build_document_window` exceeds 30 lines and combines common construction with platform-specific decoration | Extract the macOS builder customization into a small platform helper. | changed line | not-fixed (rebuttal: one linear builder chain plus an 8-line cfg(macos) block with a stated reason; extracting it needs a cfg'd helper on both sides — a stub for no gain) | 3 |
| 487 | src-tauri/src/window_manager/document_windows.rs | 161 | High | Duplication | `create_document_window_with_url` and `create_document_window` duplicate counter allocation, label formatting, positioning, building, and return handling | Make the latter build its URL and delegate to the former. | changed line | fixed | 3 |
| 488 | src-tauri/src/window_manager/document_windows.rs | 189 | High | Logic & Correctness | The preallocated-label API accepts any `&str` without reserving it or advancing `WINDOW_COUNTER`; a restored `doc-N` can collide with a later automatic allocation, and this routing-critical case is untested | Accept an opaque reservation returned by the allocator, or reconcile/check labels against live windows atomically. | changed line | not-fixed (rebuttal: all three callers obtain the label from allocate_window_label(), which advances WINDOW_COUNTER; no path reuses a persisted label) | 1 |
| 489 | src-tauri/src/window_manager/document_windows.rs | 195 | Low | Shortcuts & Patches | A malformed or overflowing `doc-N` label silently receives cascade position zero, hiding invalid allocator state | Validate the label and return an error instead of `unwrap_or(0)`. | pre-existing line | fixed | 3 |
| 490 | src-tauri/src/window_manager/document_windows.rs | 276 | High | Logic & Correctness | Dock reopen checks `is_dir()` but returns the original recent path, so a symlink or renamed path can redirect the workspace after validation | Reuse canonical workspace validation and pass only its canonical result onward. | changed line | fixed | 1 |
| 491 | src-tauri/src/window_manager/path_validation.rs | 26 | High | Logic & Correctness | The claimed security gate authorizes any existing supported-extension file, including relative paths resolved from process CWD, without proving user selection or prior authorization | Require an absolute path plus a backend-issued picker/bookmark token or containment under an already authorized root. | changed line | fixed (partly: both gates now require an ABSOLUTE path, since a security gate must not resolve against the process CWD; the "prove user selection" half is rebutted — a picker/bookmark token cannot exist while recents and session-restore reach these commands without a dialog) | 1 |
| 492 | src-tauri/src/window_manager/path_validation.rs | 27 | High | Logic & Correctness | Canonicalization returns another pathname, not a stable filesystem identity; replacing that canonical target before the later grant can still redirect access | Bind authorization to an open handle/file identity or fail closed after identity verification at the point of use. | changed line | not-fixed (rebuttal: binding to an open handle is what workflow/dir_fd.rs does for WRITES, where redirection is an integrity escalation; for a window URL the canonical path is the value that flows on, and fs_scope.test.rs pins that a link re-pointed after the grant is refused) | 1 |
| 493 | src-tauri/src/window_manager/path_validation.rs | 51 | High | Logic & Correctness | Any directory — including `"."`, the filesystem root, or another arbitrary absolute directory — is accepted as a trusted workspace root | Require absolute, explicitly authorized workspace paths and reject overly broad roots. | changed line | fixed (partly: absolute now required; "reject overly broad roots" not implemented — / as a workspace is a strange choice, not an escalation, and no threshold survives scrutiny) | 1 |
| 494 | src-tauri/src/window_manager/path_validation.rs | 52 | High | Logic & Correctness | Workspace canonicalization and `is_dir()` are separate path-based checks, and the returned name is re-resolved when the window mounts | Carry a validated directory handle/bookmark or repeat identity-aware validation at use time. | changed line | not-fixed (rebuttal: same as #492 — the canonical directory is what the window is scoped to; a handle/bookmark carried through window creation is a redesign, and the residual needs a concurrent local attacker) | 1 |
| 495 | src-tauri/src/workflow/actions.rs | 73 | Low | Shortcuts & Patches | Missing `action/` prefixes are silently accepted, contradicting the module’s `action/*` contract and hiding malformed workflows | Require the prefix or explicitly document and test the compatibility syntax. | changed line | fixed | 3 |
| 496 | src-tauri/src/workflow/actions.rs | 87 | Medium | Logic & Correctness | Arbitrary workflow content is written verbatim to logs, allowing control-character injection and accidental persistence of sensitive prompt data | Use structured/escaped logging and avoid logging full message content. | changed line | not-fixed (rebuttal: action/notify's entire contract is to surface the message the user's own workflow asked to surface — it also RETURNS it as the step output, so not logging it makes the action a no-op) | 2 |
| 497 | src-tauri/src/workflow/actions.rs | 100 | High | Logic & Correctness | `validate_path` canonicalizes first, but `read_bounded` later opens that pathname again; an ancestor swapped for a symlink between those operations can read outside the workspace | Open relative to a validated directory descriptor with no-follow semantics and verify containment from the opened handle. | changed line | not-fixed (rebuttal: validate_path canonicalizes, so every static symlink is caught; the residual is a concurrent local attacker inside the user's own workspace, who reads those files directly. Descriptor anchoring exists on the WRITE side, where redirection is an integrity escalation) | 1 |
| 498 | src-tauri/src/workflow/actions.rs | 115 | Medium | Refactoring Debt | `save_file` exceeds 30 lines and mixes validation, directory preparation, ownership conversion, task scheduling, committing, and error translation | Split preparation and blocking commit into focused helpers. | changed line | fixed | 3 |
| 499 | src-tauri/src/workflow/actions.rs | 126 | High | Shortcuts & Patches | On Windows, `create_parents_within` falls back to path-based `create_dir_all`; a junction swap can create directories outside the workspace, and this critical path has no runnable Windows test | Implement handle-relative Windows directory creation or disable `save-file` on Windows until it can fail closed. | changed line | fixed | 3 |
| 500 | src-tauri/src/workflow/actions.rs | 126 | Medium | Logic & Correctness | Directory creation and file commit are separate operations; if commit fails, newly created directories remain despite the action returning an error | Return the created-directory set and safely roll it back, or explicitly expose the partial-success outcome. | changed line | not-fixed (rebuttal: this is mkdir -p followed by a failed write; the directories are empty and inside the user's own workspace, and an unwinding rmdir would race a concurrent writer for a cosmetic gain) | 2 |
| 501 | src-tauri/src/workflow/actions.rs | 126 | High | Duplication | The directory and commit phases repeat `spawn_blocking`, join-error conversion, and contextual error-wrapping logic | Extract a typed blocking-I/O helper that preserves operation context. | changed line | fixed | 3 |
| 502 | src-tauri/src/workflow/actions.rs | 132 | Medium | Logic & Correctness | Save input has no size limit and is duplicated into a new `Vec`, permitting large workflow values to double memory use and exhaust disk | Enforce a write limit and transfer owned bytes without duplicating the entire input. | changed line | not-fixed (rebuttal: the bytes are already resident — the parameter is a &str the runner holds — and every producer is capped: read-file 10 MiB, read-folder 100 MiB; a write cap below those would refuse the compose case the folder budget exists to enable) | 2 |
| 503 | src-tauri/src/workflow/actions.rs | 138 | High | Shortcuts & Patches | On Windows, `commit_inside_workspace` has a documented check-then-write junction race that can redirect the final write outside the workspace | Use directory-handle-relative Windows file creation/rename APIs or refuse the operation on unsupported platforms. | changed line | not-fixed (rebuttal: same finding as row 524, settled in round 2 — commit.rs states the residual and names the primitive that would close it, NtCreateFile with a parent HANDLE, an ntdll interface with no safe binding here; commit.rs is also not in this group) | 3 |
| 504 | src-tauri/src/workflow/actions.rs | 138 | High | Logic & Correctness | Dropping or aborting `save_file` does not cancel an already-running `spawn_blocking` commit, so a workflow may write after being reported cancelled | Track and join the commit before declaring cancellation, or implement cancellation checked within the commit protocol. | changed line | not-fixed (rebuttal: save_file AWAITS its spawn_blocking and the runner awaits save_file, so the write completes before any later step observes the cancel; the only way to drop it is app teardown, where a COMPLETED atomic write is the safe outcome) | 1 |
| 505 | src-tauri/src/workflow/actions.rs | 157 | Medium | Logic & Correctness | Accept matching is case-sensitive, so `*.md` excludes `README.MD` even though VMark’s supported-extension gate is case-insensitive | Normalize ASCII extensions/patterns consistently and add mixed-case tests. | changed line | fixed | 2 |
| 506 | src-tauri/src/workflow/actions_folder.rs | 80 | High | Refactoring Debt | `read_folder` is an 85-line, high-cyclomatic-complexity function combining enumeration, filtering, containment, blocking I/O, decoding, budgeting, logging, and formatting. | Extract candidate collection, secure entry opening, decoding, and aggregation into independently testable functions. | new file | fixed | 3 |
| 507 | src-tauri/src/workflow/actions_folder.rs | 115 | Medium | Logic & Correctness | The 100 MB read limit does not bound memory: accumulated strings are copied again by `join`, producing roughly 200 MB plus per-file formatting buffers. | Append directly into one preallocated output buffer and include headers/separators in the budget. | new file | fixed (single-buffer half: read_folder appends into one String instead of Vec<String> + join, halving peak memory; framing is deliberately NOT charged — ~22 bytes x 1000 files against 100 MB, and the caps are pinned to the byte by existing tests) | 2 |
| 508 | src-tauri/src/workflow/actions_folder.rs | 118 | Low | Logic & Correctness | `to_string_lossy` can render distinct non-UTF-8 filenames identically, making output sections ambiguous despite sorting by raw names. | Reject non-UTF-8 names or encode filenames reversibly. | new file | fixed | 3 |
| 509 | src-tauri/src/workflow/actions_folder.rs | 122 | High | Logic & Correctness | Containment is checked before the file is opened; replacing the entry with an external symlink before line 135 allows reading outside the workspace. This critical race is untested. | Open relative to validated directory descriptors (`openat`/equivalent), or reject symlinks with handle-based containment verification. | new file | not-fixed (rebuttal: read_entry opens the already-canonicalized entry_path, so a planted symlink is caught; same concurrent-local-attacker residual as #497) | 1 |
| 510 | src-tauri/src/workflow/actions_folder.rs | 124 | Medium | Shortcuts & Patches | Unresolvable, unreadable, non-regular, oversized, and invalid-UTF-8 entries are silently omitted from the returned value; downstream workflows cannot distinguish complete input from partial input. | Return structured skipped-entry diagnostics or fail unless partial reads are explicitly requested. | new file | fixed | 3 |
| 511 | src-tauri/src/workflow/actions_folder.rs | 134 | Medium | Logic & Correctness | Invalid UTF-8 files are uncharged when encountered early, but become `OverBudget` before UTF-8 validation when remaining capacity is small; results therefore depend on filename ordering. | Define whether all bytes read or only emitted bytes consume the budget, then enforce that policy consistently with streaming validation. | new file | fixed | 2 |
| 512 | src-tauri/src/workflow/coherence_capture.rs | 49 | High | Duplication | This hand-written expression parser has drifted from the executor grammar: it treats any `known_id.*` value — including `read.text` — as a dependency, while execution only resolves `known_id.output`, producing false provenance edges. | Expose reference extraction from the canonical expression parser and add parser/capture parity tests. | pre-existing line | fixed | 3 |
| 513 | src-tauri/src/workflow/coherence_capture.rs | 76 | High | Logic & Correctness | Read-file paths are taken from raw `with` maps, so `${{ env.INPUT }}` and other resolved paths are submitted to coherence as literal filenames and capture fails or records the wrong input. | Record each read step’s resolved path and revision during execution and pass those resolved values to capture. | pre-existing line | not-fixed (out of reach: needs the runner to record each read step's RESOLVED path at execution time, and runner.rs is at its frozen 771-line cap — the change requires splitting it first) | 1 |
| 514 | src-tauri/src/workflow/coherence_capture.rs | 105 | High | Logic & Correctness | `workspace_root` is deliberately discarded, so action-valid paths such as `./file.md`, absolute paths inside the workspace, and equivalent symlink aliases are not normalized to coherence’s stricter relative-path contract; captures or self-edge suppression fail. | Canonicalize paths against `workspace_root`, convert them to normalized workspace-relative paths, and compare normalized identities. | changed line | fixed | 1 |
| 515 | src-tauri/src/workflow/coherence_capture.rs | 110 | High | Logic & Correctness | Every input uses `revision: None` while line 132 declares `Confidence::Exact`; resolution therefore selects the head current at capture time, not necessarily the revision whose bytes the workflow read. | Capture object/revision identifiers when each read executes and propagate explicit revisions into the save capture. | changed line | not-fixed (out of reach: same blocker as #513 — an object/revision identifier can only be captured where the read EXECUTES, in runner.rs) | 1 |
| 516 | src-tauri/src/workflow/coherence_capture.rs | 123 | Medium | Logic & Correctness | Every save-file operation is attributed to `AgentType::Model`/`workflow-genie`, including action-only workflows and static saves that never invoke a model. | Derive attribution from the actual contributing steps or use a workflow/system agent for non-model transformations. | pre-existing line | fixed | 3 |
| 517 | src-tauri/src/workflow/coherence_capture.rs | 151 | Medium | Shortcuts & Patches | Missing state, registry errors, poisoned locks, capture errors, and task panics are discarded or logged only, so a successful save can silently lack provenance with no UI signal or retry path. | Return a capture outcome or emit a coherence-degraded event and queue retryable failures. | changed line | not-fixed (rebuttal: fire-and-forget is the module header's stated contract (spec §7) — a capture must not fail the workflow step — and the proposed retryable queue plus degraded event is a new subsystem and a product decision) | 3 |
| 518 | src-tauri/src/workflow/commands.rs | 51 | Medium | Refactoring Debt | `admit_run` exceeds 30 lines and mixes policy locking, concurrency admission, filesystem resolution, parsing, and semantic validation; invalid input also owns the global running slot during validation. | Extract pure bounded request validation, then perform the final feature check and concurrency claim atomically. | changed line | not-fixed (rebuttal: the remedy would regress #559, which deliberately publishes the id BEFORE validation so a cancel arriving while the YAML is still being parsed already matches; moving validation ahead of the claim reopens exactly that) | 3 |
| 519 | src-tauri/src/workflow/commands.rs | 88 | High | Logic & Correctness | YAML deserialization accepts unknown fields, so safety-relevant typos such as `need:` or `approvals:` are silently ignored and can change execution ordering or approval behavior. | Add `#[serde(deny_unknown_fields)]` to workflow wire structs or perform explicit unknown-key validation. | changed line | fixed | 1 |
| 520 | src-tauri/src/workflow/commands.rs | 138 | Medium | Logic & Correctness | `yaml`, environment entries, provider fields, and individual step strings have no byte/count limits; the 50-step check occurs only after potentially expensive deserialization. | Enforce request-size, map-count, key-length, value-length, and nesting limits before parsing or spawning. | changed line | fixed | 2 |
| 521 | src-tauri/src/workflow/commands.rs | 162 | High | Logic & Correctness | Pre-run snapshots are computed from raw YAML paths before environment and step-output expressions are resolved; a dynamic save path can overwrite an existing file that was never snapshotted. This critical recovery path lacks command-level coverage. | Snapshot immediately before each save after resolving its target, or reject dynamic save paths that cannot be safely determined before execution. | changed line | fixed | 1 |
| 522 | src-tauri/src/workflow/commands.rs | 169 | High | Logic & Correctness | Duplicate IDs, unknown `needs`, and dependency cycles are deferred to the background runner; its early topological-sort error returns before emitting `workflow:complete`, although this command has already returned success. | Validate the graph before snapshot/spawn, and independently guarantee a terminal completion event for every spawned execution. | changed line | fixed | 1 |
| 523 | src-tauri/src/workflow/commands.rs | 201 | Low | Dead Code | `cancel_workflow` requests an `AppHandle` but never uses it. | Remove the injected parameter unless it is required by an explicitly documented command ABI constraint. | changed line | fixed | 3 |
| 524 | src-tauri/src/workflow/commit.rs | 52 | High | Logic & Correctness | The non-Unix implementation knowingly remains check-then-use: replacing the validated parent with a junction before `atomic_replace` redirects the write outside the workspace, and the critical Windows path has no runtime test. | Implement handle-relative Windows creation/replacement using a maintained safe abstraction and add Windows runtime tests; otherwise disable save-file on platforms lacking secure containment. | new file | not-fixed (rebuttal: commit.rs already states the residual and names the primitive that would close it — NtCreateFile with a parent HANDLE, an ntdll interface with no safe binding here — and the suggested alternative, disabling save-file on Windows, trades a concurrent-local-attacker race for a broken feature) | 1 |
| 525 | src-tauri/src/workflow/commit_dir.rs | 36 | Medium | Refactoring Debt | `commit_with` exceeds 30 lines and combines validation, temp creation, writing, metadata transfer, cleanup, rename, and durability handling | Extract checked temp creation and finalization into focused helpers | new file | not-fixed (rebuttal: a descriptor-anchored sequence where each step's safety depends on the previous one's descriptor — open, assert_within, create, fstatat, write, metadata, sync, renameat, unlink-on-failure — so splitting separates each check from the handle it was made through) | 3 |
| 526 | src-tauri/src/workflow/commit_dir.rs | 41 | Medium | Refactoring Debt | This new filesystem API returns `Result<(), String>`, erasing structured causes and extending the legacy string-error pattern | Return a typed workflow filesystem error and convert to `CommandError` only at the command boundary | new file | not-fixed (rebuttal: a pub(in crate::workflow) helper, not a command; the ratchet's unit is the command signature, and the consuming action executor is String-typed end to end) | 3 |
| 527 | src-tauri/src/workflow/commit_dir.rs | 57 | High | Logic & Correctness | `NamedTempFile::new_in(parent)` performs an unauthorized path-based file creation before descriptor validation; cleanup does not erase that external side effect | Create the temporary file with `openat` relative to the validated directory descriptor and manage it with a descriptor-relative RAII guard | new file | not-fixed (rebuttal: dir.assert_within() runs BEFORE NamedTempFile::new_in, dir.holds() catches a swapped path before a byte of user content is written, and NamedTempFile's Drop removes the stray — the claim that cleanup does not erase the side effect is refuted by the RAII delete) | 1 |
| 528 | src-tauri/src/workflow/commit_dir.rs | 74 | Medium | Logic & Correctness | The temp file is synced before permissions and xattrs are changed, so those later metadata updates are not crash-durable | Preserve metadata before the final `sync_all`, or sync the file again afterward | new file | fixed | 2 |
| 529 | src-tauri/src/workflow/commit_dir.rs | 78 | High | Logic & Correctness | Metadata preservation reopens both `target` and `temp.path()` by mutable paths, reintroducing the TOCTOU escape this module is intended to eliminate and potentially reading or modifying metadata outside the workspace | Read target metadata through `dir` with no-follow descriptor-relative operations and apply it directly to the open temp file descriptor | new file | fixed | 1 |
| 530 | src-tauri/src/workflow/commit_dir.rs | 83 | Medium | Logic & Correctness | `keep()` disables automatic cleanup before the rename; if rename and the unchecked `unlinkat` cleanup both fail, a temp file containing user data remains | Keep a descriptor-relative RAII cleanup guard armed until rename succeeds and propagate or prominently log cleanup failure | new file | fixed | 2 |
| 531 | src-tauri/src/workflow/dir_fd.rs | 29 | Medium | Logic & Correctness | The arbitrary 256-level limit rejects valid deeply nested workspaces even though the filesystem walk has a natural termination at the root | Walk until the root fixed point is reached, optionally detecting repeated identities instead of imposing a depth cap | new file | not-fixed (rebuttal: assert_within already terminates at the root fixed point and MAX_WALK is the second guard against a .. chain that never reaches it; detecting repeated identities replaces a constant-space bound with an unbounded set for a nesting depth no workspace reaches) | 2 |
| 532 | src-tauri/src/workflow/dir_fd.rs | 39 | Medium | Refactoring Debt | The new descriptor abstraction converts nearly every I/O error into `String`, losing errno and preventing reliable policy or retry decisions | Introduce a typed error enum retaining `std::io::Error` sources | new file | not-fixed (rebuttal: every place the errno matters is already branched on before stringifying — mkdir on AlreadyExists, holds/unlink on NotFound — no caller retries or makes a policy decision on the strings, and the two consumers are another fixer's files) | 3 |
| 533 | src-tauri/src/workflow/dir_fd.rs | 95 | Medium | Shortcuts & Patches | The raw syscall wrappers do not consistently handle `EINTR`, and their repeated hand-written return-code handling has already produced inconsistent error policies | Use `rustix`/`nix` wrappers or one checked-syscall helper with consistent retry and error propagation | new file | fixed | 3 |
| 534 | src-tauri/src/workflow/dir_fd.rs | 151 | Medium | Logic & Correctness | Every `fstatat` failure is converted to `Ok(false)`, hiding I/O, descriptor, permission, and interruption errors as a misleading containment mismatch | Return the underlying error; reserve `false` for a successful stat whose identity differs | new file | fixed | 2 |
| 535 | src-tauri/src/workflow/dir_fd.rs | 172 | Medium | Logic & Correctness | `unlinkat` failures are discarded, which can leave sensitive temporary content behind after a failed rename | Return `Result` and let the caller report or retry cleanup failure | new file | fixed | 2 |
| 536 | src-tauri/src/workflow/dir_fd.rs | 181 | Medium | Logic & Correctness | Directory `fsync` failure is swallowed, yet the caller reports a successful save even though the rename may not survive a crash | Return the sync result and expose a durability failure or explicit warning outcome to the caller | new file | not-fixed (rebuttal: dir_fd.rs argues it — the bytes are synced and the rename has happened, so reporting failure would misdescribe a file that is on disk; there is no third outcome the caller could act on) | 2 |
| 537 | src-tauri/src/workflow/dir_fd.rs | 190 | High | Logic & Correctness | `assert_within` proves ancestry only at one instant; an opened directory can subsequently be renamed outside the workspace, after which both callers continue writing through the now-external descriptor | Do not treat descriptor identity as immutable containment; revalidate after test/race seams and before writes, and use an OS-enforced root/capability model where a strict concurrent-attacker guarantee is required | new file | not-fixed (rebuttal: descriptor identity is the strongest primitive macOS offers; openat2(RESOLVE_BENEATH) is Linux-only and macOS is the primary platform. The residual needs a concurrent local attacker able to rename the workspace's own directories) | 1 |
| 538 | src-tauri/src/workflow/ensure_dir.rs | 32 | Medium | Refactoring Debt | The new directory-creation API returns unstructured `String` errors, contrary to the typed backend error policy | Return a typed filesystem/workflow error and map it once at the command boundary | new file | not-fixed (rebuttal: same as #526 — an internal filesystem helper, not a command signature) | 3 |
| 539 | src-tauri/src/workflow/ensure_dir.rs | 87 | Medium | Logic & Correctness | `Path::exists()` converts permission and other metadata errors into `false`, causing inaccessible existing components to be misclassified as missing and producing misleading later behavior | Use `try_exists()` and propagate non-`NotFound` failures | new file | fixed | 2 |
| 540 | src-tauri/src/workflow/ensure_dir.rs | 130 | High | Shortcuts & Patches | The non-Unix fallback ignores `workspace_root` and knowingly calls `create_dir_all` through a mutable path, allowing junction/symlink races to create directories outside the workspace | Implement handle-relative Windows traversal or refuse missing-parent creation on Windows until it can be confined safely | new file | not-fixed (rebuttal: the residual is already stated at the fallback with its mechanism and bound, and the proposed "refuse missing-parent creation on Windows" would break action/save-file for any nested path there) | 3 |
| 541 | src-tauri/src/workflow/examples.rs | 38 | High | Duplication | The two genie asset paths are manually repeated independently of the workflow’s `uses:` entries, creating a second catalog that must stay synchronized | Resolve referenced genie names from the parsed workflow through the bundled catalog/loader | pre-existing line | fixed | 3 |
| 542 | src-tauri/src/workflow/examples.rs | 44 | High | Duplication | `step_id` and `topo_order` duplicate the production runner’s ID resolution and Kahn sort, so the test can pass against behavior different from production | Expose a crate-visible production validation/resolution function and test the sample through that implementation | pre-existing line | fixed | 3 |
| 543 | src-tauri/src/workflow/examples.rs | 53 | Medium | Refactoring Debt | The duplicated `topo_order` helper exceeds 30 lines and mixes validation, graph construction, sorting, and cycle detection | Remove it in favor of the production resolver, or split those responsibilities | pre-existing line | fixed | 3 |
| 544 | src-tauri/src/workflow/examples.rs | 134 | Low | Dead Code | `genie_steps >= 1` is redundant because the following `genie_steps == 2` assertion strictly subsumes it | Remove the weaker assertion | pre-existing line | fixed | 3 |
| 545 | src-tauri/src/workflow/examples.rs | 192 | Low | Dead Code | The `read-file` contract assertion is unrelated to this sample and duplicates coverage owned by `actions.test.rs` | Remove it and keep action-contract assertions with the action executor tests | changed line | fixed | 3 |
| 546 | src-tauri/src/workflow/examples.rs | 196 | High | Logic & Correctness | The included end-to-end execution module is disabled on Windows, leaving the bundled workflow’s platform-specific save path — where the unsafe fallback exists — without runtime coverage | Decouple runner events from Tauri’s unavailable Windows mock runtime or add a Windows E2E test that executes the sample | changed line | not-fixed (out of reach: tauri::test does not exist on Windows, so the runner-event harness the sample's execution test needs cannot link there) | 1 |
| 547 | src-tauri/src/workflow/launch.rs | 33 | Medium | Shortcuts & Patches | The new helper exposes the legacy `Result<String, String>` error contract, contrary to the project’s `CommandError` migration rule. | Accept `Result<(), CommandError>` or a typed workflow error and convert the runner at the boundary. | new file | not-fixed (rebuttal: spawn_run returns JoinHandle<()>; Result<String, String> is the RUNNER's future type, not a command signature, and launch.rs declares no #[tauri::command], which is what the ratchet governs) | 3 |
| 548 | src-tauri/src/workflow/launch.rs | 37 | High | Logic & Correctness | A background `Err` or panic is only logged. Errors such as topological-sort failure occur before the runner emits `workflow:complete`, leaving frontend execution state stuck indefinitely; the tests cover guard release but not terminal-event delivery. | Make one outer lifecycle owner emit exactly one terminal event for success, error, cancellation, and panic; pass it the execution ID and add event-level tests. | new file | fixed | 1 |
| 549 | src-tauri/src/workflow/prepare.rs | 38 | High | Duplication | Execution-ID length and alphabet validation duplicate `snapshots::MAX_ID_LEN`/`validate_id`, allowing the two security-sensitive path rules to drift. | Move the constant and validator into one shared module and reuse them at both entry and snapshot boundaries. | new file | fixed | 3 |
| 550 | src-tauri/src/workflow/prepare.rs | 61 | Medium | Logic & Correctness | Invalid-input formatting includes the complete caller-controlled ID, so an arbitrarily large value defeats the intended bound by being copied into the error response. | Report its length and include only a safely truncated preview. | new file | fixed | 2 |
| 551 | src-tauri/src/workflow/prepare.rs | 69 | High | Logic & Correctness | Snapshot targets use raw `with.path` strings although paths support environment and prior-step expressions resolved later by the runner. The snapshot can therefore protect a literal placeholder while `save-file` overwrites a different existing file, breaking recovery. | Reject dynamic save paths during admission or snapshot each final resolved and validated target immediately before its first write; add env/output-path recovery tests. | new file | fixed | 1 |
| 552 | src-tauri/src/workflow/prepare.rs | 82 | Medium | Logic & Correctness | Targets are not normalized or deduplicated, so repeated or aliased paths are copied and charged against snapshot limits multiple times, potentially refusing an otherwise valid workflow. | Resolve through the sandbox and deduplicate canonical target paths before copying. | new file | fixed | 2 |
| 553 | src-tauri/src/workflow/prepare.rs | 90 | Medium | Shortcuts & Patches | `snapshot_root` introduces another new `Result<PathBuf, String>` seam despite the typed-error requirement. | Accept the resolver’s typed error generically or return `CommandError` directly. | new file | not-fixed (rebuttal: the impl FnOnce is a test seam whose failure is converted to a typed localized CommandError inside snapshot_root itself; accepting a typed error instead would move the localized_error! to the caller and duplicate it) | 3 |
| 554 | src-tauri/src/workflow/prepare.rs | 117 | High | Logic & Correctness | Snapshot failure is propagated without transactional cleanup. The callee creates the final snapshot directory before copying, so later copy, cancellation, or metadata failure can leave substantial partial data and permanently occupy the execution-ID directory. | Build in a temporary directory with an RAII cleanup guard, then atomically rename only after metadata succeeds. | new file | fixed | 1 |
| 555 | src-tauri/src/workflow/prepare.rs | 120 | Medium | Logic & Correctness | Every snapshot error is flattened into `ErrorCode::Io`; user cancellation during copying is consequently reported and logged as an I/O snapshot failure. | Return a typed snapshot error and map cancellation to `CommandError::cancelled`, preserving I/O and conflict variants separately. | new file | fixed | 2 |
| 556 | src-tauri/src/workflow/prepare.rs | 156 | Medium | Shortcuts & Patches | `.ok()` silently discards `app_data_dir` failures. Genie workflows are spawned successfully and later fail with a generic missing-directory message that loses the real platform error; pure action workflows also perform an unnecessary lookup. | Determine whether snapshots or genie steps require app data, resolve it once, preserve failures for required consumers, and skip resolution when unused. | new file | fixed | 3 |
| 557 | src-tauri/src/workflow/recent_ids.rs | 41 | High | Logic & Correctness | The fixed 256-ID window deliberately permits process-local ID reuse. For snapshot-free workflows, stale event listeners or cancellation requests for the old run can observe or cancel the new run carrying that ID. | Retain IDs for the process lifetime or incorporate a nonrepeating server/session generation into identity; add an end-to-end stale-event and cancellation test. | new file | not-fixed (rebuttal: recent_ids.rs states this decision in full — the consequential half, the snapshot namespace, is guarded ON DISK across restarts, and the event/cancel half is a client bug inside the trust boundary) | 1 |
| 558 | src-tauri/src/workflow/state.rs | 93 | Medium | Refactoring Debt | Public synchronization fields and the raw `set_engine_enabled` setter allow callers to bypass the locks and invariant-preserving transitions this module introduces | Make state fields private and expose only atomic domain operations | pre-existing line | fixed | 3 |
| 559 | src-tauri/src/workflow/state.rs | 170 | High | Logic & Correctness | Claiming `running` and publishing `current_execution` are separate operations; a cancellation arriving before `begin_execution` sees no matching execution and is permanently lost | Combine claim, ID validation, cancel reset, and ID publication into one atomic admission operation under the execution lock | changed line | fixed | 1 |
| 560 | src-tauri/src/workspace_validation.rs | 18 | Medium | Shortcuts & Patches | This Tauri command returns legacy `Result<String, String>`, losing structured error codes and violating the `CommandError` contract | Return `Result<String, CommandError>` and map filesystem, validation, and path-encoding failures to appropriate variants | pre-existing line | fixed | 3 |
| 561 | src-tauri/src/workspace_validation.rs | 19 | Medium | Logic & Correctness | Synchronous canonicalization can block the IPC delivery thread indefinitely on slow, disconnected, or automounted network filesystems | Make the command async and perform filesystem validation through `spawn_blocking` | pre-existing line | fixed | 3 |
| 562 | src-tauri/src/workspace_validation.rs | 38 | High | Logic & Correctness | The security-critical promise that symlinks resolve to the real approved directory is untested; the test merely checks `is_ok()` and would pass if the function returned the original alias | Add a symlink fixture and assert the returned value equals the canonical target path | changed line | fixed | 1 |
| 563 | src-tauri/src/workspace_validation.rs | 45 | Medium | Logic & Correctness | The test uses a predictable shared temporary filename, overwrites any existing file there, races parallel processes, and deletes a file it may not own | Use an isolated RAII temporary directory or uniquely named temporary file | changed line | fixed | 2 |
| 564 | src/components/Editor/DocumentSplit/splitKeyboardResize.ts | 27 | Low | Logic & Correctness | Arrow keys can return fractions outside the declared pane range; correctness currently depends on every consumer applying a separate downstream clamp | Clamp both arrow results with `MIN_PANE_FRACTION` and `MAX_PANE_FRACTION` | new file | not-fixed (rebuttal: splitKeyboardResize.test.ts already pins "does not clamp — the store owns the bounds, exactly as for the drag", and paneStore.setFraction clamps both the keyboard and the drag path, so there is ONE clamp site) | 3 |
| 565 | src/components/Editor/DocumentSplit/useSplitDrag.ts | 29 | Medium | Refactoring Debt | The 52-line hook combines listener ownership, global styling, geometry, pointer filtering, and capture lifecycle | Extract a drag-session helper with explicit start, move, and teardown responsibilities | new file | fixed | 3 |
| 566 | src/components/Editor/DocumentSplit/useSplitDrag.ts | 41 | Medium | Logic & Correctness | Cleanup unconditionally erases existing body cursor and user-select styles, including styles owned by another active resizer | Snapshot previous values and restore them only when this hook owns the active session | new file | fixed | 2 |
| 567 | src/components/Editor/DocumentSplit/useSplitDrag.ts | 56 | Medium | Logic & Correctness | A zero-width container produces `NaN` or `Infinity` fractions | Guard `rect.width > 0` before installing the drag session | new file | fixed | 2 |
| 568 | src/components/Editor/DocumentSplit/useSplitDrag.ts | 57 | Medium | Logic & Correctness | Document listeners process movement and termination events from every pointer, so a second touch or pen can resize or terminate the first pointer’s drag | Capture the initiating `pointerId`, reject non-primary pointers, and ignore mismatched move/up/cancel events | new file | fixed (partly: the session owns e.pointerId and other pointers are ignored; the suggested isPrimary rejection was deliberately NOT added — jsdom reports isPrimary false for every synthetic pointer event, so that guard would refuse every drag under test while only covering a second finger on a 4px bar) | 2 |
| 569 | src/components/Editor/DocumentSplit/useSplitDrag.ts | 58 | Medium | Logic & Correctness | The move listener closes over the initial `onResize`; callback changes during a drag continue invoking stale behavior | Store the latest callback in a ref or restart the session when it changes | new file | fixed | 2 |
| 570 | src/components/Editor/DocumentSplit/useSplitDrag.ts | 68 | Medium | Logic & Correctness | Pointer capture is acquired but never explicitly released during blur cleanup | Retain the target and pointer ID and call `releasePointerCapture` during cleanup when capture remains active | new file | fixed | 2 |
| 571 | src/components/Editor/SplitPaneEditor/ValidationGutter.tsx | 108 | Medium | Refactoring Debt | Every diagnostic mounts its own translation hook, and rows with rule IDs mount another through `RuleBadge`, creating O(n) subscriptions | Resolve `t` once in `ValidationGutter` and pass it to summary, row, and badge helpers | changed line | fixed | 3 |
| 572 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 67 | High | Logic & Correctness | Selection comes from one global view slice and ignores `tabId`, so simultaneously mounted split-pane workflow editors can select unrelated jobs or steps in each other | Key workflow selection state by tab/document and select the current panel’s entry | changed line | not-fixed (out of reach: view is a singleton slice with no document dimension — keying it needs a workflowStore ViewSlice change plus threading a document id through WorkflowCanvas/JobNode, outside this group; recorded in the panel header) | 1 |
| 573 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 99 | High | Logic & Correctness | Custom permission-scope edits are not represented in the preview IR while `PermissionsForm` derives displayed scopes from that unchanged prop, so a queued edit immediately renders the old value | Overlay `workflow.permissions.set` patches or make permission scopes controlled local state | changed line | fixed | 1 |
| 574 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 99 | High | Logic & Correctness | Discard generation is applied only to `JobForm` and `StepForm`; `PermissionsForm`, `ConcurrencyForm`, editable trigger fields, and the add-job draft retain discarded local values | Key all stateful editor controls by `formGen` or explicitly reset their local state | changed line | fixed | 1 |
| 575 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 120 | High | Logic & Correctness | Every panel applies the globally active `pendingPatches` queue to its own workflow; in split panes, one document’s patches can appear in another document | Scope edit queues by the panel’s tab/document identifier instead of reading the active global queue | changed line | fixed | 1 |
| 576 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 121 | Low | Refactoring Debt | Preview patches are reapplied on every parent render even when workflow and patch references are unchanged | Memoize `applyPreviewPatches` by `workflow` and `pendingPatches` | changed line | fixed | 3 |
| 577 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 145 | High | Logic & Correctness | Job/step keys contain IDs and discard generation but no workflow revision, so source or external updates to the same selected entity leave form state stale and can overwrite newer values | Synchronize local form state on entity changes or include an appropriate source revision in remount semantics | changed line | not-fixed (out of reach: the fix is per-seeded-field re-seeding inside JobForm/StepForm; the panel-level remedy the finding proposes is worse than the bug — a content-derived key remounts whenever a re-parse shifts a job's source range, discarding uncommitted edits on any source keystroke) | 1 |
| 578 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 166 | Medium | Refactoring Debt | `AddJobControl` is a 74-line component mixing validation, keyboard behavior, state transitions, and rendering | Extract validation and a focused job-ID form component | changed line | not-fixed (rebuttal: AddJobControl is already its own 132-line module, extracted by round-2 #581; the panel only mounts it) | 3 |
| 579 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 175 | High | Duplication | Job-ID normalization and the validation regex are duplicated between submission and disabled-state calculation | Extract one `validateJobId` result and reuse it for submission, button state, and feedback | changed line | not-fixed (rebuttal: jobIdProblem is the one rule, and submit and the button's disabled both derive from usable) | 3 |
| 580 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 189 | Medium | Shortcuts & Patches | Add-job labels, placeholder, submit text, and cancel text are hardcoded in English | Move all user-facing strings into the `workflowEditor` translation namespace | changed line | not-fixed (rebuttal: every string in AddJobControl is t("form.job.add.*") / t("common:cancel")) | 3 |
| 581 | src/components/Editor/WorkflowEditor/WorkflowEditorPanel.tsx | 194 | Medium | Logic & Correctness | The job-ID input has no accessible label and invalid IDs fail silently | Add a localized label or `aria-label`, expose validation text with `aria-describedby`, and set `aria-invalid` | changed line | fixed | 2 |
| 582 | src/components/Editor/WorkflowEditor/stepSelection.ts | 25 | Low | Logic & Correctness | The function returns a shared mutable `NONE` object, so a consumer can corrupt all subsequent empty results | Freeze the sentinel with readonly fields or return a fresh object | new file | fixed | 3 |
| 583 | src/components/Editor/WorkflowEditor/stepSelection.ts | 45 | High | Logic & Correctness | Step lookup and neighbor navigation assume IDs are unique, but synthesized IDs can collide; duplicate IDs select or navigate to the first matching step and can edit the wrong step | Use a guaranteed-unique stable identity or index-based selection and navigation | new file | not-fixed (out of reach: the collision is created in src/lib/ghaWorkflow/parser/steps.ts — explicitId ?? step-N can equal an explicit id: step-1; stepSelection.ts only consumes ids, so the fix is to synthesize non-colliding ids upstream) | 1 |
| 584 | src/components/Editor/WorkflowEditor/useActionlintDiagnostics.ts | 103 | Low | Logic & Correctness | The promise chain handles fulfillment only; if `onOutcome` or toast delivery throws, the chain remains rejected and all future lint requests are skipped with an unhandled rejection | Recover rejected tails and isolate outcome-delivery errors before storing `chainRef.current`. | new file | fixed | 3 |
| 585 | src/components/Editor/WorkflowEditor/useActionlintDiagnostics.ts | 107 | Medium | Logic & Correctness | A hung external `actionlint` invocation never settles, permanently blocking every newer queued lint and continuing after unmount | Enforce a killable backend timeout/cancellation and let the queue advance after timeout. | new file | fixed (frontend half: each run is bounded by ACTIONLINT_TIMEOUT_MS so a hung spawn no longer wedges the serialized queue; killing the child process is gha_lint's on the Rust side, noted in the code) | 2 |
| 586 | src/components/Editor/WorkflowEditor/useStepFocusRestore.ts | 30 | High | Logic & Correctness | `document.querySelector` is global; with multiple mounted workflow panes, navigation can focus a button in another pane, and every panel’s hook responds to the shared selection change | Accept a panel/root ref and query only inside that workbench, preferably using direct button refs. | new file | fixed | 1 |
| 587 | src/components/Editor/WorkflowEditor/useStepFocusRestore.ts | 31 | Medium | Shortcuts & Patches | Focus restoration depends on English substrings inside translated `aria-label` values, so it fails in non-English locales and whenever copy changes | Use locale-independent `data-*` targets or explicit refs. | new file | not-fixed (rebuttal: buttons are found by data-step-nav, not an aria-label substring — round-2 #586 already did this) | 3 |
| 588 | src/components/Editor/WorkflowEditor/useStepFocusRestore.ts | 33 | Medium | Logic & Correctness | The fixed Next → Previous priority ignores navigation direction; moving backward usually restores focus to Next, so another Enter reverses direction | Preserve the initiating control/direction and focus its counterpart after remount. | new file | fixed | 2 |
| 589 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 105 | Medium | Refactoring Debt | The 101-line component combines document identity migration, focus ownership, queue binding, save concurrency, notifications, and rendering | Extract focused hooks/services for document binding and save orchestration. | pre-existing line | not-fixed (rebuttal: each named concern is already its own effect or callback with its audit rationale, and GhaWorkflowWorkbench.test.tsx covers all of them in 24 cases) | 3 |
| 590 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 110 | High | Logic & Correctness | The save lock is component-local, so another workbench or save command can concurrently write the same document; a later stale write can overwrite newer YAML and both commits can clear the patch queue | Serialize saves in a shared document-keyed coordinator and recompute/version-check immediately before writing and committing. | changed line | fixed | 1 |
| 591 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 128 | High | Duplication | Document-ID construction duplicates the already-imported `workflowDocumentIdFor` contract, creating a DRY drift point for queue identity | Use the shared helper while retaining `filePath` as the reactive dependency. | changed line | fixed | 3 |
| 592 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 139 | High | Logic & Correctness | Every document-ID change is treated as Save As; if the component receives a different `tabId` without unmounting — or passes through `null` — patches from the old tab are renamed into the new tab’s queue | Track the previous tab separately and call `renameDocument` only when the same tab’s path changes. | changed line | not-fixed (rebuttal: tabId cannot change without a remount — Editor.tsx renders <SplitPaneEditor key={tabId-formatConfig.id} tabId={tabId}>, so the renameDocument branch can only fire on a filePath change of one fixed tab, which is a Save As) | 1 |
| 593 | src/components/Editor/WorkflowPanel/GhaWorkflowWorkbench.tsx | 200 | High | Logic & Correctness | Multiple workbenches mount editors against one singleton `workflowStore.view`; selecting a common job/step ID in one pane simultaneously selects and exposes that form in every other pane | Key view state by document or pass document-local selection state into each editor and canvas. | changed line | not-fixed (out of reach: same singleton view slice as #572) | 1 |
| 594 | src/components/FindBar/FindBar.tsx | 57 | Medium | Refactoring Debt | The component function remains roughly 85 lines and mixes five subscriptions, focus/keyboard orchestration, command dispatch, derived display state, and layout | Extract a small view-model hook and keep this component primarily declarative. | changed line | not-fixed (rebuttal: the component was already split in round 2 — 247 to 141 lines across useFindBarFocus, useFindBarKeyboard and FindBarControls; what is left is 5 store selectors, 3 one-line dispatch callbacks and JSX, with no rule written twice) | 3 |
| 595 | src/components/FindBar/FindBar.tsx | 79 | Medium | Logic & Correctness | Closing unmounts the focused input/button without restoring the pre-open editor focus, leaving keyboard focus on `body` unless an unrelated external mechanism intervenes | Capture the previously focused editor target on open and restore it after close. | pre-existing line | fixed | 3 |
| 596 | src/components/FindBar/FindBarControls.tsx | 88 | Low | Logic & Correctness | Every button declaration omits `type`, so nesting the controls inside a form would also submit that form. | Add `type="button"` to the toggle, navigation, and replace buttons. | new file | fixed | 3 |
| 597 | src/components/FindBar/FindBarControls.tsx | 111 | Medium | Logic & Correctness | Disabling `enableRegexSearch` only hides the toggle; if `useRegex` is already true, regex remains active with no visible way to disable it. | Reset `useRegex` when the setting is disabled, or keep the active toggle visible. | new file | fixed | 2 |
| 598 | src/components/FindBar/FindBarControls.tsx | 138 | Low | Refactoring Debt | `hasMatches` is misleading because the value means “has a valid current match”; `matchCount` may be positive while this prop is false. | Rename it to `hasCurrentMatch`. | new file | not-fixed (out of reach: the prop name is written at two call sites in src/components/FindBar/FindBar.tsx, in another group; renaming only the declaration breaks the build) | 3 |
| 599 | src/components/FindBar/useFindBarFocus.ts | 47 | Medium | Logic & Correctness | After Mod+E on an already-open bar, `select()` runs before React commits the seeded query and does not reliably focus the input, so the new query can end up unfocused or with its selection collapsed. | Focus and select after the store-driven DOM update, using a layout effect or scheduled callback. | new file | fixed | 2 |
| 600 | src/components/FindBar/useFindBarFocus.ts | 49 | Low | Shortcuts & Patches | The raw `"use-selection-for-find"` event name is an unchecked cross-module string contract; a producer/consumer typo silently breaks the command. | Export and reuse a shared event constant. | new file | fixed | 3 |
| 601 | src/components/FindBar/useFindBarKeyboard.ts | 44 | High | Duplication | The find and replace callbacks are near-duplicate dispatchers with repeated IME, Enter, Escape, and Tab handling, creating drift risk between the two inputs. | Extract a shared key dispatcher parameterized by the Enter action and Tab destination. | new file | fixed | 3 |
| 602 | src/components/FindBar/useFindBarKeyboard.ts | 53 | Low | Logic & Correctness | Escape closes the bar without preventing default or stopping propagation, allowing the same keystroke to reach surrounding/global Escape handlers; the duplicate occurs at line 70. | Consume Escape before calling `onClose()`. | new file | fixed | 3 |
| 603 | src/components/GeniePicker/GeniePicker.tsx | 47 | High | Refactoring Debt | `GeniePicker` spans roughly 459 lines and owns loading, focus restoration, filtering, history, invocation lifecycle, keyboard routing, scrolling, and rendering, far beyond the project’s approximately 300-line file guideline. | Split lifecycle, list derivation, keyboard handling, and response actions into focused hooks/components. | pre-existing line | fixed | 3 |
| 604 | src/components/GeniePicker/GeniePicker.tsx | 90 | Medium | Logic & Correctness | The open lifecycle is keyed to `[isOpen, filterScope]`: changing scope while open reruns initialization and overwrites `previousFocusRef` with a picker element, while reopening with the same scope does not rerun it. | Track an explicit open-session generation and separate transition-only focus capture from scope synchronization. | pre-existing line | fixed | 3 |
| 605 | src/components/GeniePicker/GeniePicker.tsx | 95 | Medium | Dead Code | The outer rejection handler does not receive normal loading failures because `loadGenies()` catches them internally; failures therefore look like an empty genie list and this warning path is effectively bypassed. | Have the store expose/propagate a load error and render it explicitly. | pre-existing line | not-fixed (out of reach: loadGenies in src/stores/aiStore/genies.ts catches its own failure and reports it as an empty list; surfacing it needs an error field on that store, another fixer's group. The dead .catch is kept as a backstop and now says so) | 3 |
| 606 | src/components/GeniePicker/GeniePicker.tsx | 96 | High | Duplication | The open effect repeats the same five-state reset implemented again by `resetInput()` at lines 180–185. | Centralize the reset operation and add only the open-specific scope assignment separately. | pre-existing line | fixed | 3 |
| 607 | src/components/GeniePicker/GeniePicker.tsx | 123 | High | Duplication | Genie searching and category grouping duplicate algorithms already exposed by the directly imported genies store, including the category fallback. | Extract shared pure filtering/grouping helpers and use them in both places. | pre-existing line | fixed | 3 |
| 608 | src/components/GeniePicker/GeniePicker.tsx | 138 | Medium | Logic & Correctness | `recents` reads `getRecent()` imperatively but neither subscribes to nor depends on `recentGenieNames`; `addRecent()` can therefore leave the displayed recents stale until `genies` changes. | Subscribe to `recentGenieNames` or expose a reactive recent-genies selector. | pre-existing line | fixed | 3 |
| 609 | src/components/GeniePicker/GeniePicker.tsx | 156 | Low | Shortcuts & Patches | `"Uncategorized"` is hardcoded user-facing English and duplicates the store fallback. | Use a translated fallback supplied by shared grouping logic. | pre-existing line | fixed | 3 |
| 610 | src/components/GeniePicker/GeniePicker.tsx | 193 | High | Logic & Correctness | `settleInvocation` has no invocation/open-session ownership; an older canceled or workflow invocation can settle later and close a newly reopened picker that happens to be in input mode. | Associate settlement with an invocation/session token and mutate the picker only if that token is still current. | changed line | fixed | 1 |
| 611 | src/components/GeniePicker/GeniePicker.tsx | 207 | High | Logic & Correctness | Accept operates on the globally focused suggestion, not the suggestion created for the displayed preview; the suggestion store deliberately preserves an older focused ID, so Accept can apply an unrelated edit. | Store the current invocation’s suggestion ID in picker state and accept that exact ID. | changed line | fixed | 1 |
| 612 | src/components/GeniePicker/GeniePicker.tsx | 216 | High | Logic & Correctness | Retry from preview only resets picker state; it leaves the previous suggestion in the suggestion store, allowing stale edits to survive and later be accepted. | Reject/remove the preview’s exact suggestion before returning to input. | changed line | fixed | 1 |
| 613 | src/components/GeniePicker/GeniePicker.tsx | 220 | High | Logic & Correctness | Cancel calls the invocation store’s state reset instead of the `cancel` function returned by `useGenieInvocation`; it neither unregisters the stream listener nor sends backend cancellation. The Escape path repeats this at line 234. | Destructure and call the hook’s full `cancel()` operation from both paths. | changed line | fixed | 1 |
| 614 | src/components/GeniePicker/GeniePicker.tsx | 225 | High | Refactoring Debt | The keyboard callback is over 60 lines with a mode branch and a long key-condition chain, giving it high cyclomatic complexity. | Extract response-mode handling and table-driven input-mode commands. | changed line | fixed | 3 |
| 615 | src/components/GeniePicker/GeniePicker.tsx | 225 | High | Logic & Correctness | The keyboard handler is attached to the entire dialog and does not scope by event target; Enter/Tab/arrows from provider or response buttons are intercepted, and response mode prevents every non-Escape default, blocking keyboard activation and copying. | Bind navigation to the textarea or ignore descendant-control events except explicitly global Escape handling. | changed line | fixed | 1 |
| 616 | src/components/GeniePicker/GeniePicker.tsx | 259 | Medium | Logic & Correctness | Enter treats an empty `flatList` as freeform even while `loading` is true, so a user can submit text as freeform before genies finish loading — even if the eventual list contains a matching genie. | Suppress freeform confirmation/submission until loading completes. | pre-existing line | fixed | 3 |
| 617 | src/components/GeniePicker/GeniePicker.tsx | 294 | Medium | Logic & Correctness | History, dropdown, or ghost-text changes update `filter` without clearing `freeformConfirmed`; after confirming one prompt, changing it through history allows the replacement prompt to submit on its first Enter. | Reset confirmation whenever history changes the displayed prompt. | pre-existing line | fixed | 3 |
| 618 | src/components/GeniePicker/GeniePicker.tsx | 305 | High | Logic & Correctness | Outside-click dismissal calls `handleClose()` during processing without canceling the listener or provider; the hidden invocation can continue and create a suggestion after dismissal. | Make processing dismissal invoke the full cancellation path before closing. | changed line | fixed | 1 |
| 619 | src/components/GeniePicker/GeniePicker.tsx | 312 | Low | Logic & Correctness | The scroll effect depends only on `selectedIndex`; replacing the filtered/scoped list while retaining the same index does not scroll the newly selected DOM item into view. | Depend on the relevant list identity as well as the index. | pre-existing line | fixed | 3 |
| 620 | src/components/GeniePicker/GeniePicker.tsx | 366 | Medium | Logic & Correctness | The textarea remains a combobox in response mode and can report an expanded list and active descendant even though `genie-picker-list` is not rendered. | Disable/hide the combobox in response mode or gate its ARIA attributes on `isInputMode`. | changed line | fixed | 2 |
| 621 | src/components/GeniePicker/GeniePicker.tsx | 397 | Low | Logic & Correctness | The no-match hint uses raw `filter` truthiness, while submission requires `filter.trim()`; whitespace-only input displays an actionable freeform hint whose Enter action does nothing. | Derive both rendering and submission from one trimmed `hasPrompt` value. | pre-existing line | fixed | 3 |
| 622 | src/components/GeniePicker/GeniePicker.tsx | 465 | High | Logic & Correctness | Preview Reject is wired directly to `handleClose`, so it never calls `rejectSuggestion` and leaves the supposedly rejected suggestion active. | Reject the exact preview suggestion before closing. | changed line | fixed | 1 |
| 623 | src/components/GeniePicker/invocationLifecycle.ts | 30 | Low | Logic & Correctness | `run` is evaluated before entering `settleInvocation`, so a synchronous exception bypasses both `onError` and the cleanup in `finally`. | Accept a thunk and invoke it inside a `try` or promise callback. | new file | fixed | 3 |
| 624 | src/components/GeniePicker/invocationLifecycle.ts | 34 | High | Logic & Correctness | A settling invocation reads the global current picker state, so an older invocation can close a newly reopened picker when the new session is in `search` or `freeform` mode. | Associate each invocation with a session/generation token and close only when that token still owns the open picker. | new file | fixed | 1 |
| 625 | src/components/KnowledgeBasePanel/KnowledgeBasePanel.tsx | 78 | Medium | Refactoring Debt | `KnowledgeBasePanel` spans 58 lines and contains five independent lifecycle branches, weakening exhaustive state handling and making inconsistent state combinations render an empty body. | Move lifecycle dispatch into an exhaustive status switch or dedicated state renderer. | changed line | fixed | 3 |
| 626 | src/components/KnowledgeBasePanel/KnowledgeBasePanel.tsx | 87 | Medium | Logic & Correctness | The runtime probe runs once on panel mount regardless of server status and is not refreshed when the status later becomes `stopped`; the stopped view can therefore use a stale readiness result. | Enable or re-run the probe when entering the stopped state and avoid probing while it cannot be displayed. | changed line | fixed | 2 |
| 627 | src/components/KnowledgeBasePanel/KnowledgeBasePanelViews.tsx | 38 | Medium | Logic & Correctness | Multiplying `done` by 100 before division can overflow for valid finite byte counts; for example, a half-complete transfer near `Number.MAX_VALUE` is reported as 100%. | Calculate `Math.floor((done / total) * 100)` before clamping. | new file | not-fixed (rebuttal: the suggested Math.floor((done/total)*100) is WORSE — measured, 29/100 renders as 28% and 57/100 as 56%; the current (100*done)/total is exact for real byte counts, and the overflow needs a ~1e307-byte download) | 2 |
| 628 | src/components/KnowledgeBasePanel/KnowledgeBasePanelViews.tsx | 62 | Low | Logic & Correctness | The public prop permits `error === null`, but that produces a blank alert containing only Retry. | Require a non-null error string or render a localized fallback message. | new file | fixed | 3 |
| 629 | src/components/KnowledgeBasePanel/KnowledgeBasePanelViews.tsx | 80 | Medium | Refactoring Debt | `KnowledgeBaseRunning` spans 66 lines and combines toolbar actions, view-mode mutation, lazy-loading recovery, and iframe authentication rendering. | Extract the toolbar and content surface into focused components. | new file | fixed | 3 |
| 630 | src/components/KnowledgeBasePanel/KnowledgeBasePanelViews.tsx | 95 | High | Duplication | Six toolbar buttons duplicate the same type, class, label, and click-handler structure; the site/graph pair and four action buttons are copy-paste variants. | Introduce a small toolbar-button abstraction or render typed button descriptors. | new file | fixed | 3 |
| 631 | src/components/KnowledgeBasePanel/KnowledgeBasePanelViews.tsx | 139 | Medium | Logic & Correctness | `iframeUrl` is documented as a one-time authentication URL but is retained as the iframe source; switching to graph and back remounts the iframe with the consumed nonce. | Track consumption locally or clear `iframeUrl` after successful authentication so later mounts use `url`. | new file | fixed | 2 |
| 632 | src/components/KnowledgeBasePanel/KnowledgeBaseRuntimeState.tsx | 79 | Low | Refactoring Debt | The discriminated-union switch has no exhaustive `never` check; adding a new probe phase can compile while silently rendering nothing. | Add an `assertNever` default path or an explicitly exhaustive return helper. | new file | fixed | 3 |
| 633 | src/components/KnowledgeBasePanel/runtimeState.ts | 52 | Medium | Logic & Correctness | The guard accepts contradictory reports such as `cli: "ready"` with `cliSource: null`, allowing malformed payloads to pass `isRuntimeReady`. | Enforce cross-field invariants, preferably with discriminated unions. | new file | fixed | 2 |
| 634 | src/components/Sidebar/FileExplorer/FileExplorer.tsx | 75 | Medium | Refactoring Debt | `FileExplorer` spans 121 lines, exceeding the requested 30-line function threshold and mixing subscriptions, state restoration, imperative APIs, and rendering. | Extract the tree panel and imperative-action adapter. | pre-existing line | not-fixed (rebuttal: subscriptions plus JSX are all that remain; every other concern is already useFileTree/useTreeWiring/useFileExplorerOpenState/useExplorerContextMenu/useExplorerActionWiring/useQuickLookHotkey) | 3 |
| 635 | src/components/Sidebar/FileExplorer/FileExplorer.tsx | 118 | Medium | Logic & Correctness | Scroll restoration runs whenever `isLoading` becomes false, including routine watcher refreshes; it can overwrite a newer, not-yet-persisted scroll position. | Restore only once per workspace/root generation. | changed line | fixed | 2 |
| 636 | src/components/Sidebar/FileExplorer/FileExplorer.tsx | 133 | Medium | Logic & Correctness | The imperative creation methods discard promises while their public interface declares `void`, hiding completion and possible rejection from callers. | Return `Promise<void>` or catch and report failures inside the exposed methods. | pre-existing line | fixed | 3 |
| 637 | src/components/Sidebar/FileExplorer/FileExplorer.tsx | 160 | Medium | Logic & Correctness | `onScrollCapture` persists `e.target.scrollTop` for every descendant scroll event; a scrolling rename input or other nested element can overwrite the tree offset with zero. | Ignore events whose target is not the designated tree scroller. | pre-existing line | fixed | 3 |
| 638 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 45 | Medium | Refactoring Debt | The hook spans 143 lines and owns unrelated opening, creation, menu-dispatch, deletion, rename, and movement policies. | Split it into focused action hooks or pure callback factories. | new file | not-fixed (rebuttal: 258 lines, under the 300 cap; the callbacks are one cohesive contract — everything FileExplorer wires from the tree and the context menu — and splitting them would thread openFileByType, treeRef and the operations between hooks without removing any rule that exists twice) | 3 |
| 639 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 122 | Medium | Logic & Correctness | `getParentDir(path) ?? undefined` preserves an empty string, producing an invalid or unintended dialog directory for filesystem-root files; Windows drive roots are also unsafe through this helper. | Resolve the parent with the platform path API or convert empty results with `// undefined`. | new file | fixed | 2 |
| 640 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 123 | Low | Shortcuts & Patches | The forced `Promise<string | null>` cast suppresses the dialog API’s inferred contract and could hide a future array-valued result. / Remove the cast and narrow the returned value explicitly if necessary. | new file | fixed | 3 |
| 641 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 140 | Medium | Logic & Correctness | Activation discards `openFileByType` without a terminal rejection handler; react-arborist does not await activation callbacks. | Attach `.catch(...)` at this fire-and-forget boundary. | new file | fixed | 2 |
| 642 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 153 | High | Logic & Correctness | Multi-delete invokes the single-item confirmation flow repeatedly and ignores each Boolean result, so cancellation or failure can still be followed by deletion of later selections, leaving an unexpected partial batch. | Confirm and execute the batch once, or stop immediately on cancellation/failure. | new file | fixed | 1 |
| 643 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 156 | Medium | Logic & Correctness | The delete handler ignores react-arborist’s supplied node snapshot and re-resolves each ID from mutable tree state, silently skipping nodes that disappear during preceding awaits. | Accept and use the handler’s `nodes` argument. | new file | fixed | 2 |
| 644 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 166 | High | Logic & Correctness | The asynchronous move handler has no rejection boundary or in-flight guard, while react-arborist invokes `onMove` without awaiting it; rapid drops can launch concurrent filesystem moves. | Use a synchronous wrapper around a caught, serialized move worker and reject re-entry while active. | new file | fixed | 1 |
| 645 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 170 | High | Logic & Correctness | Raw multi-selection IDs are moved sequentially without removing descendants of selected folders; child-first ordering moves the child out separately, while parent-first ordering produces a failed second move. | Reduce the selection to top-level paths before moving. | new file | fixed | 1 |
| 646 | src/components/Sidebar/FileExplorer/useExplorerActionWiring.ts | 178 | Low | Dead Code | `openFileByType` is returned publicly although the sole consumer does not read it; it is only needed internally by this hook. | Remove it from the returned object. | new file | fixed | 3 |
| 647 | src/components/Sidebar/FileExplorer/useExplorerContextMenu.ts | 40 | Low | Refactoring Debt | The hook exceeds the requested 30-line function threshold and mixes workspace synchronization with DOM hit-testing. | Extract a pure event-to-menu-state helper. | new file | fixed | 3 |
| 648 | src/components/Sidebar/FileExplorer/useExplorerContextMenu.ts | 63 | Low | Shortcuts & Patches | Context resolution depends on duplicated magic DOM strings, `.file-node` and `data-node-id`, creating an undocumented coupling to row markup. | Share named constants or pass node context through a typed row-level handler. | new file | fixed | 3 |
| 649 | src/components/Sidebar/FileExplorer/useExplorerContextMenu.ts | 64 | Medium | Logic & Correctness | When a DOM row is found but its node cannot be resolved during a tree transition, the code falls back to the workspace menu, enabling root-level creation under the wrong apparent target. | Distinguish “empty area” from “unresolved row” and abort or close for the latter. | new file | fixed | 2 |
| 650 | src/components/Sidebar/FileExplorer/useFileTree.ts | 129 | Medium | Refactoring Debt | The 148-line hook combines loading, request invalidation, scheduling, watcher lifecycle, focus handling, and presentation-state gating. | Extract loading/scheduler and focus-refresh concerns into focused hooks. | pre-existing line | fixed | 3 |
| 651 | src/components/Sidebar/FileExplorer/useFileTree.ts | 178 | Low | Logic & Correctness | Stale requests are excluded from state updates but still log truncation or failure at lines 178 and 188, producing misleading diagnostics after a root change. | Check the request ID before logging or include the originating root in explicitly stale diagnostics. | changed line | fixed | 3 |
| 652 | src/components/Sidebar/FileExplorer/useFileTree.ts | 205 | Medium | Logic & Correctness | Clearing `rootPath` during an active scan never resets `isLoading`; the invalidated request’s `finally` also refuses to reset it, leaving loading true indefinitely. | Set `isLoading(false)` and reset `treeRoot` in the null-root branch. | pre-existing line | fixed | 3 |
| 653 | src/components/Sidebar/FileExplorer/useFileTree.ts | 275 | Medium | Logic & Correctness | Only `tree` is gated by `treeRoot`; `error` and `truncated` from the previous workspace remain visible until the new scan completes. | Associate all result state with its root or gate every returned status by `treeRoot === rootPath`. | changed line | fixed | 2 |
| 654 | src/components/WorkspaceRail/WorkspaceRailContextMenu.tsx | 41 | Medium | Refactoring Debt | The 113-line component combines positioning, focus restoration, dismissal, keyboard navigation, action dispatch, and rendering. | Extract reusable positioning and menu-focus hooks. | pre-existing line | fixed | 3 |
| 655 | src/components/WorkspaceRail/WorkspaceRailContextMenu.tsx | 56 | Medium | Logic & Correctness | The invoker is captured only on first mount; reusing the mounted menu for another workspace restores focus to the old element, and pointer context menus may never make the intended invoker active. | Pass the invoking element explicitly and update it for every menu opening. | pre-existing line | fixed | 3 |
| 656 | src/components/WorkspaceRail/WorkspaceRailContextMenu.tsx | 70 | Low | Logic & Correctness | Clamping is recomputed only when `position` changes, so viewport resizing or translated-label size changes can leave the menu off-screen. | Recalculate on resize and observe menu size changes. | pre-existing line | fixed | 3 |
| 657 | src/components/WorkspaceRail/WorkspaceRailContextMenu.tsx | 102 | Medium | Logic & Correctness | Tab can move focus outside the menu without dismissing it because only Escape and mouse-down are handled. | Close on Tab or when focus leaves the menu. | pre-existing line | fixed | 3 |
| 658 | src/components/WorkspaceRail/WorkspaceRailContextMenu.tsx | 120 | Medium | Logic & Correctness | TypeScript permits async functions where `() => void` is expected, but `action()` is neither awaited nor caught, allowing rejected workspace operations to become unhandled promises. | Accept `void / Promise<void>` explicitly and catch/report asynchronous failures. | pre-existing line | fixed | 3 |
| 659 | src/components/WorkspaceRail/WorkspaceRailContextMenu.tsx | 137 | Low | Logic & Correctness | Using the translated label as the React key remounts every item after a locale change and can drop keyboard focus. | Use the stable `item.action` identifier as the key. | pre-existing line | fixed | 3 |
| 660 | src/export/exportLock.ts | 131 | High | Logic & Correctness | Release performs a non-atomic owner read followed by removal; a stale takeover can install a successor between them, causing this export to delete the successor’s lock. | Implement ownership-checked release as a native atomic locking operation held for the transaction. | new file | not-fixed (out of reach: needs a native ownership-checked release; the fs plugin has no atomic compare-and-swap. Release is already owner-checked and the residual window is one filesystem round trip, opening only after this export's own lock is >=5 min stale) | 1 |
| 661 | src/export/exportLock.ts | 157 | High | Logic & Correctness | `holdsExportLock` is only a point-in-time read; a live export can pass it and then have its five-minute-old lock stolen while publishing, allowing concurrent publishers. | Use an OS-backed lock held through publication or a native renewable lease that cannot expire during active work. | new file | not-fixed (rebuttal: the advisory posture is the documented design — publish() re-checks ownership immediately before publishing, and a heartbeat was rejected because a backgrounded webview timer is throttled, which would invite the seizure it prevents) | 1 |
| 662 | src/export/exportLock.ts | 168 | Medium | Refactoring Debt | The over-30-line takeover function interleaves observation, mutation, rollback, contention classification, and cleanup in concurrency-critical code. | Move the protocol into a small native atomic primitive with explicit typed outcomes. | new file | fixed | 3 |
| 663 | src/export/exportLock.ts | 175 | Medium | Logic & Correctness | Every rename error is treated as a lost race; permission, read-only-filesystem, and I/O failures are silently converted into repeated polling and a misleading contention timeout. | Suppress only a confirmed source-not-found race and rethrow other failures. | new file | fixed | 2 |
| 664 | src/export/exportLock.ts | 176 | High | Logic & Correctness | A lock can change between the stale read and rename, so takeover temporarily removes a new live owner’s lock; `putBack` is non-atomic and may permanently discard it, opening the destination to another exporter. | Replace the read/rename/put-back protocol with native compare-and-swap or an actual filesystem lock. | new file | fixed | 1 |
| 665 | src/export/exportLock.ts | 190 | High | Logic & Correctness | If writing the replacement lock fails for a non-contention reason, the code deletes the moved-aside prior lock instead of restoring it, leaving a potentially live export unprotected. | Restore the aside lock on acquisition failure and fail closed if restoration cannot be proven. | new file | fixed | 1 |
| 666 | src/export/exportLock.ts | 205 | High | Logic & Correctness | Staleness validates only numeric `acquiredAt`; valid JSON lacking a legitimate owner or application marker is treated as a stale lock and eventually deleted. | Validate an exact versioned schema, including a non-empty owner and VMark-specific marker, before takeover. | new file | fixed | 1 |
| 667 | src/export/exportLock.ts | 258 | Medium | Shortcuts & Patches | `/exist/i` also matches non-contention messages such as “does not exist,” converting real path failures into contention waits. | Match typed error codes or narrowly recognize `EEXIST`, “File exists,” and “already exists.” | new file | fixed | 3 |
| 668 | src/export/exportPublish.ts | 57 | Medium | Refactoring Debt | The 50-line transaction function combines path construction, directory creation, backup policy, publication, rollback, and error reporting. | Split validation/planning from execution and rollback. | new file | not-fixed (rebuttal: rollback, reporting, directory creation and restore are already separate functions; what remains is one transaction loop whose ordering IS the correctness argument) | 3 |
| 669 | src/export/exportPublish.ts | 68 | High | Logic & Correctness | `tracked` entries are trusted without rejecting empty paths, `.`/`..`, traversal, or internal metadata names; crafted entries can escape the destination or replace `.vmark-export.lock`. | Normalize and validate every relative path and reject reserved names before any mutation. | new file | fixed | 1 |
| 670 | src/export/exportPublish.ts | 68 | High | Logic & Correctness | Duplicate or case-equivalent tracked paths are not rejected; a duplicate can overwrite the original backup and then report a nonexistent backup, causing data loss. | Deduplicate using target-filesystem path semantics before publishing. | new file | fixed | 1 |
| 671 | src/export/exportPublish.ts | 69 | High | Logic & Correctness | Containment is lexical only; a symlinked destination ancestor can redirect rename operations outside the selected export directory. | Canonicalize and verify ancestors or perform no-follow, destination-scoped operations in Rust. | new file | not-fixed (rebuttal: the destination is chosen by the user in a folder picker, so writing through a symlink they picked is the requested behaviour; the lexical walk in ensureDir only decides which dirs THIS export created, for rollback) | 1 |
| 672 | src/export/exportPublish.ts | 79 | High | Logic & Correctness | `exists` does not distinguish files from directories; a directory collision is moved into staging and deleted after success, potentially erasing an unrelated subtree. | Inspect entry type and refuse or explicitly preserve directory collisions. | new file | fixed | 1 |
| 673 | src/export/exportPublish.ts | 79 | High | Logic & Correctness | The `exists` check and subsequent renames are non-atomic; an external writer can create or replace `final` between operations, after which its file is overwritten without backup. | Use native no-replace/identity-checked operations or publish through an atomic directory swap. | new file | not-fixed (out of reach: needs a native no-replace/identity-checked rename or an atomic directory swap; the fs plugin exposes only a replacing rename. The destination lock is the documented mitigation for a second VMark) | 1 |
| 674 | src/export/exportPublish.ts | 131 | Medium | Logic & Correctness | A failed restore unconditionally claims the prior contents remain at `backup`; the backup may itself be missing, especially after duplicate-path or external interference. | Verify backup existence before retaining or describing it and report its actual state. | new file | fixed | 2 |
| 675 | src/export/exportPublish.ts | 143 | Medium | Logic & Correctness | Failures removing created directories are only logged, so the thrown message can claim the folder was fully restored while export-created directories remain. | Return cleanup failures from `removeQuietly` and include them in the rollback result. | new file | fixed | 2 |
| 676 | src/export/exportPublish.ts | 190 | Medium | Logic & Correctness | `ensureDir` records directories only after one recursive `mkdir` succeeds; partial creation on failure is untracked, while concurrent creation can be incorrectly recorded as export-owned and removed during rollback. | Create missing directories individually and record each only after confirmed creation. | new file | fixed | 2 |
| 677 | src/export/exportPublish.ts | 207 | High | Duplication | `removeTree` and `removeQuietly` repeat the same catch-and-log wrapper, while another near-identical `removeQuietly` exists at `src/export/exportLock.ts:242`. | Consolidate removal and warning behavior into one parameterized helper. | new file | fixed | 3 |
| 678 | src/export/exportStaging.ts | 132 | High | Logic & Correctness | `path()` accepts unchecked `../` or Windows backslash traversal, allowing writes outside the staging root and subsequent publication outside the destination | Normalize and reject absolute, empty, dot-segment, and escaping paths. | new file | fixed | 1 |
| 679 | src/export/exportStaging.ts | 133 | High | Logic & Correctness | `track()` permits duplicate paths; a duplicate can overwrite the original backup during publication and make rollback lose pre-existing content | Store paths in a validated `Set`, rejecting duplicates before publication. | new file | fixed | 1 |
| 680 | src/export/exportStaging.ts | 136 | Medium | Logic & Correctness | The stage has no lifecycle state, so concurrent or repeated `publish()`/`discard()` calls can remove files or release the lock while another operation is active | Add explicit open, publishing, retained, published, and discarded states with guarded transitions. | new file | not-fixed (rebuttal: the stage never escapes exportHtmlStaged, which is wrapped in runExclusive and calls publish() once in the try and discard() once in the catch; releaseExportLock and removeTree are already no-ops when repeated, so the five-state machine would add states no caller can reach) | 2 |
| 681 | src/export/exportStaging.ts | 142 | High | Logic & Correctness | Lock ownership is checked once before a sequence of awaited renames; a stale-lock takeover after this check allows two publishers to mutate the destination concurrently | Atomically renew a publish lease and verify or refresh ownership throughout publication. | new file | not-fixed (rebuttal: same as 661 — the check exists precisely to shrink exposure from the whole export to the publish, and is documented as doing exactly that) | 1 |
| 682 | src/export/exportStaging.ts | 148 | High | Logic & Correctness | The multi-file transaction relies on catch-based rollback; a webview or process crash during publication leaves a partially updated destination and stranded backups | Persist a recovery journal and recover it in `openStage`, or publish through an atomic directory swap. | new file | not-fixed (out of reach: needs a persisted recovery journal read back in openStage, or an atomic directory swap — a new subsystem, not a targeted edit. Real: a crash mid-publish leaves a partial destination and stranded backups) | 1 |
| 683 | src/export/exportStaging.ts | 149 | Medium | Shortcuts & Patches | Staging-tree and lock cleanup helpers swallow failures, so `publish()` can report success while sensitive backups remain or the destination remains locked | Return cleanup diagnostics as warnings and schedule/retry recovery instead of logging only. | new file | not-fixed (rebuttal: removeTree/removeQuietly swallow by documented design because publish() has already succeeded and every tracked file is at its final path, so failing there would report a completed publish as failed; both log, and the leftover tree holds only backups of files this export replaced, never the only copy) | 3 |
| 684 | src/export/exportStaging.ts | 164 | High | Duplication | `nonce()` is copied verbatim from `exportLock.ts`, creating unnecessary drift in a filesystem-safety primitive | Move nonce generation to one shared export utility. | new file | fixed | 3 |
| 685 | src/export/htmlExport.ts | 123 | High | Refactoring Debt | `exportHtmlStaged` spans 196 lines and combines staging, resource resolution, font networking, CSS assembly, writes, publication, rollback, metrics, and diagnostics with high branch complexity | Split it into resource, font, document-assembly, and publication units with typed intermediate results. | changed line | fixed | 3 |
| 686 | src/export/htmlExport.ts | 174 | High | Logic & Correctness | `standalone.html` is documented as fully embedded, but the direct resolver leaves remote HTTP(S) images unchanged even in single-file mode | Fetch and embed remote images with size/time limits, or stop claiming the artifact is standalone. | changed line | fixed | 1 |
| 687 | src/export/htmlExport.ts | 289 | Medium | Logic & Correctness | Successful re-export only replaces currently tracked files; removed images and fonts from earlier exports remain in `assets`, potentially retaining obsolete or sensitive content | Publish a manifest and transactionally remove obsolete managed files. | changed line | not-fixed (out of reach: needs a published manifest format plus a DESTRUCTIVE prune of files in the user's chosen folder — a product decision against this module's "never delete what we did not just write" design) | 2 |
| 688 | src/export/htmlExport.ts | 298 | Medium | Logic & Correctness | `totalSize` counts text and font files but omits copied folder images, so it does not represent the documented total size of exported files | Measure all staged files or include sizes of successfully copied resources. | changed line | fixed | 2 |
| 689 | src/export/htmlExport.ts | 319 | Medium | Refactoring Debt | The 319-line file exceeds the project’s approximately 300-line code-file limit | Extract font preparation and result/diagnostic construction into focused modules. | pre-existing line | fixed | 3 |
| 690 | src/export/printDocument.ts | 42 | Medium | Logic & Correctness | Printable HTML bypasses `sanitizeExportHtml`, allowing ProseMirror separators, trailing breaks, hidden preview nodes, and editor-only attributes into print output | Sanitize the body before resource resolution. | new file | fixed | 2 |
| 691 | src/export/printDocument.ts | 42 | Medium | Logic & Correctness | Remote images remain network references and are treated as successfully resolved, so offline or failed-network prints silently omit them | Embed remote resources or explicitly await and report their load failures. | new file | not-fixed (rebuttal: remote-stays-remote is the documented policy of every export path — htmlExport.ts's header states it for standalone.html — and "await their load failures" is not observable from the assembling side; the off-screen print webview owns the load) | 2 |
| 692 | src/export/printDocument.ts | 43 | Medium | Logic & Correctness | The low-level `inlineLocalImages` transformation unexpectedly emits UI toasts, coupling reusable assembly code to presentation behavior | Return the resource report and let operation-level callers display warnings. | new file | not-fixed (rebuttal: the single toast at this choke point IS the fix for #1086; hoisting it to the two callers reinstates the duplicate-warning shape that issue was about) | 2 |
| 693 | src/export/printDocument.ts | 65 | High | Duplication | `buildPrintHtml` reimplements the document shell already present in `pdfHtmlTemplate.ts`, and the copies have already diverged in behavior | Extract one configurable printable-document builder shared by print and PDF. | new file | not-fixed (out of reach: the second copy of the document shell is buildPdfExportHtml in src/export/pdfHtmlTemplate.ts, outside this group; a shell extracted only on the print side would be a third copy) | 3 |
| 694 | src/export/printDocument.ts | 75 | Medium | Shortcuts & Patches | Every print embeds the complete KaTeX stylesheet and base64 font payload even when the document contains no math | Detect math content and include KaTeX assets conditionally. | new file | fixed | 3 |
| 695 | src/export/printDocument.ts | 82 | High | Logic & Correctness | Shared print CSS explicitly depends on details elements being expanded, but this builder never calls `expandDetails`; collapsed sections therefore print without their bodies | Expand details during shared document assembly. | new file | fixed | 1 |
| 696 | src/export/useExportOperations.ts | 50 | Medium | Refactoring Debt | `exportToHtml` is 75 lines and mixes validation, dialog handling, path transformation, rendering, settings, export orchestration, diagnostics, and UI feedback | Extract folder selection/path validation and result presentation into focused helpers. | pre-existing line | fixed | 3 |
| 697 | src/export/useExportOperations.ts | 60 | High | Duplication | The identical empty-markdown guard and toast are repeated in four public operations | Centralize the guard in a shared helper. | pre-existing line | fixed | 3 |
| 698 | src/export/useExportOperations.ts | 86 | High | Logic & Correctness | If the user selects a basename exactly equal to `.html`, stripping the suffix produces the parent directory and can overwrite that directory’s existing `index.html`, `standalone.html`, or assets | Validate the stripped basename and reject or reprompt when it is empty. | pre-existing line | fixed | 1 |
| 699 | src/export/useExportOperations.ts | 113 | Medium | Logic & Correctness | The resource-warning toast uses `result.warnings.length`, which counts warning categories rather than missing resources; three missing images currently produce a count of one | Use `result.missingCount` and report font failures separately. | pre-existing line | fixed | 3 |
| 700 | src/export/useExportOperations.ts | 141 | Medium | Refactoring Debt | `exportToPdf` opens the print dialog while `exportToPdfNative` opens the PDF-export window, making the public API names materially misleading | Rename them to `printDocument` and `openPdfExportDialog`, retaining narrow compatibility wrappers if required. | pre-existing line | not-fixed (rebuttal: the names are defensible — exportToPdf reaches PDF through the system print dialog, exportToPdfNative through the native renderer, and both JSDoc headers say which; the proposed target name printDocument collides with the printDocument.ts module this file imports, and the rename must land in exportCommands.ts, another group's file) | 3 |
| 701 | src/export/useExportOperations.ts | 175 | Medium | Shortcuts & Patches | Native-PDF preparation failures expose only a generic toast while discarding the normalized actionable error available to the HTML and print paths | Use `errorDetail` or `showError` with `commandErrorMessage(error)`. | changed line | fixed | 3 |
| 702 | src/export/useExportOperations.ts | 227 | High | Duplication | The browser print render branch manually repeats the render-then-inline sequence already encapsulated by the imported `renderPrintableHtml` helper | Use `renderPrintableHtml` for the rendered branch and `inlineLocalImages` only for live-editor HTML. | changed line | fixed | 3 |
| 703 | src/export/useExportOperations.ts | 260 | Medium | Logic & Correctness | `copyAsHtml` does not sanitize the rendered ProseMirror markup, unlike folder export | Apply `sanitizeExportHtml` before writing to the clipboard. | changed line | fixed | 2 |
| 704 | src/export/useExportOperations.ts | 260 | Medium | Logic & Correctness | `copyAsHtml` neither accepts a source path nor resolves images, so copied markup can contain relative or Tauri-only asset URLs that break outside VMark | Accept `sourceFilePath` and inline resources, or explicitly expose this as raw markup copying. | changed line | fixed | 2 |
| 705 | src/export/waitForAssets.ts | 126 | Low | Dead Code | A commented-out error-state query remains in production code. | Delete it; the surrounding comment already documents that terminal errors count as ready. | pre-existing line | fixed | 3 |
| 706 | src/export/waitForAssets.ts | 183 | Medium | Refactoring Debt | `waitForAssets` is approximately 100 lines and coordinates five nested, stateful timer/reporting closures, making deadline races difficult to reason about. | Extract a small polling state machine and separate status-to-warning conversion. | pre-existing line | fixed | 3 |
| 707 | src/export/waitForAssets.ts | 187 | Medium | Logic & Correctness | Numeric options are not validated; `timeout: NaN` makes the timeout comparison permanently false, while nonpositive intervals create near-continuous polling. | Reject or normalize nonfinite/negative timeouts and require a positive finite interval. | pre-existing line | fixed | 3 |
| 708 | src/export/waitForAssets.ts | 246 | High | Logic & Correctness | The layout-deadline branch records “Layout did not settle” but can still return `success: true`, allowing export without the promised two-frame layout stabilization. | Return failure when the layout deadline wins, or represent `layoutReady` separately and include it in success. | changed line | not-fixed (rebuttal: success means the ASSETS are ready; the layout deadline exists so throttled/suspended frames do not hang an otherwise-ready export, and it already re-reads status and records the warning — returning false would re-break the case the deadline was added for) | 1 |
| 709 | src/export/waitForAssets.ts | 277 | Medium | Logic & Correctness | The next poll always waits the full interval, so a valid interval larger than the remaining budget can exceed the documented maximum timeout substantially. | Schedule with `Math.min(interval, timeout - elapsed)` or use a dedicated absolute-deadline timer. | pre-existing line | fixed | 3 |
| 710 | src/hooks/useCommandBootstrap.ts | 135 | Medium | Refactoring Debt | The effect body spans roughly 110 lines and mixes command registration, debug publication, service startup, asynchronous format discovery, listener mounting, readiness, and teardown. | Extract a transactional bootstrap function with one explicit lifecycle/disposer object. | changed line | not-fixed (rebuttal: the effect's 14 tests cover every branch it has including the StrictMode-cancel race, and the file is 277/300; the remedy's "one explicit lifecycle/disposer object" would move cancelled/unlisten across a function boundary, which is exactly what the #711/#359 ordering needs visible) | 3 |
| 711 | src/hooks/useCommandBootstrap.ts | 181 | High | Logic & Correctness | Cancellation is checked only after `mountMenuCommands` finishes mounting every listener; an unmounted or StrictMode-cancelled pass can therefore overlap the live pass and double-dispatch menu commands. | Check cancellation before mounting and make mounting abortable or immediately rollback when cancellation occurs. | changed line | fixed | 1 |
| 712 | src/hooks/useCommandBootstrap.ts | 200 | Medium | Logic & Correctness | Pandoc expansion failure is logged but not included in the readiness verdict, so line 231 can signal a complete menu even though all dynamic Pandoc bindings are absent. | Track expansion success and signal readiness only when expansion and listener mounting are both complete. | changed line | fixed | 2 |
| 713 | src/hooks/useCommandBootstrap.ts | 240 | Low | Shortcuts & Patches | Cleanup leaves the three global development debug handles published, so an unmounted bootstrap remains callable through stale automation hooks. | Make debug publication return an ownership-aware disposer and invoke it during cleanup. | changed line | not-fixed (rebuttal: none of the three handles closes over bootstrap state — runCommand, closeBrowserTab and forgetRecentWorkspace are all module-level; they live on the window object that unmount destroys, a StrictMode remount republishes them, and publishDebugHandle is DEV-gated so nothing ships) | 3 |
| 714 | src/hooks/useContentServer.ts | 104 | High | Refactoring Debt | `startServer` has high cyclomatic complexity: generation checks, trust reconciliation, nested error handling, authentication, and store transitions are interleaved across a long callback. | Extract a generation controller and a pure start/reconcile operation that returns one final state to commit. | changed line | fixed | 3 |
| 715 | src/hooks/useContentServer.ts | 125 | Medium | Logic & Correctness | Starting does not clear the previous iframe URL; after line 135 marks the new server running, the old port/nonce URL remains observable until the new authentication request settles. | Clear `iframeUrl` when entering `starting`, before exposing the new running handle. | changed line | fixed | 2 |
| 716 | src/hooks/useContentServer.ts | 135 | High | Logic & Correctness | A returned server is published as running and an authentication URL is minted before its reported trust is compared with current workspace trust, transiently exposing a server with the wrong CSP. | Validate trust immediately after `startContentServer`; reconcile before committing the handle or requesting an auth URL. | changed line | fixed | 1 |
| 717 | src/hooks/useContentServer.ts | 149 | High | Logic & Correctness | Trust enforcement fails open: a nonboolean `handle.trusted`, or a mismatch after the retry cap, is accepted as success. | Runtime-validate the field and fail closed — stop or quarantine the child and surface an error — when trust cannot be reconciled. | changed line | fixed | 1 |
| 718 | src/hooks/useContentServer.ts | 173 | Medium | Logic & Correctness | A stop result is guarded only by lifecycle generation; workspace changes do not increment that generation, so a failed stop for workspace A can set an error on workspace B. | Also compare the current root with the captured root, or invalidate lifecycle generations on every workspace change. | changed line | fixed | 2 |
| 719 | src/hooks/useContentServer.ts | 175 | High | Logic & Correctness | A failed stop may leave the child alive, yet `setError` changes its status away from `running`; subsequent trust changes are then ignored by workspace synchronization. | Re-query backend status and model operational state separately from the stop error. | changed line | fixed | 1 |
| 720 | src/hooks/useContentServer.ts | 201 | Low | Logic & Correctness | An `openInBrowser` failure is committed after an arbitrary workspace switch, potentially displaying workspace A’s failure in workspace B. | Capture the root and discard the error if the active root changed. | changed line | not-fixed (rebuttal: the file's own comment says a toast is "request-independent" and is why #719 chose it; an openInBrowser toast is feedback for the click the user just made, and discarding it after a workspace switch makes the action fail silently — the defect #719/#758 fixed in the other direction) | 3 |
| 721 | src/hooks/useContentServer.ts | 225 | High | Logic & Correctness | The exit listener checks only the current root, not local running state or a server-instance token; a delayed or externally generated same-root exit can stop and restart a newer or locally stopped session. | Track an owned server generation/instance identifier and ignore events not belonging to that active session. | changed line | not-fixed (out of reach: the content-server:exited payload is {workspaceRoot, code} — telling instances apart needs a server-instance id added Rust-side in content_server/supervisor.rs) | 1 |
| 722 | src/hooks/useContentServer.ts | 245 | Medium | Incomplete Error Handling | Failure to install the sole crash listener is only written to debug output, leaving the UI reporting a running server whose crashes will never update or restart it. | Surface a persistent supervision-disabled state or user-visible error and provide a retry path. | changed line | not-fixed (rebuttal: contentServerWarn is warn-tier, so it already reaches the production log file per utils/debug's header; and a "supervision-disabled" store state is the move useContentServer's header records as harmful, since useContentServerWorkspaceSync acts only on running) | 3 |
| 723 | src/hooks/useContentServer.ts | 250 | Medium | Logic & Correctness | Hook cleanup detaches the listener but does not invalidate pending start/stop operations, which can still commit store changes after unmount. | Increment the lifecycle generation and mark the hook disposed during unmount. | changed line | not-fixed (rebuttal: the store is a module singleton describing an app-wide root-keyed Rust child that outlives the hook, and each instance owns its own lifecycleGen; the proposed fix is actively harmful — bumping the generation on unmount makes abandon() no-op too, leaving the status stuck at starting for a server that is running) | 2 |
| 724 | src/hooks/useContentServer.ts | 254 | Medium | Logic & Correctness | Depending on `t` tears down and asynchronously reinstalls the non-buffered exit listener when translation identity changes, creating a window in which crash events are lost. | Keep translation access in a ref and mount the listener once. | changed line | fixed | 2 |
| 725 | src/hooks/useContentServerWorkspaceSync.ts | 43 | High | Logic & Correctness | Trust synchronization is restricted to `status === "running"`, although the parent explicitly allows a stop failure to leave a live child represented as `"error"`. | Base synchronization on an actual managed-root/backend-running state, not the UI status discriminant. | new file | fixed | 1 |
| 726 | src/hooks/useContentServerWorkspaceSync.ts | 44 | Medium | Logic & Correctness | A workspace switch clears only the store and does not invalidate the parent operation token; an A→B→A switch before an in-flight A start resolves passes the parent’s root-equality check and resurrects stale state. | Advance a workspace-generation token on every root change and include it in supersession checks. | new file | not-fixed (rebuttal: ContentServerManager is app-wide and keyed by root, so after A-B-A the in-flight start's handle still describes a live child for A; committing running records the truth, not stale state) | 2 |
| 727 | src/hooks/useContentServerWorkspaceSync.ts | 45 | Medium | Shortcuts & Patches | Switching workspaces deliberately never releases the previous server, so repeatedly serving and leaving workspaces can accumulate child processes until application exit. | Introduce per-window leases/reference counting and stop a root when its final consumer releases it. | new file | not-fixed (rebuttal: the header on the cited lines states the no-stop verdict and why — ContentServerManager is app-wide and keyed by ROOT so another window may be serving it, the manager reuses the child on return, and shutdown_all reaps at exit; the child count is bounded by distinct roots served, not by switches) | 3 |
| 728 | src/hooks/useGenieInvocation.ts | 96 | High | Logic & Correctness | Concurrent workflow invocations overwrite the singleton execution ID and clear prior progress while both backend workflows continue running. | Add an atomic workflow lock or explicitly cancel/queue the existing execution before `setExecution`. | changed line | fixed | 1 |
| 729 | src/hooks/useGenieInvocation.ts | 139 | Medium | Logic & Correctness | Source mode is checked before awaiting provider detection; the user can enter source mode during that wait, after which extraction and Tiptap application still proceed. | Recheck source mode immediately after `ensureProvider()` and before extraction. | changed line | fixed | 2 |
| 730 | src/hooks/useGenieInvocation.ts | 177 | Medium | Logic & Correctness | `plan(extracted)` executes before `runGenieStream` acquires its invocation lock, so line 221 records a genie as recent even when the invocation is refused or provider validation fails. | Make the stream runner return an accepted/dispatched result and record recency only after acceptance. | changed line | fixed | 2 |
| 731 | src/hooks/useGenieInvocation.ts | 180 | Medium | Refactoring Debt | `useGenieInvocation` is 76 lines and combines lifecycle ownership, cancellation, workflow routing, prompt construction, and public callbacks. | Extract invocation lifecycle/cancellation and prompt builders into focused hooks or services. | changed line | not-fixed (rebuttal: no duplication and no untested branch — 4 sibling test files cover it; the 82 lines are mostly audit reasoning in comments, which an extraction would scatter, and the file is 277/300. The one real duplication it named, the provider payload, is fixed under #762) | 3 |
| 732 | src/hooks/useGenieInvocation.ts | 186 | Medium | Logic & Correctness | The Tauri unlisten function is called directly even though it may return a rejected promise, creating an unhandled rejection during cancellation. | Use `safeUnlisten` and clear the ref in `finally`. | changed line | fixed | 2 |
| 733 | src/hooks/useGenieInvocation.ts | 196 | Medium | Logic & Correctness | Every hook instance cancels the global invocation on unmount, even when that invocation belongs to another mounted instance and this instance owns no listener. | Track the locally owned request ID and cancel only when it still matches the global active request. | changed line | not-fixed (rebuttal: both consumers are window-lifetime — GeniePickerOverlay is mounted unconditionally in App.tsx's overlays and GenieShortcutsRunner inside MainWindowRunners — so no instance outlives another's run; gating cancel on local ownership would break the picker's Cancel button, which deliberately cancels a run started from the Genies menu) | 2 |
| 734 | src/hooks/useGenieShortcuts.ts | 31 | Low | Shortcuts & Patches | `getMenuShortcuts` silently converts every unexpected store/conversion exception into “use backend defaults,” potentially re-enabling a shortcut with no diagnostic. | Log the exception and distinguish conversion failure from an intentionally absent binding. | pre-existing line | fixed | 3 |
| 735 | src/hooks/useGenieShortcuts.ts | 51 | Low | Refactoring Debt | `useGenieShortcuts` is 64 lines and owns three independent lifecycle concerns. | Split menu synchronization, invocation listening, and reload listening into focused hooks. | pre-existing line | not-fixed (rebuttal: the three concerns are already three separate effects of ~15 lines each in a 166-line file; splitting into three exported hooks adds modules without removing a hazard) | 3 |
| 736 | src/hooks/useGenieShortcuts.ts | 58 | Medium | Logic & Correctness | The asynchronous menu load is not invalidated on unmount, so it can refresh and re-show the native menu after cleanup hides it; remounts can also race the old hide request. | Add a generation/disposed guard and serialize menu visibility updates. | pre-existing line | fixed | 3 |
| 737 | src/hooks/useGenieShortcuts.ts | 61 | Medium | Logic & Correctness | The suggestion-tab subscription is permanent and its unsubscribe is discarded, so disabling/unmounting the feature leaves behavior active indefinitely. | Return and retain the unsubscribe, or move this explicitly app-lifetime watcher outside the feature hook. | changed line | not-fixed (rebuttal: initSuggestionTabWatcher is guarded by a module-level _tabWatcherInitialized flag and documented "Call once at app startup", so unsubscribing on unmount would permanently kill the watcher — a remount cannot resubscribe) | 2 |
| 738 | src/hooks/useGenieShortcuts.ts | 72 | Medium | Logic & Correctness | The listener-registration promises at lines 72 and 107 have no immediate rejection handlers; `safeUnlistenAsync` only observes rejection during later cleanup. | Attach setup-error handling immediately and retain the handled promise for cleanup. | pre-existing line | fixed | 3 |
| 739 | src/hooks/useMcpHealthCheck.ts | 53 | Medium | Logic & Correctness | Health checks are not serialized or generation-guarded; overlapping calls can let an older result overwrite a newer one and clear global `isChecking` while another check remains active. | Serialize checks or use request IDs plus a pending counter and latest-result guard. | pre-existing line | fixed | 3 |
| 740 | src/hooks/useMcpHealthCheck.ts | 53 | Medium | Refactoring Debt | `runHealthCheck` is 86 lines and mixes orchestration, status reconciliation, mapping, persistence, and error recovery. | Extract bridge-status resolution and health-result/store mapping helpers. | pre-existing line | fixed | 3 |
| 741 | src/hooks/useMcpHealthCheck.ts | 59 | Medium | Logic & Correctness | Bridge status is refreshed before another awaited sidecar call, but the earlier snapshot is used afterward; a bridge transition during the sidecar check produces a stale result. | Read current bridge status after the sidecar call or expose one atomic backend health operation. | pre-existing line | fixed | 3 |
| 742 | src/hooks/useMcpHealthCheck.ts | 65 | Medium | Logic & Correctness | A failed `refresh()` returns `null`, after which stale closure state can still produce `success: true`, hiding the failed status query. | Treat `null` as a failed health check instead of falling back to cached state. | pre-existing line | not-fixed (rebuttal: refresh() returning null means only the status COMMAND failed; the fallback is the hook's live belief maintained by Rust's mcp-server:started/stopped events, which the header calls the source of truth, and an existing test pins the fallback deliberately) | 3 |
| 743 | src/hooks/useMcpHealthCheck.ts | 66 | Medium | Logic & Correctness | `freshStatus?.port ?? port` treats a valid fresh `null` port as missing, so a stopped bridge can be reported with its stale previous port. | Use `freshStatus ? freshStatus.port : port`. | pre-existing line | fixed | 3 |
| 744 | src/hooks/useMcpHealthCheck.ts | 69 | High | Duplication | The success and failure branches duplicate result construction and nearly identical `setHealth` payloads, making field drift likely. | Build shared base/result/store payloads once and vary only `success` and `error`. | pre-existing line | fixed | 3 |
| 745 | src/hooks/useMcpHealthCheck.ts | 115 | Medium | Logic & Correctness | If the sidecar call fails after a successful refresh, the catch branch reports closure-captured `running` and `port` rather than the fresh status already obtained. | Retain the refreshed status outside the `try` and use it in error results. | changed line | fixed | 2 |
| 746 | src/hooks/useMcpServer.ts | 98 | Medium | Refactoring Debt | `useMcpServer` is 141 lines and combines queueing, reconciliation, error state, commands, and event lifecycle. | Extract a shared bridge coordinator and a dedicated event-subscription hook. | pre-existing line | not-fixed (rebuttal: the epoch/queue/adopt invariants are one ordering contract, and splitting them across a module boundary puts the contract on a seam; 273/300, no duplication) | 3 |
| 747 | src/hooks/useMcpServer.ts | 104 | High | Logic & Correctness | The operation queue and state are per hook instance, but the Rust bridge is process-global; separate consumers can issue unsynchronized start/stop/refresh operations and expose conflicting loading/error states. | Move coordination and bridge state to a module-level singleton or Zustand store. | changed line | not-fixed (rebuttal: the SHARED state converges — every instance receives mcp-server:started/stopped and adopt() drops any snapshot that raced an event, so running/port cannot disagree; only loading/error are per-instance, by design. A module singleton would not help across windows either, since each webview is its own JS realm) | 1 |
| 748 | src/hooks/useMcpServer.ts | 162 | Medium | Logic & Correctness | Errors are cleared when a command is enqueued rather than when it begins; an earlier queued command can subsequently fail, leaving its stale error after a later command succeeds, especially if the success event is delayed or lost. | Clear errors inside the serialized operation and again after a successful response. | changed line | fixed | 2 |
| 749 | src/hooks/useMcpServer.ts | 203 | Low | Logic & Correctness | A malformed `started` payload produces the internally inconsistent state `{ running: true, port: null }`. | Validate a positive integer port and refresh/report an error when the event payload is invalid. | changed line | fixed | 3 |
| 750 | src/hooks/useMcpServer.ts | 218 | Medium | Logic & Correctness | `Promise.allSettled` discards listener-registration failures, so the initial refresh may succeed while live updates are permanently unavailable and no error or retry is provided. | Inspect rejected results, report degraded monitoring, and retry or poll as a fallback. | changed line | not-fixed (rebuttal: the code comment at that Promise.allSettled states the decision and its mechanism — "If it rejects (outside Tauri) the status is still worth fetching"; a listen rejection means there is no Tauri event bridge, the same condition under which the proposed invoke-based polling fallback would also fail) | 2 |
| 751 | src/hooks/usePromptHistory.ts | 61 | High | Refactoring Debt | The 240-line hook combines seven mutable state values, filtering, cycling, dropdown navigation, ghost completion, and key routing; its high state/cyclomatic complexity makes invalid state combinations and regressions likely | Move interaction state into a reducer/state machine and extract pure filtering and keyboard-transition helpers | pre-existing line | fixed | 3 |
| 752 | src/hooks/usePromptHistory.ts | 96 | Medium | Logic & Correctness | Filtering can shrink the visible rows without resetting rawSelectedIndex; expanding the filter later restores the old raw index and unexpectedly jumps selection | Reset the raw index whenever draft or entries changes, or store only a normalized nullable selection | changed line | fixed | 2 |
| 753 | src/hooks/usePromptHistory.ts | 112 | Medium | Logic & Correctness | Cycling is documented as prefix filtering but delegates to getFilteredEntries(), whose direct implementation uses substring matching, so a draft such as "bar" can cycle to "foo bar" | Use a dedicated startsWith-based filter for cycling while retaining substring search for the dropdown | pre-existing line | fixed | 3 |
| 754 | src/hooks/usePromptHistory.ts | 126 | Low | Shortcuts & Patches | The comment says cycleIndex 0 is the oldest entry, but the directly imported store maintains MRU order, making index 0 the newest | Correct the comment to prevent future navigation logic from being changed under a false ordering assumption | pre-existing line | fixed | 3 |
| 755 | src/hooks/useSlidevControls.ts | 45 | Medium | Refactoring Debt | The 49-line hook mixes target discovery, validation, dialog policy, backend invocation, external opening, and global error-state mutation | Extract operation-level functions and leave the hook responsible only for stable React callbacks | new file | fixed | 3 |
| 756 | src/hooks/useSlidevControls.ts | 46 | High | Logic & Correctness | Neither callback has an in-flight guard; rapid preview calls can resolve out of order and publish a stale deck, while concurrent exports can target and overwrite the same output | Serialize each operation or use request generations plus an exporting/previewing guard | new file | fixed | 1 |
| 757 | src/hooks/useSlidevControls.ts | 47 | High | Duplication | Both callbacks duplicate active-deck validation and nearly identical error-boundary/store-update code, inviting divergent recovery behavior | Introduce one shared active-deck operation wrapper with centralized error handling | new file | fixed | 3 |
| 758 | src/hooks/useSlidevControls.ts | 49 | High | Logic & Correctness | Normal action errors — including no active deck, unsupported output, opener failure, and export failure — call setError(), which changes the entire content-server lifecycle status to "error" even if the server is still running | Add a separate Slidev operation-error field/action that does not mutate server lifecycle state | new file | fixed | 1 |
| 759 | src/hooks/useSlidevControls.ts | 54 | Medium | Logic & Correctness | Successful preview/export paths never clear a previous Slidev error or restore the status changed by setError(), leaving the UI in a stale error state after recovery | Clear operation errors when starting and successfully completing an operation | new file | not-fixed (rebuttal: setError appears nowhere in this file — audit #758 deliberately removed it and the header says so; deck failures are toasts, which self-dismiss, so there is no persistent Slidev error state for a success to clear) | 2 |
| 760 | src/hooks/useSlidevControls.ts | 54 | Medium | Logic & Correctness | slidevDeckPath is committed before openUrl succeeds, so an opener failure leaves state claiming that the deck was successfully previewed | Commit the preview state after openUrl succeeds, or represent server-started and browser-opened states separately | new file | not-fixed (rebuttal: startSlidevPreview having resolved means the preview SERVER started for that deck, which is what the store field documents; an external-browser failure does not falsify it, and selectSlidevDeck has no consumer outside the store) | 2 |
| 761 | src/hooks/useSlidevControls.ts | 72 | Medium | Logic & Correctness | The extension replacement regex crosses path separators: an extensionless path under a dotted directory such as /ws.v1/deck becomes /ws.pdf, while a plain extensionless filename receives no .pdf suffix | Replace only the final path component’s extension using a path utility or a separator-aware expression | new file | fixed | 2 |
| 762 | src/hooks/useWorkflowExecution.ts | 52 | High | Refactoring Debt | The 135-line hook combines listener ownership, event routing, provider resolution, ID generation, execution startup, rollback, cancellation, and approvals, producing high lifecycle complexity | Split listener management, provider-payload construction, and execution commands into focused hooks/helpers | pre-existing line | fixed | 3 |
| 763 | src/hooks/useWorkflowExecution.ts | 55 | High | Logic & Correctness | subscribeOnce is not atomic because the ref remains empty across three awaits; overlapping calls — especially React Strict Mode’s setup/cleanup/setup cycle — can register duplicate listener sets | Store an initialization promise or explicit subscribing state before the first await | pre-existing line | fixed | 1 |
| 764 | src/hooks/useWorkflowExecution.ts | 60 | High | Logic & Correctness | If the second or third listen() rejects, earlier listeners remain installed, the ref stays empty, and the void call at line 105 creates an unhandled rejection | Register transactionally with try/catch, roll back every acquired listener, and surface or log initialization failure | changed line | fixed | 1 |
| 765 | src/hooks/useWorkflowExecution.ts | 64 | High | Logic & Correctness | The repeated predicate at lines 64, 81, and 96 accepts every event when executionId is null, allowing late or other-window events to recreate statuses and approvals after completion | Require exact equality with the current non-null execution ID | changed line | fixed | 1 |
| 766 | src/hooks/useWorkflowExecution.ts | 68 | Medium | Logic & Correctness | Optional step fields are conditionally omitted, but setStepStatus replaces the whole entry; a later partial event can erase previously reported output, error, or duration | Merge partial updates into the existing step entry, or require every event to carry a complete snapshot | pre-existing line | not-fixed (rebuttal: StepStatusEvent carries skip_serializing_if and every emission is a COMPLETE snapshot; per step the order is running then terminal with no retry loop, so no later event omits a field an earlier one reported — merging would be a regression the day retries land) | 3 |
| 767 | src/hooks/useWorkflowExecution.ts | 82 | High | Logic & Correctness | Completion ignores payload.status and calls setExecution(null), whose direct store implementation also clears all stepStatuses; completed, failed, and cancelled runs become indistinguishable and their results disappear | Add a finishExecution action that records the terminal outcome and preserves final step statuses | changed line | fixed | 1 |
| 768 | src/hooks/useWorkflowExecution.ts | 101 | High | Logic & Correctness | If the component unmounts before listener registration finishes, cleanup sees an empty ref; the pending task later stores live unlisteners and permanently leaks the handlers | Use an effect-scoped cancelled flag and immediately unlisten registrations completed after cancellation | changed line | fixed | 1 |
| 769 | src/hooks/useWorkflowExecution.ts | 118 | High | Logic & Correctness | start() does not wait for subscribeOnce(), so a fast workflow can emit updates or completion before listeners exist despite the pre-generated execution ID | Await a shared listener-readiness promise before invoking run_workflow | changed line | fixed | 1 |
| 770 | src/hooks/useWorkflowExecution.ts | 146 | High | Logic & Correctness | A second concurrent start overwrites the first execution ID before the backend concurrency guard responds; if the second call is rejected, its rollback clears the store while the first run remains active | Reject or serialize starts while an execution/start is active, preserving the prior execution state | changed line | fixed | 1 |
| 771 | src/lib/browser/agent/recorderShim.src.js | 67 | High | Logic & Correctness | If the page removes or replaces the buffer element after a clearing drain, the new element has the old empty stamp; the closure therefore republishes already-drained events on the next record | Track buffer-element identity and clear the closure whenever the element is newly created or replaced | changed line | fixed | 1 |
| 772 | src/lib/browser/agent/recorderShim.src.js | 134 | Medium | Logic & Correctness | Label resolution runs before checking whether the clicked descendant is independently interactive, so clicking a link or button inside a label is incorrectly recorded as activation of the label’s control | Prefer an actionable descendant target and apply label-control resolution only to genuine label activation clicks | changed line | fixed | 2 |
| 773 | src/lib/browser/agent/recorderShim.src.js | 146 | Medium | Shortcuts & Patches | The hardcoded eight-ancestor limit can miss controls in deeply wrapped component markup and emit a roleless inner-node locator | Use a documented shared traversal budget large enough for real component trees, with cycle/root termination | changed line | fixed | 3 |
| 774 | src/lib/browser/agent/recorderShim.src.js | 149 | High | Logic & Correctness | Traversal stops at any role-bearing node, including non-actionable roles such as img or presentation; an icon inside a button can therefore be recorded instead of the button | Stop only on native interactive elements or an explicit allowlist of actionable ARIA roles | changed line | fixed | 1 |
| 775 | src/lib/browser/agent/recorderShim.src.js | 171 | High | Logic & Correctness | Click, input, and change handlers do not reject untrusted events, allowing page scripts to inject fabricated recording steps or mark an editable dirty before a later trusted focusout | Require trusted initiating events before mutating recorder state or recording actions | pre-existing line | not-fixed (rebuttal: a blanket isTrusted gate is not correct here — React/MUI/headless form libraries commit values by dispatching synthetic change/input, so the recorder would go blind on a large class of real sites) | 1 |
| 776 | src/lib/browser/agent/recorderShim.src.js | 197 | Low | Shortcuts & Patches | Trailing commas in function-call argument lists here and at lines 225, 240, 250, and 271 violate the file’s stated ES5 syntax requirement | Remove call-site trailing commas or update and enforce the actual minimum JavaScript target | pre-existing line | fixed | 3 |
| 777 | src/lib/browser/agent/recorderShim.src.js | 217 | High | Logic & Correctness | Contenteditable input is marked dirty while disarmed, and native fields are judged only when change fires; arming before focusout/change can record an edit performed before recording began | Associate dirty state with an armed-session generation and ignore commits whose edit began outside that generation | pre-existing line | fixed | 1 |
| 778 | src/lib/browser/agent/recorderShim.src.js | 227 | Medium | Logic & Correctness | The contenteditable focusout handler does not inspect relatedTarget, so moving focus between descendants of the same editing host prematurely emits and clears a type action | Record only when focus actually leaves the editing host | pre-existing line | fixed | 3 |
| 779 | src/lib/browser/agent/recorderShim.src.js | 234 | High | Duplication | Contenteditable and native change handlers duplicate locator creation, sensitivity calculation, clearing, and recording at lines 234–237 and 265–268 | Extract one recordTypeCommit(element) helper and route both handlers through it | pre-existing line | fixed | 3 |
| 780 | src/lib/browser/agent/recorderShimSensitivity.src.js | 89 | High | Logic & Correctness | The fail-closed stores use page-mutable `WeakMap` constructors and methods; construction can abort the shim, while monkey-patched `get`/`set` methods can silently return benign values without setting `marksBroken`. | Create stores through a guarded factory and call captured pristine methods from an isolated initialization context. | changed line | fixed | 1 |
| 781 | src/lib/browser/agent/recorderShimSensitivity.src.js | 94 | High | Logic & Correctness | The observer omits sensitivity sources used by `__vmarkName`, including `placeholder`, `aria-labelledby`, label text, and label associations; changing those before first focus can erase evidence that a field was sensitive. | Observe and map all accessible-name dependencies back to affected controls, including attribute, text, and child mutations. | changed line | fixed | 1 |
| 782 | src/lib/browser/agent/recorderShimSensitivity.src.js | 182 | High | Logic & Correctness | Mutation records are delivered asynchronously, so a page can change a sensitive attribute and synchronously dispatch an input/change event before the permanent mark is applied. | Retain the observer and drain `takeRecords()` before processing recorder events, or otherwise synchronize mutation classification with commit handling. | changed line | fixed | 1 |
| 783 | src/lib/browser/url.ts | 163 | Medium | Refactoring Debt | `urlForPersistence` exceeds 30 lines and combines protocol validation, path classification, userinfo removal, query filtering, and fragment reconstruction. | Extract small sanitizers for each URL component and compose them in one orchestration function. | changed line | fixed | 3 |
| 784 | src/lib/browser/url.ts | 165 | High | Logic & Correctness | The persistence boundary accepts every parseable scheme; `data:`, `file:`, and other opaque URLs can therefore persist payloads or local paths verbatim. | Require a navigable HTTP(S) URL through `parseNavigableUrl`, failing closed for every other protocol. | changed line | fixed | 1 |
| 785 | src/lib/browser/url.ts | 172 | High | Logic & Correctness | Only the password is removed from userinfo, but usernames can themselves be API tokens or HTTP Basic credentials, such as `https://TOKEN@host/`. | Strip all userinfo at persistence time, or preserve usernames only after conservative credential-shape validation. | changed line | not-fixed (rebuttal: the username is deliberately kept and the reason is documented — alice@host and bob@host are different destinations, so dropping it restores the wrong one; a password is a credential, a username is an address) | 1 |
| 786 | src/lib/browser/url.ts | 185 | High | Logic & Correctness | Fragment routes are not passed through `credentialPath`; an SPA URL such as `#/reset/<token>` is persisted intact unless its query contains a recognized credential parameter. | Classify the fragment route path separately and remove credential-bearing routes before reconstructing the hash. | pre-existing line | fixed | 1 |
| 787 | src/lib/browser/url.ts | 210 | High | Logic & Correctness | Camel-case normalization misses acronym boundaries, so common keys such as `APIToken` and `JWTToken` bypass credential detection. | Split acronym-to-word transitions as well as lowercase-to-uppercase transitions before matching. | changed line | fixed | 1 |
| 788 | src/lib/browser/url.ts | 250 | High | Logic & Correctness | `TOKEN_SEGMENT` requires both a letter and a digit, despite claiming to recognize base64url and opaque tokens; long all-letter tokens are persisted. | Use a conservative length/entropy classifier that accepts valid all-letter token shapes, or remove opaque segments by default. | changed line | not-fixed (rebuttal: the digit requirement is what keeps ordinary path shapes out — dropping it redacts /how-to-build-a-browser to the origin and camelCase API routes too; the gap is ~3.5% of base64url tokens and every widening costs more restore breakage than it buys) | 1 |
| 789 | src/lib/browser/url.ts | 258 | High | Duplication | Credential vocabulary is independently hardcoded and has drifted from the recorder classifier: names such as `pin` and `passcode` are sensitive there but not here; `state`, `csrf`, and `credential` are also missed. | Generate both classifiers from one reviewed security vocabulary or use a safe persistence allowlist. | changed line | fixed | 3 |
| 790 | src/lib/browser/url.ts | 259 | High | Logic & Correctness | The parameter-boundary regex permits a leading dot but not a trailing dot, so dotted names such as `token.value` and `access_token.value` evade redaction. | Include `.` in the trailing-boundary group and add symmetric boundary cases. | changed line | fixed | 1 |
| 791 | src/lib/formats/adapters/htmlTrust.ts | 43 | High | Logic & Correctness | The security-critical Windows custom-protocol URL branch is explicitly unverified on Windows; a wrong origin or CSP assumption disables trusted preview only on that platform. | Require a real Windows WebView2 integration/release-smoke test before treating this branch as complete. | changed line | not-fixed (out of reach: needs a Windows WebView2 run, which no unit tier can provide. Not a code defect: the branch is documented as source-derived and fails CLOSED) | 1 |
| 792 | src/lib/formats/adapters/htmlTrust.ts | 85 | Low | Logic & Correctness | The function documents a 64-character hexadecimal token contract but accepts empty or arbitrary strings, turning upstream mistakes into malformed frame requests. | Validate the token against the exact runtime contract and fail closed before constructing the URL. | changed line | not-fixed (rebuttal: the 64-hex contract IS enforced at the boundary that can act on it — token_of in protocol.rs requires exactly TOKEN_LEN ASCII-hex and answers 404 otherwise; trustedFrameUrlFor is a pure builder called during render, so throwing would reach the root error boundary and take the window down, trading an empty frame for a dead one. Three cross-language assertions added) | 3 |
| 793 | src/lib/formats/registry.ts | 47 | High | Refactoring Debt | `registerFormat` spans roughly 125 lines with many independent validation, normalization, freezing, and commit branches, creating high cyclomatic complexity. | Extract pure validation/normalization helpers and keep registration as a short atomic commit. | pre-existing line | fixed | 3 |
| 794 | src/lib/formats/registry.ts | 72 | Medium | Logic & Correctness | Extension normalization accepts embedded dots, path separators, query markers, and other values that the lookup-key algorithm cannot consistently resolve. | Validate normalized extensions against an explicit supported-key grammar or implement compound-extension lookup deliberately. | pre-existing line | fixed | 3 |
| 795 | src/lib/formats/registry.ts | 93 | High | Duplication | The WYSIWYG-component requirement is checked again with the identical predicate at line 127, making the later branch unreachable duplication. | Keep one guard with the more informative error message. | pre-existing line | fixed | 3 |
| 796 | src/lib/formats/registry.ts | 107 | Medium | Logic & Correctness | Runtime thunk validation covers only `wysiwygComponent` and `language`; malformed `loadLanguage` or `loadExtraExtensions` values survive registration and fail at first use. | Validate every lazy-function field consistently during registration. | pre-existing line | fixed | 3 |
| 797 | src/lib/formats/registry.ts | 163 | Medium | Logic & Correctness | Freezing is shallow: callers can still mutate `adapters`, filters, renderer maps, and other nested state after validation, invalidating registry invariants. | Store a deeply immutable normalized copy or recursively freeze all contract-bearing nested structures. | changed line | fixed (narrowed: config.adapters is frozen, carrying the invariant validated on the way in; a RECURSIVE freeze was deliberately rejected and documented — schemaRenderers holds React components, and a React.lazy one mutates _status/_result as its chunk resolves) | 2 |
| 798 | src/lib/formats/registry.ts | 190 | High | Logic & Correctness | An untitled document silently falls back to plain text or the first arbitrary format when Markdown is missing, contradicting the stated editor contract. | Require the Markdown registration and throw a bootstrap error if it is absent. | changed line | fixed | 1 |
| 799 | src/lib/formats/registry.ts | 211 | High | Logic & Correctness | Built-in extension lookup reuses keys containing the full basename; consequently, an extensionless file named `md`, `html`, or another registered extension is misclassified as that format. | Use full-name/stem keys only for user associations and a separately computed real extension for `byExt`. | pre-existing line | fixed | 1 |
| 800 | src/lib/formats/registry.ts | 235 | Medium | Logic & Correctness | `listFormats` exposes the registry’s live mutable array; TypeScript’s readonly return type provides no runtime protection against sorting, splicing, or insertion that bypasses the maps. | Return a copy or a permanently frozen snapshot. | changed line | fixed | 2 |
| 801 | src/lib/formats/registry.ts | 252 | Medium | Logic & Correctness | Runtime rebootstrap clears live state before new registrations are known to succeed; any later validation error leaves a partially populated registry. | Build and validate temporary indexes, then swap them into live state atomically. | pre-existing line | fixed | 3 |
| 802 | src/lib/formats/registry.ts | 252 | Low | Refactoring Debt | A production runtime operation retains the test-only-looking name `__resetRegistry`, obscuring its lifecycle significance. | Rename it to a production-oriented API such as `replaceRegistry` or `resetRegistry`, retaining a deprecated alias only if required. | pre-existing line | fixed | 3 |
| 803 | src/lib/formats/types.ts | 41 | Medium | Refactoring Debt | The coordinate contract does not define whether line/column and end positions are zero- or one-based or whether ends are inclusive, inviting cross-format off-by-one errors | Document one convention and enforce it at validator boundaries | pre-existing line | fixed | 3 |
| 804 | src/lib/formats/types.ts | 101 | Medium | Logic & Correctness | `tabId` is optional despite being the stated identity for document-scoped writes, so renderer implementations can accidentally fall back to the focused tab | Make `tabId` required or define a separate read-only renderer type | pre-existing line | not-fixed (out of reach: SplitPaneEditor is the only provider and passes tabId at both mount sites, so the optionality is unused slack — but making it required produces type errors in HtmlPreview.test.tsx and yamlWorkflowRenderer.test.tsx, both outside this group, which would raise their lint:test-types baselines) | 3 |
| 805 | src/lib/formats/types.ts | 138 | Medium | Logic & Correctness | `FormatConfig` is an optional-property bag that permits contradictory configurations, such as media formats with editor languages or WYSIWYG formats without a component | Replace it with a discriminated union keyed by `kind` | pre-existing line | not-fixed (rebuttal: registerFormat already refuses the contradictions at registration — wysiwyg without wysiwygComponent, wysiwyg WITH loadLanguage, readOnlyDefault without a save policy, non-thunk surfaces — each with a named error and tests; converting to a kind-keyed union rewrites all twelve adapter files, every one outside this group) | 3 |
| 806 | src/lib/lintEngine/ruleMeta.ts | 39 | High | Duplication | English titles are manually duplicated in locale data, and the comment explicitly acknowledges that edits can leave the copies inconsistent | Generate the English fallback from `RULE_META` or add a structural synchronization gate | new file | not-fixed (rebuttal: ruleMeta.test.ts already asserts EN_EDITOR["lint.rule."+id] === m.title for every rule AND that no orphan key exists — the structural gate the row asks for) | 3 |
| 807 | src/lib/lintEngine/rules/headingIncrement.ts | 14 | Low | Refactoring Debt | The function spans 32 lines, exceeding the requested 30-line threshold | Extract diagnostic construction or the heading visitor into a focused helper | changed line | fixed | 3 |
| 808 | src/lib/lintEngine/rules/headingIncrement.ts | 34 | Medium | Logic & Correctness | Missing MDAST offsets are replaced with `0`, causing an “exact” diagnostic on a later heading to highlight the document start | Derive the offset from source plus line/column, or represent an unavailable offset explicitly | pre-existing line | fixed | 3 |
| 809 | src/lib/lintEngine/rules/linkFragments.ts | 13 | Medium | Refactoring Debt | A pure lint rule imports slug helpers from a module with runtime Tiptap/editor dependencies, coupling linting to the editor stack | Move slug generation and uniqueness into a dependency-free utility module | changed line | not-fixed (out of reach: the fix is to split the pure generateSlug/makeUniqueSlug out of src/utils/headingSlug.ts, which statically imports @tiptap/pm/state for its navigation helpers, and that file is outside this group; a copy inside the lint engine would be exactly the drift this audit removes) | 3 |
| 810 | src/lib/lintEngine/rules/linkFragments.ts | 18 | Medium | Logic & Correctness | Every leaf `value` is treated as visible heading text, so raw inline HTML tags become part of generated slugs and produce false missing-anchor warnings | Ignore `html` nodes and share a rendered-heading-text projection with anchor generation | pre-existing line | fixed | 3 |
| 811 | src/lib/lintEngine/rules/linkFragments.ts | 28 | Low | Refactoring Debt | The 45-line function combines heading indexing and link validation in two independent tree traversals | Extract `collectHeadingSlugs` and fragment-validation helpers | pre-existing line | fixed | 3 |
| 812 | src/lib/lintEngine/rules/linkFragments.ts | 32 | High | Duplication | `usedSlugs` and `validSlugs` are updated identically and therefore contain exactly the same values | Use one slug set for both uniqueness and membership checks | pre-existing line | fixed | 3 |
| 813 | src/lib/lintEngine/rules/linkFragments.ts | 45 | Medium | Logic & Correctness | Only inline `link` nodes are checked; reference-style links whose definitions target fragments bypass W04 entirely | Resolve definitions and validate `linkReference` targets as well | pre-existing line | not-fixed (rebuttal: measured — [text][r] with [r]: #nope IS flagged W04 at 3:5, because the pipeline's reference resolution rewrites linkReference into link before rules run; pinned with 3 tests since that is another module's property) | 3 |
| 814 | src/lib/lintEngine/rules/linkFragments.ts | 51 | Medium | Logic & Correctness | Fragment comparison uses the raw URL text, so percent-encoded Unicode or characters are rejected even when they resolve to an existing heading | Safely apply `decodeURIComponent` before comparison, handling malformed encodings without throwing | pre-existing line | fixed | 3 |
| 815 | src/lib/lintEngine/rules/linkFragments.ts | 63 | Medium | Logic & Correctness | A missing node offset is coerced to `0` while retaining `uiHint: "exact"`, producing incorrect source highlighting | Derive the offset from source coordinates or emit a non-exact diagnostic when unavailable | pre-existing line | fixed | 3 |
| 816 | src/lib/lintEngine/rules/noDuplicateDefs.ts | 20 | Medium | Logic & Correctness | Positionless definitions are excluded from `seenLabels`, so a later positioned duplicate can incorrectly be treated as the first occurrence | Track every normalized label regardless of position; require position only when emitting a diagnostic | pre-existing line | fixed | 3 |
| 817 | src/lib/lintEngine/rules/noDuplicateDefs.ts | 23 | Medium | Logic & Correctness | `normalizeLabel` uses lowercase rather than CommonMark-compatible Unicode case folding, so equivalent labels such as Greek `σ` and `ς` are not detected as duplicates | Use the parser-normalized `node.identifier` or implement proper Unicode case folding | pre-existing line | fixed | 3 |
| 818 | src/lib/lintEngine/rules/noEmptyLinkText.ts | 19 | Medium | Logic & Correctness | Inline code and images nominally count as content, but their values are subsequently trimmed; whitespace-only code and empty-alt images are falsely reported as empty links | Track content-bearing node presence separately or append a non-whitespace sentinel | pre-existing line | fixed | 3 |
| 819 | src/lib/lintEngine/rules/noEmptyLinkText.ts | 25 | Medium | Logic & Correctness | `imageReference` is a leaf node and is ignored, so a link containing only a referenced image is falsely reported as empty | Handle `imageReference` as non-empty content alongside `image` | pre-existing line | not-fixed (rebuttal: measured — [![alt][img]](url) with a definition produces no E06 because the pipeline resolves imageReference into image, and without a definition the text stays literal, which is non-empty either way; pinned with 2 tests) | 3 |
| 820 | src/lib/lintEngine/rules/noMissingSpaceAtx.ts | 19 | High | Logic & Correctness | The regex contains `\\S` in a regex literal, which matches a literal backslash followed by `S`; ordinary inputs such as `#heading` are never detected | Replace it with a CommonMark-appropriate character check such as `([^ \t#\r])` and add basic positive/negative cases | pre-existing line | not-fixed (rebuttal: the literal contains a single backslash — od -c on line 19 shows ( \ S ) — and #heading is matched; grep -c for a double backslash S returns 0) | 1 |
| 821 | src/lib/lintEngine/rules/noMissingSpaceAtx.ts | 21 | High | Duplication | The rule ignores the shared precomputed `lineOffsets` and reconstructs offsets with mutable `lineOffset`, duplicating engine work and adding fragile control-flow bookkeeping | Destructure `lineOffsets`, use `lineOffsets[i]`, and remove both manual increments | pre-existing line | fixed | 3 |
| 822 | src/lib/lintEngine/rules/noMissingSpaceAtx.ts | 21 | Low | Refactoring Debt | The rule function spans 44 lines and mixes fence tracking, indentation classification, matching, offset computation, and diagnostic construction | Extract candidate detection and diagnostic creation into focused helpers | pre-existing line | fixed | 3 |
| 823 | src/lib/lintEngine/rules/noMissingSpaceAtx.ts | 28 | Medium | Logic & Correctness | Only fenced and globally indented code are excluded; YAML front matter and raw HTML blocks remain searchable, producing false positives on lines such as `#comment` | Exclude parser-identified front-matter/HTML ranges or use a block-aware source scanner | pre-existing line | fixed | 3 |
| 824 | src/lib/lintEngine/rules/noMissingSpaceAtx.ts | 38 | Medium | Logic & Correctness | Matching only the physical line start misses malformed headings inside CommonMark containers, such as `> #heading` and `- #heading` | Recognize valid blockquote/list prefixes while preserving the absolute source column | pre-existing line | fixed | 3 |
| 825 | src/lib/lintEngine/rules/noReversedLink.ts | 15 | High | Duplication | Inline-code parsing duplicates `findInlineCodeSpans` in `noSpaceInEmphasis.ts` and has already behaviorally drifted | Extract one shared CommonMark-aware code-span scanner. | pre-existing line | fixed | 3 |
| 826 | src/lib/lintEngine/rules/noReversedLink.ts | 15 | Medium | Logic & Correctness | Toggling state for every backtick mishandles multi-backtick spans, escaped backticks, unmatched runs, and multiline spans, producing false positives and negatives | Match equal-length backtick runs and preserve span state across lines. | pre-existing line | fixed | 3 |
| 827 | src/lib/lintEngine/rules/noReversedLink.ts | 27 | Medium | Logic & Correctness | The regex flags escaped opening parentheses such as `\(text)[url]` and misses nested text such as `(a (b))[url]` | Replace the flat regex with an escape-aware balanced-delimiter scanner. | pre-existing line | fixed | 3 |
| 828 | src/lib/lintEngine/rules/noReversedLink.ts | 29 | Medium | Refactoring Debt | The 37-line rule exceeds the requested function limit and nests scanning four levels deep while manually reconstructing offsets | Extract match collection and diagnostic emission, and use the supplied `lineOffsets`. | pre-existing line | fixed | 3 |
| 829 | src/lib/lintEngine/rules/noReversedLink.ts | 36 | Medium | Logic & Correctness | Container-prefixed fenced code, such as a fence inside a blockquote, is not excluded by the imported tracker and can emit diagnostics from code content | Mask code-node source ranges from MDAST or make fence tracking container-aware. | pre-existing line | fixed | 3 |
| 830 | src/lib/lintEngine/rules/noSpaceInEmphasis.ts | 27 | High | Refactoring Debt | `findInlineCodeSpans` is a high-complexity custom parser with five loops and numerous branches in one 39-line function | Extract and test a reusable code-span tokenizer/state machine. | pre-existing line | fixed | 3 |
| 831 | src/lib/lintEngine/rules/noSpaceInEmphasis.ts | 27 | Medium | Logic & Correctness | Code spans are calculated independently per line even though CommonMark code spans may cross line endings | Carry unmatched backtick-run state across lines or use parsed positional data. | pre-existing line | fixed | 3 |
| 832 | src/lib/lintEngine/rules/noSpaceInEmphasis.ts | 74 | High | Logic & Correctness | Both regexes require whitespace at both ends, so they miss the advertised one-sided cases `* italic*` and `*italic *` | Detect opening-side and closing-side whitespace independently. | pre-existing line | not-fixed (rebuttal: a one-sided opener cannot be told from a LIST BULLET by regex — "* some *emph* here" matches exactly — nor from prose preceding an emphasis run; the header now says symmetric-only and records why) | 1 |
| 833 | src/lib/lintEngine/rules/noSpaceInEmphasis.ts | 74 | Medium | Logic & Correctness | The regexes ignore backslash escaping and flag literal forms such as `\* text \*` and `\_ text \_` | Check delimiter escape parity before accepting a match. | pre-existing line | fixed | 3 |
| 834 | src/lib/lintEngine/rules/noSpaceInEmphasis.ts | 82 | Medium | Shortcuts & Patches | The arbitrary ASCII-only three-character operand rule misclassifies ordinary arithmetic using negative numbers, long identifiers, or CJK identifiers | Use Unicode-aware identifier and numeric token recognition without the hardcoded length cap. | pre-existing line | fixed (alphabet and sign only: \w{1,3} to a Unicode letter/number class with an optional numeric sign, so CJK and signed numbers stop flagging; the 3-character cap is KEPT deliberately because removing it makes "some * emphasized * text" operand-like on all three flanks and silences the rule on exactly the shape it exists to catch — that case is now a test) | 3 |
| 835 | src/lib/lintEngine/rules/noSpaceInEmphasis.ts | 108 | Medium | Refactoring Debt | The 51-line rule nests line, pattern, match, and condition processing more than three levels deep | Extract per-line matching and diagnostic construction helpers. | pre-existing line | fixed | 3 |
| 836 | src/lib/lintEngine/rules/noSpaceInEmphasis.ts | 115 | Medium | Logic & Correctness | Container-prefixed fenced code is scanned because the tracker recognizes only root-style fence prefixes | Exclude MDAST code ranges or support blockquote/list containers. | pre-existing line | fixed | 3 |
| 837 | src/lib/lintEngine/rules/noSpaceInEmphasis.ts | 134 | Medium | Logic & Correctness | Only the match start is checked against code spans; a match beginning outside code but using a closing marker inside code is still reported | Reject matches whose full range intersects any code span. | pre-existing line | fixed | 3 |
| 838 | src/lib/lintEngine/rules/noUndefinedRefs.ts | 27 | High | Logic & Correctness | The flat regex treats escaped `\[` syntax as a reference and cannot correctly parse nested link text or escaped closing brackets in labels | Use an escape-aware reference scanner or parser-provided positional tokens. | pre-existing line | fixed | 1 |
| 839 | src/lib/lintEngine/rules/noUndefinedRefs.ts | 29 | High | Refactoring Debt | The 104-line function has high cyclomatic complexity and mixes definition collection, fence state, tokenization, classification, and emission | Split it into definition collection, context masking, reference extraction, and diagnostic phases. | pre-existing line | fixed | 3 |
| 840 | src/lib/lintEngine/rules/noUndefinedRefs.ts | 43 | High | Duplication | Fence tracking, inline-code masking, and reference extraction duplicate logic in `noUnusedDefs.ts` and existing helpers, creating inconsistent behavior | Centralize source-context masking and reference-token extraction. | pre-existing line | fixed | 3 |
| 841 | src/lib/lintEngine/rules/noUndefinedRefs.ts | 57 | Medium | Logic & Correctness | Fence detection misses container-prefixed fences and accepts invalid backtick fences whose info string contains backticks | Use MDAST code positions or implement complete fence-opening rules. | pre-existing line | fixed | 3 |
| 842 | src/lib/lintEngine/rules/noUndefinedRefs.ts | 74 | Medium | Logic & Correctness | Only a root-form definition’s first line is skipped; reference-looking text in a multiline definition title is scanned as document content | Mask every line covered by each MDAST definition position. | pre-existing line | fixed | 3 |
| 843 | src/lib/lintEngine/rules/noUndefinedRefs.ts | 78 | Medium | Logic & Correctness | Inline-code stripping handles only same-line, single-backtick spans, so double-backtick and multiline code produce false undefined-reference errors | Reuse a CommonMark-compatible code-span masker. | pre-existing line | fixed | 3 |
| 844 | src/lib/lintEngine/rules/noUndefinedRefs.ts | 85 | Medium | Logic & Correctness | Reference-looking text inside raw HTML blocks or comments is reported even though Markdown does not parse it as a reference there | Exclude MDAST HTML-node ranges before scanning. | pre-existing line | fixed | 3 |
| 845 | src/lib/lintEngine/rules/noUnusedDefs.ts | 25 | High | Duplication | The reference scanner substantially duplicates `noUndefinedRefs.ts`, including fence state, code masking, regex groups, and label classification | Extract a shared iterator returning normalized reference tokens and source ranges. | changed line | fixed | 3 |
| 846 | src/lib/lintEngine/rules/noUnusedDefs.ts | 25 | High | Refactoring Debt | `findReferencedLabels` is a 67-line high-cyclomatic function combining context tracking, regex execution, classification, and normalization | Decompose it into reusable masking and token-classification helpers. | changed line | fixed | 3 |
| 847 | src/lib/lintEngine/rules/noUnusedDefs.ts | 35 | Medium | Logic & Correctness | Container-prefixed fenced code is scanned, allowing bracket text in code to suppress legitimate unused-definition warnings | Exclude MDAST code ranges or implement container-aware fence tracking. | changed line | fixed | 2 |
| 848 | src/lib/lintEngine/rules/noUnusedDefs.ts | 53 | High | Logic & Correctness | A definition inside a blockquote/list is not recognized as a definition line and therefore counts its own `[label]` as usage; continuation titles can likewise create fake usage | Mask complete MDAST definition ranges rather than recognizing definition lines with regex. | pre-existing line | fixed | 1 |
| 849 | src/lib/lintEngine/rules/noUnusedDefs.ts | 57 | Medium | Logic & Correctness | Single-backtick, same-line replacement leaves references inside multi-backtick and multiline code spans visible | Reuse a CommonMark-compatible code-span masker. | pre-existing line | fixed | 3 |
| 850 | src/lib/lintEngine/rules/noUnusedDefs.ts | 63 | Medium | Logic & Correctness | Escaped bracket text and reference-looking content in raw HTML blocks can be counted as real usage | Make extraction escape-aware and exclude HTML-node ranges. | changed line | fixed | 2 |
| 851 | src/lib/lintEngine/rules/noUnusedDefs.ts | 63 | Low | Shortcuts & Patches | An identical global regex is allocated for every source line despite the peer rule explicitly hoisting it | Hoist the shared pattern, or preferably eliminate both copies through a shared scanner. | changed line | fixed | 3 |
| 852 | src/lib/lintEngine/rules/noUnusedDefs.ts | 73 | High | Logic & Correctness | Inline links and images such as `[foo](url)` and `![foo](url)` are counted as shortcut references, incorrectly marking an unrelated `[foo]: ...` definition as used | When no second bracket exists, reject matches immediately followed by an inline-link destination. | pre-existing line | fixed | 1 |
| 853 | src/lib/lintEngine/rules/noUnusedDefs.ts | 93 | Low | Refactoring Debt | The exported rule is 36 lines, exceeding the requested function limit | Extract definition-to-diagnostic conversion into a small helper. | changed line | fixed | 3 |
| 854 | src/lib/lintEngine/rules/noUnusedDefs.ts | 119 | Medium | Logic & Correctness | Missing MDAST start offsets are silently replaced with zero, causing diagnostics to point at the document beginning | Compute the offset from `lineOffsets`, or represent an unavailable offset explicitly. | changed line | fixed | 2 |
| 855 | src/lib/lintEngine/rules/requireAltText.ts | 15 | Medium | Logic & Correctness | Only `image` nodes are visited, so reference-style images (`imageReference`) with missing alt text escape W02. | Visit both `image` and `imageReference` nodes and handle their shared `alt` field. | pre-existing line | fixed | 3 |
| 856 | src/lib/lintEngine/rules/requireAltText.ts | 28 | Low | Logic & Correctness | A positioned node whose optional start offset is absent is assigned offset 0, causing navigation to jump to the document start. | Derive the offset from the shared line index/source position instead of fabricating zero. | pre-existing line | fixed | 3 |
| 857 | src/lib/lintEngine/rules/tableColumnCount.ts | 13 | Low | Refactoring Debt | The function spans 38 lines, exceeding the requested 30-line threshold and combining traversal, validation, and diagnostic construction. | Extract a small helper that constructs the row diagnostic. | changed line | fixed | 3 |
| 858 | src/lib/lintEngine/rules/tableColumnCount.ts | 40 | Low | Logic & Correctness | A row with line/column data but no optional MDAST offset is reported at offset 0. | Accept the line index and derive the absolute offset from `line` and `column`. | pre-existing line | fixed | 3 |
| 859 | src/lib/lintEngine/rules/unclosedFencedCode.ts | 17 | Medium | Refactoring Debt | The 55-line function has deeply nested state transitions and mixes recognition, state mutation, offset tracking, and diagnostic emission. | Extract fence-line classification and closing-fence validation helpers. | pre-existing line | fixed | 3 |
| 860 | src/lib/lintEngine/rules/unclosedFencedCode.ts | 26 | High | Logic & Correctness | The raw line scanner is not CommonMark container-aware: it misses fences inside block quotes/list items and can treat fence-like lines inside HTML blocks as real fences. | Use container-aware parser/token events or inspect parsed block boundaries instead of scanning raw line prefixes. | pre-existing line | not-fixed (out of reach: needs an mdast-driven container-aware rewrite of the rule and its suite — the line scanner is the algorithm, not a bug in it. Real: fences in blockquotes are missed, a false negative in a lint hint) | 1 |
| 861 | src/lib/lintEngine/rules/unclosedFencedCode.ts | 34 | Low | Logic & Correctness | `trim()` accepts Unicode whitespace after a closing fence, although CommonMark permits only spaces and tabs; this can falsely close a fence. | Validate the suffix explicitly with a spaces/tabs pattern, accounting for a retained CR in CRLF input. | pre-existing line | fixed | 3 |
| 862 | src/lib/lintEngine/rules/unclosedFencedCode.ts | 41 | Medium | Logic & Correctness | A backtick opening fence is accepted even when its info string contains a backtick, which CommonMark explicitly disallows. | Before entering fence state, reject backtick openers whose remaining text contains a backtick. | pre-existing line | fixed | 3 |
| 863 | src/lib/lintEngine/rules/unclosedFencedCode.ts | 49 | High | Duplication | The rule recomputes absolute line offsets even though `LintLineIndex.lineOffsets` exists specifically to centralize that calculation. | Destructure `lineOffsets` from the rule context and calculate `openOffset` from `lineOffsets[i]`. | pre-existing line | fixed | 3 |
| 864 | src/lib/lintEngine/yaml.ts | 11 | Low | Shortcuts & Patches | The module comment claims reuse of `collectYamlDiagnostics`, but the imported function is `collectYamlParseErrors`; the documentation is stale. | Replace the obsolete function name in the comment. | pre-existing line | fixed | 3 |
| 865 | src/lib/lintEngine/yaml.ts | 38 | Medium | Refactoring Debt | Every diagnostic rescans the source from offset zero, producing O(source length × diagnostic count) work in an editor-facing lint path. | Build line-start offsets once and use binary search, or process diagnostics in offset order. | changed line | fixed | 3 |
| 866 | src/lib/lintEngine/yaml.ts | 64 | Low | Logic & Correctness | The offset converter recognizes only LF; valid YAML using bare CR line endings receives incorrect line and column values. | Recognize LF, CRLF, and bare CR while building the shared line-start index. | pre-existing line | fixed | 3 |
| 867 | src/plugins/shared/fenceDelimiter.ts | 143 | Medium | Refactoring Debt | `parseFenceDelimiter` recompiles one of two regular expressions for every scanned document line | Precompile the commonmark and deep-indent expressions and select the appropriate constant | changed line | fixed | 3 |
| 868 | src/plugins/shared/fenceDelimiter.ts | 186 | High | Logic & Correctness | `listItemStart` measures UTF-16 offsets, not visual columns; tabs in quote/list prefixes make sibling and nested items compare incorrectly, causing fence ranges to terminate too early or run on | Track CommonMark columns with four-column tab stops while walking prefix parts | pre-existing line | fixed | 1 |
| 869 | src/plugins/toolbarActions/sourceBlockPlacement.ts | 94 | High | Logic & Correctness | `trim()` classifies NBSP and other Unicode whitespace as an empty Markdown line, so block insertion replaces and silently deletes real content | Test blankness with `/^[ \t]*$/`, matching CommonMark | changed line | fixed | 1 |
| 870 | src/plugins/toolbarActions/sourceBlockPlacement.ts | 153 | Medium | Logic & Correctness | Out-of-range cursor offsets are clamped only when a prefix exists; the early return forwards negative or oversized offsets directly and can produce an invalid CodeMirror selection | Clamp the raw offset to `[0, text.length]` before the prefix branch | changed line | fixed | 2 |
| 871 | src/plugins/toolbarActions/sourceBlockPlacement.ts | 174 | High | Duplication | `continuationPrefix` reimplements container grammar incorrectly: it handles only quotes-then-one-list, accepts invalid marker whitespace/digit counts, and counts a task checkbox as structural indentation; blocks inserted into task or nested list/quote items can become indented code or escape their container | Centralize a column-aware continuation-prefix helper based on `containerPrefixParts`; do not include task-checkbox text in list indentation | pre-existing line | fixed | 3 |
| 872 | src/plugins/toolbarActions/sourceBlockPlacement.ts | 254 | High | Logic & Correctness | Heading detection uses `\s+`, so plain text such as `###\u00a0Title`, which is not a CommonMark heading, loses its literal hashes when a list marker is applied | Restrict heading whitespace to `[ \t]+` | pre-existing line | fixed | 1 |
| 873 | src/plugins/toolbarActions/sourceInsertActions.ts | 56 | Low | Dead Code | The `insertCodeBlock` JSDoc is separated from that function by another JSDoc and `blockBodyForWrapping`, leaving it orphaned and attaching no documentation to the exported action | Move the comment directly above `insertCodeBlock` | pre-existing line | fixed | 3 |
| 874 | src/plugins/toolbarActions/sourceInsertActions.ts | 108 | High | Logic & Correctness | The default fence scan cannot pair list-opened fences with closers indented four or more raw spaces, including ordinary `10. ```…` and nested-list fences; toggling then treats the block as unclosed, restores the wrong range, and leaves the real closer behind | Make fence pairing container-context-aware by subtracting the opener’s list continuation column from candidate closer indentation | pre-existing line | fixed | 1 |
| 875 | src/plugins/toolbarActions/sourceInsertActions.ts | 212 | High | Duplication | Adaptive delimiter selection exists only in `insertCodeBlock`; selection-aware diagram builders use a fixed triple-backtick fence, so selected content containing a matching delimiter line closes the generated block early | Centralize collision-safe fenced-block construction and use it for every fenced builder | pre-existing line | not-fixed (out of reach: the fixed triple-backtick fence is built by buildFencedBlock in src/plugins/sourceContextDetection/sourceInsertions.ts, outside this group; handleBuildInsert only receives the finished builder) | 3 |
| 876 | src/plugins/toolbarActions/sourceInsertActions.ts | 252 | High | Logic & Correctness | Destructive replacements rely on `selectionBlockSpan`’s documented line-based approximation; a paragraph adjacent to an interrupting `1. item` or HTML block can absorb and transform an untouched neighboring block | Resolve block boundaries from the CodeMirror Markdown syntax tree before replacement | pre-existing line | not-fixed (out of reach: needs the CodeMirror Markdown syntax tree — already the standing limitation documented in blockSpan.ts's header with the same fix named) | 1 |
| 877 | src/plugins/toolbarActions/sourceInsertActions.ts | 267 | High | Duplication | Local `quoteDepth` duplicates the exported implementation in `fenceDelimiter.ts`, creating another Markdown-container rule that can drift | Import and reuse the shared `quoteDepth` helper | changed line | fixed | 3 |
| 878 | src/plugins/toolbarActions/sourceUnfence.ts | 54 | High | Logic & Correctness | Continuation removal treats list widths as UTF-16 lengths and accepts only literal spaces; tab-padded markers or tab-indented body lines are not consumed correctly, so unfencing duplicates tabs or changes indentation | Replace the generated regex with a column-aware space/tab consumer using CommonMark tab stops | new file | fixed | 1 |
| 879 | src/services/commands/CommandBus.ts | 35 | Medium | Logic & Correctness | Command definitions remain mutable and are stored/returned by reference; mutating an ID can desynchronize the definition from its registry key and bypass registration invariants | Mark fields readonly and expose frozen definitions or defensive copies | pre-existing line | fixed | 3 |
| 880 | src/services/commands/CommandBus.ts | 52 | Medium | Logic & Correctness | A throwing localization getter propagates through search and can break the entire command palette because only `when` predicates are isolated | Catch resolution failures per command, log the command ID, and omit or safely label the faulty command | pre-existing line | fixed | 3 |
| 881 | src/services/commands/CommandBus.ts | 177 | Low | Refactoring Debt | `unregisterOwner` removes commands but leaves the owner's generation token, violating the “current token” invariant and leaking entries for transient owners | Delete `OWNER_GENERATION[owner]` when unregistering the owner | changed line | fixed | 3 |
| 882 | src/services/commands/CommandBus.ts | 249 | Medium | Logic & Correctness | Search lowercases strings without Unicode normalization or locale-aware folding, so canonically equivalent and compatibility-form input may not match | Normalize query and searchable fields consistently and use locale-aware case folding | pre-existing line | fixed | 3 |
| 883 | src/services/commands/claimCommands.ts | 20 | Low | Refactoring Debt | The 34-line registrar embeds claim-extraction business logic inside a registration callback, reducing testability and reuse | Extract a named selection-to-draft handler | changed line | fixed | 3 |
| 884 | src/services/commands/claimCommands.ts | 24 | High | Logic & Correctness | A single sentinel cannot prove the two-command batch is complete or owned by this registrar; partial registration, foreign collision, or HMR leaves stale/missing commands permanently skipped | Register both definitions atomically with `registerCommands` and a stable owner token | changed line | fixed | 1 |
| 885 | src/services/commands/claimCommands.ts | 33 | Medium | Shortcuts & Patches | The extraction command has no editor scope or `when` predicate, so it remains available when its workspace, document, editor, or non-empty-selection prerequisites are absent and then silently does nothing | Add editor scope and an availability predicate | changed line | fixed | 3 |
| 886 | src/services/commands/claimCommands.ts | 38 | High | Logic & Correctness | Defaulting a missing context to `"main"` can resolve the wrong document or no document in secondary windows | Require the invoking window label or derive the current window label | changed line | not-fixed (rebuttal: every dispatch path supplies windowLabel — menuListener, CommandPalette via resolveCommandContext, WelcomeScreen and useKeybindingRouter all pass one — and ?? "main" is the codebase-wide Ctx convention across ~10 registrars; changing two would create the drift it means to prevent) | 1 |
| 887 | src/services/commands/claimCommands.ts | 39 | High | Logic & Correctness | The command reads the generic Tiptap registration rather than the active editor and never verifies its tab against the active document; split/source mode can use a hidden stale selection and attach the wrong provenance | Resolve the active editor and tab together, verify the tab matches the active document, and support or explicitly reject source selections | changed line | fixed | 1 |
| 888 | src/services/commands/clipboardCommands.ts | 42 | High | Logic & Correctness | Surface resolution uses only `sourceMode`; split view has `sourceMode === false`, so clipboard actions target WYSIWYG even when the source pane is focused | Resolve the focused active editor from the editor store, including split-pane source overrides | changed line | not-fixed (out of reach: a SplitPaneEditor source pane registers with nothing resolveClipboardSurface can read — setActiveSourceView is the markdown SourceEditor's and setContextMenuSourceView fires on right-click, not focus; closing it needs a focus registration in SplitPaneEditor/SourcePane.tsx) | 1 |
| 889 | src/services/commands/clipboardCommands.ts | 46 | High | Duplication | Every entry duplicates the command ID as a translation key, creating four independent opportunities for the values to drift | Remove `key` and derive `commands:${id}` | changed line | fixed | 3 |
| 890 | src/services/commands/clipboardCommands.ts | 58 | High | Logic & Correctness | The `edit.copy` sentinel cannot distinguish a complete owned batch from partial, foreign, or stale HMR registrations, potentially leaving clipboard commands missing or outdated | Build all definitions and use owner-based atomic `registerCommands` | pre-existing line | fixed | 1 |
| 891 | src/services/commands/clipboardCommands.ts | 61 | Medium | Shortcuts & Patches | Clipboard commands have no editor scope or availability check; with no registered editor, fallback behavior can operate on whichever DOM element retained focus | Gate each command on a valid concrete editor target | changed line | fixed | 3 |
| 892 | src/services/commands/clipboardCommands.ts | 65 | High | Logic & Correctness | Only a surface enum is passed into an asynchronous clipboard operation; paste/native invocation can re-resolve a different editor after an await and modify the tab or pane selected meanwhile | Snapshot the concrete target and tab, then revalidate them immediately before applying the operation | changed line | not-fixed (rebuttal: the macOS path is trigger_webview_edit, which sends a selector down the responder chain and structurally cannot take a target, so "snapshot the concrete target" is not expressible for the shipping path) | 1 |
| 893 | src/services/commands/exportCommands.ts | 4 | Low | Shortcuts & Patches | The header says this module registers six export commands, but the eager registrar registers five and the lazy registrar adds six more | Correct the documentation to distinguish five eager commands from six lazy Pandoc formats | pre-existing line | fixed | 3 |
| 894 | src/services/commands/exportCommands.ts | 27 | High | Logic & Correctness | Falling back to `"main"` exports the wrong document or silently fails when a secondary-window caller omits context | Require `windowLabel` or derive the current window label | pre-existing line | not-fixed (rebuttal: same as #886 — no dispatch path omits windowLabel) | 1 |
| 895 | src/services/commands/exportCommands.ts | 28 | Medium | Logic & Correctness | The WYSIWYG flush runs outside both the re-entry guard and the failure-containment block; rejected re-entry still causes a side effect, and a throwing flusher rejects command dispatch | Acquire the guard first and contain flush failures inside the guarded operation | pre-existing line | fixed | 3 |
| 896 | src/services/commands/exportCommands.ts | 30 | Medium | Shortcuts & Patches | Document export commands remain visible without an active document and report successful dispatch while performing no work | Add editor scope and a `when` predicate based on active-document availability | pre-existing line | fixed | 3 |
| 897 | src/services/commands/exportCommands.ts | 54 | Medium | Shortcuts & Patches | Unexpected failures, including lazy-chunk loading failures, are only logged; the user receives no failure indication | Show a localized error notification or rethrow to a centralized command error handler | pre-existing line | fixed | 3 |
| 898 | src/services/commands/exportCommands.ts | 62 | Low | Refactoring Debt | The eager registrar is 48 lines and mixes batch construction with command-specific behavior | Build declarative definitions separately and register them as one batch | changed line | not-fixed (rebuttal: the remedy is already the code — buildExportCommandSpecs() is a pure declarative list and registerExportCommands() is a 3-line one-batch register; splitting five self-contained specs into five functions is churn) | 3 |
| 899 | src/services/commands/exportCommands.ts | 66 | High | Logic & Correctness | The HTML sentinel cannot prove the five-command batch is complete or owned by this module; partial registration and HMR can leave missing or stale definitions | Use atomic owner-based registration for the complete eager batch | changed line | fixed | 1 |
| 900 | src/services/commands/exportCommands.ts | 104 | Medium | Shortcuts & Patches | `openUrl` failures are neither logged nor shown to the user, unlike the other export commands | Wrap the operation in the shared failure-handling path and provide a localized error | changed line | fixed | 3 |
| 901 | src/services/commands/exportCommands.ts | 113 | Low | Dead Code | `__resetExportCommandsRegistration` is an exported no-op compatibility shim that misleadingly suggests it resets state | Remove it and update callers to reset the command bus directly | changed line | fixed | 3 |
| 902 | src/services/commands/exportCommands.ts | 122 | Medium | Refactoring Debt | `registerPandocFormatCommands` exceeds 30 lines and nests the loop, registration callback, document callback, and error handling more than three levels deep | Generate definitions separately and delegate execution to the shared document-export runner | changed line | fixed | 3 |
| 903 | src/services/commands/exportCommands.ts | 126 | High | Logic & Correctness | `hasCommand(id)` silently accepts any existing command, including a foreign collision, contradicting the comment that genuine collisions still throw and preserving stale HMR handlers | Register the complete Pandoc batch with a stable owner so own replacements and foreign collisions are distinguishable | changed line | fixed | 1 |
| 904 | src/services/commands/exportCommands.ts | 131 | High | Duplication | Pandoc handlers duplicate the `withDoc` plus `try/catch/menuError` envelope that `registerDocExportCommand` exists to centralize, and the two paths already differ in notification behavior | Extend the shared helper with customizable titles and failure callbacks, then use it for Pandoc formats | changed line | fixed | 3 |
| 905 | src/services/commands/exportCommands.ts | 147 | Low | Dead Code | The module dynamically re-imports `@/i18n` inside the error path even though the same instance is already statically imported | Use the existing `i18n` binding and keep the recovery path dependency-minimal | changed line | fixed | 3 |
| 906 | src/services/commands/formatCommands.ts | 86 | High | Logic & Correctness | Checking only the first command cannot detect foreign ownership or recover partial registration; a later `registerCommand` failure leaves an incomplete batch that every retry skips | Build all definitions first and register them atomically with `registerCommands("format-commands", definitions)` | changed line | fixed | 1 |
| 907 | src/services/commands/formatCommands.ts | 110 | Low | Shortcuts & Patches | Exported reset function is an explicit no-op compatibility shim that misleads callers into expecting state restoration | Remove it and update callers, or replace it with a meaningful bus reset owned by the test harness | changed line | fixed | 3 |
| 908 | src/services/commands/lintCommands.ts | 68 | Low | Shortcuts & Patches | Exported reset function is another no-op compatibility shim and creates a false testing contract | Remove it and update callers to reset `CommandBus` directly | changed line | fixed | 3 |
| 909 | src/services/commands/menuCommandsReady.ts | 52 | Medium | Logic & Correctness | A second signal is documented as a bug but is silently discarded; a failed first mount permanently prevents a successful retry from restoring readiness | Report conflicting duplicate signals, or introduce mount generations if remounting is valid | changed line | fixed | 2 |
| 910 | src/services/commands/menuCommandsReady.ts | 67 | Low | Logic & Correctness | `budgetMs` is unvalidated; negative, non-finite, or timer-overflow values can produce an effectively immediate timeout | Validate and clamp the delay to a documented finite non-negative range | changed line | fixed | 3 |
| 911 | src/services/commands/menuCommandsReady.ts | 89 | Medium | Logic & Correctness | Reset drops pending waiter callbacks without clearing their timers or settling their promises, allowing stale asynchronous work to cross test boundaries | Resolve the captured waiters with `false` before resetting so each callback clears its timer | changed line | fixed | 2 |
| 912 | src/services/commands/menuListener.ts | 105 | High | Refactoring Debt | `mountMenuCommands` is roughly 79 lines with multiple loops, nested branches, asynchronous registration, payload parsing, dispatch, error handling, and teardown ownership; its cyclomatic complexity makes lifecycle defects difficult to isolate | Extract preflight validation, payload decoding, binding registration, command dispatch, and teardown construction into focused helpers | changed line | fixed | 3 |
| 913 | src/services/commands/menuListener.ts | 127 | Medium | Logic & Correctness | Teardown ignores `safeUnlistenAll`’s returned empty array while retaining the original `const` array, so calling `off` twice invokes every unlistener twice | Store the returned empty array in mutable state or guard teardown with a disposed flag | pre-existing line | not-fixed (rebuttal: teardown is guarded by tornDown, declared on the line above the cited code — if (tornDown) return; tornDown = true; — so calling off twice cannot invoke any unlistener twice, and the idempotence is documented there as audit #711) | 3 |
| 914 | src/services/commands/menuListener.ts | 130 | High | Logic & Correctness | Menu teardown disposes the per-window editor-action owner even though that owner is shared with Command Palette dispatch; remounting or unmounting the menu can cancel another caller’s queued retry and silently drop an edit | Move owner disposal to the editor-host/window lifecycle, or introduce independently reference-counted ownership | pre-existing line | not-fixed (rebuttal: mountMenuCommands' teardown has exactly one caller — useCommandBootstrap's effect cleanup, mounted once per window by useEditorLifecycle — so its lifetime IS the editor host's, which is the disposer editorActionOwner's docblock names as correct; a StrictMode remount costs at most a 50ms retry timer) | 1 |
| 915 | src/services/commands/menuListener.ts | 167 | High | Logic & Correctness | The routing payload is forwarded unchanged as command arguments: a label-only string becomes an argument, while `[data, windowLabel]` exposes the entire transport envelope instead of `data` | Decode routing separately and pass `Array.isArray(payload) ? payload[0] : undefined` to `executeCommand` | changed line | fixed | 1 |
| 916 | src/services/commands/menuListener.ts | 167 | High | Logic & Correctness | The boolean result from `executeCommand` is ignored, so an absent command target silently no-ops while the mount can still be reported complete | Validate command targets during preflight and distinguish missing commands from ordinary `when` rejections | changed line | fixed | 1 |
| 917 | src/services/commands/miscCommands.ts | 34 | Medium | Refactoring Debt | The 154-line registrar mixes settings, palettes, destructive history operations, media cleanup, help, and AI-folder behavior. | Split command-spec construction by domain and register the resulting batch atomically. | changed line | fixed | 3 |
| 918 | src/services/commands/miscCommands.ts | 38 | High | Logic & Correctness | Checking only `app.preferences` can silently suppress every other registration when that ID already has a foreign registration; standalone mid-batch failures can also leave a partial registry that future calls skip. | Register the complete command set atomically with `registerCommands` and a module-specific owner. | changed line | fixed | 1 |
| 919 | src/services/commands/miscCommands.ts | 78 | Medium | Logic & Correctness | `clearAllHistory()` absorbs filesystem failures, so execution continues to log success and emit `historyCleared` after deletion failed. | Return a structured success result and emit/log completion only when deletion succeeds. | pre-existing line | not-fixed (rebuttal: clearAllHistory surfaces its own failure with toast.error(dialog:toast.historyClearAllFailed); what follows is a DEV-only log line and emitHistoryCleared, which makes panels re-read from disk and show the truth; a structured result needs services/history/historyRecovery.ts, outside this group) | 3 |
| 920 | src/services/commands/miscCommands.ts | 107 | Medium | Logic & Correctness | `clearWorkspaceHistory()` returns `0` for both “nothing found” and total failure; the caller nevertheless logs completion and emits `historyCleared`. | Return an outcome distinguishing success, partial failure, and total failure. | pre-existing line | not-fixed (rebuttal: same shape — clearWorkspaceHistory toasts historyClearWorkspaceFailed on both total and partial failure, and the 0 return is read only by a DEV log) | 3 |
| 921 | src/services/commands/miscCommands.ts | 150 | Medium | Logic & Correctness | All three help-link handlers allow `openUrl()` rejection to escape without user-facing feedback. | Route URL opens through a shared error-handling helper that logs and shows a localized failure toast. | changed line | fixed | 2 |
| 922 | src/services/commands/miscCommands.ts | 178 | Medium | Shortcuts & Patches | Opening the Genies folder catches failures but only writes a debug log, leaving an interactive command visibly doing nothing. | Display a localized error toast or dialog after logging. | changed line | fixed | 3 |
| 923 | src/services/commands/miscCommands.ts | 191 | Low | Dead Code | `__resetMiscCommandsRegistration` is an exported no-op retained solely as a compatibility shim and does not reset anything its name promises. | Remove it and migrate callers to reset the command bus explicitly. | changed line | fixed | 3 |
| 924 | src/services/commands/paneCommands.ts | 32 | Medium | Logic & Correctness | Sync-scroll, close-pane, and focus-other-pane commands have no `when` predicates; outside an active split they are exposed despite being no-ops, while sync-scroll mutates invisible latent state. | Add availability predicates based on a live enabled split and, for focus, a valid target pane. | changed line | fixed | 2 |
| 925 | src/services/commands/paneCommands.ts | 77 | Low | Dead Code | `__resetPaneCommandsRegistration` is an exported no-op compatibility shim. | Remove the export and update reset callers to reset the command bus. | changed line | fixed | 3 |
| 926 | src/services/commands/recentFilesCommands.ts | 69 | Medium | Logic & Correctness | `exists()` rejection is unhandled, and a successful existence check does not prove the path is a readable regular file; the later `OpenOutcome` is discarded, so unreadable stale entries survive despite the function’s documentation. | Catch preflight errors and use the open operation’s outcome to distinguish cancellation, closure, and read failure. | pre-existing line | fixed | 3 |
| 927 | src/services/commands/recentFilesCommands.ts | 87 | Medium | Logic & Correctness | Every non-cancelled replacement failure is presented as “file not found” and offers removal, although failures can originate from ingestion, workspace switching, ownership, or other internal processing. | Return typed failure reasons from the replacement helper and offer removal only for confirmed missing/unreadable source paths. | pre-existing line | fixed | 3 |
| 928 | src/services/commands/recentFilesCommands.ts | 116 | Medium | Logic & Correctness | The new-window route does not perform stale-entry cleanup, so the same missing recent file is removable in create/replace routes but remains indefinitely in this route. | Centralize path validation before routing or classify the IPC error and offer removal on confirmed absence. | pre-existing line | fixed | 3 |
| 929 | src/services/commands/recentFilesCommands.ts | 129 | High | Logic & Correctness | The first-command sentinel can silently skip `file.openRecent` when `file.clearRecent` is already registered, and it does not provide atomic standalone registration. | Use one owner-scoped `registerCommands` batch. | changed line | fixed | 1 |
| 930 | src/services/commands/recentFilesCommands.ts | 183 | High | Duplication | The five-way open-action dispatcher duplicates the centralized file-open decision executor and has already drifted in activation and error-handling behavior. | Extend and reuse the shared executor, injecting recent-entry cleanup behavior where necessary. | pre-existing line | fixed | 3 |
| 931 | src/services/commands/recentFilesCommands.ts | 186 | Medium | Logic & Correctness | Activating an existing tab with plain `setActiveTab` bypasses workspace-owner activation, potentially leaving the sidebar and workspace context on a different workspace from the document. | Use `activateTabWithWorkspaceContext`. | pre-existing line | fixed | 3 |
| 932 | src/services/commands/recentFilesCommands.ts | 192 | Medium | Logic & Correctness | Failure to establish the file’s workspace is only logged; the file then opens under the previous workspace context without warning. | Surface the failure with a localized warning and reuse the shared open-decision behavior. | pre-existing line | fixed | 3 |
| 933 | src/services/commands/recentFilesCommands.ts | 220 | Low | Dead Code | `__resetRecentFilesCommandsRegistration` is an exported no-op compatibility shim. | Remove it after migrating callers to the command-bus reset API. | changed line | fixed | 3 |
| 934 | src/services/commands/recentWorkspacesCommands.ts | 33 | High | Logic & Correctness | The first-command sentinel can silently suppress `workspace.openRecent` and cannot guarantee atomic registration outside the outer bootstrap transaction. | Register both commands as an owner-scoped atomic batch. | changed line | fixed | 1 |
| 935 | src/services/commands/recentWorkspacesCommands.ts | 61 | Medium | Refactoring Debt | The command callback spans roughly 65 lines and nests validation, permission-sensitive I/O, dirty-document policy, IPC, restoration, UI state, and recents mutation. | Extract a focused recent-workspace orchestration function and reuse the shared transition service. | changed line | fixed | 3 |
| 936 | src/services/commands/recentWorkspacesCommands.ts | 69 | High | Logic & Correctness | The filesystem existence check runs before granting workspace access and is not caught; known recent workspaces outside the static Tauri scope can therefore reject without feedback. | Grant workspace access before filesystem probing and handle permission errors explicitly. | changed line | fixed | 1 |
| 937 | src/services/commands/recentWorkspacesCommands.ts | 69 | Medium | Logic & Correctness | `exists()` accepts regular files as well as directories, allowing malformed or programmatic arguments to install a file path as the workspace root. | Use filesystem metadata and require a directory before proceeding. | changed line | fixed | 2 |
| 938 | src/services/commands/recentWorkspacesCommands.ts | 113 | High | Duplication | The no-dirty-tabs branch reimplements the shared workspace-opening workflow while omitting its access grant and top-level error boundary, creating a correctness-critical drift point. | Keep the dirty-tab decision locally, then delegate the accepted in-window transition to `openWorkspaceByPath`. | pre-existing line | fixed | 3 |
| 939 | src/services/commands/recentWorkspacesCommands.ts | 131 | Low | Dead Code | `__resetRecentWorkspacesCommandsRegistration` is an exported no-op compatibility shim. | Remove it and update callers to reset the registry directly. | pre-existing line | fixed | 3 |
| 940 | src/services/commands/registerAllCommands.ts | 4 | Low | Refactoring Debt | Header claims thirteen registrations and says non-editor registrations are second-call no-ops; the function now invokes fifteen registrars, while owner-based registrars replace their batches | Remove the brittle count and document the actual replace-own lifecycle | pre-existing line | fixed | 3 |
| 941 | src/services/commands/viewCommands.ts | 31 | High | Logic & Correctness | Local zoom bounds `[12,32]` conflict with the settings store’s valid `[8,48]` range; Zoom In at 48 reduces the size to 32, while Zoom Out at 8 increases it to 12 | Use centralized appearance bounds and guarantee each command is monotonic | pre-existing line | fixed | 1 |
| 942 | src/services/commands/viewCommands.ts | 42 | Medium | Refactoring Debt | `buildViewCommandSpecs` spans 229 lines, far beyond the 30-line review threshold and leaving the file at 292 lines | Split specifications into cohesive mode, sidebar, display, and zoom builders | changed line | fixed | 3 |
| 943 | src/services/commands/viewCommands.ts | 55 | Medium | Logic & Correctness | Cleanup runs before `toggleSourceModeWithCheckpoint` performs its YAML/workflow rejection, so a refused switch still flushes state and closes popups | Centralize cleanup after validation in the mode-toggle service | changed line | not-fixed (out of reach: the refusal guard lives in toggleSourceModeWithCheckpoint in src/services/history/unifiedHistory.ts, another fixer's file — the cleanup must move INSIDE it, after the guard, since flushActiveWysiwygNow() must precede the switch; fixing it in viewCommands alone would duplicate looksLikeWorkflowPath, which that guard's comment says must stay the single entry point) | 2 |
| 944 | src/services/commands/viewCommands.ts | 82 | Medium | Logic & Correctness | Both explorer toggle commands omit availability checks and discard the helper’s boolean result; without an active workspace they silently do nothing while remaining executable | Add a workspace `when` predicate or handle `false` with user feedback | changed line | fixed | 2 |
| 945 | src/services/commands/viewCommands.ts | 174 | Medium | Logic & Correctness | `setWysiwygMode` checks only global source/split flags and returns early for a document in forced-source mode, bypassing the imported toggle helper’s forced-source exit logic | Base the decision on effective document mode or delegate to a centralized set-WYSIWYG operation | pre-existing line | fixed | 3 |
| 946 | src/services/commands/viewCommands.ts | 175 | High | Duplication | The markdown-split branch calls `cleanupBeforeModeSwitch` here and again inside `toggleMarkdownSplitWithCheckpoint`, duplicating popup resets and WYSIWYG flushes | Let each toggle helper own cleanup exactly once | pre-existing line | fixed | 3 |
| 947 | src/services/commands/viewCommands.ts | 283 | Medium | Logic & Correctness | Registration is atomic only for the main view batch; pane and lint batches are registered afterward, so a direct call can leave earlier batches replaced when a later registration throws | Wrap all three registrations in one transaction or combine their specifications into one preflighted batch | changed line | not-fixed (rebuttal: registerAllCommands snapshots the whole registry and calls restoreCommandRegistry(before) on any throw (audit #453) — the transaction already exists one level up and was built precisely because per-registrar atomicity is not the unit that matters; a bare registerViewCommands() call happens only in tests) | 2 |
| 948 | src/services/commands/viewCommands.ts | 292 | Low | Dead Code | `__resetViewCommandsRegistration` is an exported no-op retained solely as a compatibility shim | Remove it and update callers to reset the command bus directly | changed line | fixed | 3 |
| 949 | src/services/commands/windowCommands.ts | 28 | Medium | Logic & Correctness | Failure to enumerate windows is outside all error handling, causing the entire command to reject despite the handler’s stated best-effort behavior | Catch enumeration failure and log it consistently | new file | fixed | 2 |
| 950 | src/services/commands/windowCommands.ts | 39 | High | Logic & Correctness | The current window is focused unconditionally without visibility or minimized checks, so a minimized current window — or hidden renderer context — can be brought onscreen contrary to the stated invariant | Apply the same eligibility checks to the current window before focusing it last | new file | fixed | 1 |
| 951 | src/services/commands/workspaceCommands.ts | 67 | Low | Refactoring Debt | `openWorkspaceFolder` exceeds 30 lines and embeds a large platform-capability rationale inside control flow | Extract the dialog configuration and permission rationale into a named constant or helper | changed line | fixed | 3 |
| 952 | src/services/commands/workspaceCommands.ts | 93 | Medium | Logic & Correctness | The boolean result from `openWorkspaceByPath` is discarded, so a failed or partially completed open produces no user-facing failure response | Inspect the result and surface an appropriate localized error | changed line | not-fixed (rebuttal: openWorkspaceWithConfig never throws — every failure path calls openWorkspaceWithDefaults, which opens the workspace anyway — so a false here means the workspace DID open and a later restore step threw; a "failed to open workspace" toast would be false) | 2 |
| 953 | src/services/commands/workspaceCommands.ts | 105 | Medium | Shortcuts & Patches | Unexpected persistence, dirty-close, or finalization failures are neither logged with command context nor converted to user feedback and therefore reject through the command bus | Catch unexpected close failures, log them, and show a localized failure toast while preserving cancellation semantics | changed line | fixed | 3 |
| 954 | src/services/commands/workspaceCommands.ts | 106 | High | Logic & Correctness | The active rail workspace is resolved only after asynchronous persistence; the user can activate another workspace during that await, causing Close Workspace to close the wrong instance | Capture and validate the target instance before the first await | changed line | fixed | 1 |
| 955 | src/services/commands/workspaceCommands.ts | 148 | Low | Dead Code | `__resetWorkspaceCommandsRegistration` is an exported no-op compatibility shim | Remove it and update callers to reset the command bus directly | changed line | not-fixed (out of reach: __resetWorkspaceCommandsRegistration is also imported by src/services/commands/recentWorkspacesCommands.test.ts, which belongs to another group; deleting the export would break a file outside this one. The other four shims of this class were removed) | 3 |
| 956 | src/services/contentServer/slidevDeck.ts | 25 | Medium | Logic & Correctness | Missing or unsupported extensions silently become `"pdf"`, allowing PDF data to be written to misleading paths such as `deck.ppt` or extensionless files | Validate against `pdf`, `png`, and `pptx`; reject unsupported extensions or normalize the path to `.pdf` | new file | not-fixed (rebuttal: the sole caller validates the derived format against the chosen path and refuses deck.ppt and extensionless outputs with a localized toast; that guard is audit #361, cited in the code) | 2 |
| 957 | src/services/editor/activeSelectionText.ts | 65 | Medium | Logic & Correctness | The primary selection is returned without confirming its recorded tab ID matches the currently active tab; stale editor registrations can seed Find from the wrong document, and line 67 only compares the two surfaces with each other | Resolve the current active tab and require the selected surface’s tab ID to match it before returning text | new file | not-fixed (rebuttal: useFocusedPaneTiptapRegistration registers only from the focused, non-hidden, non-preview pane with an identity-guarded clear, and paneStore.syncActiveTab mirrors the focused pane's tab into tabStore.activeTabId — the ADR-1 alias invariant — so the registration's tab id IS the active tab) | 2 |
| 958 | src/services/genieInvocation/cancelRequest.ts | 45 | High | Logic & Correctness | A failed cancellation is deliberately hidden whenever a newer invocation is running, contradicting the requirement to expose a potentially still-running and billing provider request | Report cancellation failures through a request-independent notification surface instead of suppressing them to protect the singleton invocation state | new file | fixed | 1 |
| 959 | src/services/genieInvocation/cancelRequest.ts | 45 | Medium | Logic & Correctness | The asynchronous rejection guard uses only `isRunning`, creating an ABA race: if a newer invocation starts and finishes before the old cancellation rejects, `setError` overwrites the newer invocation’s successful/idle state with a stale error | Track an invocation generation or cancellation token and apply the failure only when that generation is still current | new file | fixed | 2 |
| 960 | src/services/genieInvocation/providerValidation.ts | 32 | Low | Logic & Correctness | A missing active provider returns silently, contradicting the function’s user-feedback contract and hiding state races | Show the no-provider toast or return a typed validation failure for the caller to display | new file | fixed | 3 |
| 961 | src/services/genieInvocation/providerValidation.ts | 37 | Medium | Logic & Correctness | A non-REST provider passes validation when its CLI entry is missing; persisted unknown provider strings or incomplete detection therefore reach Rust as unsupported providers | Require a matching, available CLI entry and return a discriminated CLI/REST validation result | new file | not-fixed (rebuttal: the module header states the scope — two refusals, everything else is the runner's problem; a missing cliProviders entry is not "the binary was not found", and the provider reaches Rust, which answers with a typed CommandError that failInvocation surfaces) | 2 |
| 962 | src/services/genieInvocation/providerValidation.ts | 45 | Medium | Logic & Correctness | REST validation checks only API-key truthiness: whitespace keys pass, key-optional providers pass without any config, and required custom endpoint/model fields may remain blank | Validate trimmed, provider-specific required fields and reject missing REST configurations | new file | fixed | 2 |
| 963 | src/services/genieInvocation/streamRunner.ts | 97 | High | Logic & Correctness | The global Tiptap editor is not verified to belong to `ctx.tabId`; tab focus and editor registration can change independently, allowing dispatch into the wrong document | Use the tab-bound active editor and require `activeWysiwygTabId === ctx.tabId` before dispatch | pre-existing line | fixed | 1 |
| 964 | src/services/genieInvocation/streamRunner.ts | 104 | High | Logic & Correctness | Auto-apply reads document state but never checks `readOnly`, so a programmatic transaction can mutate a read-only duplicate despite editor UI restrictions | Refuse direct application when the originating document is read-only and enforce the same rule at suggestion acceptance | changed line | fixed | 1 |
| 965 | src/services/genieInvocation/streamRunner.ts | 107 | High | Logic & Correctness | Captured `from`/`to` positions are reused after an arbitrarily long stream without checking same-tab edits; typing during generation can shift the range and corrupt content, and late-created suggestions miss prior remapping | Capture a document revision and reject or remap the result when the originating document changes | changed line | fixed | 1 |
| 966 | src/services/genieInvocation/streamRunner.ts | 130 | Medium | Logic & Correctness | `trim()` changes generated content, including meaningful leading indentation and trailing newlines | Use trimming only for the emptiness check and apply/store the original accumulated response | changed line | not-fixed (rebuttal: the trim is the normalization that makes a model response safe to splice into a document RANGE — providers routinely emit a leading/trailing newline, and applying the raw accumulation would insert stray blank lines at every replacement; the finding trades a universal regression for a rare indentation case with no measurement) | 2 |
| 967 | src/services/genieInvocation/streamRunner.ts | 170 | High | Logic & Correctness | The event handler checks only the payload request ID, not whether that request is still active; a queued event after cancellation can finish, fail, or modify state belonging to a newer invocation | Require the invocation store’s current request ID to match before processing every chunk | changed line | fixed | 1 |
| 968 | src/services/genieInvocation/streamRunner.ts | 182 | Medium | Logic & Correctness | Stream accumulation has no byte, character, or chunk limit and duplicates the growing response in local and Zustand state, permitting excessive memory use and UI stalls | Enforce a total response limit and cancel/fail the request when exceeded | changed line | not-fixed (rebuttal: the response is bounded by the provider's own output limit and the duplication is one string plus the picker's copy; an arbitrary cap would fail legitimate whole-document rewrites, which scope: "document" is a shipped genie scope for, and no stall is measured) | 2 |
| 969 | src/services/genieInvocation/streamRunner.ts | 185 | Medium | Logic & Correctness | There is no terminal-state guard, so duplicate `done` frames can apply the edit or add the suggestion multiple times | Add a request-local terminal flag before processing error/done frames | pre-existing line | not-fixed (rebuttal: the terminal guard already exists one level out — every terminal path clears the store's requestId, and the chunk handler drops any frame whose id is not the ACTIVE one, added by #967 on the line above the citation; pinned from the outside, and removing the guard turns both tests red) | 3 |
| 970 | src/services/genieInvocation/streamRunner.ts | 186 | High | Logic & Correctness | Exceptions from markdown parsing, transaction construction, dispatch, or result stores occur in the later event callback and bypass `runGenieStream`’s outer catch; listener and invocation state can remain stuck | Wrap chunk processing in request-aware `try/catch/finally` cleanup | pre-existing line | fixed | 1 |
| 971 | src/services/genieInvocation/streamRunner.ts | 214 | Low | Refactoring Debt | `runGenieStream` exceeds 30 lines and combines validation, identity capture, locking, UI setup, listener ownership, dispatch, and failure handling | Extract a request-owned stream session and a separate backend-dispatch helper | pre-existing line | not-fixed (rebuttal: already split three ways for the size gate — providerValidation, applyGenieResult, streamRunnerContext — and what is left is a linear sequence with no duplication; re-extracting a "session" would re-merge the request-vs-shared ownership the audit just separated) | 3 |
| 972 | src/services/genieInvocation/streamRunner.ts | 226 | Medium | Shortcuts & Patches | The `"unknown"` tab sentinel converts a broken invariant into orphaned suggestions or an unusable invocation | Fail before acquiring the lock unless a live originating tab and document exist | pre-existing line | fixed | 3 |
| 973 | src/services/genieInvocation/streamRunner.ts | 247 | High | Logic & Correctness | No watchdog handles a missing terminal frame; if invocation succeeds or hangs without `done`/`error`, the listener and singleton lock remain indefinitely | Add a bounded terminal timeout that cancels Rust and performs request-scoped cleanup | pre-existing line | not-fixed (rebuttal: the elapsed timer plus the user-facing Cancel — which reaches Rust via cancel_ai_prompt — is the designed escape from a stalled provider, and any fixed watchdog would abort legitimately long CLI generations; a request timeout belongs to the Rust provider layer that owns the request) | 1 |
| 974 | src/services/genieInvocation/streamRunner.ts | 269 | High | Logic & Correctness | A rejection from an old cancelled request unconditionally fails the current store and releases the shared `listenerRef`, which may already hold a newer request’s listener | Keep each unlisten function local to its request and mutate state only when the active request ID still matches | pre-existing line | fixed | 1 |
| 975 | src/services/genieInvocation/streamRunner.ts | 270 | Medium | Logic & Correctness | `errorMessage` renders typed Tauri `CommandError` objects as `[object Object]` | Use `commandErrorMessage` and handle the `cancelled` code without presenting it as a failure | pre-existing line | fixed (the substance: the [object Object] half is REFUTED — run_ai_prompt is Result<(), String>, where errorMessage is correct and the ratchet is deliberately silent; the real defect next to it was failInvocation's unscoped picker write, now gated on still owning the invocation) | 3 |
| 976 | src/services/navigation/restoreWorkspaceTabs.ts | 71 | Medium | Logic & Correctness | Media restoration executes outside either catch block; any media initialization, ownership, or persistence exception aborts restoration of all remaining sibling tabs | Catch media-path failures inside `restoreOnePath`, roll back safely, log the cause, and return `false` | pre-existing line | fixed | 3 |
| 977 | src/services/navigation/restoreWorkspaceTabs.ts | 71 | Medium | Logic & Correctness | Reusing the normal media-open path adds every restored media file to Recent Files, while restored text files do not, producing inconsistent session-restore side effects | Add a restore option that suppresses recent-file mutation | pre-existing line | not-fixed (out of reach: needs a suppress-recents option on src/services/navigation/openMediaFile.ts, outside this group's file set; the claim is CONFIRMED by reading — openMediaFileInNewTab calls useRecentFilesStore.addFile, so restored media enters Recent Files while restored text does not) | 3 |
| 978 | src/services/navigation/restoreWorkspaceTabs.ts | 76 | Low | Shortcuts & Patches | The read-error catch discards the actual exception and labels permission, encoding, and filesystem failures identically | Capture and pass the error to `workspaceWarn` | pre-existing line | fixed | 3 |
| 979 | src/services/navigation/restoreWorkspaceTabs.ts | 88 | High | Logic & Correctness | The dedup check consults document `filePath`, while `createTab` deduplicates using tab metadata; an existing tab still loading without a document slips through, is returned as though newly created, and is then re-ingested | Make tab creation atomic and return `{ tabId, created }`, or deduplicate against the same tab-level identity | changed line | fixed | 1 |
| 980 | src/services/navigation/restoreWorkspaceTabs.ts | 102 | High | Logic & Correctness | Rollback uses the user-facing `closeTab`: it can close a pre-existing deduplicated tab, records a false recently-closed entry, and does not itself reverse document or ownership mutations | Use a dedicated transactional rollback that runs only for a confirmed new tab and removes every associated state entry | changed line | fixed | 1 |
| 981 | src/services/navigation/restoreWorkspaceTabs.ts | 127 | Medium | Logic & Correctness | The persisted list is explicitly untrusted but has no maximum length, allowing a corrupted session to trigger unbounded sequential filesystem reads | Impose a documented maximum restored-tab count and warn when truncating | pre-existing line | not-fixed (rebuttal: the list is written by the app from the user's own open tabs, per-entry schema validation already rejects malformed entries, and any cap would silently drop tabs a real user had open — the worse failure — with no mechanism to justify the number) | 3 |
| 982 | src/services/navigation/restoreWorkspaceTabs.ts | 137 | High | Logic & Correctness | Each awaited read is followed by an activating `createTab`; user focus changes during restoration can be repeatedly overwritten by later paths | Read first and commit synchronously, or create restored tabs without activation and preserve a user-activation epoch | pre-existing line | not-fixed (rebuttal: restoration deliberately owns activation for its duration — that is what makes the workspace open on its last file — and the suggested "create without activation" changes observable behaviour beyond a defect fix) | 1 |
| 983 | src/services/navigation/restoreWorkspaceTabs.ts | 154 | Medium | Logic & Correctness | Removing the startup blank through ordinary `closeTab` records a synthetic Untitled entry in closed-tab history and emits normal close/activation side effects | Replace the blank tab directly or remove it through a silent restoration-cleanup action | pre-existing line | fixed | 3 |
| 984 | src/services/navigation/restoreWorkspaceTabs.ts | 163 | High | Logic & Correctness | Collapsing the entire MRU after a loop containing awaits erases genuine user activations that occurred while files were loading | Mark only synchronous restoration activations with the existing `restore` origin instead of resetting the complete MRU | pre-existing line | not-fixed (rebuttal: collapseMruToActive's docblock already considered and rejected the restore origin, with the mechanism: the loop awaits per path, so a dynamic origin scope leaks across the await and mislabels genuine activations) | 1 |
| 985 | src/services/navigation/restoreWorkspaceTabs.ts | 185 | Medium | Logic & Correctness | Split restoration calls `setActiveTab` and `openSplit` under the default `user` origin, fabricating MRU history immediately after restoration cleanup | Wrap the synchronous split restoration in a `restore` activation origin | pre-existing line | fixed | 3 |
| 986 | src/services/runtimeWiring.ts | 62 | Medium | Logic & Correctness | stopAll mutates stops with reverse() but never consumes it, so repeated disposal invokes every cleanup again in the opposite order | Make disposal idempotent by popping entries from the array and guarding completion | changed line | not-fixed (rebuttal: the premise is false against the current code — stopAll iterates [...stops].reverse(), a COPY, with a comment naming exactly this bug; and no path disposes twice) | 2 |
| 987 | src/services/runtimeWiring.ts | 62 | High | Logic & Correctness | One throwing disposer aborts all remaining cleanup; during startup rollback it also masks the original startup exception | Attempt every disposer, collect cleanup failures, and preserve the startup error as the primary cause | changed line | fixed | 1 |
| 988 | src/services/workflow/saveGhaWorkflowDocument.ts | 68 | High | Logic & Correctness | commit validates only content equality, not document existence, path identity, or queue generation; closing or renaming the tab during the write can bind the global workflow store to a phantom/different document and clear patches saved elsewhere | Capture the original document identity and revision, revalidate both, and update that document's stored queue without rebinding globally | new file | fixed | 1 |
| 989 | src/services/workflow/saveGhaWorkflowDocument.ts | 71 | Medium | Logic & Correctness | Removing written patches uses clearPatches followed by one queuePatch update per remainder; synchronous subscribers observe an empty intermediate queue and can have newer same-target patches overwritten during reconstruction | Add one atomic store action that removes the exact written patch identities in a single state update | new file | not-fixed (rebuttal: remaining is captured BEFORE clearPatches(), so the reconstruction is faithful; the dedupQueue overwrite needs a subscriber that re-enters queuePatch synchronously from a store notification, and every consumer is a React form component that queues from user input) | 2 |
| 990 | src/services/workflow/saveGhaWorkflowDocument.ts | 77 | High | Logic & Correctness | Same-tab saves are not serialized; overlapping calls can capture different patch snapshots, after which one commit changes the content and the other skips its editor update but still clears patches present only in its serialized result | Add a per-tab single-flight queue and recompute document and patches after acquiring it | new file | fixed | 1 |
| 991 | src/services/workflow/saveGhaWorkflowDocument.ts | 85 | Medium | Shortcuts & Patches | String equality conflates parse failure, failed/stale patch application, and a legitimate no-op; every case leaves an indistinguishable permanently pending queue | Return a discriminated serialization result with parse, application, and no-op outcomes and provide recovery for invalid patches | new file | fixed | 3 |
| 992 | src/services/workflow/saveGhaWorkflowDocument.ts | 87 | High | Logic & Correctness | The awaited lazy import delays saveToPath's save-target claim; a later Save As can submit first and then be treated as older, allowing this stale operation to restore the old path | Load before capturing state, or revalidate path/revision after import and claim ordering when the operation begins | new file | fixed | 1 |
| 993 | src/stores/aiStore/invocation.ts | 18 | High | Duplication | hasActiveStatus duplicates state derivable from isRunning, error, and showSuccess, forcing every transition and timer callback to maintain the same invariant manually | Remove the stored flag and expose a selector computing isRunning // error !== null // showSuccess | changed line | fixed | 3 |
| 994 | src/stores/aiStore/invocation.ts | 72 | Medium | Refactoring Debt | The Zustand initializer is over 60 lines and combines state transitions, concurrency guarding, and timer ownership in one function | Extract request-scoped transition helpers and a small timer controller | changed line | not-fixed (rebuttal: the timer "controller" is already two module-level handles plus clearTimers(), and the duplicated-invariant part — the one real hazard — is the withStatus extraction under #993) | 3 |
| 995 | src/stores/aiStore/invocation.ts | 80 | High | Logic & Correctness | Running state is published before elapsedInterval is assigned; a synchronous Zustand subscriber can cancel during set(), miss the timer in clearTimers(), and leave an orphan interval mutating idle or later state | Assign a generation-scoped timer before publishing, or verify the request generation inside every timer callback | pre-existing line | fixed | 1 |
| 996 | src/stores/aiStore/invocation.ts | 89 | Medium | Logic & Correctness | elapsedSeconds counts timer callbacks rather than elapsed time, so background throttling and system sleep make the displayed duration materially incorrect | Store startedAt and derive elapsed seconds from Date.now() on each tick | changed line | fixed | 2 |
| 997 | src/stores/aiStore/invocation.ts | 94 | High | Logic & Correctness | finish is not request-scoped; a delayed completion from a canceled invocation can see a newer invocation running and terminate it | Require the completing requestId/generation and no-op unless it matches the active request | changed line | fixed | 1 |
| 998 | src/stores/aiStore/invocation.ts | 105 | High | Logic & Correctness | Success state is published before successTimeout is assigned; a synchronous subscriber can start another request whose clearTimers misses this timeout, which later hides that newer request's active status | Assign the timeout before publishing or generation-guard its callback | changed line | fixed | 1 |
| 999 | src/stores/aiStore/invocation.ts | 118 | High | Logic & Correctness | setError is not request-scoped and does not even require an active invocation, so a late failure from an old request can terminate and overwrite a newer request | Accept and validate requestId/generation; use a separate action for non-invocation validation errors | changed line | fixed | 1 |
| 1000 | src/stores/aiStore/invocation.ts | 131 | Low | Logic & Correctness | dismissError treats an empty-string error as absent, leaving error and hasActiveStatus uncleared even though setError accepts any string | Check error === null instead of using a truthiness test | changed line | fixed | 3 |
| 1001 | src/stores/workflowStore.ts | 181 | Medium | Refactoring Debt | The 156-line store factory pushes the file to 339 lines, above the project’s ~300-line limit, and couples five independent domains in one initializer | Extract typed slice creators while retaining one combined store | pre-existing line | fixed | 3 |
| 1002 | src/stores/workflowStore.ts | 207 | Medium | Logic & Correctness | setGraph clears the active step and statuses but retains executionId, leaving a replaced graph associated with an old execution | Clear executionId when replacing the graph, or provide an explicit atomic carry-over action | pre-existing line | not-fixed (rebuttal: the only production caller of setGraph is the source pane's debounced re-parse, which fires on every keystroke including during a run; finishExecution matches on executionId, so clearing it there leaves a live run unfinishable and every "is a run active" read false while the backend still works — a test now PINS the retention) | 3 |
| 1003 | src/stores/workflowStore.ts | 223 | High | Logic & Correctness | setStepStatus is not scoped to an execution, so a delayed event from an earlier run can populate the current run after setExecution clears its statuses | Require executionId and ignore updates that do not match the current execution | changed line | fixed | 1 |
| 1004 | src/stores/workflowStore.ts | 260 | Medium | Logic & Correctness | queuePatch permits patches while boundDocumentId is null, but the first bindToDocument replaces that unbound queue with the document stash and silently loses those edits | Reject unbound patches or transfer the unbound queue during the first bind | pre-existing line | fixed | 3 |
| 1005 | src/stores/workflowStore.ts | 312 | High | Logic & Correctness | applyAndSerialize accepts no document ID and reads whichever queue is active at invocation time, allowing a delayed save for one tab to apply another tab’s patches | Accept a document ID and resolve or snapshot that document’s queue explicitly | changed line | fixed | 1 |
| 1006 | src/stores/workflowStore.ts | 317 | High | Shortcuts & Patches | Malformed YAML and thrown serialization errors return the original text without any failure signal, so callers cannot distinguish success from lost edits | Return a typed success/error result and preserve the queue on failure | changed line | fixed | 3 |
| 1007 | src/stores/workflowStore.ts | 318 | High | Logic & Correctness | applyPatch returns void and silently no-ops stale targets, allowing a batch to serialize only some requested edits while appearing successful | Make patch application report applied/rejected status and abort or surface partial failure | changed line | not-fixed (out of reach: applyPatch in src/lib/ghaWorkflow/save/mutators.ts returns void, so partial application is undetectable from here; it needs a per-patch applied/skipped result) | 1 |
| 1008 | src/stores/workflowStore.ts | 320 | High | Logic & Correctness | maxAliasCount: -1 disables YAML alias-expansion limits, allowing a hostile workflow to cause excessive memory or CPU use when formatting preservation is disabled | Retain a finite alias limit and report limit failures | changed line | fixed | 1 |
| 1009 | src/stores/workflowStore.ts | 331 | High | Logic & Correctness | enqueueApproval overwrites the sole pending request; concurrent requests are lost, and a stale dismissApproval call can clear a newer request | Store a queue or keyed map and dismiss by executionId plus stepId | changed line | fixed | 1 |
| 1010 | src/stores/workflowStore.ts | 339 | Low | Shortcuts & Patches | The legacy IRPatch compatibility re-export has no deprecation or removal path | Migrate consumers to the defining module, mark the export deprecated, and schedule removal | pre-existing line | fixed | 3 |
| 1011 | src/stores/workspaceStore.ts | 87 | Medium | Refactoring Debt | The Zustand initializer spans 137 lines, well above the requested 30-line function threshold | Extract focused workspace, config, exclusion, and trust action creators | pre-existing line | not-fixed (rebuttal: each action is 5-15 lines and independent, and action-creator factories would thread set/get for no removed hazard; the one part with real logic, persist normalization, WAS extracted, and the file is 292/300) | 3 |
| 1012 | src/stores/workspaceStore.ts | 92 | Medium | Logic & Correctness | openWorkspace accepts an empty or whitespace-only rootPath and creates contradictory workspace-mode state that bootstrapConfig later treats as closed | Validate rootPath before updating state and reject blank values | changed line | fixed | 2 |
| 1013 | src/stores/workspaceStore.ts | 125 | Medium | Logic & Correctness | updateConfig shallow-copies caller-provided identity and ai objects despite the documented deep-clone invariant, allowing later caller mutation to alter state without set or persistence | Deep-clone the fully merged config at the action boundary | pre-existing line | fixed | 3 |
| 1014 | src/stores/workspaceStore.ts | 142 | Medium | Logic & Correctness | addExcludedFolder accepts empty strings and multi-segment paths, although isPathExcluded only matches exact individual path segments; such entries persist but never work | Validate a nonempty single folder segment or extend the matcher to support relative paths | pre-existing line | fixed | 3 |
| 1015 | src/stores/workspaceStore.ts | 156 | Low | Logic & Correctness | removeExcludedFolder writes a new config even when the requested folder is absent, causing unnecessary subscriber notifications and persistence writes | Return without set when filtering does not change the array | pre-existing line | fixed | 3 |
| 1016 | src/stores/workspaceStore.ts | 168 | High | Duplication | setLastOpenTabs duplicates the lastOpenTabs branch already implemented by updateConfig, creating two mutation paths whose validation and cloning can drift | Delegate to updateConfig({ lastOpenTabs: tabs }) | pre-existing line | fixed | 3 |
| 1017 | src/stores/workspaceStore.ts | 224 | Medium | Shortcuts & Patches | Persisted state has no Zustand schema version, migrate hook, or normalizing merge, so rehydration bypasses normalizeWorkspaceConfig and can expose legacy or malformed config directly to actions | Add version/migrate and normalize persisted config during merge | pre-existing line | fixed | 3 |
| 1018 | src/stores/workspaceStore.ts | 244 | Medium | Logic & Correctness | Re-exporting the mutable DEFAULT_EXCLUDED_FOLDERS array lets consumers mutate the defaults used for every future workspace | Define it as a frozen readonly value and expose only the readonly type or a copy | pre-existing line | fixed | 3 |
| 1019 | src/stores/workspaceStore.ts | 253 | Low | Shortcuts & Patches | The recents-store compatibility re-exports preserve an obsolete import path without a deprecation or removal plan | Deprecate the exports, migrate consumers, and remove them in a defined release | changed line | not-fixed (out of reach: 13 production consumers across other groups' files; deleting the alias is a repo-wide import migration) | 3 |
| 1020 | src/components/Editor/WorkflowEditor/JobForm.tsx | 49 | High | Logic & Correctness | Found by the round-2 components fixer while judging #577, not by the audit: `commitIfChanged` compares the field against the PREVIEW value, which already carries the queued patch, so focusing and blurring an already-committed field takes the equal branch and calls `cancelPatchForTarget` — silently discarding the user's committed edit. `StepForm.tsx` has the same shape | Compare against the value the patch was computed FROM (the pre-edit IR), not the preview; add a test that focus+blur on a committed field leaves the queued patch intact | pre-existing line | fixed (wider than reported: JobForm name/runs-on/if AND StepForm name/run/working-directory/if plus every with: row and the expression modal; TriggerForm/PermissionsForm/ConcurrencyForm immune. Fix threads a structural-only baseline IR from the panel — a mount-time snapshot would not have worked because remount re-seeds from the preview) | 1 |
