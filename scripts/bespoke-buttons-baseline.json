{
  "//": "THREE budgets, all RATCHET DOWN ONLY \u2014 never raise any of them. To lower one, migrate a component to `.vm-btn` (src/styles/button-shared.css) and delete its bespoke class. Enforced by scripts/check-bespoke-buttons.mjs (pnpm lint:bespoke-buttons, in check:all).",
  "//maxBespokeButtonClasses": "By NAME: classes defined in src/**/*.css whose name contains btn/button, excluding the canonical .vm-btn / .popup-icon-btn / .universal-toolbar-btn.",
  "//maxBespokeButtonClasses-correction": "78, not 73, after a MEASUREMENT FIX (audit 20260815-163607 #12): the collector anchored at line start, so only the FIRST class of a selector was seen and 5 real bespoke buttons — `.tiptap-editor .code-copy-btn` among them — were absent from the budget entirely. The true pre-migration count was 85, not 80; seven classes were then deleted. Still a decrease against the merge base, so no allowRaise entry is required. Do not read the 73→78 step as a regression.",
  "maxBespokeButtonClasses": 78,
  "//maxStyledButtonClasses": "By USAGE: classes applied to a <button> in src/**/*.tsx whose CSS re-derives a button surface (padding plus a border or background). Added because the name check was evadable by naming \u2014 `.workspace-approval-approve` styled a real button with its own padding, radius and border and was never counted, so the budget read 88/88 while the drift the gate exists to catch was happening. 61 of these 80 are invisible to the name check. This is a NEW measurement of a previously unmeasured surface, NOT a raised budget.",
  "maxStyledButtonClasses": 77,
  "//maxShapeDriftClasses": "By SHAPE: classes applied to a <button> whose declared padding, border-radius or font-size differs from `.vm-btn`'s, compared AFTER resolving var() so a literal and its token spelling count as equal. The first two budgets count controls; this one measures WHICH token each control picked \u2014 the drift that survives a fully tokenised codebase, where `.approval-dialog__btn` was 6/14 + radius-md + font-md against the canonical 6/12 + radius-sm + font-sm and both passed every existing gate. A NEW measurement of a previously unmeasured axis, NOT a raised budget.",
  "//maxShapeDriftClasses-scope": "80 is a superset on purpose: it includes icon-only buttons (padding: 0) and pill chips (--radius-pill), which are legitimately not text buttons. Triage moves each entry into one of three outcomes \u2014 migrate to `.vm-btn`, promote a genuinely-missing variant onto the primitive, or record `/* button-shape-ok: <reason> */` in the rule body. A bare marker with no reason is rejected. Do not exempt in bulk; an exemption is a claim that the deviation is intended.",
  "maxShapeDriftClasses": 80
}
