{
  "_comment": "Files where a merge kept ONE side whole and the other side's change was RELOCATED, not lost. Each value must name where it was re-applied, so the claim is checkable. Detected by scripts/check-merge-drops.mjs.",
  "_verified": [
    "WI-DP1.1 (2026-08-09): both entries verified and removed. Each named a merge",
    "that relocated a change rather than losing it, and each relocation still holds:",
    "  src/hooks/closeSave.ts -> resolveSaveFilters re-applied as saveFiltersForFilePath,",
    "    now at src/services/windowClose/closeSaveShared.ts:66, used by closeSave.ts:98",
    "    and closeSaveBatch.ts:76.",
    "  src/hooks/mcpBridge/v2/workspace.ts -> disk-open ingest routing re-applied, now at",
    "    src/services/mcpBridge/v2/workspaceOpen.ts:137 (ingestExternalContent(..., \"disk-open\")).",
    "Both KEYS had gone stale: WI-10 moved these trees hooks/ -> services/ on 2026-08-04",
    "and the allowlist kept naming the old paths for five days. This gate has no",
    "staleness check, so nothing noticed — see the plan's Outstanding work."
  ]
}
