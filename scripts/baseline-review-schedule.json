{
  "//": [
    "WI-AF3.2/3.3 — which committed baselines are DEBT, what paying them down means,",
    "and which are not debt at all. Finding F5: ~450 frozen units across 27 baselines,",
    "none of which had moved since they were measured. A ratchet stops debt growing;",
    "it never asks anyone to pay it.",
    "",
    "THERE ARE NO DEADLINES HERE, AND THAT IS DELIBERATE. An earlier revision of this",
    "file carried a review date per baseline. Those dates were invented by the agent",
    "that wrote them — a fortnightly stagger starting seven weeks out — and stamped",
    "with `owner: maintainer`, which recorded a guess as somebody else's commitment.",
    "Only one baseline here has any paydown history to extrapolate from (file-size:",
    "153 -> 92 over two months); mock-boundaries has exactly one commit, its own",
    "creation, so no honest rate exists for it at any confidence.",
    "",
    "What IS measurable is staleness: how long a baseline has gone unchanged, and how",
    "big it currently is. `scripts/check-review-schedule.mjs --report` reports both",
    "and .github/workflows/baseline-review.yml keeps one rolling issue up to date.",
    "Debt that stops moving surfaces on its own, without anyone pretending to know",
    "when it should have been finished.",
    "",
    "ORDER matters and is grounded: `tracked` is listed worst-first by measured size",
    "and reachability, which is a claim about this repo, not about a calendar.",
    "",
    "EXEMPT is a claim that a baseline is not debt, and requires a reason. Vendored",
    "upstream corpora and inventories of what exists are not things anyone should be",
    "asked to drive to zero. Every manifest entry must appear in exactly one of",
    "`tracked` or `exempt`, and every key here must name a real manifest entry."
  ],
  "tracked": {
    "scripts/mock-boundaries-baseline.json": "zero — tests use the real store (setState/reset) or a recorded store-factory seam",
    "scripts/command-error-baseline.json": "zero — every command returns Result<T, CommandError>",
    ".dependency-cruiser-known-violations.json": "down — each frozen edge either deleted or re-expressed through a plugins/shared seam",
    "scripts/plugin-store-coupling-baseline.json": "zero on every channel — options and seams, not ambient lookups",
    "scripts/file-size-baseline.json": "down — split files, lower the number; never raise",
    "scripts/bespoke-buttons-baseline.json": "down — each bespoke class converted to .vm-btn / .popup-icon-btn",
    "scripts/extension-budget.json": "down — per-edge licenses retired as plugins decouple",
    "src/utils/markdownPipeline/__tests__/spec/specDeltas.json": "down — each declared divergence fixed or ruled a deliberate dialect choice",
    "src/utils/markdownPipeline/__tests__/spec/specRoundtripDeltas.json": "down — same",
    "src/utils/markdownPipeline/conformance/expectedDeltas.ts": "down — same",
    "src/utils/markdownPipeline/__tests__/fidelity/fidelityLedger.ts": "down — same",
    "scripts/test-types-baseline.json": "zero — every test file typechecks under tsconfig.test.json; 2,503 errors in 376 files on adoption, all pre-existing",
    "scripts/theme-contrast-baseline.json": "failing lists empty for every theme — Phase 1 of the UI-consistency plan retints the catalog; ansiFloor/exempt stay as reasoned permanent exceptions (D10)",
    "scripts/design-tokens-baseline.json": "both lists empty — each rgba literal moves onto a token (or the sanctioned color-mix pair) and each className literal onto a token-backed class (WI-UI1.x/2.2)",
    "scripts/ui-consistency-baseline.json": "every list empty — C10 empties in WI-UI1.8, C5 in WI-UI2.1, C7/C8 in WI-UI2.3, C4 in WI-UI3.1/3.2, C9/C11 in WI-UI3.x; permanent exceptions become ui-ok markers, not entries",
    "scripts/gate-tests-baseline.json": "empty — each listed gate gains a sibling scripts/<stem>.test.* and its entry is removed",
    "scripts/i18n-copy-baseline.json": "empty — each remaining multi-sentence description is reworded (or Q3 is revisited for multi-sentence copy) and its entry removed"
  },
  "exempt": {
    "scripts/shell-slots-baseline.json": "An INVENTORY, not debt: the identity list of the surfaces App.tsx mounts. It should track reality, and reality has ~20 surfaces. Driving it to zero would mean an app with no UI.",
    "scripts/i18n-untranslated-baseline.json": "Already zero — the ~1,160-entry debt was paid. The gate exists to keep it there, so there is nothing to schedule.",
    "scripts/i18nIdenticalAllowlist.ts": "A permanent registry of genuinely untranslatable strings (runner labels, literal paths), each with a stated reason and checked for staleness in both directions. Not a number anyone should drive down.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/commonmark-0.31.2.json": "Vendored upstream conformance corpus — test DATA, not debt.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/gfm-extensions.json": "Vendored upstream corpus.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/cmark-regression.json": "Vendored upstream corpus.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/cmark-gfm-regression.json": "Vendored upstream corpus.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/cmark-gfm-extensions.json": "Vendored upstream corpus.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/pulldown-cjk-emphasis.json": "Vendored upstream corpus.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/pulldown-wikilinks.json": "Vendored upstream corpus.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/pulldown-math.json": "Vendored upstream corpus.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/markdown-it-extras.json": "Vendored upstream corpus.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/markdown-it-xss.json": "Vendored upstream corpus — XSS payloads that must keep being escaped.",
    "src/utils/markdownPipeline/__tests__/spec/corpus/tiptap-conversion.json": "Vendored corpus for the md→ProseMirror oracle.",
    "scripts/baseline-review-schedule.json": "The register itself. Its `tracked` list is an identity set the ratchet holds, but a register of registers is bureaucracy, not debt.",
    "scripts/merge-drop-allowlist.json": "Already zero (WI-DP1.1): both claims verified as real relocations and removed. The gate keeps it empty, so there is nothing left to schedule.",
    "scripts/type-aware-baseline.json": "Already zero (2026-08-14): opened at 152 findings on adoption and all 152 were paid down the same day — 132 unhandled promises, 16 [object Object] sites, and a never-typed guard that made pointer arithmetic operate on never. The file is empty and the gate keeps it there, so there is nothing left to schedule. A new entry is a real regression, not a number to raise.",
    "scripts/knip-baseline.json": "Already zero (WI-DP1.2): all 75 warn-tier findings resolved. The gate keeps it at zero, so there is nothing left to schedule."
  }
}
