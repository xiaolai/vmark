# CC-Suite Configuration

Project-specific settings for cc-suite commands.
Generated by `/cc-suite:init`. Edit freely — all fields are optional.

## Project

- **Stack**: TypeScript, React 19, Rust, Tauri v2, Zustand v5, Vitest v4
- **Test command**: pnpm test
- **Source directories**: src/, src-tauri/src/

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.3-codex
- **Default effort**: high
- **Default audit type**: mini
- **Default sandbox**: workspace-write

## Audit Focus

quality-first

Additional instructions appended to every audit's developer-instructions:

```text
Prioritize code quality findings. Flag untested critical paths, high cyclomatic complexity, and DRY violations as High regardless of other severity heuristics.
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
node_modules/
dist/
build/
coverage/
*.min.js
*.bundle.js
*.lock
vendor/
.git/
target/
src-tauri/target/
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.
Use this to tell Codex about your project's conventions, architecture, or constraints.

```text
This is a TypeScript + Rust/Tauri v2 desktop app (VMark — AI-friendly markdown editor).
Frontend: React 19, Zustand v5 (use selectors, never destructure stores), Tailwind v4, shadcn/ui v4.
Backend: Rust with Tauri v2 commands returning Result<T, String>.
Follow existing patterns in src/components/, src/stores/, src/plugins/, src-tauri/src/.
Keep code files under ~300 lines. Use design tokens from src/styles/index.css, never hardcode colors.
Test with Vitest — TDD is mandatory for stores, hooks, utils, and bug fixes.
```
