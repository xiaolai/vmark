# TDD Plan: 2026-08-03 Architecture Review — Comprehensive Fix/Build Plan

Source findings: `dev-docs/deep-researches/20260803-architecture-review.md` (A1–E4, all 18 verified),
`dev-docs/20260803-architecture-review-note.md` (superseded four-tier grouping),
`dev-docs/audit/20260803-verifier-stack-second-assessment.md` (tsconfig costs, mutation sequencing,
hot-exit Zod boundary, newGroupDelay hazard).

Ordering: **WI-18 (mock-boundary gate) lands FIRST** — its policy protects every other WI's new
tests, so the enforcement must exist before WI-1's tests are written (Codex cross-review
2026-08-03: "enforce first; agent discipline alone is the silent-failure vector"). Then
correctness bugs and data-loss-class items (WI-1..3), fiction-killing gates (WI-4..8), stalled
migrations (WI-9..11), structural investments (WI-12..21).
Every WI follows RED → GREEN → REFACTOR. Assertion levels reference `tdd-guardian:policy-core`
(Level 1 output, 2 side-effect, 3 real integration, 4 state, 5 mock-return+output; 6-7 never alone).
macOS behavior must never regress. Files stay under ~300 lines. Ratchets only go down.

**Amended 2026-08-03 after Codex cross-review** (archived at
`dev-docs/audit/20260803-crosscutting-notes-codex-review.md`). The amendments, applied throughout:
(1) WI-18 executes first and uses an identity baseline covering every Vitest mocking variant;
(2) one-time "temporary edit, recorded in PR, reverted" RED proofs are replaced by **durable
meta-tests** that inject the violation at runtime into an isolated fixture tree/scratch repo and
assert the real gate goes red — PR demos survive only as supplementary acceptance for live paths;
(3) real-command config gates are layered: structural wiring check + durable negative canary +
exact-SHA positive acceptance + recurring liveness; (4) consequential behavioral pins live in the
decision ledger (`.claude/tdd-guardian/decisions-20260803.md`), not one-line test headers — pin
the invariant at RED, the mechanism only when it is user-visible contract; (5) `lint:eager` must
be extended (denylist families + coverage) before WI-12/13 can cite it — today it checks only 4
chunk families in `dist/index.html` and cannot see xyflow/codemirror/tiptap/GHA-workbench.

---

## Work Items

### WI-1: Source link popup — stale-range and retarget guards (A1, data-corruption class)
- **Description**: Port the WYSIWYG guards to the Source link popup. `saveLinkChanges`
  (`src/plugins/sourceLinkPopup/sourceLinkActions.ts:83-98`) dispatches captured offsets blindly;
  `src/plugins/sourcePopup/createSourcePopupPlugin.ts:151` closes only on `selectionSet && !docChanged`,
  so an MCP/AI edit while the popup is open makes save rewrite unrelated text. Port
  `linkRangeIsIntact` semantics (reference: `src/plugins/linkPopup/LinkPopupView.ts:178-182`,
  commit c89c1656): before any mutation, verify the captured range still holds the same link; on
  doc change while open, either remap offsets through the CodeMirror change mapping or close the
  popup. Also port `shouldReshow` semantics so retargeting to a different link refreshes popup
  fields instead of keeping the previous URL (second bug c89c1656 fixed, structurally live in Source).
- **Acceptance criteria**:
  - [ ] RED first: failing reproducer — doc mutates (simulating MCP edit) while Source link popup is open, save no longer rewrites unrelated text.
  - [ ] Save aborts (or offsets remap correctly) when the captured range no longer contains the original link text.
  - [ ] Doc change while popup open either remaps the tracked range or closes the popup — it can never dispatch stale offsets.
  - [ ] Reopening the popup on a different link shows that link's URL/text, never the previous target's.
  - [ ] WYSIWYG popup behavior unchanged (its existing tests stay green).
  - [ ] Gate: `pnpm test src/plugins/sourceLinkPopup src/plugins/sourcePopup` then `pnpm check:all` — both exit 0.
- **Required tests**:
  - Reproducer: insert text before the link after popup opens, save → document text is exactly the expected string (link edited, surrounding text untouched) — assertion level: Level 1
  - Guard clause: the link range is deleted entirely while popup open, save → no dispatch, document unchanged, popup closed — assertion level: Level 1 + Level 4
  - Remap path: edit after the link (offsets unaffected), save → correct edit at correct position — assertion level: Level 1
  - Retarget: open on link A, retarget to link B → popup state holds B's href/text — assertion level: Level 4
  - Save with empty URL / whitespace URL (boundary) → defined behavior (unlink or reject), doc verified — assertion level: Level 1
  - CJK/multibyte text surrounding the link — offsets stay correct after remap — assertion level: Level 1
  - Mock boundary: none — real CodeMirror `EditorState`/transactions (repo pattern for source-mode plugin tests).

### WI-2: `tabStore.activeTabId` pane-desync invariant (A2, correctness)
- **Description**: Under split view, `activeTabId` desyncs from panes because the "route through
  `activateTabInFocusedPane`" convention is honored at 5 of ~31 call sites and skipped by
  `tabStore.createTab` itself and the default branch of `activateTabWithWorkspaceContext.ts`.
  Invert enforcement: make `tabStore.setActiveTab` (`src/stores/tabStore.ts`) pane-aware — or add an
  `onActivate` seam injected at the composition root (pattern: `setTabExistenceGuard`) — so every
  activation updates the owning pane. Delete the 5 now-redundant duplicated split checks. Add a DEV
  invariant assertion (fail loud in dev when `activeTabId` is not the focused pane's active tab).
- **Acceptance criteria**:
  - [ ] RED first: failing test showing `createTab` under split view leaves `activeTabId` pointing at a tab no pane displays; test passes after fix.
  - [ ] All activation paths (createTab, activateTabWithWorkspaceContext default branch, direct `setActiveTab` callers) converge pane state and `activeTabId`.
  - [ ] The 5 duplicated call-site split checks are deleted; behavior preserved by the centralized seam.
  - [ ] `paneTabIntegration.test.ts:47-51` no longer creates a desync state without asserting — it now asserts the invariant.
  - [ ] DEV-only invariant assertion exists and fires on injected desync (test proves it); compiled out / silent in production.
  - [ ] Gate: `pnpm test src/stores src/services/navigation` then `pnpm check:all` — both exit 0.
- **Required tests**:
  - `createTab` in a split layout → focused pane's active tab === `tabStore.activeTabId` — assertion level: Level 4
  - `setActiveTab` targeting a tab owned by the non-focused pane → both pane state and `activeTabId` consistent (defined single behavior) — assertion level: Level 4
  - `activateTabWithWorkspaceContext` default-config branch → invariant holds — assertion level: Level 4
  - Close-active-tab-in-pane transition → next activation keeps invariant (state transition) — assertion level: Level 4
  - DEV assertion: artificially desync stores → assertion throws/reports in DEV mode — assertion level: Level 1 (thrown error)
  - Single-pane (no split) regression: all existing activation tests stay green — assertion level: existing suite (Level 1/4)
  - Mock boundary: none — real Zustand stores (real stores are cheap; policy rule 3).

### WI-3: Zod at the hot-exit / session-state read boundary (data-loss class)
- **Description**: A corrupt read of persisted session state (hot exit, session tabs) loses unsaved
  work. Add Zod schemas at the read boundary of `src/services/persistence/hotExit/*`
  (`restoreDispatch.ts`, `instanceContextState.ts`) and the session-tab restore path
  (`src/services/navigation/restoreWorkspaceTabs.ts`). Behavior on invalid data is fail-safe:
  never crash restore; salvage valid entries per-item; quarantine (preserve, don't delete) any
  payload that fails parsing so no bytes of user work are destroyed. Unknown extra fields pass
  through (forward compatibility).
- **Acceptance criteria**:
  - [ ] RED first: failing test feeding a corrupted hot-exit payload into restore — restore must not throw and must not silently discard the file.
  - [ ] Valid payloads round-trip identically through the schema (no data narrowing regressions).
  - [ ] Partially corrupt payload → valid entries restored, invalid entries quarantined with a preserved artifact.
  - [ ] Unknown fields survive read→write (passthrough), guarding against version skew across app updates.
  - [ ] Schema lives in `services/persistence` (services tier — ADR-013), file(s) under 300 lines.
  - [ ] Gate: `pnpm test src/services/persistence` then `pnpm check:all` — both exit 0.
- **Required tests**:
  - Valid full payload → restored state equals input fixture — assertion level: Level 1
  - Truncated/garbage JSON → restore completes, quarantine artifact written, original bytes preserved — assertion level: Level 2
  - One tab entry invalid among N → N−1 restored, 1 quarantined — assertion level: Level 1 + Level 2
  - Empty file / missing file / empty object (boundaries) → clean empty restore, no throw — assertion level: Level 1
  - Unknown extra fields → present after round-trip — assertion level: Level 1
  - Real Zod parse (never mocked); fs faked only at the `@tauri-apps/*` boundary with a stateful fake — mock boundary per policy rule 4.

### WI-4: Mutation-testing infrastructure repair — sequencing step 1 (D1)
- **Description**: Mutation testing has never produced a successful report (8/8 runs failed,
  hidden by `continue-on-error: true`). Root causes: `src-tauri/mutants.toml` is silently ignored
  (cargo-mutants reads `.cargo/mutants.toml`; `src-tauri/.cargo/` doesn't exist) so Rust mutates
  unfiltered into its 60-min timeout; the Stryker job dies in the dry run ("Initial test run timed
  out"). Fix both BEFORE any threshold exists (sequencing constraint from the second assessment):
  relocate the cargo-mutants config to where the tool reads it, fix the Stryker dry-run timeout
  (`dryRunTimeoutMinutes` / narrowed `mochaOptions`-equivalent vitest command), remove
  `continue-on-error` in favor of an on-failure notification step in `mutation.yml`.
- **Acceptance criteria**:
  - [ ] `src-tauri/.cargo/mutants.toml` exists at the path cargo-mutants actually reads; the old ignored file is removed.
  - [ ] `cargo mutants --list --manifest-path src-tauri/Cargo.toml` output reflects the filter (strictly fewer mutants than an unfiltered listing — recorded in the PR).
  - [ ] Stryker dry run completes locally: `pnpm mutation:ts` reaches the mutation phase (no "Initial test run timed out").
  - [ ] `mutation.yml` no longer has `continue-on-error: true`; a failed run opens/updates a GitHub issue (on-failure step).
  - [ ] One `workflow_dispatch` run of `mutation.yml` completes green end-to-end (first successful report in project history) — **SHA-pinned**: dispatch with an explicit `--ref`, capture the run ID, and assert `headSha`, workflow identity, run conclusion AND per-job conclusions via `gh run view --json` (a bare `gh run watch` can watch the wrong run/revision).
  - [ ] A staleness guard fails CI if a `mutants.toml` reappears at the ignored legacy path (two-way allowlist principle).
  - [ ] The on-failure issue step is verified, not just present: structurally (workflow parses; the step exists with the correct `if: failure()` condition) and behaviorally once (one deliberately-failing dispatch on a branch shows the issue created/updated, recorded in the PR).
  - [ ] Gate: `gh workflow run mutation.yml --ref <branch> && gh run view <captured-id> --json headSha,conclusion,jobs` shows the pinned SHA green; `pnpm check:all` unaffected.
- **Required tests**:
  - Staleness guard script: legacy-path config present in fixture tree → non-zero exit + message; absent → exit 0 — assertion level: Level 1
  - Config/CI changes themselves are config-tier (10-tdd exception) — verified by the runnable gates above, not unit tests.

### WI-5: Formatter-idempotence property slice + Stryker break threshold (D1 follow-on)
- **Description**: With WI-4 green, pull the formatter-idempotence slice forward (the verified-core
  plan's own property catalogue marks it "cheap, independent, Stryker already scoped to these
  modules"). Add a fast-check property test asserting `format(format(x)) === format(x)` over the
  formatter modules in Stryker's 9 configured globs (CJK formatter and markdown formatting
  utilities). Then set `"break"` in `stryker.config.json` to a floor measured from the first green
  run (currently `"break": null` means `high: 85` reports configuration, not a gate).
- **Acceptance criteria**:
  - [ ] Depends on WI-4 (no threshold before the runs succeed).
  - [ ] Property test exists using fast-check (already a dependency, 4 importing files) with a fixed seed strategy consistent with the existing property suites.
  - [ ] `stryker.config.json` `"break"` is a number ≤ the measured score of the first green run (ratchet floor, never raised above measured reality).
  - [ ] A break-threshold violation actually fails — proven **durably** by a committed canary meta-test: a tiny fixture project (test resource, not app code) with a known surviving mutant and a dedicated Stryker config whose `break` the mutant violates; the meta-test runs real Stryker on it and asserts non-zero exit. The repo suite stays green because the expected-failure lives inside the meta-test. (Replaces the one-time gutted-test PR demo — one demo proves one version of the gate bit once; the canary proves it keeps biting.)
  - [ ] Gate: `pnpm test src/lib/cjkFormatter` (or the formatter module path) exits 0; `pnpm mutation:ts` exits 0 with the break threshold set.
- **Required tests**:
  - Idempotence property: for generated markdown/CJK inputs, `format(format(x)) === format(x)` — assertion level: Level 1 (determinism category)
  - Boundary corpus cases: empty string, whitespace-only, mixed CJK/Latin, RTL fragment, very long line — same idempotence assertion — assertion level: Level 1
  - Mock boundary: none — pure functions, real implementations.

### WI-6: Delete `browserStore` and the gate line certifying fiction (E4)
- **Description**: `src/stores/browserStore.ts` is a dead, unwired policy store; the "unbounded
  webview leak" it supposedly caps was REFUTED (surfaces are active-page-only, bounded stricter
  than the unwired cap), yet `scripts/check-browser-phase.sh:291` runs its unit test under the
  label "WI-1.6 live-webview cap enforced", certifying enforcement that does not exist, and the
  plan the script gates against no longer exists. Evidence supports DELETE over wire: delete the
  store, its test (`src/stores/__tests__/browserStore.test.ts`), the phase-gate line, and the false
  `@coordinates-with` header claim.
- **Acceptance criteria**:
  - [ ] `src/stores/browserStore.ts` and `src/stores/__tests__/browserStore.test.ts` deleted; zero remaining imports (`grep -rn "browserStore" src scripts` matches only the distinct `browserApprovalStore`/`browserSessionStore`/etc. names).
  - [ ] `scripts/check-browser-phase.sh` no longer runs the deleted test nor claims "live-webview cap enforced".
  - [ ] The false `@coordinates-with` header on the store's coordinator is corrected (22-comment-maintenance rule).
  - [ ] The real bounding mechanism (active-page-only surface lifecycle) is pinned by a test if not already — the property the gate should have checked.
  - [ ] Knip/coverage baselines updated downward only.
  - [ ] Gate: `bash scripts/check-browser-phase.sh 1` (or its current phase arg) exits 0 without the fiction line; `pnpm check:all` exits 0.
- **Required tests**:
  - Lifecycle bound pin: opening/navigating browser tabs never yields more live webview surfaces than active pages (the property the deleted cap pretended to enforce) — assertion level: Level 4
  - Deletion itself needs no new tests beyond the pin; regression safety = full suite green.

### WI-7: Pre-push tag leg → `gh api` check-runs verification + stale doc claims (D5)
- **Description**: Since 2026-07-27 branch protection, the pre-push main leg re-verifies what the
  remote already rejects, and the tag leg re-runs a 6–10+ min local gate that is NOT verifying a
  by-construction-green commit in all cases. Replace the tag leg in `.githooks/pre-push` with a
  seconds-fast `gh api repos/xiaolai/vmark/commits/<sha>/check-runs` verification that `frontend`
  and `rust` are green on the tagged commit; keep the full local gate available behind an explicit
  offline env flag. Fix the four stale "~3 min" claims (AGENTS.md, `.claude/rules/40-version-bump.md`,
  `.claude/rules/60-ai-governance.md`, and the fourth located by grep). Rule §9 applies: this is
  fixing the gate, not bypassing it — the check moves from proxy (re-run) to property (CI verdict
  on the exact SHA).
- **Acceptance criteria**:
  - [ ] New `scripts/check-tag-green.sh` (testable unit) verifies both required checks are `completed`+`success` on the tag's commit; pending or failed → push refused with a clear message.
  - [ ] `.githooks/pre-push` tag leg calls the script; full local gate runs instead when `VMARK_OFFLINE_GATE=1` (or `gh` unreachable → refuse with instructions, never silently pass).
  - [ ] The timing claim is **centralized, not grep-chased**: one authoritative statement (in `.githooks/pre-push`'s header) states the measured cost and the new tag-leg behavior; every other location (AGENTS.md, `.claude/rules/40-version-bump.md`, `60-ai-governance.md`, `scripts/setup-local-git.mjs`, `.claude/commands/bump.md`) either references it or is corrected to match. `grep -rn "~3 min"` across those files finding zero stale claims is the smoke check, not the mechanism — a grep for the exact byte-string cannot catch "three minutes" or future drift.
  - [ ] Branch-leg behavior for `main` documented consistently with §10 (protection is authoritative).
  - [ ] Gate: `bash scripts/check-tag-green.sh <green-sha>` exits 0 and `bash scripts/check-tag-green.sh <red-or-pending-sha>` exits 1 (exercised via stubbed `gh` in tests); `pnpm check:all` exits 0.
- **Required tests**:
  - Script with stubbed `gh` returning both-green fixture → exit 0 — assertion level: Level 1
  - One check failed / one pending / check absent (three fixtures) → exit 1 with distinct messages — assertion level: Level 1
  - `gh` binary missing or network error → exit 1, message points at the offline flag — never a silent pass (guard clause) — assertion level: Level 1
  - Mock boundary: `gh` CLI stub on PATH (network boundary — acceptable per policy rule 4).

### WI-8: Plugin-isolation per-edge freezing; retire plugin-wide `pathNot` licenses (B3)
- **Description**: The plugin-isolation rule in `.dependency-cruiser.cjs` grants 17 plugin-wide
  `pathNot` licenses masking ~130 production edges; 2 exemptions mask zero violations (dead, no
  staleness check). `sourceMathPopup` needs exactly one edge yet holds a plugin-wide license.
  Delete the "verified dependency" pathNot block; regenerate `.dependency-cruiser-known-violations.json`
  so every existing edge is frozen individually (per-edge, ratchets down). Move the generic
  host/coordination utilities out of `src/plugins/sourcePopup/` into `src/plugins/shared/` so
  sourcePopup stops being a second de-facto shared layer.
- **Acceptance criteria**:
  - [ ] RED first: with the pathNot block deleted and before regeneration, `pnpm lint:deps` fails listing the real edges (recorded), then the regenerated known-violations freezes exactly those.
  - [ ] Zero plugin-wide licenses remain in the plugin-isolation rule; the 2 dead exemptions are gone.
  - [ ] A NEW plugin→app edge fails the gate — proven **durably** by a committed meta-test: generate a minimal fixture project in a tmpdir, run the REAL dependency-cruiser config against it with one injected forbidden edge, and assert the real `depcruise` invocation exits non-zero naming that edge. (Replaces the one-time temporary-edge PR demo; a supplementary live demo on the real tree is optional, done in an isolated worktree, never the shared checkout — the repo's own selftest-interruption incident is the reason.)
  - [ ] Generic utils moved from `sourcePopup/` to `plugins/shared/` with their tests; importers updated; known-violations shrinks accordingly (never grows).
  - [ ] Gate: `pnpm lint:deps` exits 0; `pnpm lint:extension-budget` exits 0; `pnpm check:all` exits 0.
- **Required tests**:
  - Moved shared utils keep their existing behavior tests (moved alongside, all green) — assertion level: existing (Level 1/4)
  - Durable gate-sensitivity meta-test: real dep-cruiser config over a fixture tree with an injected forbidden edge → non-zero exit naming the edge — assertion level: Level 3 (real tool, isolated tree)
  - Known-violations freeze verified by the gate run itself on the real graph — assertion level: Level 3 (real dep-cruiser over real sources)
  - Mock boundary: none.

### WI-9: Revert T09 — re-inline the 14 popup-store shim slices as standalone stores (C1)
- **Description**: T09 stalled as a permanent facade: 1 mega-store (`src/stores/popupStore/`) +
  14 shims + a 110-line shim engine (`src/stores/_shimHelper.ts`), 101 consumer files still on
  legacy APIs, no coordination feature ever built on the merged store (`modeSwitchCleanup.ts:23-50`
  closes three popups via three different APIs), new popups already abandoned the pattern, and
  `_shimHelper.getInitialState` deviates from Zustand semantics (latent bug). Post-T09 practice
  says revert: re-inline each of the 14 slices as a standalone `use<X>Store`, delete
  `src/stores/popupStore/` and `src/stores/_shimHelper.ts` (+ its test).
- **Acceptance criteria**:
  - [ ] RED first: each re-inlined store gets behavior tests written against the legacy public API BEFORE the mechanical re-inline (contract pinned so the revert cannot drift).
  - [ ] All 14 slices are standalone stores following 50-codebase-conventions §1 (naming, guarded keyed updates); each store file under 300 lines.
  - [ ] `src/stores/popupStore/` and `src/stores/_shimHelper.ts` deleted; zero imports remain (`grep -rn "popupStore\|_shimHelper" src` empty).
  - [ ] `modeSwitchCleanup.ts` closes popups through one uniform per-store API (the three-API split is gone).
  - [ ] The 101 consumer files compile unchanged where the shim API was already the store API; changed imports are mechanical.
  - [ ] `getInitialState` Zustand-semantics deviation is gone by construction (standalone stores use native semantics) — pinned by test.
  - [ ] Gate: `pnpm test src/stores` then `pnpm check:all` — both exit 0.
- **Required tests**:
  - Per re-inlined store: open/close/update state transitions match pinned legacy behavior — assertion level: Level 4
  - `getInitialState`/reset semantics per store match native Zustand behavior (the shim deviation's regression test) — assertion level: Level 1
  - `modeSwitchCleanup` closes all registered popups (state verified on each store, not call args) — assertion level: Level 4
  - Rapid repeated open/close (idempotency, stateful behavior) — assertion level: Level 4
  - Mock boundary: none — real stores.

### WI-10: hooks→services migration for the ~74 non-React files + dep-cruiser rule (B1)
- **Description**: 74 of 146 non-test files in `src/hooks/` (8,402 lines) have no React import;
  `useFileSave.ts`/`useTabOperations.ts` export no hooks; `fileCommands.ts`/`tabCommands.ts` state
  in their headers they are stranded; the 4,568-line MCP bridge is the largest occupant. Continue
  the 20260722 tier-restoration plan mechanically: file logic → `src/services/files/`, tab logic →
  `src/services/tabs/`, window-close logic → `src/services/windowClose/`, `src/hooks/mcpBridge/` →
  `src/services/mcpBridge/` keeping exactly one wiring hook (`useMcpBridge`) in hooks/. Then add
  the missing dependency-cruiser rule over `from: ^src/hooks` so regression is structural, plus the
  companion rule that `^src/services` never imports `^src/hooks`.
- **Acceptance criteria**:
  - [ ] Migration proceeds in reviewable batches (files+tests move together; each batch leaves `pnpm check:all` green) — no big-bang commit.
  - [ ] After migration, every non-test file remaining in `src/hooks/` imports React (verified by the new gate).
  - [ ] `.dependency-cruiser.cjs` has rules: (a) `^src/services` may not depend on `^src/hooks`; (b) `^src/hooks` non-React business modules are impossible (hooks depend on services, not vice versa); known-violations for any stragglers frozen per-edge and ratcheting down.
  - [ ] `src/hooks/mcpBridge/` reduced to the single wiring hook; handlers/dedup/revisionTracker live in `src/services/mcpBridge/` (ADR-013 tiers).
  - [ ] Stale header comments ("lives in hooks/ because…") updated per 22-comment-maintenance.
  - [ ] Gate: `pnpm lint:deps` exits 0 with the new rules active; `pnpm check:all` exits 0.
- **Required tests**:
  - Moved modules carry their existing test suites (green at every batch boundary) — assertion level: existing (Level 1/4)
  - New rule proof — durable meta-test (same mechanism as WI-8): real dep-cruiser config over a fixture tree containing a services→hooks import → non-zero exit naming the edge; plus the hooks-are-React rule proven against a fixture non-React hooks file — assertion level: Level 3 (real tool, isolated tree)
  - The one remaining `useMcpBridge` wiring hook: mount registers listener, unmount cleans up (refs-based cleanup per §2), with a Level 4 state assertion on the service it wires — assertion level: Level 4
  - Mock boundary: `@tauri-apps/*` only.

### WI-11: Four-channel plugin coupling ratchet + `hostDocument` bind fix (B2)
- **Description**: `scripts/check-plugin-store-coupling.mjs` greps only the literal `@/stores`;
  39 plugin files import `@/services`, 10 `@/hooks`, 3 `@/components`, all ungated — and imported
  services are themselves store-coupled (`resolveMediaSrc.ts` → documentStore+tabStore;
  `unifiedHistory.ts` → five stores), so the zero baseline cannot see transitive coupling. Extend
  the checker to four channels (`@/stores`, `@/services`, `@/hooks`, `@/components`) with a frozen
  per-plugin, per-channel baseline in `scripts/plugin-store-coupling-baseline.json` that ratchets
  down only. **All four channels parse imports** — including the legacy `@/stores` channel, which
  today greps the literal and counts prose in comments (a documented landmine that cost two
  debugging rounds, per 00-engineering-principles). Migrating it is safe because its baseline is
  zero: zero stays zero under either counting rule, so the parser change cannot mask a regression.
  Update the 00-engineering-principles "do not write @/stores in a comment" note in the same
  change (rule 22). Fix
  `src/plugins/shared/hostDocument.ts:30` — the module whose purpose is host independence — to
  receive its service dependency via `bindX()` at the composition root instead of hard-linking it.
- **Acceptance criteria**:
  - [ ] RED first: checker fixture tests written against the four-channel spec before extending the script.
  - [ ] Checker reports per-plugin, per-channel counts; any count above baseline fails; a count below baseline also fails until the win is recorded (two-way ratchet, house standard).
  - [ ] Baseline regenerated to freeze the current 39/10/3 reality; `@/stores` channel stays at zero.
  - [ ] `hostDocument.ts` has no static `@/services` import; default + `bindHostDocument()` seam wired once at the composition root; seam default matches app behavior (00-engineering-principles lesson).
  - [ ] Gate: `pnpm lint:store-coupling` exits 0; `pnpm check:all` exits 0.
- **Required tests**:
  - Checker on fixture tree with a new `@/services` import above baseline → exit 1, names the file and channel — assertion level: Level 1
  - Checker on fixture with an improvement not recorded in baseline → exit 1 (two-way staleness) — assertion level: Level 1
  - Checker on fixture matching baseline exactly → exit 0 — assertion level: Level 1
  - Comment containing the literal (not an import) → not counted, in EVERY channel including `@/stores` (the documented false-positive class is removed, not preserved) — assertion level: Level 1
  - Type-only import (`import type { X } from "@/services/x"`) → counted (the 00-engineering "type-only counts" lesson) — assertion level: Level 1
  - `hostDocument` unbound → working default; bound → host value used; both behaviors asserted on output — assertion level: Level 1
  - Mock boundary: none — run the real script on fixture directories (child-process, real fs tmpdir).

### WI-12: Shell-slots gate → import-graph assertion; `KbGraphView` React.lazy (B4)
- **Description**: The ADR-007 "slot registration mechanism" does not exist; `scripts/check-shell-slots.mjs`
  counts a name regex that misses 6 of 14 mounted surfaces (43%), and pluralization exits the regex
  (the escape hatch is the evasion vector). Replace the regex with an import-graph assertion:
  enumerate the surfaces actually mounted from `App.tsx`/`AppShell` composition via the dependency
  graph (dep-cruiser JSON or a TS AST walk) and ratchet that identity list. Independently, make
  `KbGraphView` `React.lazy` — the only boot-eager xyflow path (123 kB + dagre 39 kB rides the KB
  chain onto boot; the GHA canvas is already lazy). Correct the false header in
  `KnowledgeBasePanel.tsx:10` claiming slot registration.
- **Acceptance criteria**:
  - [ ] RED first: new gate test proves the current regex undercount (fixture reproducing a missed surface), then the import-graph version counts all 14.
  - [ ] Gate asserts an identity list (named surfaces), not a count; new surface = explicit baseline edit; `shell-slots-baseline.json` converted accordingly.
  - [ ] `KbGraphView` loads via `React.lazy` + Suspense; xyflow/dagre absent from the entry chunk.
  - [ ] **`lint:eager` extended before it is cited**: `scripts/check-eager-chunks.mjs`'s DENYLIST today has only 4 families (mermaid/graph/graphviz/export) — xyflow and dagre are not in it, so the unextended gate passing proves nothing about this WI. Add the xyflow/dagre chunk families to DENYLIST first; the extended gate is RED against today's build (they ARE preloaded) and goes green with the lazy change — a true RED→GREEN on the real artifact.
  - [ ] `KnowledgeBasePanel.tsx` header no longer claims a mechanism that doesn't exist.
  - [ ] Gate: `pnpm lint:shell-slots` exits 0; `pnpm build && pnpm lint:eager` exits 0 with the extended denylist; `pnpm check:all` exits 0.
- **Required tests**:
  - Gate script on fixture with an unlisted mounted surface → exit 1 naming it — assertion level: Level 1
  - Gate script on fixture where a listed surface was removed but baseline not updated → exit 1 (two-way) — assertion level: Level 1
  - KB panel behavior: panel opens and graph view renders after lazy resolution (async boundary) — assertion level: Level 3 (React Testing Library, real lazy component)
  - Entry-chunk exclusion asserted by the existing `check-eager-chunks` gate against the real build — assertion level: Level 2 (build output side effect)
  - Mock boundary: none for the gate; jsdom + real components for the panel.

### WI-13: Lazy format adapters — `wysiwygComponent`/`language` as import thunks (E3)
- **Description**: `bootstrapFormats()` runs unconditionally before `import("./App")`; the markdown
  adapter statically imports TiptapEditorInner + lintEngine + codemirror language-data, dragging
  the ~1.64 MB codemirror vendor chunk (+459 kB tiptap) onto the cold-start modulepreload of
  Settings and PDF-export windows; the always-on yaml adapter statically imports the entire GHA
  workbench. Change `FormatConfig` (`src/lib/formats/types.ts:126`) so `wysiwygComponent` and
  `language` are `() => import()` thunks: bootstrap registers metadata eagerly (extension routing,
  path keys stay synchronous), surfaces load lazily at first editor mount. One structural change
  moves the whole stack off the entry chunk.
- **Acceptance criteria**:
  - [ ] RED first via the **extended** eager gate: add the codemirror, tiptap, and GHA-workbench chunk families to `check-eager-chunks.mjs` DENYLIST — today's gate cannot see them (4-family denylist), so citing the unextended gate would be a real command checking the wrong property. Since the app has a single HTML entry shared by all windows, `dist/index.html`'s modulepreload list IS the Settings/PDF cold-start graph — the extended denylist is the correct artifact. The extended gate is RED against today's build and green after the thunk change.
  - [ ] `FormatConfig.wysiwygComponent` and `.language` are thunks; registry metadata (extensions, dispatch, path keys) remains synchronously available — all existing registry/dispatch tests green.
  - [ ] Editor windows behave identically: first mount awaits the thunk once; subsequent mounts reuse the resolved module (no duplicate fetch/eval).
  - [ ] macOS document-window cold start unregressed (no behavior change on the primary surface).
  - [ ] Gate: `pnpm test src/lib/formats` exits 0; `pnpm build && pnpm lint:eager && pnpm size` exit 0.
- **Required tests**:
  - Registry lookup by extension returns metadata synchronously (no thunk evaluation) — assertion level: Level 1
  - Thunk resolves to the same component/language module the static import produced (identity/behavior) — assertion level: Level 1
  - Double-resolution caching: two mounts → one module evaluation (determinism/idempotency) — assertion level: Level 1
  - Thunk rejection (simulated dynamic-import failure) → defined error surface, not a blank editor (failure path) — assertion level: Level 1
  - Entry-chunk composition asserted against the real Vite build via `lint:eager` — assertion level: Level 2
  - Mock boundary: dynamic import failure injected at the thunk seam only.

### WI-14: Crate-wide `CommandError` + rule 50 §10 amendment (C3)
- **Description**: Rule 50 §10 canonizes `Result<T, String>` for all 161 commands while AGENTS.md
  mandates `t!()`; ~370 raw-English `format!` error sites are invisible to `lint:i18n`, and 8
  hand-rolled typed enums are each flattened to String at the boundary (`genie_step.rs:112-116`).
  Introduce one crate-wide `CommandError` (thiserror) serializing `{code, message, i18nKey?, detail?}`;
  amend `.claude/rules/50-codebase-conventions.md` §10 to mandate it for new commands; migrate
  module-by-module starting where the frontend branches on error content (string sniffing today),
  converting the 8 existing enums via `#[from]`/`impl From`. Add a ratcheting count of remaining
  `Result<T, String>` command signatures so the migration cannot stall (the C1 lesson).
- **Acceptance criteria**:
  - [ ] RED first (Rust): serialization-shape test for `CommandError` written before the type; frontend discrimination test written before the first migrated module.
  - [ ] `CommandError` serializes exactly `{code, message, i18nKey?, detail?}` across all variants; `From` impls cover the 8 existing enums (no information lost at the boundary).
  - [ ] The modules where the frontend currently string-sniffs are migrated first; frontend branches on `code`, not message text.
  - [ ] Rule 50 §10 amended: new commands return `Result<T, CommandError>`; the ratchet script counts remaining `Result<T, String>` signatures and fails on increase.
  - [ ] User-facing variants carry `i18nKey` resolved via `t!()` keys in `src-tauri/locales/en.yml` (closing the lint:i18n blind spot for migrated modules).
  - [ ] Gate: `cargo test --manifest-path src-tauri/Cargo.toml` exits 0; `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` exits 0; `pnpm check:all` exits 0.
- **Required tests**:
  - Each `CommandError` variant serializes to the exact expected JSON shape — assertion level: Level 1
  - `From<ExistingEnum>` conversions preserve code + detail per variant (table-driven) — assertion level: Level 1
  - A migrated command's error path returns the typed error through the real Tauri serialization path (Rust integration test) — assertion level: Level 3
  - Frontend: handler branches on `code` for retryable/fatal/permission-denied and produces the correct user-visible outcome — assertion level: Level 1
  - Ratchet script fixture: signature count above baseline → exit 1; below without baseline update → exit 1 — assertion level: Level 1
  - Mock boundary: none in Rust; frontend mocks only the `invoke` boundary (`@tauri-apps/*`) returning real serialized fixtures.

### WI-15: MCP sidecar zod schemas as single source of truth (E1)
- **Description**: MCP payload contracts are hand-written in 3+ places (zod + `BridgeRequest`
  union in the sidecar, untyped pass-through in Rust, per-field `typeof` narrowing in the
  frontend); the parity test checks operation NAMES only — the class of defect that produced the
  born-unreachable `windowId` path (`routing.rs:114-117` reads a field no shipped tool sends and
  zod would strip). Make `server/mcp/` zod schemas authoritative: generate the frontend TS types
  and the operations manifest from them (generation over verification — the review's meta-rule),
  commit generated output with a drift check, and extend the parity test to diff field names per
  operation. Remove or repair the unreachable `windowId` branch.
- **Acceptance criteria**:
  - [ ] RED first: field-level parity test written against current state — it must fail on (or explicitly ledger) today's known field drifts before generation lands.
  - [ ] A generator (`pnpm gen:mcp-contracts` or similar) emits frontend types + the operations manifest from the sidecar zod schemas; committed output + a CI drift check (regenerate and diff → clean).
  - [ ] Parity test asserts per-operation field-name sets across sidecar schema, frontend consumption, and the manifest.
  - [ ] `routing.rs` `windowId` branch either receives a real sender or is deleted with its misleading error message.
  - [ ] Hand-written duplicate type definitions deleted (single source of truth, not four copies that agree today).
  - [ ] Gate: `pnpm test:sidecar` exits 0; the drift check (regenerate + `git diff --exit-code` on generated files) exits 0; `pnpm check:all` exits 0.
- **Required tests**:
  - Generator determinism: two runs produce byte-identical output — assertion level: Level 1
  - Field-level parity: for every operation, schema field set === manifest field set === frontend-consumed set — assertion level: Level 1
  - A fixture schema with an added field → parity test fails naming operation+field (RED proof) — assertion level: Level 1
  - Sidecar behavior: real zod parse rejects an out-of-contract payload; accepted payload round-trips — assertion level: Level 1 (real Zod, never mocked)
  - Mock boundary: none — real schemas, real generator, real parse.

### WI-16: Manifest-driven baseline ratchet over all committed baselines (D2)
- **Description**: `scripts/check-baseline-ratchet.mjs` covers 1 of ~8 committed baselines; every
  other baseline is self-attested in the same commit — the exact bypass §9/§10 documents as the
  threat model — and `required_approving_review_count: 0` means no human review is structural.
  Generalize into a manifest-driven ratchet enumerating every baseline/allowlist file
  (`bespoke-buttons-baseline.json`, `extension-budget.json`, `file-size-baseline.json`,
  `i18n-untranslated-baseline.json`, `knip-baseline.json`, `merge-drop-allowlist.json`,
  `plugin-store-coupling-baseline.json`, `shell-slots-baseline.json`, plus
  `.dependency-cruiser-known-violations.json` and `scripts/i18nIdenticalAllowlist.ts`), comparing
  against the merge target in CI so a PR cannot raise its own floor. Convert aggregate-count
  baselines to identity baselines where cheap (counts permit like-for-like swaps to pass silently).
- **Acceptance criteria**:
  - [ ] RED first: fixture tests for the generalized ratchet written before extending the script.
  - [ ] A manifest lists every baseline file with its comparison mode (identity / per-key count / scalar); a baseline file existing outside the manifest fails the gate (two-way staleness).
  - [ ] CI (`ci.yml`) runs the ratchet on every PR against the merge-base version of each baseline; any raise fails with the offending key.
  - [ ] At least the newest two gates (2026-08-02 vintage) plus any converted count baselines use identity form.
  - [ ] Gate: `node scripts/check-baseline-ratchet.mjs` exits 0 locally. Durable sensitivity comes from the scratch-git meta-tests (fixture base + head commits with a raised baseline → real script exits 1) — a separate "test PR raising a baseline" demo is redundant with those and not required; one live PR demo is optional supplementary acceptance.
- **Required tests**:
  - Fixture pair (base vs head) with a raised count → exit 1 naming file+key — assertion level: Level 1
  - Identity baseline with a like-for-like swap (same count, different entry) → exit 1 — assertion level: Level 1
  - Lowered baseline → exit 0 — assertion level: Level 1
  - Baseline file on disk missing from manifest → exit 1 (staleness both directions) — assertion level: Level 1
  - Malformed baseline JSON → exit 1, fails closed (guard clause) — assertion level: Level 1
  - Mock boundary: none — real script over tmpdir fixtures.

### WI-17: Tier-0 journeys in scheduled CI + per-flow jsdom integration tests (D3)
- **Description**: Tier-0 data-integrity flows (save / open / autosave / external-change) are
  verified only by the manual e2e harness; the CI tier for them is mock choreography
  (`useFileSave.test.ts` mocks the stores AND `saveToPath`; 74/79 assertions are call-choreography),
  zero workflows reference e2e, and the smoke checklist the tier0-matrix doc points to does not
  exist. Two legs: (1) wire the Tier-0 e2e journey subset into a scheduled, non-blocking CI job
  (the repo already runs scheduled suites); (2) add per-flow jsdom integration tests that run the
  REAL stores, REAL services, and REAL flow composition, mocking only `@tauri-apps/*` behind a
  stateful in-memory fs fake (write→read returns written bytes; external mtime change observable).
- **Acceptance criteria**:
  - [ ] RED first: the save-flow integration test is written first and fails if save composition breaks (verified by temporarily gutting the flow, then restoring).
  - [ ] Stateful fs fake lives in `src/test/` (shared harness), documented, no silent-success defaults (the setup.ts false-confidence class).
  - [ ] Four flows covered end-to-end in jsdom: save (content reaches fake fs), open (fake fs content reaches editor state), autosave (debounced write happens with correct bytes), external-change (mtime/content change triggers the reload/conflict path).
  - [ ] A scheduled workflow runs the Tier-0 e2e journey subset (non-blocking, with on-failure issue step — same pattern as WI-4); the dead smoke-checklist pointer in the tier0-matrix doc is fixed.
  - [ ] `useFileSave.test.ts` choreography tests replaced or demoted — the integration tests carry the Tier-0 claim.
  - [ ] Gate: `pnpm test src/test/tier0` (or chosen path) exits 0 inside `pnpm check:all`; one SHA-pinned dispatch of the tier0 workflow green (same `gh run view --json headSha,conclusion,jobs` discipline as WI-4 — never a bare `gh run watch`).
- **Required tests**:
  - Save flow: edit → save → fake fs contains the exact serialized bytes — assertion level: Level 2
  - Open flow: fake fs fixture → document state equals parsed fixture — assertion level: Level 1
  - Autosave: edit → debounce elapses (fake timers) → bytes written once, correct content — assertion level: Level 2
  - External change: fake fs mutates under an open doc → conflict/reload behavior observable in store state — assertion level: Level 4
  - Failure path: fake fs write rejects (disk error) → dirty state retained, error surfaced, no data discarded — assertion level: Level 1 + Level 4
  - Concurrency: save racing external change → deterministic winner per the concurrency model recorded in the decision ledger (D6 in `.claude/tdd-guardian/decisions-20260803.md`) — a data-integrity protocol decision, not a per-test pin — assertion level: Level 4
  - Mock boundary: `@tauri-apps/*` only, via the stateful fake — real stores, real services (policy rules 3-4).

### WI-18: Mock-boundary ratchet gate — tests must not mock `@/stores/*` (D4) — **EXECUTES FIRST**
- **Description**: 1,411 `vi.mock("@/…")` calls across 451 test files with 144 `as unknown as`
  casts defeat type-checking of fakes — an ungated contract-drift hole violating the repo's own
  "mock boundaries, not logic" rule, worst in the file-save/tab wiring layer. Add a ratcheting gate
  and **land it before WI-1** so every test this plan writes is protected by enforcement, not
  discipline. Codex-amended mechanics: the baseline is an **identity list** — (test file, mocking
  API, resolved module target) triples — not per-file counts, because counts permit like-for-like
  swaps (the exact defect WI-16 exists to kill). Coverage: ALL Vitest module-mocking variants
  (`vi.mock`, `vi.doMock`, `vi.mock(import(...))`, relative-path mocks resolving into `src/stores/`,
  `__mocks__/` directories) and ALL test-adjacent files (`*.test.*`, `src/test/` helpers, setup
  files) — no variant is "documented out of scope". The sanctioned alternative to mocking a store
  is using the real store (setState/reset) or an explicit store-factory seam with a recorded reason.
- **Acceptance criteria**:
  - [ ] RED first: fixture tests for the checker precede the script.
  - [ ] `scripts/check-mock-boundaries.mjs` + `pnpm lint:mock-boundaries` wired into `check:all`; identity baseline frozen at current reality; two-way ratchet (improvement must be recorded).
  - [ ] Identity form: replacing a mocked store with a different mocked store at the same count fails; every mocking variant and `__mocks__` dir is detected; helper/setup files are scanned.
  - [ ] Baseline registered in the WI-16 ratchet manifest from day one (no new self-attested baseline); until WI-16 lands, the baseline file carries a header comment marking it manifest-pending.
  - [ ] Tests written in WI-1..17 of this plan pass the gate with zero new entries.
  - [ ] Gate: `pnpm lint:mock-boundaries` exits 0; `pnpm check:all` exits 0.
- **Required tests**:
  - Fixture test file adding a `vi.mock("@/stores/x")` not in baseline → exit 1 naming file + target — assertion level: Level 1
  - Identity swap: remove baselined mock A, add unbaselined mock B (count unchanged) → exit 1 naming B — assertion level: Level 1
  - `vi.doMock("@/stores/x")` and a relative-path mock resolving into `src/stores/` → both counted — assertion level: Level 1
  - `__mocks__` directory shadowing a store module → counted — assertion level: Level 1
  - Mock in a `src/test/` helper file (not `*.test.*`) → counted — assertion level: Level 1
  - Fixture removing one below baseline without recording → exit 1 (two-way) — assertion level: Level 1
  - `vi.mock("@tauri-apps/api")` (a legitimate boundary) → not counted — assertion level: Level 1
  - String occurrence in a comment vs a real mock call → not counted (parse, don't grep) — assertion level: Level 1
  - Mock boundary: none — real script over fixtures.

### WI-19: Dark-feature verdicts — workflow flag split, backend enforcement, guard repair (E2)
- **Description**: Browser (~17.7k LOC) and workflow system (~17.8k LOC) are the #2/#3 investments,
  default-off, yet all scaffolding is always-on and the backend ignores the flags entirely (zero
  flag hits in src-tauri; ~166 commands registered unconditionally). Governance drift is worse:
  `.claude/hooks/gha-tdd-guard.mjs` `SCOPED` (line 127) lists four paths that no longer exist while
  the real shipped engine frontend is unguarded, and the plan files it cites are gone. Do three
  things: (1) split `advanced.workflowEngine` into `advanced.workflowViewer` and
  `advanced.workflowEngine` flags (`src/services/featureFlags/workflowFeatureFlag.ts`) with a
  settings migration; (2) make the backend respect the flags — workflow-engine and browser commands
  check their flag and return the WI-14 typed `FeatureDisabled` error when off; (3) fix
  `gha-tdd-guard.mjs` to scope the paths that actually exist. Record a one-paragraph
  ship-on / dark-with-dated-exit / extract verdict per feature in `.claude/rules/60-ai-governance.md`.
- **Acceptance criteria**:
  - [ ] RED first: settings-migration test and backend flag-off tests precede implementation.
  - [ ] Old persisted `advanced.workflowEngine: true` migrates to both new flags true (no user regression); fresh installs default both off.
  - [ ] Viewer works with engine off; engine paths inert unless engine flag on — frontend and backend agree.
  - [ ] Flag-gated Rust commands return typed `FeatureDisabled` (code, i18nKey) when the flag is off — enforcement in the backend, not only UI hiding.
  - [ ] `gha-tdd-guard.mjs` `SCOPED` contains only existing paths covering the shipped engine frontend; dead plan citations removed; hook blocks an unguarded write in its scope (proven by its test fixture).
  - [ ] One-paragraph verdict per dark feature (browser, workflow) committed to the governance rules.
  - [ ] Gate: `pnpm test` exits 0; `cargo test --manifest-path src-tauri/Cargo.toml` exits 0; `pnpm check:all` exits 0.
- **Required tests**:
  - Settings migration: `{workflowEngine: true}` → both flags true; absent → both false; partial states table-driven — assertion level: Level 1
  - Backend: engine command invoked with flag off → `FeatureDisabled` error shape (Rust test through real serialization) — assertion level: Level 1
  - Backend: same command with flag on → normal result (no regression) — assertion level: Level 1
  - Frontend selectors: viewer-on/engine-off renders viewer, hides engine affordances — assertion level: Level 4
  - `gha-tdd-guard.mjs`: write to a real scoped path without sibling test → blocked; allow-listed file types pass (fixture-driven) — assertion level: Level 1
  - Mock boundary: none in Rust; frontend uses real settings store.

### WI-20: Rust state rule + migrate `mcp_bridge/state.rs` and `hot_exit/coordinator.rs` to `.manage()` (C2)
- **Description**: Two competing Rust state architectures: ~39-40 genuinely mutable process-global
  statics across 29 files vs 4 `.manage()` states, with visible cope-machinery — generation
  counters, `cfg(test)` JoinHandle forks, hand-rolled poison recovery ×3 in
  `src-tauri/src/hot_exit/coordinator.rs`. No rule says which to use (`WorkflowRunnerState` went
  one way, the analogous `BridgeState` the other). Write the one-paragraph rule in
  `.claude/rules/50-codebase-conventions.md` §10: new state uses `.manage()`/`State<T>`; statics
  only where no `AppHandle` can reach (and idiomatic constants — regexes, ID counters — stay).
  Migrate the two files with the most cope-machinery first: `src-tauri/src/mcp_bridge/state.rs`
  and `src-tauri/src/hot_exit/coordinator.rs`, deleting the generation counters, `cfg(test)` forks,
  and hand-rolled poison recovery they exist to compensate for.
- **Acceptance criteria**:
  - [ ] RED first: behavior of both modules pinned by tests against the public API before the migration (test isolation no longer needs generation counters afterwards — that is the proof).
  - [ ] Both modules hold state via `.manage()`; process-global mutable statics removed from them; `cfg(test)` forks and the ×3 poison recovery deleted.
  - [ ] Rule paragraph committed; names the decision boundary and the allowed static exceptions.
  - [ ] macOS behavior identical: full Rust suite green, MCP bridge and hot-exit flows unchanged (WI-17's integration leg exercises hot exit end-to-end).
  - [ ] Gate: `cargo test --manifest-path src-tauri/Cargo.toml` exits 0; `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` exits 0; `cargo fmt --manifest-path src-tauri/Cargo.toml --check` exits 0.
- **Required tests**:
  - BridgeState lifecycle: request registered → response resolves it; duplicate/unknown IDs rejected (behavior on managed state, not statics) — assertion level: Level 1 + Level 4
  - Hot-exit coordinator: capture → restore round-trip through the managed state — assertion level: Level 1
  - Concurrent access: parallel writers on the managed state never deadlock/poison (the machinery being deleted claimed to handle this) — assertion level: Level 4
  - Test isolation: two tests using fresh managed state don't observe each other (replaces generation counters) — assertion level: Level 4 (determinism)
  - Mock boundary: none — real tokio runtime in tests, per existing src-tauri patterns.

### WI-21: Take `noImplicitOverride` (30 errors) and `exactOptionalPropertyTypes` (132) (verifier-stack assessment §1)
- **Description**: Measured costs at `66bcf618`: `noImplicitOverride` = 30 errors (nearly free),
  `exactOptionalPropertyTypes` = 132 (bounded single-session task). Enable both in
  `tsconfig.json` and fix all resulting errors properly — `override` keywords where real, and for
  exact-optional: distinguish `T | undefined` from absent-key semantics at each site rather than
  blanket-widening types. `noUncheckedIndexedAccess` (948) is explicitly NOT taken (see Deferred).
- **Acceptance criteria**:
  - [ ] Both flags on in `tsconfig.json`; `npx tsc -p tsconfig.json --noEmit` exits 0.
  - [ ] No runtime behavior change: full existing suite green before/after (type-only changes are a 10-tdd exception; the suite is the regression guard). Any fix that DOES change runtime behavior (a real latent bug surfaced by exact-optional) gets its own failing-first reproducer test.
  - [ ] No `as`-cast or `any` escapes introduced to silence the flags (grep-reviewed in the diff).
  - [ ] Gate: `npx tsc -p tsconfig.json --noEmit` exits 0 and `pnpm check:all` exits 0.
- **Required tests**:
  - Existing suite is the guard for the mechanical majority — assertion level: existing (Level 1/4)
  - Per surfaced latent bug (if any): failing reproducer before the fix — assertion level: Level 1

---

## Risks & Assumptions

- **Assumption**: file:line evidence in the review is current at `66bcf618`; each WI re-verifies
  its anchors before RED (the review itself caught two stale rows in the older plan).
- **Assumption**: `sourceLinkActions.ts` lives at `src/plugins/sourceLinkPopup/` (verified),
  not `sourcePopup/` as the task text says; the plugin split across two directories is itself part
  of finding A1's "parallel universe" observation.
- **Risk (WI-9, WI-10)**: the popup-store revert (101 consumer files) and hooks→services move
  (~74 files) are large mechanical diffs. Mitigation: contract-pinning tests BEFORE the move,
  batch-wise green gates, no behavior changes in the same commit as a move.
- **Risk (WI-4/5)**: mutation runtimes are inherently long; the break threshold must be set from a
  measured green run, or it recreates the coverage-floor flake-generator class the review flags.
- **Risk (WI-5)**: any future undo/redo property work must respect the `newGroupDelay` hazard
  (nondeterministic step grouping by wall clock) — injected clock or `newGroupDelay: 0` required.
  This plan deliberately scopes the property slice to formatter idempotence, which has no clock.
- **Risk (WI-13)**: lazy thunks introduce an async boundary at first editor mount on macOS, the
  primary platform. Mitigation: resolution caching test, failure-path test, and manual cold-start
  check of a document window before merge.
- **Risk (WI-14)**: `CommandError` touches the frontend error-handling contract; partial migration
  means two error shapes coexist. Mitigation: the signature ratchet makes the transitional state
  visible and monotonically shrinking, and frontend handlers must handle both shapes until the
  ratchet hits zero.
- **Risk (WI-16)**: comparing baselines against the merge-base in CI requires fetch depth /
  base-ref access in `ci.yml`; if unavailable on some event types, the gate must fail closed, not
  skip silently.
- **Risk (WI-19)**: backend flag enforcement can break MCP automation that calls workflow/browser
  commands with flags off; the typed `FeatureDisabled` error (WI-14 dependency) is the contract
  that keeps that failure loud and actionable.
- **Dependency ordering**: **WI-18 executes FIRST** (Codex amendment — the gate must exist before
  WI-1's tests are written; discipline-only enforcement is the silent-failure vector this plan
  targets); WI-5 depends on WI-4; WI-19's typed error depends on WI-14's `CommandError` (or lands
  with a minimal precursor variant); WI-16 registers WI-18's baseline in its manifest when it lands.
- **Decision ledger**: consequential behavioral pins (WI-1 remap-vs-close, WI-2 non-focused-pane
  activation, WI-13 retry-after-rejection, WI-15 strip-vs-reject per boundary class, WI-17
  save-vs-external-change concurrency model) are recorded with rationale in
  `.claude/tdd-guardian/decisions-20260803.md` and referenced from test-file headers — pin the
  invariant at RED; pin the mechanism only when it is user-visible contract.
- **Assumption**: `pnpm check:all` remains the frontend gate and
  `cargo test --manifest-path src-tauri/Cargo.toml` the Rust gate throughout; pre-push semantics
  change only where WI-7 says so, under rule §9 (fixing the gate, with user-visible documentation).

## Deferred / Out of Scope

- **Popup-layer convergence (A1 follow-on)**: extracting per-feature action cores over a
  surface-agnostic applier, merging the two ~85%-identical popup view bases, and adding popups to
  the parity harness — the review's "then apply the toolbar recipe" step. Deferred: WI-1 removes
  the live corruption path; convergence is a separate structural program the size of this plan's
  largest items.
- **Wiring the browserStore cap (E4 alternative)**: rejected because the verifier REFUTED the leak
  the cap would bound — surfaces are active-page-only, already stricter than the unwired cap.
- **Deleting `mutation.yml` outright (D1 alternative)**: rejected because the task and assessment
  support repair, and WI-5's threshold makes the fixed workflow earn its runner minutes.
- **`noUncheckedIndexedAccess` (948 errors)**: needs a per-module policy, not a global flip — the
  common retrofit "fix" is a `!` assertion that converts a type question into 948 unverified
  claims. Take per-module later where indexing is genuinely unproven.
- **Full `Result<T, String>` → `CommandError` migration of all 161 commands**: WI-14 lands the
  type, the rule, the first modules, and the ratchet; the remainder is mechanical continuation the
  ratchet keeps honest.
- **The 32 unverified medium/low findings** (src/lib fourth tier, invoke-site gating, check:all
  monolith ordering, MCP write-lock hold time, etc.): directionally credible but not adversarially
  verified; out of scope until verified.
- **Browser/workflow extract-to-crate or cargo-feature split**: E2's verdict paragraphs (WI-19)
  decide ship/dark/extract; executing an "extract" verdict is its own plan.
- **CJK IME WebKit-tier changes**: untouched by every WI here; `pnpm test:browser` remains the
  guard if any WI unexpectedly grazes composition handling.
- **Migrating `src/test/setup.ts` off silent-success defaults** (`invoke: vi.fn(() =>
  Promise.resolve())` at setup.ts:143): the false-confidence class is real (Codex-verified), but
  flipping the global default to loud-fail would ripple through hundreds of existing tests — too
  much blast radius for this plan. WI-17's stateful fake establishes the loud-fail contract for
  new tests; repo-wide migration is its own follow-up, trackable by a future ratchet on
  setup.ts-default reliance.
- **Gate-liveness audit (recurring)**: Codex's fourth verification layer — a scheduled check that
  external gates (mutation, tier0 e2e) actually ran recently and their on-failure paths still fire.
  WI-4/WI-17's on-failure issue steps are the first increment; a periodic "does this gate still
  run / still bite" audit is deferred as process, not code.
