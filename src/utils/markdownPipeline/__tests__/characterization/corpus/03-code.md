A fenced JavaScript block:

```js
function greet(name) {
  return `Hello, ${name}!`;
}
```

A fenced Rust block:

```rust
fn main() {
    println!("Hello, world!");
}
```

A fence with no language:

```
plain preformatted text
  indented line
```

Inline code with a backtick inside: `` a ` b ``.
