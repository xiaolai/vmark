/**
 * MCP Bridge — Section Handlers (barrel).
 *
 * Purpose: Higher-level section operations — update, insert, and move
 *   content by heading-delimited sections in the document.
 *
 * @module hooks/mcpBridge/sectionHandlers
 */

export { handleSectionUpdate } from "./section/updateHandler";
export { handleSectionInsert } from "./section/insertHandler";
export { handleSectionMove } from "./section/moveHandler";
