/**
 * Purpose: Single source of truth for the inactive-selection CSS class
 *   shared between the ProseMirror plugin and the CodeMirror plugin.
 *   The literal also appears in `inactive-selection.css`; keep them in
 *   sync if the class is ever renamed.
 *
 * @module plugins/inactiveSelection/constants
 */
export const INACTIVE_SELECTION_CLASS = "vmark-inactive-selection";
